---
topic: FestGrid PRD
updated: 2026-08-24T15:28
---

- (decision) Added EventInfo.postId (Section 4.1) linking an event to its source Post; clarified Post interface (Section 4.7) doc comment. Reason: EventInfo has no image field by design (PRD gap confirmed while creating implementation Stories 1.3b/1.2a) — an event's image resolves via this relation to Post.imageUrl, not a stored field on EventInfo.
- (decision) Added Schedule.eventId (4.4) and Post.accountId (4.7) back-references, closing FK gaps found during architecture review with Winston (same pattern as EventInfo.postId).
- (decision) Added User, Subscription, Favorite, CalendarEntry, Report, ApiKey, and DefaultLocationChangeRequest interfaces (Section 4) — entities implied by prose (3.2, 3.7, 3.9, 3.11) but never modeled. Found during architecture review with Winston.
- (decision) SocialMediaAccountProfile.defaultLocation is account-level (shared across subscribers), not per-subscription. Reason: extraction runs once per shared post (Tier 2 round-robin), so a per-user default would create ambiguity about which subscriber's value applies to the shared EventInfo.
- (decision) defaultLocation edits apply immediately (no pre-approval gate); moderators are emailed and can accept or revert via DefaultLocationChangeRequest in Moderator Tools (3.9.3). Reason: avoids blocking extraction on moderator latency while still giving moderators an audit/override path.
- (decision) Adopted soft-delete (deletedAt) convention for EventInfo, Favorite, CalendarEntry, Subscription, ApiKey instead of hard deletes, to preserve moderation/audit history. Detailed indexing strategy (partial indexes on deletedAt) deferred to project-context.md / architecture spine, not PRD-level.
- (event) PRD updated: Schedule.eventId, Post.accountId, EventInfo.deletedAt, SocialMediaAccountProfile.defaultLocation added; new interfaces User/Subscription/Favorite/CalendarEntry/Report/ApiKey/DefaultLocationChangeRequest (4.8-4.14) added; Section 3.7/3.9.3 prose updated for default-location moderation flow.
- (event) epics.md amended (direct edit, bmad-create-epics-and-stories skipped -- its Step 1 re-extracts/overwrites from template, unsuitable for a 2-story amendment on an already-detailed backlog): Story 3.3 narrowed to first-set-only + amendment note; new Story 3.3b (edit default location, immediate apply + moderator email + DefaultLocationChangeRequest); Story 4.7 extended with accept/revert of pending DefaultLocationChangeRequest rows. New FR66/FR67 added. sprint-status.yaml updated with 3-3b-edit-an-accounts-default-location: backlog.
- (decision) Added Section 3.13 (Vote for Social Media Accounts) and 3.14 (Embeddable Discovery Widget), destined for a new Epic 6. Decided during prior discovery: voting is open to any authenticated user immediately (not gated behind the unbuilt free_user tier); new account entries create a real SocialMediaAccountProfile validated against the scraper registry; ranking is a single global list; category is never assigned to unscraped accounts; voter location is used only as a per-viewer, non-persisted ranking weight (never stored as an account's location) to avoid repurposing nearby-search consent and deanonymizing low-vote accounts.
- (decision) Widget (3.14) is public/no-auth, general-purpose filter combination (not limited to the existing single-account public page), reuses existing card/calendar components, has no in-widget event-detail navigation (ics export only), requires an embed-generator UI with dynamic per-domain CSP whitelisting (Section 4.16, own route only), postMessage auto-resize, and a mandatory Powered-by-FestDaily button.
- (decision) Added AccountVote (4.15) and EmbedDomain (4.16) interfaces; added Security NFR bullets for widget CSP scoping and the location-data-reuse boundary; added Monetization bullet linking the vote list to both Phase 1 and Phase 2 (Section 6).
- (change) epics.md Requirements Inventory: backfilled missing FR68 (public per-account page, already live in prose and FR Coverage Map since the 2026-08-02 correct-course but never added to the FR list); added FR69-FR84 for the vote and widget features; added NFR19-NFR20. FR Coverage Map intentionally left unassigned for FR69-FR84 -- deferred to bmad-create-epics-and-stories when Epic 6 is drafted.
- (assumption) Deferred to implementation (non-blocking, flagged in PRD as callouts): account-submission dedup strategy, anti-abuse/rate-limiting on new votable-account submissions, whether voters are notified when their account gets subscribed, widget traffic rate-limiting/caching strategy, and whether embed-domain registration requires ownership verification.
- (event) Ran full Reviewer Gate (rubric walker) on updated PRD: 2 high, 5 medium, 6 low findings (see review-rubric.md). Applied 8 fixes, deferred 5 pre-existing/structural items.
- (decision) Fixed [high] displayName/accountId population gap: vote-time validation against the scraper registry now explicitly includes a lightweight profile lookup (existence + public metadata, no scraping/AI) that populates SocialMediaAccountProfile fields directly -- no placeholder state. Fixed [high] missing KPIs: added Vote-to-Subscription Conversion and Widget Reach to Section 7.
- (decision) Fixed [medium] AccountVote/EmbedDomain missing soft-delete: added deletedAt to both (4.15/4.16) per AD-8 convention -- votes are withdrawable, embed domains are deregisterable; added FR85/FR86 to epics.md; updated project-context.md's Soft-Delete Convention table to list both tables. Fixed [medium] wrong cross-refs in 3.14 (3.3->3.3.1, 3.6->3.7). Fixed [medium] widget rate-limiting had no MVP default: added explicit 'no rate limiting for MVP, monitored via PostHog' posture. Fixed [medium] 3.14 absent from Goals: added a clause tying the widget to the existing User Acquisition goal rather than adding a new Section 2 goal. Fixed [low] widget tier scope and [low] widget monetization stance: both now explicit (tier-independent, free/ungated for MVP).
- (assumption) Deferred, not fixed (pre-existing document-wide patterns, out of this update's scope): no UJs for the new voter/widget-embedder protagonists (document has zero UJs anywhere); Security section's boilerplate opening clause; Section 2 Goals reading as vision theater; no Glossary/ID scheme and the pre-existing 3.4-3.6 section-numbering off-by-one drift; whether embed-domain registration needs ownership verification. Revisit if a future PRD pass tackles document-wide structure.
- (event) Polish pass (structure + prose) applied to new sections only (3.13, 3.14, 4.15, 4.16, new NFR/Monetization/KPI bullets) per doc_standards -- consolidated the widget's acquisition-goal justification from three restatements (intro + 2 bullets) into the 3.14 intro once; dropped a dangling Section 6 cross-reference that pointed to nothing; tightened one awkward sentence in 3.13. Schema interfaces and NFR/KPI bullets needed no changes -- matched established document patterns cleanly.
- (event) PRD finalized (status: draft -> final). Update cycle added Sections 3.13/3.14, schema 4.15/4.16, FR69-FR86, NFR19-20, for the upcoming Epic 6 (Vote for Social Media Accounts, Embeddable Discovery Widget).
- (event) epics.md: Epic 6 (Community Voting and Embeddable Distribution) appended directly after Epic 5 (bmad-create-epics-and-stories skipped -- its Step 1 overwrites epics.md wholesale from template, unsuitable for appending one epic to an already-detailed backlog; same reasoning as the 2026-08-02 3.3b amendment, confirmed with user before proceeding). FR Coverage Map updated with FR69-FR86 -> Epic 6. Backfilled a duplicate-NFR bug my own earlier edit introduced (NFR19/20 collided with pre-existing entries; new bullets renumbered to NFR25/26).
- (event) Ran an Epic 6 readiness sweep (Gate 1 + Gate 3, bmad-epic-readiness-check methodology, epic-readiness/epic-6-readiness.md) before drafting individual stories, mirroring Epic 5's precedent. Findings: (1) two backend GraphQL API layer stories needed, split by data domain -- 6.1a (account votes) and 6.6a (embed domains), not one combined layer, mirroring Epic 4's per-domain split rather than Epic 2/3/5's one-per-epic pattern; (2) a new 6.7a story for the widget's dynamic frame-ancestors CSP middleware (genuinely novel infra -- apps/web/middleware.ts today is pure next-intl locale routing with zero network calls), scoped Epic-6-only since no second consumer exists (Gate 1 finding, not Gate 3); (3) the vote-time 'lightweight profile lookup' PRD 3.13 assumes was found to be the exact unbuilt capability Story 3.4's 2026-08-07 Forward note already flagged as needed by Story 3.1/3.2's subscribe forms -- two independent cross-epic consumers clears Gate 3's reuse bar, resolved as an amendment to Story 3.3c's ScraperAdapter interface (lookupAccountProfile method) rather than a new story or Epic 0 promotion.
- (decision) Epic 6 story order: 6.1a, 6.1 (vote), 6.2 (ranked list + near-me), 6.3 (withdraw vote), 6.4 (autocomplete on subscribe -- flagged as reopening Story 3.1/3.2's already review/ready-for-dev UI, a sequencing risk noted for whoever implements it), 6.5 (widget embed generator, no backend needed), 6.6a, 6.6 (domain register/deregister), 6.7a (CSP middleware), 6.7 (public widget page, reuses EventListView/WeeklyCalendarView/ICS export as-is). sprint-status.yaml updated with all 11 new backlog entries plus epic-6/epic-6-retrospective keys.
- (decision) Revised Section 3.14/4.16/4.17 per user design review: widget is now a persisted Widget entity (own id, editable post-creation, embeds auto-update) rather than filters-in-a-URL; EmbedDomain whitelist rescoped from per-owner to per-widget (widgetId FK, not ownerUserId); domain matching changed from implicit subdomain inclusion to explicit exact-hostname-or-wildcard patterns validated against a Public Suffix List at registration (prevents whitelisting shared-hosting domains like *.vercel.app); localhost handled as an ordinary registered pattern, no special-casing. Embed mechanism is now a script+placeholder-div pair (supports multiple widgets per page, auto-updates on config change) with a raw iframe URL as fallback for script-stripping embed contexts. EmbedDomain renumbered 4.16->4.17; new Widget interface inserted at 4.16.
- (event) epics.md revised to match the widget-entity/PSL/script-embed PRD changes: new Story 6.5a (widget-config backend layer, own widgets table) inserted before 6.5; Story 6.5 revised to persist via 6.5a and produce a script+placeholder-div snippet (embed.js, multi-instance via querySelectorAll, DOM-ready handling) plus a raw-iframe fallback; Story 6.6a revised from per-owner to per-widget EmbedDomain (widgetId FK replacing ownerUserId), domain field renamed pattern, exact-hostname-or-explicit-wildcard matching with tldts-based Public Suffix List validation on wildcard registration, isEmbedDomainAllowed renamed isOriginAllowedForWidget(widgetId, origin); Story 6.6 revised to a per-widget domain screen; Story 6.7a's middleware now extracts widgetId from the route and calls the renamed query; Story 6.7's route changed from query-param-driven to /widget/{widgetId} reading persisted config via widgetById, with an explicit not-found/soft-deleted empty state. sprint-status.yaml: added 6-5a backlog entry. FR list: FR77 reworded (persisted entity, not URL params), FR82/83/86 lightly reworded for per-widget scoping, new FR87 (wildcard+PSL validation) and FR88 (script+div embed, multi-widget-per-page, iframe fallback) added; FR Coverage Map and Epic 6's FRs-covered line updated to include them.
- (decision) Amended Section 3.7 (AI-Assisted Location Inference: post/account-scraped metadata only, never a subscriber's saved location per the Section 5 Location Data Reuse Boundary; system-key fallback narrowly scoped to this one call, worded to not read as Phase 2 launching early) and Account Profile Backfill from scraped posts; Section 4.14 DefaultLocationChangeRequest.changedByUserId made optional and new changeSource (USER|AI_INFERENCE) discriminator added, per user-approved direction from sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.1; Section 4.5 defaultLocation doc comment cross-references changeSource. Track A (bmad-correct-course) of that proposal -- unblocks Story 3.4m.
- (event) PRD amendment finalized (updated timestamp bumped to 2026-08-24); status remains 'final'. epics.md FR-list backfill (new FR number(s) for this capability, matching the established pattern for prior amendments) deliberately NOT done in this pass -- flagged as a follow-up, not silently skipped.
