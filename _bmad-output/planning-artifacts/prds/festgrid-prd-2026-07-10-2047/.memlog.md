---
topic: FestGrid PRD
updated: 2026-08-29T00:44
---

- (decision) Added EventInfo.postId (Section 4.1) linking an event to its source Post; clarified Post interface (Section 4.7) doc comment. Reason: EventInfo has no image field by design (PRD gap confirmed while creating implementation Stories 1.3b/1.2a) — an event's image resolves via this relation to Post.imageUrl, not a stored field on EventInfo.
- (decision) Added Schedule.eventId (4.4) and Post.accountId (4.7) back-references, closing FK gaps found during architecture review with Winston (same pattern as EventInfo.postId).
- (decision) Added User, Subscription, Favorite, CalendarEntry, Report, ApiKey, and DefaultLocationChangeRequest interfaces (Section 4) — entities implied by prose (3.2, 3.7, 3.9, 3.11) but never modeled. Found during architecture review with Winston.
- (decision) SocialMediaAccountProfile.defaultLocation is account-level (shared across subscribers), not per-subscription. Reason: extraction runs once per shared post (Tier 2 round-robin), so a per-user default would create ambiguity about which subscriber's value applies to the shared EventInfo.
- (decision) defaultLocation edits apply immediately (no pre-approval gate); moderators are emailed and can accept or revert via DefaultLocationChangeRequest in Moderator Tools (3.9.3). Reason: avoids blocking extraction on moderator latency while still giving moderators an audit/override path.
- (decision) Adopted soft-delete (deletedAt) convention for EventInfo, Favorite, CalendarEntry, Subscription, ApiKey instead of hard deletes, to preserve moderation/audit history. Detailed indexing strategy (partial indexes on deletedAt) deferred to project-context.md / architecture spine, not PRD-level.
- (event) PRD updated: Schedule.eventId, Post.accountId, EventInfo.deletedAt, SocialMediaAccountProfile.defaultLocation added; new interfaces User/Subscription/Favorite/CalendarEntry/Report/ApiKey/DefaultLocationChangeRequest (4.8-4.14) added; Section 3.7/3.9.3 prose updated for default-location moderation flow.
- (event) epics.md amended (direct edit, bmad-create-epics-and-stories skipped -- its Step 1 re-extracts/overwrites from template, unsuitable for a 2-story amendment on an already-detailed backlog): Story 3.3 narrowed to first-set-only + amendment note; new Story 3.3b (edit default location, immediate apply + moderator email + DefaultLocationChangeRequest); Story 4.7 extended with accept/revert of pending DefaultLocationChangeRequest rows. New FR66/FR67 added. sprint-status.yaml updated with 3-3b-edit-an-accounts-default-location: backlog.
- (decision) Added Section 3.13 (Vote for Social Media Accounts) and 3.14 (Embeddable Discovery Widget), destined for a new Epic 6. Decided during prior discovery: voting is open to any authenticated user immediately (not gated behind the unbuilt free_user tier); new account entries create a real SocialMediaAccountProfile validated against the scraper registry; ranking is a single global list; category is never assigned to unscraped accounts; voter location is used only as a per-viewer, non-persisted ranking weight (never stored as an account's location) to avoid repurposing nearby-search consent and deanonymizing low-vote accounts.
- (decision) Widget (3.14) is public/no-auth, general-purpose filter combination (not limited to the existing single-account public page), reuses existing card/calendar components, has no in-widget event-detail navigation (ics export only), requires an embed-generator UI with dynamic per-domain CSP whitelisting (Section 4.16, own route only), postMessage auto-resize, and a mandatory Powered-by-FestDaily button.
- (decision) Added AccountVote (4.15) and EmbedDomain (4.16) interfaces; added Security NFR bullets for widget CSP scoping and the location-data-reuse boundary; added Monetization bullet linking the vote list to both Phase 1 and Phase 2 (Section 6).
- (change) epics.md Requirements Inventory: backfilled missing FR68 (public per-account page, already live in prose and FR Coverage Map since the 2026-08-02 correct-course but never added to the FR list); added FR69-FR84 for the vote and widget features; added NFR19-NFR20. FR Coverage Map intentionally left unassigned for FR69-FR84 -- deferred to bmad-create-epics-and-stories when Epic 6 is drafted.
- (assumption) Deferred to implementation (non-blocking, flagged in PRD as callouts): account-submission dedup strategy, anti-abuse/rate-limiting on new votable-account submissions, whether voters are notified when their account gets subscribed, widget traffic rate-limiting/caching strategy, and whether embed-domain registration requires ownership verification.
- (event) Ran full Reviewer Gate (rubric walker) on updated PRD: 2 high, 5 medium, 6 low findings (see review-rubric.md). Applied 8 fixes, deferred 5 pre-existing/structural items.
- (decision) Fixed [high] displayName/accountId population gap: vote-time validation against the scraper registry now explicitly includes a lightweight profile lookup (existence + public metadata, no scraping/AI) that populates SocialMediaAccountProfile fields directly -- no placeholder state. Fixed [high] missing KPIs: added Vote-to-Subscription Conversion and Widget Reach to Section 7.
- (decision) Fixed [medium] AccountVote/EmbedDomain missing soft-delete: added deletedAt to both (4.15/4.16) per AD-8 convention -- votes are withdrawable, embed domains are deregisterable; added FR85/FR86 to epics.md; updated project-context.md's Soft-Delete Convention table to list both tables. Fixed [medium] wrong cross-refs in 3.14 (3.3->3.3.1, 3.6->3.7). Fixed [medium] widget rate-limiting had no MVP default: added explicit 'no rate limiting for MVP, monitored via PostHog' posture. Fixed [medium] 3.14 absent from Goals: added a clause tying the widget to the existing User Acquisition goal rather than adding a new Section 2 goal. Fixed [low] widget tier scope and [low] widget monetization stance: both now explicit (tier-independent, free/ungated for MVP).
- (assumption) Deferred, not fixed (pre-existing document-wide patterns, out of this update's scope): no UJs for the new voter/widget-embedder protagonists (document has zero UJs anywhere); Security section's boilerplate opening clause; Section 2 Goals reading as vision theater; no Glossary/ID scheme and the pre-existing 3.4-3.6 section-numbering off-by-one drift; whether embed-domain registration needs ownership verification. Revisit if a future PRD pass tackles document-wide structure.
- (event) Polish pass (structure + prose) applied to new sections only (3.13, 3.14, 4.15, 4.16, new NFR/Monetization/KPI bullets) per doc_standards -- consolidated the widget's acquisition-goal justification from three restatements (intro + 2 bullets) into the 3.14 intro once; dropped a dangling Section 6 cross-reference that pointed to nothing; tightened one awkward sentence in 3.13. Schema interfaces and NFR/KPI bullets needed no changes -- matched established document patterns cleanly.
- (event) PRD finalized (status: draft -> final). Update cycle added Sections 3.13/3.14, schema 4.15/4.16, FR69-FR86, NFR19-20, for the upcoming Epic 6 (Vote for Social Media Accounts, Embeddable Discovery Widget).
- (event) epics.md: Epic 6 (Community Voting and Embeddable Distribution) appended directly after Epic 5 (bmad-create-epics-and-stories skipped -- its Step 1 overwrites epics.md wholesale from template, unsuitable for appending one epic to an already-detailed backlog; same reasoning as the 2026-08-02 3.3b amendment, confirmed with user before proceeding). FR Coverage Map updated with FR69-FR86 -> Epic 6. Backfilled a duplicate-NFR bug my own earlier edit introduced (NFR19/20 collided with pre-existing entries; new bullets renumbered to NFR25/26).
- (event) Ran an Epic 6 readiness sweep (Gate 1 + Gate 3, bmad-epic-readiness-check methodology, epic-readiness/epic-6-readiness.md) before drafting individual stories, mirroring Epic 5's precedent. Findings: (1) two backend GraphQL API layer stories needed, split by data domain -- 6.1a (account votes) and 6.6a (embed domains), not one combined layer, mirroring Epic 4's per-domain split rather than Epic 2/3/5's one-per-epic pattern; (2) a new 6.7a story for the widget's dynamic frame-ancestors CSP middleware (genuinely novel infra -- apps/web/middleware.ts today is pure next-intl locale routing with zero network calls), scoped Epic-6-only since no second consumer exists (Gate 1 finding, not Gate 3); (3) the vote-time 'lightweight profile lookup' PRD 3.13 assumes was found to be the exact unbuilt capability Story 3.4's 2026-08-07 Forward note already flagged as needed by Story 3.1/3.2's subscribe forms -- two independent cross-epic consumers clears Gate 3's reuse bar, resolved as an amendment to Story 3.3c's ScraperAdapter interface (lookupAccountProfile method) rather than a new story or Epic 0 promotion.
- (decision) Epic 6 story order: 6.1a, 6.1 (vote), 6.2 (ranked list + near-me), 6.3 (withdraw vote), 6.4 (autocomplete on subscribe -- flagged as reopening Story 3.1/3.2's already review/ready-for-dev UI, a sequencing risk noted for whoever implements it), 6.5 (widget embed generator, no backend needed), 6.6a, 6.6 (domain register/deregister), 6.7a (CSP middleware), 6.7 (public widget page, reuses EventListView/WeeklyCalendarView/ICS export as-is). sprint-status.yaml updated with all 11 new backlog entries plus epic-6/epic-6-retrospective keys.
- (decision) Revised Section 3.14/4.16/4.17 per user design review: widget is now a persisted Widget entity (own id, editable post-creation, embeds auto-update) rather than filters-in-a-URL; EmbedDomain whitelist rescoped from per-owner to per-widget (widgetId FK, not ownerUserId); domain matching changed from implicit subdomain inclusion to explicit exact-hostname-or-wildcard patterns validated against a Public Suffix List at registration (prevents whitelisting shared-hosting domains like *.vercel.app); localhost handled as an ordinary registered pattern, no special-casing. Embed mechanism is now a script+placeholder-div pair (supports multiple widgets per page, auto-updates on config change) with a raw iframe URL as fallback for script-stripping embed contexts. EmbedDomain renumbered 4.16->4.17; new Widget interface inserted at 4.16.
- (event) epics.md revised to match the widget-entity/PSL/script-embed PRD changes: new Story 6.5a (widget-config backend layer, own widgets table) inserted before 6.5; Story 6.5 revised to persist via 6.5a and produce a script+placeholder-div snippet (embed.js, multi-instance via querySelectorAll, DOM-ready handling) plus a raw-iframe fallback; Story 6.6a revised from per-owner to per-widget EmbedDomain (widgetId FK replacing ownerUserId), domain field renamed pattern, exact-hostname-or-explicit-wildcard matching with tldts-based Public Suffix List validation on wildcard registration, isEmbedDomainAllowed renamed isOriginAllowedForWidget(widgetId, origin); Story 6.6 revised to a per-widget domain screen; Story 6.7a's middleware now extracts widgetId from the route and calls the renamed query; Story 6.7's route changed from query-param-driven to /widget/{widgetId} reading persisted config via widgetById, with an explicit not-found/soft-deleted empty state. sprint-status.yaml: added 6-5a backlog entry. FR list: FR77 reworded (persisted entity, not URL params), FR82/83/86 lightly reworded for per-widget scoping, new FR87 (wildcard+PSL validation) and FR88 (script+div embed, multi-widget-per-page, iframe fallback) added; FR Coverage Map and Epic 6's FRs-covered line updated to include them.
- (decision) Amended Section 3.7 (AI-Assisted Location Inference: post/account-scraped metadata only, never a subscriber's saved location per the Section 5 Location Data Reuse Boundary; system-key fallback narrowly scoped to this one call, worded to not read as Phase 2 launching early) and Account Profile Backfill from scraped posts; Section 4.14 DefaultLocationChangeRequest.changedByUserId made optional and new changeSource (USER|AI_INFERENCE) discriminator added, per user-approved direction from sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.1; Section 4.5 defaultLocation doc comment cross-references changeSource. Track A (bmad-correct-course) of that proposal -- unblocks Story 3.4m.
- (event) PRD amendment finalized (updated timestamp bumped to 2026-08-24); status remains 'final'. epics.md FR-list backfill (new FR number(s) for this capability, matching the established pattern for prior amendments) deliberately NOT done in this pass -- flagged as a follow-up, not silently skipped.
- (decision) Reconciled Section 5's blanket 'we do not sell your data to third parties' against Section 6's planned aggregated-data collection for organizer ad targeting. Added new Section 5 'Private vs. Sellable Data Boundary' bullet: personal/identifiable data promise narrowed but kept intact; a distinct aggregated/anonymized category (interest+geo trend data) is explicitly sellable to tourism boards/venues/partners, governed by the same k-anonymity standard already used by the vote list's Near Me ranking (Section 3.13: city/province bucketing, no raw coordinates, suppressed under 5 distinct contributors). Section 6's organizer ad-targeting, Localized Advertising, and Partnerships bullets reworded to reference this boundary instead of the vaguer 'anonymized where possible' language, and to explicitly rule out third-party behavioral ad networks / sale of identifiable profiles. Reason: investor-discussion thread flagged the prior blanket promise as in tension with planned ad monetization; reusing the existing 3.13 k-anonymity pattern avoids CCPA/CPRA 'sale of personal information' exposure while still enabling the aggregate-data revenue line.
- (event) PRD amendment finalized (updated timestamp already 2026-08-24, status remains 'final'). Scope was a targeted wording/policy reconciliation (Section 5 + 6), not a new capability -- no new FRs, no epics.md/sprint-status.yaml changes needed. Full Reviewer Gate not run given contained scope and reuse of an already-reviewed pattern (3.13 k-anonymity); offered to user as optional follow-up.
- (decision) Amended Section 3.7 (new 'Moderator Override' bullet: a moderator may directly set a corrected defaultLocation via the same mechanism a subscriber uses, requires no further review, auto-supersedes other pending requests for the account); Section 4.14 (DefaultLocationChangeSource gains MODERATOR, DefaultLocationChangeStatus gains SUPERSEDED); Section 4.5 defaultLocation doc comment cross-references the new moderator path. Trigger: surfaced while confirming Story 3.4m's Pre-Coding Approval Gate -- user asked why a moderator can only ACCEPT/REVERT a bad AI guess rather than correcting it directly, exposing a pre-existing gap in Story 3.3b's moderator design. Per user-approved sprint-change-proposal-2026-08-24-moderator-location-override.md.
- (event) PRD amendment finalized (status remains 'final', updated timestamp already 2026-08-24). epics.md FR-list backfill for this capability deliberately NOT done in this pass, matching the established pattern from the earlier 2026-08-24 AI-inference amendment -- flagged as a follow-up. Next: bmad-architecture for AD-11 (moderator override on subscriber-scoped mutations), then bmad-ux for the shared account-location component.
- (event) Update sourced from _bmad-output/specs/spec-ai-prompt-event-filter/SPEC.md (+ companions filter-vocabulary.md, brownfield.md), itself derived via bmad-spec from a bmad-forge-idea session (_bmad-output/forge/ai-prompt-event-filter/). Extracted directly (parent already held full SPEC content, authored it this session) rather than via subagent.
- (decision) Added Section 3.15 (AI Prompt-Based Custom Event Filter): on-demand single-shot prompt-to-filter, locked vocabulary (account/type/category/keyword/date-range/day-of-week/location-proximity-or-region/venueType/isFree), deterministic template-rendered summary as sole transparency layer (no edit UI ever), 'My AI Filters' list page, FilterHub icon-trigger + collapsed-summary-row integration. Documented rejected alternatives (PostGIS, general/open chatbot, full 'AI mode' toggle) inline via a new '> Considered and rejected' callout, extending the doc's existing '> Deferred to implementation' callout convention. Tied to the existing Engagement goal (Section 2) via intro clause, not a new Section 2 bullet -- same pattern used for the Widget/Acquisition tie-in.
- (decision) Formalized EventFilterInput (Section 4.18, new) as the shared filter shape for Discovery, Widget (4.16, filters field migrated off Record<string, unknown>), and AI Event Filters. Includes DateRangeFilter/DateAnchor/DateOffsetUnit/DayOfWeek (closed date grammar) and LocationFilter (proximity or adminArea region mode). Placed as 4.18 (appended after 4.17 EmbedDomain) rather than renumbering earlier interfaces, to avoid cascading cross-reference updates elsewhere in the doc.
- (decision) Added AIEventFilter (Section 4.19, new): the persisted saved-filter entity for 'My AI Filters', with a caveats: string[] field. Design call (not explicit in the source SPEC, which describes WHAT not schema HOW): caveats must be stored per-record, not recomputed at render time, because an out-of-vocabulary prompt fragment (e.g. a price threshold) has no representation in resolvedFilter once parsed -- without storing it, a reloaded saved filter would silently lose the caveat the user originally saw. Reconciles the SPEC's 'live-rendered, never frozen' summary requirement (the rendering is still deterministic/live from stored state) with 'never silently dropped' (the caveat text itself must persist somewhere).
- (decision) Extended LocationDetails (4.3) with city, province, adminArea, venueType. While doing so, fixed a pre-existing PRD/code drift flagged in brownfield.md: city/province were already returned by geoapify-client.ts since Story 2.4b/2.5a but never documented on this interface -- added them alongside the two genuinely new fields rather than compounding the drift.
- (decision) Added Section 8.4 (Recurring AI Filter Digests) and 8.5 (Cross-Currency Price-Threshold Filtering) to Post-MVP, capturing the v2 backlog items from the forge session (recurring digest scheduler/quota fan-out; global numeric ticketPrice normalization + FX-rate source).
- (event) Ran Reviewer Gate (rubric walker only, finalize_reviewers is empty for this project) scoped to the new/changed content (review-rubric-ai-filter.md). Verdict: Decision-readiness thin, Substance adequate, Strategic coherence strong, Done-ness clarity broken, Scope honesty thin, Downstream usability thin, Shape fit adequate-with-caveat. 3 high, 2 medium, 4 low findings.
- (decision) Confirmed with user: AI filter prompt extraction is BYOK-gated (contributing_user with a saved Gemini key only; no system-key fallback, no unauthenticated/free_user access) -- consistent with Section 3.7/3.11's existing BYOK-only stance for every other Gemini call except the narrowly-scoped Default Location inference carve-out. Added as new 'Access and Funding' bullet in 3.15.
- (decision) Fixed [high] flagship example unresolvable by schema: swapped 'this weekend' for 'next week' (expressible via {THIS_WEEK,+1,WEEK}); added a Deferred-to-implementation callout noting compound-day phrasing like 'this weekend' isn't expressible in the deliberately-closed v1 date grammar and correctly degrades to a caveat via the general degrade mechanism, same as any other unsupported fragment.
- (decision) Fixed [high] proximity-mode had no stated extraction mechanism: clarified in 3.15's Locked Filter Vocabulary bullet that EventFilterInput's proximity-radius field is NOT prompt-extracted (a prompt can't productively set precise coordinates) -- it stays populated only via FilterHub's existing manual location control (Section 3.3), same as today. Only region/admin-area text match is genuinely AI-extracted from the prompt. This matches what filter-vocabulary.md (the spec companion) already said; the PRD prose had overstated it.
- (decision) Fixed [medium] Widget/EventFilterInput scope mismatch: added a clause to 3.14 stating the widget generator UI exposes only its pre-existing filter subset for MVP even though Widget.filters now types as the fuller EventFilterInput -- the new fields (dateRange/dayOfWeek/venueType/isFree) are reserved for future exposure, not a scope expansion of Epic 6's widget generator.
- (decision) Fixed [medium] FilterHub used as an undefined proper noun: named it explicitly in Section 3.1 ('referred to elsewhere in this document as FilterHub') where the control it refers to is actually described.
- (change) Fixed 3 low findings: corrected 'Section 3.2' cross-ref to '3.2 and 3.7' in 3.15's Saving and Reuse bullet (Subscriptions live in 3.7, not 3.2); added a doc-comment on EventFilterInput.dateRange stating it composes (AND) with dayOfWeek; tied 3.15's prompt-resolution wait state to Section 3.12's existing blocking-overlay pattern.
- (event) Polish pass (structure + prose) applied to new/changed content only, per doc_standards. Structural: split 3.15's overloaded 'Locked Filter Vocabulary' bullet into two (vocabulary + a new 'Degrading Gracefully' bullet), separated the two stacked blockquote callouts into distinct blocks, tightened a doubled Section 3.7 citation. Prose: 8 fixes (dangling modifier in Access and Funding, redundant clause in 3.14, awkward Widget cross-ref phrasing, missing relative pronoun, slash-shorthand expanded, indirect 'data means' construction reworded, FilterHub/Discovery terminology unified across 4.16 and 4.18, choppy caveats doc-comment phrasing). Full findings/edits: review-rubric-ai-filter.md plus this session's conversation.
- (event) PRD amendment finalized (status remains 'final', updated bumped to 2026-08-28). epics.md FR-list backfill for this capability deliberately NOT done in this pass, matching the established pattern from prior amendments -- flagged as a follow-up, not silently skipped. No sprint-status.yaml or project-context.md changes made -- this pass was scoped to prd.md only per user's explicit ask; project-context.md's Soft-Delete Convention table should add AIEventFilter alongside the other AD-8 tables when that follow-up work happens. Next: bmad-create-epics-and-stories (or a targeted epics.md amendment) to turn Section 3.15's 5 capabilities into FRs and backlog stories.
- (decision) Added 5 new EventCategory values (Section 4.1): TECHNOLOGY, TRAVEL_AND_TOURISM, EDUCATION, CAREER, AUTOMOTIVE. Found while auditing apify-runs scraped-post samples for AI-extraction prompt improvements: confirmed real gap for academic-competition events (Chaanakya Ekadanta Olympiad, KOMNAS -- 22% of sampled captions, dated/scheduled, no existing category fit) plus user-specified gaps (hackathon, career fair, automotive fair) and reclassified two PROMOTION-type tenant-ad posts (gaming gadget, travel luggage) as legitimate promo events needing TECHNOLOGY/TRAVEL_AND_TOURISM categories rather than being dismissed as non-events. Implemented end-to-end following the exact precedent of spec-religion-and-spirituality-category.md: shared-types enum, PRD 4.1, events.graphql, Drizzle pgEnum + migration 0038 (ALTER TYPE ADD VALUE IF NOT EXISTS ... BEFORE OTHER), en/id locale keys, GraphQL codegen regenerated (backend+web). AI-extraction needed no direct edit -- build-gemini-request.ts derives its allowed-category list from Object.values(EventCategory) programmatically. All affected test suites (domain, backend 267 tests, web 278 tests) pass; build clean.
- (event) Implemented sprint-change-proposal-2026-08-28.md Items 1-3 end-to-end (Item 4, EventCategory additions, already shipped separately in commit f4ffc5c). Item 1 (confidence gate): Gemini location-inference prompt now returns a confidence score (packages/domain/src/scraper/account-enrichment.ts); LOCATION_INFERENCE_CONFIDENCE_THRESHOLD env var (default 0.5); DefaultLocationChangeStatus gained AWAITING_APPROVAL/REJECTED (migration 0040, hand-adjusted ALTER TYPE ... BEFORE per established precedent); applyDefaultLocationChange branches to AWAITING_APPROVAL without touching defaultLocation when confidence is low; resolveDefaultLocationChange mutation gained APPROVE/REJECT actions alongside existing ACCEPT/REVERT. Item 2 (badge): new moderatorPendingItemCount GraphQL query (combined pending-reports + pending-location-changes count); new CountBadge UI primitive; wired into UserMenu.tsx (next to Moderator Items entry) and AppShell.tsx (avatar overlay when menu closed), polled every 60s via react-query refetchInterval -- first such polling-interval precedent in this codebase. Item 3 (hashtag search): posts.hashtags column + GIN index (migration 0039, hand-adjusted since installed drizzle-kit 0.21.4 doesn't serialize .using() into its snapshot/generator, same class of gap as AD-8 rule 3); hashtag capture through the full Instagram/Apify scraper pipeline (instagram-adapter.ts, persist-scraped-post.ts, process-apify-async-result.ts, replay-actor-run.ts), lowercased for case-insensitive exact match; buildEventsQueryCondition.ts routes a #-prefixed search to hashtags via the existing array-overlap 'in' operator (no new drizzle-where.ts operator needed).
- (note) Deferred, flagged not silently skipped: no email notification wired for a new AWAITING_APPROVAL item (unlike the existing PENDING_REVIEW moderator-alert email) -- the Moderator Pending-Item Badge (Item 2) covers ongoing visibility instead; a dedicated email would need new copy distinguishing 'needs your decision' from the existing 'already applied, FYI' template. TODO left in apply-default-location-change.ts.
- (event) Verification: full build (shared-types/domain/database/ui/backend/web) clean. Tests: domain 166/166, ui 309/309, web 278/278, backend 265/267 -- the 2 backend failures (subscriptions.test.ts 'mySubscriptions pendingExtractionCount', a unique-constraint collision) confirmed pre-existing and unrelated via git stash + isolated re-run against clean master before restoring this work. Required a manual 'pnpm migrate' in packages/database to apply migrations 0038-0040 to the local dev DB before backend integration tests would pass -- not run automatically by the test script.
