[workflow]

# Overrides merge per BMad structural rules: scalars override, arrays append.

activation_steps_prepend = []
activation_steps_append = []

persistent_facts = [
  "Token-efficiency: Step 2 ('Load the full epic scope') instructs loading project-context.md and docs/infrastructure/index.md — both are already loaded via this file's own persistent_facts at activation. Do not re-read them a second time in Step 2; only load the architecture spine (festgrid-architecture-spine.md, not a persistent fact) and any specific docs/infrastructure/ shards the epic's scope plausibly touches, which are the genuinely new reads at that step.",
  "Token-efficiency: when Step 3 dispatches the single Winston (bmad-agent-architect) subagent to evaluate Gate 1 and Gate 3, explicitly inline the relevant excerpts from project-context.md's Technology Stack/Critical Implementation Rules and the architecture spine's AD entries (already loaded at activation and in Step 2) directly into the subagent's prompt, in addition to the epic's story ACs and cross-epic references. Both gates' heuristics require cross-checking against these rules (e.g. 'a named reusable utility mandated in project-context.md', 'infra with no IaC/deploy story') — without inlining them, the subagent is likely to re-read those same files itself from a cold context, duplicating a read the orchestrator already paid for.",
]

on_complete = ""
