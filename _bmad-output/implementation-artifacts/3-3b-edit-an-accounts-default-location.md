---
baseline_commit: d2afee54b15b519cb78db05f7ac93fdf31d3829f
---
# Story 3.3b: Edit an account's default location

## Story Details

- Epic: 3
- Story ID: 3.3b
- Story Key: 3-3b-edit-an-accounts-default-location
- Status: review (AC15-19 amendment, AD-11 moderator override; AC1-14 already delivered)

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a user,
I want to be able to view and edit the default location already set for a social media account I'm subscribed to,
so that I can correct or update it if it's wrong or outdated.

## Acceptance Criteria

1. **Given** I am on `/settings/subscriptions` (Story 3.2/3.3) and one of my active subscriptions' account already has a `SocialMediaAccountProfile.defaultLocation` set (Story 3.3), **when** the row renders, **then** the formatted location is shown along with an edit action (pencil/"Edit" affordance) — not the read-only-only display Story 3.3 shipped. [epics.md AC, EXPERIENCE.md "Default Location for a Subscribed Account"]
2. **And** clicking the edit action opens Story 3.3's `SetDefaultLocationDialog` in a new "edit mode" — the same `LocationPickerField`/`MapPickerSheet` experience Story 3.3 already built, reused rather than reinvented — prefilled with the account's current formatted address and coordinates. [epics.md AC, user-confirmed 2026-08-08: extend the existing dialog rather than build a literal in-table inline form]
3. **And** confirming a new location persists it onto `SocialMediaAccountProfile.defaultLocation` (never onto the `Subscription` row) via a new `editAccountDefaultLocation(accountId: ID!, input: SetAccountDefaultLocationInput!): SocialMediaAccountProfile!` mutation — **built and owned by this story**, backend-GraphQL-only (never a direct DB write from `apps/web`), reusing Story 3.3's `SetAccountDefaultLocationInput` (no new input type — same `placeId`/`latitude`/`longitude` shape), `resolveLocationInputMode`/Story 0.16's Geolocation adapter, and `formatLocationDetails`, exactly as `setAccountDefaultLocation` already does. [epics.md AC]
4. **And** the change is persisted to `SocialMediaAccountProfile.defaultLocation` immediately — there is no pre-approval gate blocking the save (FR66). [epics.md AC]
5. **And** the edit is performed via a backend GraphQL mutation, guarded by `requireAuth` (Story 0.17) — not a direct database write from `apps/web`. [epics.md AC]
6. **And** confirming the caller is an active subscriber of this account (to authorize the edit) uses `activeOnly(table)` (Story 0.22), not a hand-written `isNull(...)` clause — mirroring `setAccountDefaultLocation`'s existing check. [epics.md AC]
7. **And** the mutation rejects with a `GraphQLError` (`extensions.code = 'INVALID_STATE_TRANSITION'`) if the account has no `defaultLocation` set yet — editing requires an existing value; the first-time-set path is Story 3.3's `setAccountDefaultLocation`, not this mutation. [Derived — symmetric guard to `setAccountDefaultLocation`'s existing "already set" rejection]
8. **And** every edit inserts a new `DefaultLocationChangeRequest` row (PRD §4.14) capturing `accountId`, `changedByUserId` (the editing user), `previousLocation` (the account's `defaultLocation` immediately before this edit), `newLocation`, and `status: PENDING_REVIEW` — **regardless of whether an earlier edit on the same account is still `PENDING_REVIEW`** (stacking is allowed; no server-side guard blocks a second edit while a review is pending). [epics.md AC; user-confirmed 2026-08-08: allow stacking rather than blocking concurrent edits — see Dev Notes → Forward Note for Story 4.7 for the moderator-queue dedup contract this creates]
9. **And** saving the change triggers a best-effort email notification to every user with `role = 'moderator'` (`users` table, Story 0.17's role model), per FR67 — reusing Story 0.15's existing `sendTemplatedEmail` outbound email adapter (already `done`, already anticipates "moderator alerts" as a consumer per its own Note) via a new `DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT` template key, rather than introducing a new email pathway. A failed send (e.g. one moderator's address bounces) must never fail the mutation or block the save (FR66's "no pre-approval gate" extends to notification delivery too). [epics.md AC]
10. **And** every subscriber of this account will see the new `defaultLocation` applied to subsequent extractions, since the field is account-level, not per-subscriber (Story 3.3's amendment) — no change needed beyond what this story's mutation already does. [epics.md AC]
11. **And** `SocialMediaAccountProfile.hasPendingDefaultLocationReview: Boolean!` is exposed (a new computed field, resolved via an existence check against `DefaultLocationChangeRequest` rows with `status = PENDING_REVIEW` for that account) and selected by `getMySubscriptions` (Story 3.2/3.3's query) so `/settings/subscriptions` can render a "Pending Review" badge on the row, matching `design-artifacts/UX-festgrid-run-1/EXPERIENCE.md`'s "Default Location Pending Review" state pattern. [user-confirmed 2026-08-08 — absorbed into this story rather than left as a gap; see Dev Notes → Architecture & UX Gate Findings]
12. **And** all user-facing strings (edit action label, edit dialog title, "Pending Review" badge text, success/error toasts) are sourced through next-intl from Story 3.2/3.3's existing `SubscriptionsPage` namespace, with new entries added to both `apps/web/locales/en.json` and `apps/web/locales/id.json`. [project-context.md i18n rule, persistent fact]
13. **And** the save action is wrapped in `BlockingLoader` (a critical, persisted mutation), per `project-context.md`'s Loaders rule — matching Story 3.3's existing dialog behavior. [persistent fact]
14. **And** a `subscription_default_location_edited` (`{ accountId }`) PostHog analytics event fires on successful save. [persistent fact — AD-5]

### Amendment (2026-08-24) — AD-11 Moderator Override on Subscriber-Scoped Mutations

Added via `sprint-change-proposal-2026-08-24-moderator-location-override.md`, approved. Story 3.3b's original build (AC1-14 above) is already shipped on `master` (`review` status) and is otherwise unaffected — AC15-19 below are additive.

15. **And** `editAccountDefaultLocation`'s authorization accepts EITHER the existing active-subscription check (AC6, unchanged) OR the caller holding the `moderator` role (`AuthenticatedUser.role`, AD-7 rule 3/4) — either path alone is sufficient, not both required. The subscriber path's existing behavior (queries run, error codes/messages, response shape) is byte-for-byte unchanged, and every pre-existing test in `social-media-accounts.test.ts` for this mutation continues to pass unmodified. [PRD §3.7 "Moderator Override" (amended 2026-08-24); festgrid-architecture-spine.md AD-11 rule 1]
16. **And** when the caller is authorized via the moderator path (not via an active subscription), the inserted `DefaultLocationChangeRequest` is written with `changeSource: 'MODERATOR'` and `status: 'ACCEPTED'` (never `PENDING_REVIEW`), with `reviewedByModeratorId`/`reviewedAt` populated at insert time — the moderator's own edit *is* the review, requiring no further action from anyone, including themselves. The subscriber path (AC15) continues to insert `status: 'PENDING_REVIEW'`, now with an explicit `changeSource: 'USER'` (previously implicit). [PRD §3.7/§4.14 (amended 2026-08-24); AD-11 rule 2]
17. **And** on every successful call to `editAccountDefaultLocation` — regardless of `changeSource` — every other `DefaultLocationChangeRequest` for the same `accountId` still at `status: 'PENDING_REVIEW'` (excluding the row this call itself just inserted) is updated to a new terminal `status: 'SUPERSEDED'` (not actionable, never re-enterable). This is not moderator-specific: it fixes a pre-existing gap where `editAccountDefaultLocation` had no de-duplication and a subscriber alone could already stack multiple `PENDING_REVIEW` rows for one account — confirmed live by `apps/web/.../moderator/items/moderator-items-content.tsx`, which renders every pending row with no grouping/dedupe logic today. Because `SocialMediaAccountProfile.hasPendingDefaultLocationReview` (AC11) and `pendingDefaultLocationChanges` (Story 4.7) both already filter on `status = 'PENDING_REVIEW'`, superseding stale rows removes them from both surfaces automatically, with no change needed to either resolver. [PRD §4.14 (amended 2026-08-24); AD-11 rule 3]
18. **And** the account-row location display, "Pending Review" badge, and edit-pencil trigger currently hand-built inline in `subscriptions-content.tsx` (the `sub.account.defaultLocation` true-branch) are extracted into a new, presentational, framework-agnostic `AccountLocationField` component at `packages/ui/src/features/locations/AccountLocationField.tsx` — accepting `location`, `isPendingReview`, `onEdit`, and a typed `labels` prop (no `next-intl`/react-query/generated-GraphQL import, mirroring `LocationPickerField`'s existing pattern) — using `StatusBadge`'s existing `pendingReview` variant instead of the current hand-styled yellow `<span>`. `subscriptions-content.tsx` renders this component in place of its inline markup with no visible/behavioral change. `SetDefaultLocationDialog` needs no changes (already generic via `accountId`/`mode`/`initialLocation`). Wiring this component into `/moderator/items` is Story 4.7's own separate amendment — out of scope here. [design-artifacts/UX-festgrid-run-1/EXPERIENCE.md "Account Location Field" (added 2026-08-24); Gate 2 run fresh — no split required, see Dev Notes]
19. **And** `/settings/subscriptions`'s call site continues to always invoke `editAccountDefaultLocation` through the ordinary subscriber flow — it performs no role check and never produces `changeSource: MODERATOR`/self-resolved semantics, even when the logged-in user happens to hold the `moderator` role. Which mutation semantics apply is decided by which page/call-site is calling, not by the caller's role — `AccountLocationField` (AC18) itself contains no role-branching logic; only Story 4.7's own `/moderator/items` call site (not built here) is wired to the moderator path. [design-artifacts/UX-festgrid-run-1/EXPERIENCE.md "Account Location Field" — "Review-required vs. self-resolved is decided by the page, not the viewer's role"]

## Tasks / Subtasks

- [x] **Task 1: Database — `default_location_change_requests` table** (AC: 8)
  - [x] **Sequencing check (Pre-Coding Approval Gate item):** confirm Story 3.3 (`setAccountDefaultLocation`, `social_media_account_profiles.default_location`) has landed before starting — this story's edit mutation depends on it.
  - [x] Add to `packages/database/schema.ts`: a `defaultLocationChangeStatusEnum = pgEnum('default_location_change_status', ['PENDING_REVIEW', 'ACCEPTED', 'REVERTED'])` (matching PRD §4.14's `DefaultLocationChangeStatus` enum exactly) and a `defaultLocationChangeRequests` table: `id` (uuid pk), `accountId` (uuid, FK to `social_media_account_profiles.id`, not null), `changedByUserId` (uuid, FK to `users.id`, not null), `previousLocation` (jsonb, `LocationDetails`, nullable — always populated in practice by this story's flow since AC7 requires an existing value, but left nullable to match the PRD interface's `previousLocation?`), `newLocation` (jsonb, `LocationDetails`, not null), `status` (the new enum, default `PENDING_REVIEW`, not null), `reviewedByModeratorId` (uuid, FK to `users.id`, nullable), `reviewedAt` (timestamptz, nullable), `createdAt` (timestamptz, `defaultNow()`, not null). No `deletedAt`/soft-delete column — this table is explicitly excluded from AD-8's soft-delete binding (an audit/moderation-queue record, not user-owned data) and no `updatedAt` — the PRD interface only defines `createdAt`/`reviewedAt`.
  - [x] Add a composite index `index('idx_default_location_change_requests_account_status').on(t.accountId, t.status)` — supports both this story's `hasPendingDefaultLocationReview` existence check and Story 4.7's future moderator-queue listing. A plain composite index, **not** a partial index scoped to `WHERE status = 'PENDING_REVIEW'` — AD-8 rule 3's documented `drizzle-kit` partial-index bug only needs to be worked around for AD-8-*bound* soft-delete tables; this table isn't one, so the simpler non-partial index avoids that workaround entirely.
  - [x] Add `defaultLocationChangeRequestsRelations` (`accountProfile: one(socialMediaAccountProfiles, ...)`, `changedByUser: one(users, { fields: [changedByUserId] })`) following the existing relations pattern in `schema.ts`.
  - [x] Run `pnpm --filter database run generate` (drizzle-kit) to produce the migration SQL file; commit it per AD-3.
  - [x] **Amendment (2026-08-24, AD-11):** add `SUPERSEDED` to `defaultLocationChangeStatusEnum` (schema.ts:32) → `pgEnum('default_location_change_status', ['PENDING_REVIEW', 'ACCEPTED', 'REVERTED', 'SUPERSEDED'])` (AC17). Also add a new `defaultLocationChangeSourceEnum = pgEnum('default_location_change_source', ['USER', 'AI_INFERENCE', 'MODERATOR'])` and a `changeSource` column on `defaultLocationChangeRequests`: `changeSource: defaultLocationChangeSourceEnum('change_source').default('USER').notNull()` (AC16). **Sequencing note (verified against current code, not assumed):** neither `changeSource` nor any `AI_INFERENCE` handling exists anywhere in the codebase today — Story 3.4m (PRD §4.14's originally-intended owner of this column) is still `ready-for-dev`, not started, while this story is further along (`review`, being amended); this amendment is therefore the one that actually introduces the column first. Include all three ratified enum values now so Story 3.4m does not need to re-touch the enum later, but do **not** relax `changedByUserId`'s `NOT NULL` constraint — `AI_INFERENCE` rows (no human actor) need it nullable, and that remains Story 3.4m's own responsibility when it's implemented, not this story's.
  - [x] Re-run `pnpm --filter database run generate` to produce the new migration SQL (new enum value, new enum type, new column); commit per AD-3.
- [x] **Task 2: Backend — `editAccountDefaultLocation` schema and resolver** (AC: 3, 4, 5, 6, 7, 8)
  - [x] Extend `apps/backend/src/schema/social-media-accounts.graphql` (Story 3.1a/3.3's file): add `hasPendingDefaultLocationReview: Boolean!` directly into the existing `type SocialMediaAccountProfile { ... }` block (AC11), and `extend type Mutation { editAccountDefaultLocation(accountId: ID!, input: SetAccountDefaultLocationInput!): SocialMediaAccountProfile! }` — reuses Story 3.3's `SetAccountDefaultLocationInput` as-is, no new input type.
  - [x] Run `pnpm --filter backend run codegen` to regenerate `apps/backend/src/generated/resolvers-types.ts`.
  - [x] In `apps/backend/src/schema/resolvers.ts`, add `editAccountDefaultLocation` to the `Mutation` map, mirroring `setAccountDefaultLocation`'s structure (Story 3.3, same file): `requireAuth(context)`; look up the caller's active subscription via `activeOnly(subscriptions)` scoped to `eq(subscriptions.userId, authUser.userId)` and `eq(subscriptions.accountId, accountId)` — throw `NOT_FOUND` if none; look up the `social_media_account_profiles` row — throw `NOT_FOUND` if absent; if `defaultLocation` is `null`, throw `GraphQLError('No default location set yet', { extensions: { code: 'INVALID_STATE_TRANSITION' } })` (AC7); capture `previousLocation = profile.defaultLocation` before overwriting; call `resolveLocationInputMode(input)`/`resolveLocation(...)` exactly as `setAccountDefaultLocation` does to produce `newLocation`; `UPDATE social_media_account_profiles SET default_location = newLocation` (AC4); `INSERT INTO default_location_change_requests` with `{ accountId, changedByUserId: authUser.userId, previousLocation, newLocation, status: 'PENDING_REVIEW' }` — no pre-insert check for an existing pending row (AC8, stacking allowed); return the updated profile via `buildOptimizedDrizzleSelect(socialMediaAccountProfiles, info)` + `formatLocationDetails`, matching `setAccountDefaultLocation`'s return shape.
  - [x] After the insert (Task 2's resolver, same function): query `db.select().from(users).where(eq(users.role, 'moderator'))`; for each moderator, call `sendTemplatedEmail('DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT', moderator.email, { accountDisplayName, previousLocationText, newLocationText, moderatorReviewUrl })` (Task 3) wrapped so a rejected promise is caught and logged (`console.error`), never re-thrown — use `Promise.allSettled` across all moderators so one failure doesn't stop the others, and the overall notification step never blocks or fails the mutation's own return (AC9).
  - [x] Add a `SocialMediaAccountProfile: { hasPendingDefaultLocationReview: async (parent) => {...} }` resolver map entry (a new top-level key in the `resolvers` export, alongside the existing `Subscription: {...}` entry): `SELECT id FROM default_location_change_requests WHERE account_id = parent.id AND status = 'PENDING_REVIEW' LIMIT 1`, return `rows.length > 0` (AC11). `buildOptimizedDrizzleSelect` already safely ignores this field when building `social_media_account_profiles` selects (it only maps GraphQL field names that match real Drizzle columns), so no change needed there.
  - [x] Create/extend `apps/backend/src/schema/social-media-accounts.test.ts` (Story 3.3's file) covering: happy path via `placeId`; happy path via `latitude`/`longitude`; rejects with `INVALID_STATE_TRANSITION` when `defaultLocation` is not yet set; rejects with `NOT_FOUND` when the caller has no active subscription to the account; rejects with `NOT_FOUND` for a non-existent `accountId`; requires authentication; a `default_location_change_requests` row is inserted with the correct `previousLocation`/`newLocation`/`status: PENDING_REVIEW`; a second edit while an earlier request is still `PENDING_REVIEW` succeeds and inserts a *second* row rather than being rejected (AC8); moderator emails are attempted for every `role = 'moderator'` user and a rejected `sendTemplatedEmail` call does not fail the mutation (mock `sendTemplatedEmail` to reject once and assert the mutation still resolves).
  - [x] Add a unit/integration test for the new `SocialMediaAccountProfile.hasPendingDefaultLocationReview` resolver: `true` when a `PENDING_REVIEW` row exists for the account, `false` when none exists.
  - [x] **Amendment (2026-08-24, AD-11) — auth branch (AC15):** replace the unconditional active-subscription check with: call `requireAuth(context)` first (unchanged — both paths require login); compute `const isModerator = authUser.role === 'moderator'` (mirrors the existing `context.user?.role === 'moderator'` pattern already used at `resolvers.ts:2629`/`2704` for the `event`/`eventBySlug` resolvers — do **not** call `requireModerator(context)` directly here, since that throws for non-moderators before the subscription check ever runs, which would break the "either path satisfies" requirement); only run the existing active-subscription lookup/`NOT_FOUND` check when `!isModerator`. All other guards (`NOT_FOUND` account lookup, `INVALID_STATE_TRANSITION` when `defaultLocation` is null, `resolveLocationInputMode`/`resolveLocation`) run unchanged for both paths.
  - [x] **Amendment — self-resolved insert (AC16):** branch the `default_location_change_requests` insert on `isModerator`: moderator → `{ ..., status: 'ACCEPTED', changeSource: 'MODERATOR', reviewedByModeratorId: authUser.userId, reviewedAt: new Date() }`; subscriber (existing path) → `{ ..., status: 'PENDING_REVIEW', changeSource: 'USER' }` (now explicit). Capture the inserted row's `id` via `.returning({ id: defaultLocationChangeRequests.id })` — needed by the next step.
  - [x] **Amendment — supersede-on-write (AC17):** immediately after the insert above, run `db.update(defaultLocationChangeRequests).set({ status: 'SUPERSEDED' }).where(and(eq(defaultLocationChangeRequests.accountId, accountId), eq(defaultLocationChangeRequests.status, 'PENDING_REVIEW'), ne(defaultLocationChangeRequests.id, <just-inserted row's id>)))`. The `ne(id, ...)` exclusion is required so the row just inserted (subscriber path, itself `PENDING_REVIEW`) is never immediately superseded by its own write; harmless no-op for the moderator path (its own row is never `PENDING_REVIEW`).
  - [x] **Amendment — GraphQL/DB status parity:** add `SUPERSEDED` to `default-location-change-requests.graphql`'s `DefaultLocationChangeRequestStatus` enum (Story 4.7's file) so it stays in sync with the DB enum, even though no resolver in this story's own scope currently returns a `SUPERSEDED` row through it (`pendingDefaultLocationChanges` already filters to `PENDING_REVIEW` only — this closes the moderator-queue dedupe gap automatically, with no change needed to that resolver or to `moderator-items-content.tsx`). Run `pnpm --filter backend run codegen`.
  - [x] **Amendment — tests:** extend the `editAccountDefaultLocation` test block with: a moderator caller with **no** active subscription to the account still succeeds (AC15); the inserted row for a moderator caller has `status: 'ACCEPTED'`, `changeSource: 'MODERATOR'`, `reviewedByModeratorId`/`reviewedAt` populated (AC16); a non-moderator, non-subscriber caller is still rejected with `NOT_FOUND` exactly as before (AC15 regression); a second successful edit (by a subscriber or a moderator) marks a prior still-`PENDING_REVIEW` row for the same account `SUPERSEDED`, while an already-`ACCEPTED`/`REVERTED` row and rows on other accounts are left untouched (AC17); every pre-existing test in this block (auth requirement, `INVALID_STATE_TRANSITION`, subscriber happy path, stacking) is re-run unmodified and still passes (AC15 regression guard).
- [x] **Task 3: Backend — email template** (AC: 9)
  - [x] Add `DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT` to `packages/domain/src/email/types.ts`'s `EmailTemplateKey` union and `EmailTemplateVariables` (`{ accountDisplayName: string; previousLocationText: string; newLocationText: string; moderatorReviewUrl: string }`).
  - [x] Add the matching entry to `packages/domain/src/email/templates.ts`'s `EMAIL_TEMPLATES`, mirroring `DANGEROUS_EVENT_MODERATOR_ALERT`'s structure/tone exactly (subject referencing the account, html/text bodies showing the previous → new location change and a link to `{{moderatorReviewUrl}}`).
  - [x] Extend `packages/domain/src/email/render-template.test.ts` with a case for the new template key (interpolation only — matches the existing per-key test pattern).
- [x] **Task 4: Backend — `WEB_APP_BASE_URL` env var for `moderatorReviewUrl`** (AC: 9)
  - [x] Add `webAppBaseUrl: string` to `apps/backend/src/env.ts`'s `BackendEnv` interface and `loadBackendEnv()`, sourced from `process.env.WEB_APP_BASE_URL`, defaulting to `'http://localhost:3000'` when unset (mirrors `geminiModel`'s default-value pattern in the same file — this is the first consumer of `sendTemplatedEmail` in real resolver code, so no base-URL env var exists yet anywhere in the backend).
  - [x] Add `WEB_APP_BASE_URL` to `.env.example` with a comment noting it's used to build the moderator-review link in outbound emails (e.g. `WEB_APP_BASE_URL=https://festdaily.app` for production).
  - [x] In the resolver (Task 2), build `moderatorReviewUrl` as `` `${loadBackendEnv().webAppBaseUrl}/moderator/items` `` — the same route named in `design-artifacts/UX-festgrid-run-1/EXPERIENCE.md` (`/moderator/items`, Story 4.7, not yet built) that `DANGEROUS_EVENT_MODERATOR_ALERT` already points to by convention.
- [x] **Task 5: Frontend — GraphQL query/mutation and codegen** (AC: 3, 11)
  - [x] Extend `apps/web/src/features/subscriptions/queries.graphql`'s `getMySubscriptions` operation (Story 3.2/3.3's file): add `hasPendingDefaultLocationReview` inside the existing `account { ... }` selection, alongside `defaultLocation`.
  - [x] Add to `apps/web/src/features/subscriptions/mutations.graphql` (Story 3.2/3.3's file):
    ```graphql
    mutation editAccountDefaultLocation($accountId: ID!, $input: SetAccountDefaultLocationInput!) {
      editAccountDefaultLocation(accountId: $accountId, input: $input) {
        id
        defaultLocation {
          coordinates {
            lat
            lng
          }
          formattedAddress
          placeName
        }
        hasPendingDefaultLocationReview
      }
    }
    ```
  - [x] Run `pnpm --filter web run codegen` to regenerate `apps/web/src/generated/graphql.ts` with the updated `GetMySubscriptionsQuery` shape and the new `useEditAccountDefaultLocationMutation` hook.
- [x] **Task 6: Frontend — extend `SetDefaultLocationDialog` with an edit mode** (AC: 1, 2, 3, 4, 11, 13, 14)
  - [x] Extend `apps/web/src/app/[locale]/settings/subscriptions/set-default-location-dialog.tsx` (Story 3.3's file) with a `mode: 'set' | 'edit'` prop (default `'set'`, preserving Story 3.3's existing behavior/call site unchanged) and an optional `initialLocation?: LocationDetails` prop (required when `mode === 'edit'`).
  - [x] When `mode === 'edit'` and the dialog opens: initialize `addressSearch` from `initialLocation.formattedAddress` (or `placeName` as fallback) and `pendingCoords`/`mapViewState` from `initialLocation.coordinates` — reusing the exact same `resolvedPreview`/`LocationPickerField` rendering path Story 3.3 already built for the coordinate-capture flow (AC2), rather than a separate prefill code path. The user can re-search/re-pick to change the value, or submit unchanged.
  - [x] Branch the submit handler on `mode`: `'set'` calls `useSetAccountDefaultLocationMutation` (unchanged, Story 3.3); `'edit'` calls the new `useEditAccountDefaultLocationMutation` (Task 5) with the same `accountId`/`input` shape (AC3).
  - [x] Branch dialog title/success-toast copy on `mode` (Task 8's new i18n keys) — title becomes `t("defaultLocationEditDialogTitle")`, success toast becomes `t("defaultLocationEditedToast")`, which also acknowledges the change was sent for moderator review (matching `EXPERIENCE.md`'s "informative, not alarming" confirmation-copy requirement for the "Default Location Pending Review" pattern).
  - [x] On successful edit-mode save: invalidate/refetch `getMySubscriptions` (so the row's `hasPendingDefaultLocationReview` updates to `true`), fire `subscription_default_location_edited` (`{ accountId }`) via `usePostHog()` (AC14), keep the existing `BlockingLoader` wrapping (AC13, unchanged from Story 3.3).
  - [x] Extend `set-default-location-dialog.test.tsx` (Story 3.3's file): edit-mode prefill renders the existing address/coordinates; edit-mode submit calls `useEditAccountDefaultLocationMutation` (not `useSetAccountDefaultLocationMutation`); edit-mode success shows the edit-specific toast copy and fires `subscription_default_location_edited`.
- [x] **Task 7: Frontend — row edit action and "Pending Review" badge** (AC: 1, 11)
  - [x] Extend `apps/web/src/app/[locale]/settings/subscriptions/subscriptions-content.tsx` (Story 3.2/3.3's file): when `sub.account.defaultLocation` is present, render the formatted value alongside an inline edit action (a small pencil-icon button, matching this app's established inline-row convention — no extracted component, single consumer, trivial state — confirmed no Gate 2 gap) that opens `SetDefaultLocationDialog` with `mode="edit"` and `initialLocation={sub.account.defaultLocation}`. If `sub.account.hasPendingDefaultLocationReview` is `true`, render a "Pending Review" badge next to the location (a plain styled `<span>`, matching the existing inline platform-tag badge already in this file — no new `packages/ui` component, per `EXPERIENCE.md`'s "Default Location Pending Review" state pattern).
  - [x] Track which account's edit dialog is open via a new `editingAccountId` state (parallel to the existing `selectedAccountId` used for the Story 3.3 "set" dialog — the two dialogs stay distinct instances since they're rendered by the same underlying `SetDefaultLocationDialog` component with different `mode`/`accountId`/`initialLocation` props, not merged into one shared piece of state, to keep the "set" and "edit" trigger call sites independent and avoid a stale-`initialLocation` bug if a user closes a "set" flow and immediately opens an "edit" flow for a different row).
  - [x] Extend `subscriptions-content.test.tsx` (Story 3.2/3.3's file): row shows the edit action (not just read-only text) when `account.defaultLocation` is present; row shows a "Pending Review" badge when `account.hasPendingDefaultLocationReview` is `true`, and no badge when `false`; clicking the edit action opens `SetDefaultLocationDialog` with `mode="edit"` and the correct `accountId`/`initialLocation`.
  - [x] **Amendment (2026-08-24, AD-11, AC18):** replace the inline location/badge/edit-button markup (the `sub.account.defaultLocation` true-branch) with `<AccountLocationField location={sub.account.defaultLocation} isPendingReview={!!sub.account.hasPendingDefaultLocationReview} onEdit={() => setEditingAccountId(sub.account.id)} labels={{ editLabel: t("editDefaultLocationLabel"), pendingReviewLabel: t("pendingReviewBadgeLabel") }} />` from `@festgrid/ui` (new Task 10). The `sub.account.defaultLocation` false-branch ("Set Default Location" link) is unchanged, still inline. `editingAccountId` state/dialog wiring (original Story 3.3b build) is unchanged.
  - [x] Update `subscriptions-content.test.tsx`'s existing edit-action/badge assertions to account for the extraction (rendered through `AccountLocationField` now, not raw markup) — the assertions themselves (edit action visible when `defaultLocation` present, badge visible only when `hasPendingDefaultLocationReview`, clicking edit opens the dialog with the right `accountId`/`initialLocation`) are otherwise unchanged.
- [x] **Task 8: i18n — `SubscriptionsPage` namespace additions** (AC: 12)
  - [x] Add to the existing `SubscriptionsPage` object in `apps/web/locales/en.json` (Story 3.2/3.3's namespace): `editDefaultLocationLabel` (edit action's accessible label), `defaultLocationEditDialogTitle`, `defaultLocationEditedToast` (acknowledges the save and that it's pending moderator review, per `EXPERIENCE.md`), `pendingReviewBadgeLabel`.
  - [x] Mirror every new key into `apps/web/locales/id.json` with real Indonesian translations.
  - [x] **Amendment (2026-08-24):** no new keys required — AC18's `AccountLocationField` reuses the existing `editDefaultLocationLabel`/`pendingReviewBadgeLabel` keys, passed in via its `labels` prop from `subscriptions-content.tsx`'s existing `t()` calls.
- [x] **Task 10: Frontend — shared `AccountLocationField` component** (AC: 18) — *Added 2026-08-24, AD-11 amendment*
  - [x] Create `packages/ui/src/features/locations/AccountLocationField.tsx` + `AccountLocationField.types.ts` (mirroring `LocationPickerField`'s existing file pair): props `location: LocationDetails`, `isPendingReview: boolean`, `onEdit: () => void`, `labels: AccountLocationFieldLabels` (`{ editLabel: string; pendingReviewLabel: string }`). Renders the formatted location text (`location.formattedAddress || location.placeName`), a `<StatusBadge variant="pendingReview" label={labels.pendingReviewLabel} />` only when `isPendingReview`, and an icon-only edit button (`Pencil`, `lucide-react`) with `aria-label={labels.editLabel}` calling `onEdit`. No `next-intl`, `react-query`, or generated-GraphQL import — `packages/ui` stays presentational/framework-agnostic, matching `LocationPickerField`'s `labels`-prop pattern.
  - [x] Gate 2 findings (fresh subagent review, no split required — see Dev Notes) to fold into this component regardless: give the edit button an explicit minimum `24x24` CSS-pixel hit area (e.g. `min-h-6 min-w-6 flex items-center justify-center`, replacing the current bare `p-1` wrapping a `h-3.5 w-3.5` icon, which is borderline under the accessibility floor) while keeping the visible icon size unchanged; ensure the badge (a non-interactive `<span>`, via `StatusBadge`) is never a tab stop; keep `aria-label` wording caller-suppliable via `labels.editLabel` rather than hardcoded, so a future consuming page can make it more specific (e.g. including the account's display name) without touching this component.
  - [x] Add `packages/ui/src/core/status-badge.tsx`'s missing `superseded` variant (`"bg-slate-100 dark:bg-slate-900/30 text-slate-800 dark:text-slate-200 px-2 py-0.5 rounded font-medium shrink-0 text-xs"`, matching `DESIGN.md`'s `status_badge.superseded` token added 2026-08-24) to `StatusBadgeProps["variant"]`'s union and the switch statement — not consumed by this story's own UI (nothing in this story's scope displays a superseded request), but keeps the shared primitive in sync with the ratified design token now that `SUPERSEDED` is a real status (Task 1), so Story 4.7's later amendment can consume it without touching this file again.
  - [x] Export `AccountLocationField`/`AccountLocationFieldLabels` from `packages/ui/src/features/locations/index.ts` (already re-exported by `packages/ui/src/index.ts`'s existing `export * from './features/locations'` — no change needed there).
  - [x] Add `AccountLocationField.test.tsx` (mirroring `LocationPickerField.test.tsx`'s conventions): renders the location text; renders the `pendingReview`-variant badge only when `isPendingReview` is `true`; clicking the edit button calls `onEdit`; the edit button exposes the supplied `aria-label`.
- [x] **Task 9: Verification** (AC: all)
  - [x] `pnpm --filter backend run test`, `pnpm --filter web run test` pass, including all new/extended test files, with no regression in existing suites (including Story 3.3's own `social-media-accounts.test.ts`, `set-default-location-dialog.test.tsx`, `subscriptions-content.test.tsx`).
  - [x] `pnpm build` and `pnpm lint` clean at the repo root, including new generated GraphQL types and the new Drizzle migration.
  - [x] Manual smoke check (Completion Notes): as a user subscribed to an account with a default location already set, see the edit action on its row; open it, see the current value prefilled; change it and save — see the row update immediately to the new value and show a "Pending Review" badge; edit it again before any moderator action — confirm the save still succeeds (no error) and a second `default_location_change_requests` row exists for the account; confirm a moderator-role test user's inbox (or local-dev console log per Story 0.15a) shows a `DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT` email for each edit.
  - [x] **Amendment (2026-08-24, AD-11):** also verify `packages/ui`'s new `AccountLocationField.test.tsx` and the updated `status-badge.tsx` pass/build; verify the new moderator-path/self-resolved/supersede backend tests (Task 2 amendment) pass alongside every pre-existing test in `social-media-accounts.test.ts`, unmodified (AC15's regression requirement).
  - [x] Manual smoke check addendum: as a `moderator`-role user with no active subscription to a given account, call `editAccountDefaultLocation` for that account (e.g. via GraphQL Playground/Postman until Story 4.7's own amendment wires a moderator-side UI trigger) — confirm `defaultLocation` updates immediately, the inserted row is `status: ACCEPTED`/`changeSource: MODERATOR` with `reviewedByModeratorId`/`reviewedAt` populated and no further action needed, and any prior `PENDING_REVIEW` row for that account flips to `SUPERSEDED` (confirm it drops off `pendingDefaultLocationChanges` and the "Pending Review" badge disappears from `/settings/subscriptions`).

## Dev Notes

### Architecture & UX Gate Findings

- **Gate 1 & Gate 3:** Sourced from the swept `epic-3-readiness.md` (`swept: true`, re-run 2026-08-07, explicitly lists `3.3b` in `stories_covered`) — no Gate 1 gap (this story writes exclusively through backend GraphQL mutations, never a direct DB/domain write from `apps/web`, consistent with the sweep's explicit confirmation "3.1a/3.2/3.3/3.3b write via backend GraphQL mutations... never a direct DB/domain write from `apps/web`"); no Gate 3 gap (the sweep's `activeOnly(table)` AC correction was already applied directly to Story 3.3b's epics.md entry, and its cross-epic table-dependency check explicitly confirms "Epic 4's Stories 4.7/4.8... correctly declare `Depends on:` back to the specific Epic 3 stories that originate the tables/flags they read (3.1a, 3.2, 3.3a, **3.3b**, 3.5)" — i.e. this story originating the `default_location_change_requests` table for Story 4.7 to later read is already the sweep's expected, correctly-sequenced shape, not a missed gap).
  - **Lightweight guard (fresh, story-specific) — found three things the epic-wide sweep and epics.md's own AC text did not fully anticipate, all resolved with the user via `AskUserQuestion` before this story was drafted:**
    1. **UI trigger pattern.** `EXPERIENCE.md`'s literal user-flow text names an "In-Table Add Form" pattern for the default-location field, but Story 3.3's actual shipped implementation is a modal `Dialog` (`SetDefaultLocationDialog`), not an inline in-table form. Building this story's edit flow to the letter of the UX doc would have produced a visually/interactionally inconsistent pair of "set" vs. "edit" experiences on the same row. **User confirmed:** extend the existing dialog with an edit mode (Tasks 6-7) rather than build a new inline form — consistent with what's actually shipped, no new component.
    2. **"Pending Review" badge.** `EXPERIENCE.md`'s "Default Location Pending Review" state pattern explicitly requires the subscriptions row to show a "Pending Review" badge until a moderator resolves the change — but epics.md's own AC list for Story 3.3b never mentions exposing this via GraphQL or rendering it; only Story 4.7 (moderator-side page) was in scope for the *review* half of the flow. Left unaddressed, the subscriber-facing half of `EXPERIENCE.md`'s own state pattern would have shipped unimplemented. **User confirmed:** absorb it into this story (AC11, Tasks 2/5/7) — it's a small, single-consumer extension of the exact row/query this story is already modifying, not a Gate 2-worthy split (see Gate 2 below) and not Story 4.7's concern (that story owns the moderator's own accept/revert view, not the subscriber's badge).
    3. **Concurrent-edit handling.** Neither epics.md nor `EXPERIENCE.md` specifies what happens if a user edits an account's default location again while an earlier edit's `DefaultLocationChangeRequest` is still `PENDING_REVIEW`. **User confirmed:** allow stacking (AC8) — every edit always inserts a new row, no server-side block — **but** the moderator-facing queue (Story 4.7, out of scope here) must only ever surface one actionable item per account. See "Forward Note for Story 4.7" below for the exact contract this creates, since that story doesn't exist yet and must honor it when built.
  - **A fourth, smaller item resolved without a question (mechanical, no real tradeoff):** Story 0.15's `sendTemplatedEmail` adapter is `done` but has never actually been called from any resolver yet (only from its own adapter tests) — this story is its first real consumer. Confirmed **not** a gap: Story 0.15's own Note already lists "moderator alerts" as an anticipated consumer type, and this story only needs to add one template key (Task 3) plus one small `WEB_APP_BASE_URL` env var (Task 4) to use it — normal feature-scoped work, not a missing foundational layer.
- **Gate 2 (UI Complexity & Reusability):** Run fresh (this story postdates the swept sweep). **Verdict: No gap.** Every UI piece this story adds is either (a) reuse of an already-extracted `packages/ui` component (`LocationPickerField`/`MapPickerSheet`, Story 3.3d) via an existing `apps/web` dialog's new mode, or (b) trivial, single-consumer inline markup matching an already-established inline convention in the same file (`subscriptions-content.tsx`'s existing platform-tag `<span>` badge, the row's existing inline "Set Default Location" trigger). Nothing here is reused across ≥2 places, none of it has non-trivial standalone state — the bar Gate 2 exists to catch (per `story-split-gate.md`'s own definition) isn't met by an edit-mode prop and a conditional badge span.

#### Amendment (2026-08-24) — AD-11 Moderator Override on Subscriber-Scoped Mutations (AC15-19)

Source: `sprint-change-proposal-2026-08-24-moderator-location-override.md` (approved), routed PRD (§3.7/§4.14) → Architecture (AD-11) → UX (`EXPERIENCE.md` "Account Location Field") → this story.

- **Gate 1 & Gate 3 (this amendment):** Sourced from the same swept `epic-3-readiness.md` (`swept: true`, still explicitly lists `3.3b` in `stories_covered`) — the amendment does not change this story's Gate 1/3 posture: the new auth branch is pure application-layer logic inside the same already-approved backend GraphQL resolver (no new API surface, no direct DB/domain access from `apps/web`, no new infra/IaC), and it introduces no new foundational/cross-cutting dependency (it reuses `AuthenticatedUser.role`/AD-7's existing role model verbatim, adds one DB enum/column via the same Drizzle-migration mechanism already used by this story's Task 1, and touches no i18n/analytics/app-shell/codegen foundation beyond what's already established). **Lightweight guard (fresh, amendment-specific):** confirmed no new external service, data entity, or infra dependency was introduced by this amendment beyond what the original sweep already covers — no fresh Gate 1/3 subagent dispatch needed.
- **Gate 2 (UI Complexity & Reusability), run fresh via subagent (Freya persona) against the `AccountLocationField` extraction specifically:** **Verdict: No gap — legitimate task inside this amendment, not a split.** Reasoning: (a) no non-trivial states per Gate 2's own examples — no media, no own loading/error state (parent owns the query), exactly one boolean-driven visual toggle (badge shown/hidden) reusing `StatusBadge`'s already-shipped `pendingReview` variant, not inventing a new one; it doesn't even own the "no location yet" case, which stays inline; (b) not reused across ≥2 places *within this story's own scope* — the second eventual consumer (`/moderator/items`) is explicitly Story 4.7's own separate future amendment, not built here; extracting a single call site into a component shaped for future reuse is normal refactoring hygiene, not the multi-consumer complexity Gate 2 targets; (c) no UX-specified detail is missing from the draft scope — every behavioral detail in `EXPERIENCE.md`'s "Account Location Field" section (props shape, `StatusBadge` reuse, no role-branching, edit-trigger callback) is already reflected in Task 10. **Recommendations folded into Task 10 regardless of the "no gap" verdict:** explicit ≥24×24px hit target on the edit button (current inline markup's bare `p-1` around a `h-3.5 w-3.5` icon is borderline under the accessibility floor); the badge must never be a tab stop; `aria-label` wording stays caller-suppliable via the `labels` prop rather than hardcoded, so a future consumer can make it more specific (e.g. include the account name) without touching the component; a typed `AccountLocationFieldLabels` interface (mirroring `LocationPickerField.types.ts`), not an untyped inline object.
- **Escape-hatch items surfaced during this amendment (documented per `story-split-gate.md`'s Escape Hatch, not silently absorbed or silently changed):**
  1. **AC7's `INVALID_STATE_TRANSITION` guard is *not* relaxed for the moderator path** — it applies identically to both callers. Noticed edge case, out of scope for this amendment's 5 ACs: once Story 3.4m's (not-yet-built) `AI_INFERENCE` path exists, a moderator `REVERT`ing an AI-inferred change on an account that had no prior `defaultLocation` (`previousLocation: null`) would leave `defaultLocation` null again, and this mutation's existing AC7 guard would then block a moderator from directly re-correcting it (they'd need `setAccountDefaultLocation`'s null-accepting semantics instead, which this amendment does not grant moderator access to). Flagged here for whichever story next touches this interaction (likely Story 3.4m or a future 4.7 refinement) — not something this amendment's scope asked for or should silently absorb.
  2. **AC9's moderator-alert email is not suppressed for moderator-sourced (self-resolved) writes.** Neither the sprint-change-proposal, the PRD amendment, nor AD-11 call for suppressing it, so a moderator's own direct correction still triggers the existing best-effort `DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT` email to every `role = 'moderator'` user (including, potentially, the acting moderator themselves) — functions as an audit/visibility notice to other moderators rather than an actionable review request. Left unchanged rather than silently altering AC9's existing behavior; noted here alongside this section's pre-existing "no rate-limiting" item as a candidate future refinement, not a defect.
  3. **The moderator's own `ACCEPTED` status choice for the self-resolved insert (AC16)** was not explicitly spelled out in AD-11/PRD beyond "already resolved, never `PENDING_REVIEW`" — resolved directly (no user question needed, mechanical): `ACCEPTED` mirrors `resolveDefaultLocationChange`'s existing `ACCEPT` branch's field population (`status: 'ACCEPTED'`, `reviewedByModeratorId`, `reviewedAt`), and semantically the moderator is "accepting" the new value they themselves just supplied — `REVERTED` would incorrectly imply restoring a prior value rather than setting a new corrected one.

### Data Type Compatibility & Migration Requirements

- **Compatibility finding: one new table required.** `default_location_change_requests` (PRD §4.14's `DefaultLocationChangeRequest`) does not exist in `packages/database/schema.ts` today — this story creates it (Task 1), matching the project's established precedent of scoping a table to the story/epic that first needs to write it (e.g. Story 3.1a's `social_media_account_profiles`, Story 2.6a's `user_settings`) rather than to Epic 0.
- **Impacted contracts:** new `packages/database/schema.ts` table + enum (Task 1); new GraphQL `hasPendingDefaultLocationReview: Boolean!` field on `SocialMediaAccountProfile` and `editAccountDefaultLocation` mutation, added to Story 3.1a/3.3's `social-media-accounts.graphql` file (not redeclared elsewhere, reuses Story 3.3's `SetAccountDefaultLocationInput`); Story 3.2/3.3's `getMySubscriptions` query extended to select `hasPendingDefaultLocationReview` on `account`; new `apps/web/src/features/subscriptions/mutations.graphql` operation. Generated counterparts: `apps/backend/src/generated/resolvers-types.ts`, `apps/web/src/generated/graphql.ts` (new `useEditAccountDefaultLocationMutation` hook, updated `GetMySubscriptionsQuery` shape). New `EmailTemplateKey`/`EmailTemplateVariables` entry in `packages/domain/src/email/types.ts` (Task 3). No `packages/shared-types` change — `LocationDetails` is reused as-is for both `previousLocation`/`newLocation`.
- **Required DB migration changes:** one new `drizzle-kit`-generated migration adding the `default_location_change_status` enum and `default_location_change_requests` table (Task 1), committed per AD-3. No changes to any existing table.
- **Required TypeScript type changes:** new `packages/database/schema.ts` exports (`defaultLocationChangeRequests`, `defaultLocationChangeStatusEnum`, relations) and the new codegen output above. No `packages/shared-types` change needed.
- **Backward compatibility and rollout notes:** purely additive — a new table, a new mutation, and a widened query selection on an already-additive field (Story 3.3's `defaultLocation`). `setAccountDefaultLocation` (Story 3.3) is completely untouched by this story; the two mutations coexist, gated by opposite `defaultLocation`-null-vs-set preconditions (AC7 vs. Story 3.3's AC6), so there is no shared-write race between them for the same account state.
- **Verification checks:** Task 2's extended `social-media-accounts.test.ts` asserts the edit mutation's guard conditions, the audit-row insert (including the stacking case), and the best-effort moderator-email behavior; Task 2's new resolver test asserts `hasPendingDefaultLocationReview`'s true/false cases; Task 6/7's frontend tests assert the edit-mode dialog and row badge/action rendering.

#### Amendment (2026-08-24) — AD-11 Moderator Override

- **Compatibility finding: two real gaps confirmed by reading current code, not assumed.** (1) `packages/database/schema.ts`'s `defaultLocationChangeStatusEnum` (line 32) is currently `['PENDING_REVIEW', 'ACCEPTED', 'REVERTED']` — no `SUPERSEDED` value exists, despite PRD §4.14 (amended 2026-08-24) already specifying it. (2) The `defaultLocationChangeRequests` table (schema.ts:405-417) has **no `changeSource` column at all** — no `changeSource` string appears anywhere in `packages/database/schema.ts`, `apps/backend/src/schema/*.graphql`, `apps/backend/src/schema/resolvers.ts`, or `apps/backend/src/generated/resolvers-types.ts` (verified via full-repo search). This is a real sequencing artifact: Story 3.4m (the story PRD §4.14 originally implied would introduce `changeSource`/`AI_INFERENCE`) is `ready-for-dev`, unimplemented — this amendment is the first actual code to touch the column.
- **Impacted contracts:** `packages/database/schema.ts` — new `defaultLocationChangeSourceEnum` (`'USER' | 'AI_INFERENCE' | 'MODERATOR'`), new `changeSource` column (`default('USER').notNull()`) on `defaultLocationChangeRequests`; `defaultLocationChangeStatusEnum` gains `'SUPERSEDED'`. `apps/backend/src/schema/default-location-change-requests.graphql`'s `DefaultLocationChangeRequestStatus` GraphQL enum gains `SUPERSEDED` to stay in sync with the DB enum (no GraphQL exposure of `changeSource` is required by this story's ACs — write-only for now). `packages/ui/src/core/status-badge.tsx`'s `StatusBadgeProps["variant"]` union gains `"superseded"`, matching `DESIGN.md`'s `status_badge.superseded` token (already committed 2026-08-24, code was not yet in sync). No `packages/shared-types` change.
- **Required DB migration changes:** one new `drizzle-kit`-generated migration (Task 1 amendment) adding: the `SUPERSEDED` enum value to the existing `default_location_change_status` type; a new `default_location_change_source` enum type; a new `change_source` column on `default_location_change_requests`. **Explicitly not changed:** `changedByUserId`'s `NOT NULL` constraint stays as-is — relaxing it for `AI_INFERENCE`'s no-human-actor case is Story 3.4m's responsibility, not this amendment's (this amendment's own `USER`/`MODERATOR` paths both always have a real `authUser.userId`).
- **Required TypeScript type changes:** new `defaultLocationChangeSourceEnum` export from `packages/database/schema.ts`; regenerated `apps/backend/src/generated/resolvers-types.ts` (new `SUPERSEDED` enum member) and `apps/web/src/generated/graphql.ts` (same, if the enum is selected anywhere client-side — currently it is not, so likely no client-visible diff beyond the type file itself); new `AccountLocationFieldLabels` type in `packages/ui`.
- **Backward compatibility and rollout notes:** purely additive — a new enum value, a new column with a safe default (`'USER'`), a new optional auth path. Existing rows backfill implicitly to `changeSource = 'USER'` via the column default (no data migration/backfill script needed since the column is `NOT NULL DEFAULT 'USER'`, which Postgres applies to existing rows automatically on `ADD COLUMN`). The subscriber path's insert shape is unchanged in effect (now explicit rather than implicit). No shared-write race is introduced: the supersede-update (AC17) only ever targets *other* rows for the same `accountId`, excluded by the just-inserted row's own `id`.
- **Verification checks:** Task 2's amended tests assert the moderator-path insert's exact `status`/`changeSource`/`reviewedByModeratorId`/`reviewedAt` values; the supersede-update's exclusion-by-id correctness (the newly inserted `PENDING_REVIEW` row for a subscriber must never supersede itself); regression coverage that every pre-existing test in `social-media-accounts.test.ts` still passes unmodified (AC15).

### Project Structure Notes

- **This story has a real code dependency on Story 3.3, which is `review` (implemented, not yet `done`) as of this story's creation** — it extends files Story 3.3 created (`social-media-accounts.graphql`, `resolvers.ts`'s `setAccountDefaultLocation`/`Subscription.account`, `subscriptions-content.tsx`, `set-default-location-dialog.tsx`, `queries.graphql`/`mutations.graphql`) and reuses Story 3.3d's `LocationPickerField`/`MapPickerSheet` (also `review`). Do not start Tasks 2/5-7 until Story 3.3 has landed (already coded per its own File List; confirm its tests are green before extending its files).
- New backend: none (Task 1 modifies `schema.ts` directly; Task 2 extends Story 3.3's existing `social-media-accounts.test.ts`).
- Modified backend: `packages/database/schema.ts` (new table/enum/relations, Task 1); `apps/backend/src/schema/social-media-accounts.graphql` (Story 3.1a/3.3's file — new field + `extend type Mutation`); `apps/backend/src/schema/resolvers.ts` (new `editAccountDefaultLocation` resolver, new `SocialMediaAccountProfile.hasPendingDefaultLocationReview` resolver); `packages/domain/src/email/{types.ts, templates.ts}` (new template key, Task 3); `apps/backend/src/env.ts` (new `webAppBaseUrl`, Task 4); `.env.example` (new `WEB_APP_BASE_URL`, Task 4).
- Modified frontend: `apps/web/src/features/subscriptions/{queries.graphql, mutations.graphql}` (Story 3.2/3.3's files); `apps/web/src/app/[locale]/settings/subscriptions/{set-default-location-dialog.tsx, set-default-location-dialog.test.tsx, subscriptions-content.tsx, subscriptions-content.test.tsx}` (Story 3.2/3.3's files); `apps/web/locales/{en,id}.json`; `apps/web/src/generated/graphql.ts` and `apps/backend/src/generated/resolvers-types.ts` (codegen output).
- **No conflicts detected** with Story 3.3's already-shipped "first-time set" path — `setAccountDefaultLocation` and `editAccountDefaultLocation` are two distinct mutations gated by opposite preconditions on the same column; this story only ever reads/extends Story 3.3's files, it does not modify `setAccountDefaultLocation`'s own resolver body.
- **Forward Note for Story 4.7 (Moderator Items page, not yet created)** — a binding contract this story's stacking decision (AC8) creates: when Story 4.7 queries `default_location_change_requests` for its moderator queue, it must dedupe by `accountId` to surface only the **most-recently-created** `PENDING_REVIEW` row per account (`ORDER BY created_at DESC`, first row per `account_id`) — this story allows multiple stacked edits to accumulate `PENDING_REVIEW` rows for the same account before a moderator acts, but only the latest reflects the account's actual current `defaultLocation` (which always reflects the *latest* edit regardless of review status, per AC4/FR66) and is the one a moderator's accept/revert action should be evaluated against. When Story 4.7 resolves the latest row (accept or revert), it must also mark every older still-`PENDING_REVIEW` row for that same `accountId` as resolved (e.g. `ACCEPTED`, mirroring whatever the latest row's outcome was) rather than leaving them permanently stuck in `PENDING_REVIEW` with no further UI ever surfacing them — otherwise they'd silently accumulate as orphaned rows. This story does not implement any of Story 4.7's read/accept/revert logic — it only guarantees the write-side data (`previousLocation` on each row is always the value immediately before *that specific* edit, so the chain is reconstructable) is correct for Story 4.7 to build against.
  - **Superseded by this amendment (2026-08-24, AD-11):** the dedupe mechanism above is now moot for Story 4.7 — AC17's supersede-on-write means at most one `PENDING_REVIEW` row per `accountId` ever exists at query time (every earlier one is flipped to `SUPERSEDED` the moment a later edit succeeds), so `pendingDefaultLocationChanges` naturally returns zero or one row per account with no `ORDER BY`/dedupe logic needed in Story 4.7 at all. This closes the gap directly: `moderator-items-content.tsx` was confirmed (read directly this session) to have no dedupe/grouping logic today, which is exactly why stacked rows currently all surface — AC17 fixes that at the write layer instead of requiring a read-layer fix in Story 4.7.

### Amendment Project Structure Notes (2026-08-24, AD-11)

- **This amendment has no code dependency on Story 3.4m** — despite Task 1's amendment introducing the `changeSource` enum PRD §4.14 originally associated with Story 3.4m's `AI_INFERENCE` value, this amendment only ever writes `'USER'`/`'MODERATOR'`, never `'AI_INFERENCE'`; Story 3.4m (`ready-for-dev`) can proceed independently and will simply find the enum/column already in place when it's built.
- **This amendment has no code dependency on Story 4.7** — Story 4.7 is already `done` (shipped `moderator-items-content.tsx`/`pending-location-change-row.tsx`) and is unaffected by this amendment's DB/resolver changes (its own `resolveDefaultLocationChange`/`pendingDefaultLocationChanges` resolvers are untouched; the `SUPERSEDED` status simply removes stale rows from what it already queries). Wiring `AccountLocationField` into `/moderator/items` and adding a moderator-side trigger for the `editAccountDefaultLocation` moderator path are Story 4.7's own separate future amendment (see Out of Scope) — this story does not block on it, nor does it block Story 4.7's amendment from starting once this one lands.
- New backend: none (all changes extend existing files — `schema.ts`, `resolvers.ts`, `social-media-accounts.graphql`, `default-location-change-requests.graphql`).
- New frontend: `packages/ui/src/features/locations/{AccountLocationField.tsx, AccountLocationField.types.ts, AccountLocationField.test.tsx}` (Task 10).
- Modified backend (amendment): `packages/database/schema.ts` (new enum/column, Task 1); `apps/backend/src/schema/resolvers.ts` (`editAccountDefaultLocation`'s auth/insert/supersede logic, Task 2); `apps/backend/src/schema/default-location-change-requests.graphql` (`SUPERSEDED` status value, Task 2); `apps/backend/src/schema/social-media-accounts.test.ts` (new test cases, Task 2).
- Modified frontend (amendment): `apps/web/src/app/[locale]/settings/subscriptions/{subscriptions-content.tsx, subscriptions-content.test.tsx}` (Task 7); `packages/ui/src/core/status-badge.tsx` (`superseded` variant, Task 10); `packages/ui/src/features/locations/index.ts` (new export, Task 10).
- **No conflicts** with this story's own original AC1-14/Tasks 1-9 — every amendment is additive to files those tasks already modify, no original code path is removed or behaviorally altered for the subscriber flow (AC15's explicit regression requirement).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-3.3b] — this story's authoritative AC.
- [Source: _bmad-output/planning-artifacts/epics.md#Story-3.3] — the prerequisite "first-time set" story this one extends without modifying; its Amendment 2 is what originally deferred editing to this story.
- [Source: _bmad-output/planning-artifacts/epic-readiness/epic-3-readiness.md] — the swept Gate 1/3 sweep this story cites (`stories_covered` includes `3.3b`; explicitly validates the Epic 3 → Epic 4 table-dependency direction this story's new table creates).
- [Source: design-artifacts/UX-festgrid-run-1/EXPERIENCE.md] — "Default Location for a Subscribed Account" user flow (steps 2/4, the edit trigger and immediate-apply-with-review behavior) and "Default Location Pending Review" (the full state-pattern this story implements the subscriber-facing half of).
- [Source: _bmad-output/planning-artifacts/prds/festgrid-prd-2026-07-10-2047/prd.md#4.14] — `DefaultLocationChangeRequest` interface (fields, `DefaultLocationChangeStatus` enum) this story's new table matches exactly; PRD §3.9.3/FR66/FR67 for the immediate-apply/moderator-notify behavior.
- [Source: _bmad-output/planning-artifacts/festgrid-architecture-spine.md#AD-7] — rule 5, explicitly anticipating `DefaultLocationChangeRequest` moderation as extending (not bypassing) `requireModerator`'s single enforcement surface — relevant to Story 4.7, not this story (this story never calls `requireModerator`), cited here for continuity.
- [Source: _bmad-output/planning-artifacts/festgrid-architecture-spine.md#AD-8] — confirms `DefaultLocationChangeRequest` is excluded from the soft-delete binding list (audit/log-like), consistent with this story's schema choice (no `deletedAt`).
- [Source: _bmad-output/implementation-artifacts/3-3-set-a-default-location-for-a-subscription.md] — `setAccountDefaultLocation`'s exact resolver pattern this story's `editAccountDefaultLocation` mirrors; `SetDefaultLocationDialog`'s existing state machine this story extends with a `mode` prop; `subscriptions-content.tsx`'s existing inline-row conventions (platform-tag badge, "Set Default Location" trigger) this story's edit action/badge follow.
- [Source: apps/backend/src/lib/email/adapter.ts, packages/domain/src/email/{types.ts, templates.ts}] — `sendTemplatedEmail`'s exact signature and the `DANGEROUS_EVENT_MODERATOR_ALERT` template this story's new template mirrors.
- [Source: apps/backend/src/lib/auth/context.ts] — `AuthenticatedUser.role`/`requireModerator` already exist (Story 0.17), confirming the role model this story's moderator-lookup query relies on (`users.role = 'moderator'`) is already in place.
- [Source: packages/graphql-select/optimized-select.ts] — confirms `buildOptimizedDrizzleSelect` safely ignores GraphQL fields with no matching Drizzle column (like the new `hasPendingDefaultLocationReview`), so no change is needed there for the new computed field to coexist with the dynamic select.
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-08-24-moderator-location-override.md] — full trigger/evidence/analysis for the AD-11 amendment (AC15-19); approved, routed PRD → Architecture → UX → this story.
- [Source: _bmad-output/planning-artifacts/prds/festgrid-prd-2026-07-10-2047/prd.md#3.7, #4.14] — §3.7 "Moderator Override" paragraph (amended 2026-08-24) and §4.14's `MODERATOR`/`SUPERSEDED` enum additions this amendment's ACs implement exactly.
- [Source: _bmad-output/planning-artifacts/festgrid-architecture-spine.md#AD-11] — "Moderator Override on Subscriber-Scoped Mutations", rules 1-3, the authoritative rule text AC15-17 encode.
- [Source: design-artifacts/UX-festgrid-run-1/EXPERIENCE.md — "Account Location Field" and "Default Location Pending Review" § "Superseded"] — the shared-component spec (AC18) and the page-decides-semantics rule (AC19), both added 2026-08-24 in this same amendment wave.
- [Source: apps/backend/src/schema/resolvers.ts:2629, 2704] — existing `context.user?.role === 'moderator'` direct-check precedent (the `event`/`eventBySlug` resolvers) this amendment's auth branch mirrors, rather than calling `requireModerator(context)` (which would throw before the subscription-check fallback could run).
- [Source: apps/web/src/app/[locale]/moderator/items/moderator-items-content.tsx] — read directly this session; confirmed **no** dedupe/grouping logic exists for stacked pending requests today, the real gap AC17's supersede-on-write closes.
- [Source: packages/ui/src/features/locations/LocationPickerField.tsx, LocationPickerField.types.ts] — the `labels`-prop, framework-agnostic pattern `AccountLocationField` (Task 10) mirrors.

## Global Rules References

- [x] `_bmad-output/project-context.md` — Loaders (`BlockingLoader` on save), State Management (Server State via React Query/codegen, `packages/ui` components stay presentational), i18n rules, Optimized DB Queries (`buildOptimizedDrizzleSelect`/`formatLocationDetails`), Database Access (Drizzle ORM only), Soft-Delete Convention (confirms this new table is correctly *not* soft-deleted).
- [x] `story-content-structure.md` — canonical section order followed.
- [x] `_bmad-output/planning-artifacts/festgrid-architecture-spine.md` — AD-3 (Drizzle-kit-generated migrations, committed), AD-5 (analytics event naming), AD-6 (i18n), AD-7 (single `requireAuth`/`requireModerator` enforcement surface — rule 5 anticipates this exact PRD entity), AD-8 (soft-delete exclusion for audit/log-like tables), **AD-11 (added 2026-08-24) — Moderator Override on Subscriber-Scoped Mutations, rules 1-3 (AC15-19 amendment)**.
- [x] `docs/infrastructure/index.md` — no infra/deployment change in this story (pure application-layer GraphQL + frontend + a new env var read at runtime; reuses Story 0.14's already-provisioned SES adapter, no new AWS resource). Amendment (AD-11) is also pure application-layer — no infra change.

## Implementation Plan (Rule-Compliant)

### File Change Plan

- **New (original scope):** none (this story extends existing files rather than creating new ones — even the DB migration is drizzle-kit-generated into the existing `packages/database/migrations/` directory, not a hand-authored new source file).
- **Modified (original scope):** `packages/database/schema.ts` (+ generated migration); `apps/backend/src/schema/social-media-accounts.graphql`; `apps/backend/src/schema/resolvers.ts`; `apps/backend/src/schema/social-media-accounts.test.ts`; `packages/domain/src/email/{types.ts, templates.ts, render-template.test.ts}`; `apps/backend/src/env.ts`; `.env.example`; `apps/web/src/features/subscriptions/{queries.graphql, mutations.graphql}`; `apps/web/src/app/[locale]/settings/subscriptions/{set-default-location-dialog.tsx, set-default-location-dialog.test.tsx, subscriptions-content.tsx, subscriptions-content.test.tsx}`; `apps/web/locales/{en,id}.json`; `apps/web/src/generated/graphql.ts` and `apps/backend/src/generated/resolvers-types.ts` (codegen output).
- **Not modified (original scope):** `packages/shared-types` (reuses `LocationDetails` as-is); `packages/domain/src/user-locations` (`resolveLocationInputMode` reused as-is, no changes); `packages/ui` (no new/changed component — `LocationPickerField`/`MapPickerSheet` consumed exactly as Story 3.3 already integrates them); Story 3.3's `setAccountDefaultLocation` resolver body (untouched, only extended-alongside in the same file).

#### Amendment (2026-08-24, AD-11) File Change Plan

- **New:** `packages/ui/src/features/locations/AccountLocationField.tsx`, `AccountLocationField.types.ts`, `AccountLocationField.test.tsx` (Task 10).
- **Modified:** `packages/database/schema.ts` (+ new generated migration, Task 1 amendment); `apps/backend/src/schema/resolvers.ts` (`editAccountDefaultLocation`, Task 2 amendment); `apps/backend/src/schema/default-location-change-requests.graphql` (`SUPERSEDED` status, Task 2 amendment); `apps/backend/src/schema/social-media-accounts.test.ts` (new test cases, Task 2 amendment); `apps/web/src/app/[locale]/settings/subscriptions/{subscriptions-content.tsx, subscriptions-content.test.tsx}` (Task 7 amendment); `packages/ui/src/core/status-badge.tsx` (`superseded` variant, Task 10); `packages/ui/src/features/locations/index.ts` (new export, Task 10); `apps/backend/src/generated/resolvers-types.ts` (codegen output).
- **Not modified:** `SetDefaultLocationDialog` (already generic, AC18 confirms no change needed); Story 3.3's `setAccountDefaultLocation` (still untouched); `resolveDefaultLocationChange`/`pendingDefaultLocationChanges` resolvers and `moderator-items-content.tsx` (Story 4.7's files — unaffected, see Amendment Project Structure Notes); `packages/shared-types` (no change); `changedByUserId`'s `NOT NULL` constraint (explicitly not relaxed, see Data Type Compatibility amendment).

### Rule Mapping

- Backend-only mutation layer, never a direct DB/domain write from `apps/web` → `story-split-gate.md` Gate 1 → `editAccountDefaultLocation` resolver (Task 2), consumed only via the generated hook (Task 5).
- Reuse over reinvention (`SetAccountDefaultLocationInput`, `resolveLocationInputMode`, `resolveLocation`, `formatLocationDetails`, `activeOnly`, `sendTemplatedEmail`, `LocationPickerField`/`MapPickerSheet`, `SetDefaultLocationDialog`) → this story's Gate Findings + `AskUserQuestion` decisions → Tasks 2, 3, 6.
- Database schema code-first, migrations committed → AD-3 → Task 1.
- Single `requireAuth` enforcement surface, `activeOnly(table)` instead of hand-written `isNull` → AD-7 rule 3, AD-8 rule 2 → AC5/AC6, Task 2.
- Audit table correctly excluded from soft-delete binding → AD-8 → Task 1's schema design (no `deletedAt`).
- Non-blocking-vs-blocking loaders → `project-context.md` Loaders rule → `BlockingLoader` on save (Task 6, unchanged from Story 3.3).
- i18n, no hardcoded strings → `project-context.md`/AD-6 → Task 8.
- PostHog analytics events named/payload-shaped explicitly → AD-5 → AC14/Task 6.
- Best-effort, non-blocking side effect (moderator email) → FR66's "no pre-approval gate" extended to notification delivery, user-confirmed stacking decision → AC9/Task 2's `Promise.allSettled` handling.
- Idempotency/state-transition guard (`INVALID_STATE_TRANSITION`) → AD-8-adjacent app convention (`deleteApiKey`/`removeSubscription`/`setAccountDefaultLocation` precedent) → AC7/Task 2.
- Additive second auth path, not a moderator-only fork → AD-11 rule 1 → AC15/Task 2's `isModerator` branch.
- Moderator's own edit is the review (self-resolved insert) → AD-11 rule 2 → AC16/Task 2.
- Stale pending requests superseded on any successful write → AD-11 rule 3 → AC17/Task 2.
- Reuse over reinvention, extract-not-duplicate once ≥2 real/near-term consumers exist → Gate 2 subagent verdict (no split, extract as a task) → AC18/Task 10.
- Mutation semantics follow the calling page, not the caller's role → `EXPERIENCE.md` "Account Location Field" (page-decides-semantics rule) → AC19 (no code change to `subscriptions-content.tsx`'s call site required — the rule is upheld by what this amendment does *not* do).

### Verification Plan

- `apps/backend/src/schema/social-media-accounts.test.ts` (extended): both input modes, `INVALID_STATE_TRANSITION` guard (no existing value), active-subscriber authorization, auth requirement, audit-row insert correctness, stacking behavior, best-effort email failure isolation (Task 2).
- New resolver test: `SocialMediaAccountProfile.hasPendingDefaultLocationReview` true/false cases (Task 2).
- `packages/domain/src/email/render-template.test.ts` (extended): new template key interpolation (Task 3).
- `apps/web/.../set-default-location-dialog.test.tsx` (extended), `subscriptions-content.test.tsx` (extended): edit-mode prefill/submit/toast, edit action + "Pending Review" badge rendering (Tasks 6-7).
- `pnpm build`, `pnpm lint`, full test suite at repo root — no regressions, including Story 3.3's and Story 3.3d's own test suites (this story extends files they own).

#### Amendment (2026-08-24, AD-11) Verification Plan

- `social-media-accounts.test.ts` (amended `editAccountDefaultLocation` block): moderator-caller-without-subscription success, self-resolved insert field values (`status`/`changeSource`/`reviewedByModeratorId`/`reviewedAt`), non-moderator/non-subscriber still rejected, supersede-on-write exclusion-by-id correctness, full regression of every pre-existing test in the block (Task 2 amendment).
- `packages/ui/src/features/locations/AccountLocationField.test.tsx` (new): location text rendering, conditional `pendingReview`-variant badge, edit-button click/`aria-label` (Task 10).
- `subscriptions-content.test.tsx` (amended): existing edit-action/badge assertions still pass through the new `AccountLocationField` composition (Task 7 amendment).
- `pnpm --filter database run generate` produces a clean, committable migration (new enum value + enum type + column); `pnpm build`/`pnpm lint` clean repo-wide including the amended files.

## Pre-Coding Approval Gate

- [x] Scope confirmation: this story builds the `editAccountDefaultLocation` mutation, the new `default_location_change_requests` audit table, the moderator email notification, the `hasPendingDefaultLocationReview` field, and the row's edit action + "Pending Review" badge on Story 3.2/3.3's `/settings/subscriptions` page. It does not build Story 4.7's moderator accept/revert view or query, and does not modify Story 3.3's `setAccountDefaultLocation` mutation.
- [x] Architecture and boundary confirmation: `editAccountDefaultLocation` confined to `apps/backend`'s GraphQL layer, reusing Story 0.16's Geolocation adapter and Story 0.15's email adapter; no direct DB/external-API access from `apps/web`; no new `packages/domain` logic added (reuses `resolveLocationInputMode` as-is); new DB table has no soft-delete column, matching AD-8's exclusion of audit/log-like tables.
- [x] **Sequencing confirmation (specific to this story):** confirm Story 3.3 (`setAccountDefaultLocation`, `subscriptions-content.tsx`, `set-default-location-dialog.tsx`, `queries.graphql`/`mutations.graphql`) and Story 3.3d (`LocationPickerField`/`MapPickerSheet`) are implemented before starting Tasks 2/5-7 — both are `review` (coded but not yet `done`) as of this story's creation; confirm their test suites are green before extending their files.
- [x] Testing plan confirmation: backend integration tests (Task 2), email template test (Task 3), frontend integration tests (Tasks 6-7) as scoped above.
- [x] Gate 1/2/3 prerequisites confirmed done or gap accepted: Gate 1/3 sourced from swept `epic-3-readiness.md` (no gap; explicitly validates this story's new table against Story 4.7's forward dependency); Gate 2 run fresh, no gap (all UI reuses existing components/conventions); lightweight guard surfaced three items (UI trigger pattern, "Pending Review" badge scope, concurrent-edit/stacking behavior), all resolved with the user via `AskUserQuestion` before drafting — see Dev Notes → Architecture & UX Gate Findings and → Forward Note for Story 4.7.
- [x] Explicit human approval state (Default: **pending approval**).

### Amendment (2026-08-24, AD-11) Pre-Coding Approval Gate — AC15-19

- [x] **Scope confirmation:** this amendment extends `editAccountDefaultLocation` with a second, additive auth path for moderators (AC15), self-resolved moderator writes (AC16), supersede-on-any-write (AC17), a shared `AccountLocationField` component wired into `/settings/subscriptions` only (AC18), and confirms/encodes the existing page-decides-semantics behavior (AC19, no code change). It does **not** build Story 4.7's own `AccountLocationField` wiring into `/moderator/items`, does **not** add a moderator-side UI trigger for the moderator path (manual/GraphQL-Playground smoke check only per Task 9), and does **not** relax AC7's `INVALID_STATE_TRANSITION` guard for moderators (see Dev Notes escape-hatch item 1) or suppress AC9's moderator-alert email for self-resolved writes (escape-hatch item 2).
- [x] **Architecture and boundary confirmation:** auth branch computed from `AuthenticatedUser.role` directly (mirroring the existing `resolvers.ts:2629`/`2704` precedent), not via a new enforcement helper — no change to AD-7's single `requireAuth`/`requireModerator` enforcement surface (the existing helpers are still used for every other call site); new DB enum/column added via the same Drizzle-migration mechanism as Task 1's original table; no new API surface, infra, or cross-cutting foundational dependency (Gate 1/3 sourced from the still-valid swept `epic-3-readiness.md`, no fresh gap).
- [x] **Gate 2 confirmation:** run fresh via subagent (Freya persona) specifically against the `AccountLocationField` extraction — verdict **no gap**, extraction proceeds as Task 10 inside this amendment, not a split story; a11y recommendations (≥24×24px hit target, non-focusable badge, typed `labels` interface) folded into Task 10 directly.
- [x] **Sequencing confirmation:** this amendment has no dependency on Story 3.4m (`ready-for-dev`, unimplemented — this amendment introduces the shared `changeSource` enum ahead of it, see Data Type Compatibility amendment) or on Story 4.7 (`done`, unaffected — see Amendment Project Structure Notes). No blocking prerequisite outstanding.
- [x] **Testing plan confirmation:** amended backend integration tests (Task 2 — moderator path, self-resolved fields, supersede exclusion, full regression of pre-existing tests), new `packages/ui` component tests (Task 10), amended frontend tests (Task 7).
- [x] **Escape-hatch items explicitly accepted, not silently absorbed:** (1) AC7's guard not relaxed for moderators — flagged as a future Story 3.4m-adjacent consideration; (2) AC9's moderator email not suppressed for self-resolved writes — flagged as a possible future refinement.
- [x] Explicit human approval state (Default: **pending approval** — reopens approval for this story given the new AC15-19 scope; AC1-14's own approval history is unaffected).

## Testing Requirements

- [x] Backend integration tests (`social-media-accounts.test.ts`, extended): `editAccountDefaultLocation`'s `placeId`/coordinate input modes, `INVALID_STATE_TRANSITION` when no prior value exists, `NOT_FOUND` for a non-subscriber/non-existent account, auth requirement, `default_location_change_requests` row correctness (including the stacking case — a second edit while an earlier row is still `PENDING_REVIEW` succeeds and creates a second row), best-effort moderator-email isolation (a rejected `sendTemplatedEmail` call does not fail the mutation); a new test for `SocialMediaAccountProfile.hasPendingDefaultLocationReview`'s true/false resolution.
- [x] `packages/domain/src/email/render-template.test.ts` (extended): `DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT` template interpolation.
- [x] Frontend integration tests: `set-default-location-dialog.test.tsx` (extended — edit-mode prefill, edit-mode mutation call, edit-specific toast copy, analytics event), `subscriptions-content.test.tsx` (extended — conditional edit action, "Pending Review" badge visibility).
- [x] E2E: not required as a dedicated flow per `project-context.md`'s testing-trophy philosophy — integration coverage above satisfies the Definition of Done; the manual smoke check in Task 9 substitutes for a scripted E2E given the cross-story (3.3/3.3d) dependencies.

### Amendment (2026-08-24, AD-11) — AC15-19

- [x] Backend integration tests (`social-media-accounts.test.ts`, amended): moderator caller with no active subscription succeeds (AC15); non-moderator/non-subscriber still rejected `NOT_FOUND` (AC15 regression); moderator-path insert has `status: ACCEPTED`, `changeSource: MODERATOR`, `reviewedByModeratorId`/`reviewedAt` populated (AC16); subscriber-path insert now has explicit `changeSource: USER` (AC16); a second successful edit (subscriber or moderator) supersedes a prior still-`PENDING_REVIEW` row for the same account, excluding the just-inserted row and rows on other accounts (AC17); every pre-existing test in this block passes unmodified (AC15).
- [x] `packages/ui/src/features/locations/AccountLocationField.test.tsx` (new): location text; `pendingReview`-variant badge shown only when `isPendingReview`; edit-button click calls `onEdit`; edit button exposes the supplied `aria-label` (AC18).
- [x] `subscriptions-content.test.tsx` (amended): existing edit-action/badge assertions still pass through the `AccountLocationField` composition (AC18).
- [x] E2E: not required — same testing-trophy rationale as the original story; the manual smoke check addendum in Task 9 covers the moderator path end-to-end given no moderator-side UI trigger exists yet (Story 4.7's own future amendment).

## Deliverables Checklist

- [x] `default_location_change_requests` table + migration, committed.
- [x] `editAccountDefaultLocation` mutation, fully tested, extending Story 3.1a/3.3's `social-media-accounts.graphql`.
- [x] `SocialMediaAccountProfile.hasPendingDefaultLocationReview` field, resolved and tested.
- [x] `DEFAULT_LOCATION_CHANGE_MODERATOR_ALERT` email template, wired to fire (best-effort) to every `role = 'moderator'` user on save.
- [x] `SetDefaultLocationDialog` edit mode + row edit action + "Pending Review" badge, both integration tested.
- [x] `SubscriptionsPage` i18n additions in both `en.json` and `id.json`.
- [x] `subscription_default_location_edited` PostHog event wired via `usePostHog()`.
- [x] Forward Note for Story 4.7 recorded in Dev Notes (moderator-queue dedup contract — **superseded by AC17's write-side fix, see Amendment Project Structure Notes**).
- [x] **(Amendment, AD-11)** `editAccountDefaultLocation` moderator auth path (AC15), self-resolved insert (AC16), supersede-on-write (AC17) — backend tested, subscriber-path regression confirmed.
- [x] **(Amendment)** `default_location_change_source` enum + `changeSource` column, `SUPERSEDED` status value on both the DB enum and the `DefaultLocationChangeRequestStatus` GraphQL enum — migrated, committed.
- [x] **(Amendment)** Shared `AccountLocationField` component (`packages/ui`), wired into `/settings/subscriptions`, fully tested (AC18).
- [x] **(Amendment)** `StatusBadge`'s `superseded` variant added, matching the `DESIGN.md` token (AC18).

## Out of Scope

- Story 4.7 (Moderator Items page): the actual moderator-facing list/accept/revert UI and its query over `default_location_change_requests` — this story only writes the audit rows and sends the notification email; see Dev Notes → Forward Note for Story 4.7 for the dedup contract that story must implement given this story's stacking decision. **(Amendment note: AC17's supersede-on-write now satisfies that contract at the write layer, so Story 4.7 needs no read-side dedupe change — but it still needs its own separate amendment to wire `AccountLocationField` into `/moderator/items` alongside the existing Accept/Revert actions and to add a UI trigger for the moderator path, neither of which this story builds.)**
- Any change to Story 3.3's `setAccountDefaultLocation` mutation or its "first-time set" UI path — untouched, coexists with this story's mutation via opposite preconditions.
- A literal "In-Table Add Form" inline-editing UI matching `EXPERIENCE.md`'s pattern name verbatim — user-confirmed deviation in favor of extending Story 3.3's already-shipped modal dialog (see Dev Notes → Architecture & UX Gate Findings, item 1).
- Rate-limiting or de-duplicating moderator email sends across rapid repeated edits to the same account — every edit sends a fresh notification; not flagged as a problem by the user during this story's creation, but noted here as a possible future refinement if moderators report alert fatigue. **(Amendment: this now also includes moderator-sourced self-resolved edits, which still trigger the same alert email to every moderator despite requiring no review — see Dev Notes escape-hatch item 2.)**
- Any change to Story 3.6's AI-extraction fallback logic — unaffected; that logic already reads whatever `SocialMediaAccountProfile.defaultLocation` currently holds, regardless of which mutation (set or edit) last wrote it.
- **(Amendment, AD-11)** Story 3.4m's `AI_INFERENCE` path and its need to relax `changedByUserId` to nullable — this amendment introduces the shared `changeSource` enum/column ahead of Story 3.4m but does not implement AI inference or touch `changedByUserId`'s `NOT NULL` constraint; that remains Story 3.4m's own scope when it's built.
- **(Amendment, AD-11)** Relaxing AC7's `INVALID_STATE_TRANSITION` guard for the moderator path — a noticed edge case (a moderator `REVERT`ing an AI-inferred change on an account with `previousLocation: null` would then be blocked from directly re-correcting it via this mutation) but not in scope for this amendment's 5 ACs; relevant only once Story 3.4m's `AI_INFERENCE` path actually exists. See Dev Notes escape-hatch item 1.
- **(Amendment, AD-11)** A moderator-side UI trigger for the new moderator auth path — this amendment only builds the mutation-side capability; verified via manual GraphQL call per Task 9's smoke-check addendum. A UI trigger is Story 4.7's own future amendment.

## Definition of Done

- [x] AC1-19 satisfied and demonstrated via the tests in Testing Requirements (AC1-14 already delivered/shipped; AC15-19 new in this amendment).
- [x] Backend and frontend test suites pass; no regression in existing suites (including Story 3.3's and Story 3.3d's own test suites, and this story's own pre-amendment `social-media-accounts.test.ts`/`subscriptions-content.test.tsx` coverage — AC15's explicit regression requirement).
- [x] `pnpm build` and `pnpm lint` clean for all touched packages, including the new `packages/ui/src/features/locations/AccountLocationField.tsx`.
- [x] `en.json`/`id.json` both updated — no hardcoded user-facing strings (amendment requires none new — reuses existing keys, AC18).
- [x] New migrations committed per AD-3, including the amendment's enum/column additions.

## Completion Status

review (AC1-19, AD-11 amendment)

**2026-08-24 (`bmad-create-story`):** Reopened for AC15-19 (moderator override, `AD-11` — `sprint-change-proposal-2026-08-24-moderator-location-override.md`, approved). AC1-14 remain as originally delivered and are unaffected by this amendment; the subscriber path's existing behavior and tests must stay byte-for-byte unchanged (AC15). Gate 1/3 sourced from the still-valid swept `epic-3-readiness.md` (no fresh gap); Gate 2 run fresh via subagent (Freya persona) specifically against the new `AccountLocationField` extraction — verdict: no gap, proceeds as a task (Task 10) inside this amendment. Two items surfaced during drafting were resolved without altering behavior and left as flagged escape-hatch notes rather than silently absorbed or changed: AC7's guard is not relaxed for moderators, and AC9's moderator-alert email is not suppressed for self-resolved writes — see Dev Notes → Architecture & UX Gate Findings → Amendment.

## Dev Agent Record

### Agent Model Used

Gemini CLI (YOLO Mode)

### Debug Log References

Completed via CLI. Verified build and ran full test suites across packages/ui, apps/backend, and apps/web.

### Completion Notes List

- Implemented AD-11 amendment (AC15-19) on master.
- Added SUPERSEDED status and default_location_change_source enums, and change_source column to DB schema.
- Extended editAccountDefaultLocation resolver in resolvers.ts to handle moderator paths, self-resolved status, and supersede-on-write.
- Added unit/integration tests in social-media-accounts.test.ts verifying both paths.
- Extracted shared AccountLocationField component to packages/ui/src/features/locations with standard A11y enhancements (>=24x24px button, non-focusable badge, typed labels interface).
- Integrated the new component in apps/web's settings/subscriptions/subscriptions-content.tsx.
- Confirmed 100% test pass on backend, web, and UI, and clean pnpm build/lint.

### File List

- packages/database/schema.ts
- packages/database/migrations/0034_bright_ken_ellis.sql
- apps/backend/src/schema/default-location-change-requests.graphql
- apps/backend/src/schema/resolvers.ts
- apps/backend/src/generated/resolvers-types.ts
- apps/backend/src/schema/social-media-accounts.test.ts
- packages/ui/src/core/status-badge.tsx
- packages/ui/src/features/locations/AccountLocationField.types.ts
- packages/ui/src/features/locations/AccountLocationField.tsx
- packages/ui/src/features/locations/AccountLocationField.test.tsx
- packages/ui/src/features/locations/index.ts
- apps/web/src/app/[locale]/settings/subscriptions/subscriptions-content.tsx
- apps/web/src/app/[locale]/settings/subscriptions/subscriptions-content.test.tsx
