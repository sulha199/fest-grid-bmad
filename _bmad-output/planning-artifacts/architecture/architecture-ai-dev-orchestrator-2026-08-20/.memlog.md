---
scope: AI Dev Orchestrator initiative -- LangGraph/TypeScript multi-model spec-driven dev automation, 9Router-routed, standalone local sandbox v1 (see SPEC-ai-dev-orchestrator)
purpose: build-substrate
altitude: initiative
updated: 2026-08-21T00:05
---

- (direction) Working mode: Coaching path. Deliverables: ARCHITECTURE-SPINE.md plus a walkthrough doc/deck at Finalize step 5
- (decision) Paradigm: Hexagonal / Ports and Adapters. Core = LangGraph state machine plus node decision logic (Planner/Complex Worker/Speed Worker/Tester/Reviewer/HITL). Boundary ports: LLMPort (9Router calls), ExecPort (fs + child_process against TARGET_REPO_PATH), NotifyPort (transactional email), HITLPort (terminal readline). Each port has a real adapter and a fake/sandbox adapter for testing the graph without touching a real shell, real tokens, or a real email send. User confirmed (accepted the recommendation).
- (decision) State persistence: SQLite checkpointer via @langchain/langgraph-checkpoint-sqlite (local .db file). Resumes mid-epic after a crash/kill instead of restarting; completed stories are additionally safe via CAP-11's per-story git commit regardless
- (decision) Email provider for CAP-8 HITL timeout escalation: Resend (REST API + official Node SDK), not AWS SES -- avoids SES's Lambda-role credential resolution and sandbox-mode/domain-verification friction that festgrid's own 0-15 adapter relies on, inappropriate for a local CLI tool
- (decision) Test runner: Vitest for ports/adapters and node decision logic, chosen over node:test/tsx (festgrid's own precedent) for stronger ESM/TS ergonomics on a fresh standalone project
- (decision) TypeScript version: 6.x latest stable, not TypeScript 7.0 -- mature tooling ecosystem (ESLint plugins, tsx, editor support) over the ~4-month-old Go-native rewrite, prioritizing fast local iteration over bleeding-edge compiler speed
- (version) Node.js 24 (Active LTS as of 2026-08; Node 20 EOL 2026-04-30, Node 22 minimum supported by openai SDK)
- (version) typescript 6.0.3
- (version) @langchain/langgraph 1.4.12
- (version) @langchain/langgraph-checkpoint-sqlite 1.0.1
- (version) openai (SDK client pointed at 9Router base URL) 7.5.0
- (version) resend 6.20.0 (requires Node 20+, satisfied by pinned Node 24)
- (version) vitest 4.1.11
- (decision) Audit logging: structured per-run JSONL log file under logs/, appended for every LLM call, shell command, verdict, and HITL event alongside terminal output and the SQLite checkpointer
- (decision) Config loading: single validated env module (env.ts) resolves and validates all env vars fail-fast at startup (NINE_ROUTER_BASE_URL/API_KEY, ORCH_MODEL_*, TARGET_REPO_PATH, HITL_NOTIFY_EMAIL, HITL_TIMEOUT_MS, MAX_AUTO_FIX_ATTEMPTS, RESEND_API_KEY) rather than scattered process.env reads
- (decision) Dependency direction: core (graph wiring + node decision logic) depends only on port interfaces it defines; concrete adapters (9Router client, fs/child_process executor, Resend client, readline HITL) implement those ports and are wired in by a single composition root at startup. Core never imports a concrete adapter or Node built-in I/O module directly -- prevents a node bypassing its port and calling fs/child_process/fetch/9Router directly
- (decision) Core entities: Epic { id, title, stories: Story[] }; Story { id, epicId, title, acceptanceCriteria: string[], tag: standard|complex, status: pending|in_progress|approved|needs_human, autoFixAttempts: number }; ReviewVerdict = APPROVE | AUTO_FIX | NEEDS_HUMAN (matches SPEC.md CAP-2 rubric)
- (event) Reviewer Gate ran: lint_spine.py clean; rubric-walker, version-verification, and adversarial-incompatibility subagent reviews complete. Findings triaged and fixed at Finalize (not deferred): AD-1 port signatures underspecified, AD-5 tasks_queue write-ownership/reducer missing, AD-4 git-add scope ambiguous, AD-2/AD-3 ordering and rubric-vs-override contradiction, AD-8 logger-caller ambiguity, adapter error-handling policy missing, stale versions (checkpoint-sqlite 1.0.1->1.0.4, resend 6.20.0->6.21.0), stack.md Node version contradiction, SPEC.md/state-machines.md AUTO_FIX-executor contradiction (corresponding worker vs Complex-Worker-only diagram edge).
- (decision) Fix: port method contracts fixed in AD-1 -- LLMPort.complete({role, systemPrompt, messages}) takes NodeRole per call (adapter resolves role to alias internally, not pre-bound); ExecPort.run({cmd, args, cwd}) takes an argv array, never a raw shell string, to avoid shell injection from LLM-generated commands; NotifyPort.send({to, subject, body}); HITLPort.prompt({summary, expand}) returns the human's free-form response. The timeout race (readline promise vs setTimeout) is owned by the HITL node in core, not by HITLPort/NotifyPort adapters -- core calls both ports, adapters only perform their single I/O action
- (decision) Fix: AD-5 tasks_queue write ownership -- Planner is the only node that adds/restructures stories (initial decomposition); every other node updates only its own assigned story's fields (status, autoFixAttempts) via a keyed-merge reducer on the tasks_queue channel (keyed by story id), never a full-array overwrite, so two nodes touching different stories in the same run cannot clobber each other. Story is defined once in core/types.ts and referenced, not redefined, elsewhere. CLI resume behavior made explicit: dev an epic <name> checks the SQLite checkpointer for an existing thread for that epic+TARGET_REPO_PATH before starting a fresh one
- (decision) Fix: AD-4 git-add scope resolved as git add -A (whole working tree), justified by CAP-5's serial story loop -- GitCheckpoint always fires immediately after a story's APPROVE, before the next story starts, so the working tree only ever contains that one story's changes at commit time; no file-provenance list needed on current_code
- (decision) Fix: AD-2/AD-3 rewritten to remove the override-vs-rubric contradiction -- the AUTO_FIX ceiling is one of the rubric's own NEEDS_HUMAN triggers (matches state-machines.md), not a separate override layer. Ordering made explicit: story.autoFixAttempts increments immediately after an AUTO_FIX patch is applied and Tester still fails, before the next Reviewer call; the Reviewer node checks the counter against MAX_AUTO_FIX_ATTEMPTS as its first step and short-circuits straight to NEEDS_HUMAN (no extra LLM call) once at ceiling -- so MAX=1 means exactly one real AUTO_FIX attempt is ever made
- (decision) Fix: AD-8 audit logger is called only by core nodes, never by adapters, preventing double- or zero-logging when adapters are swapped for fakes in tests -- a node logs before and after each port call it makes
- (decision) New: adapter error-handling policy -- every port throws a typed OrchestratorError { message, recoverable: boolean, cause } on failure; the calling node catches it and either retries within the existing AUTO_FIX budget (if recoverable, e.g. a transient network error) or routes straight to HITL (if not recoverable, e.g. an auth failure) -- resolves the previously-undecided abort/surface/HITL dimension
- (version) Fix: @langchain/langgraph-checkpoint-sqlite corrected to 1.0.4 (matches what @langchain/langgraph 1.4.12 itself depends on; 1.0.1 was stale)
- (version) Fix: resend corrected to 6.21.0 (was one patch behind at 6.20.0)
- (assumption) TypeScript pinned at 6.0.3 rather than npm-latest 7.0.2 is a deliberate, documented choice (see earlier decision: mature tooling ecosystem over the ~1-month-old Go-native rewrite), not an oversight -- noted inline in the Stack table so a future reader doesn't mistake it for staleness
- (decision) Capability map fix: CAP-6 (manual review + patch apply) Governed-by column corrected to include AD-3 (retry ceiling applies to manual-review AUTO_FIX too) and AD-4 (checkpoint commit fires the same way for a manual-review-applied patch)
- (event) Reviewer Gate fixes applied to ARCHITECTURE-SPINE.md; lint_spine.py re-run clean (0 findings). Cross-doc fixes also applied to the driving spec (SPEC.md CAP-6, state-machines.md, stack.md). spine finalized
- (event) Rendering produced: interactive HTML+SVG walkthrough deck published at https://claude.ai/code/artifact/8f9e18be-f334-4b36-a711-ee01cd0a69fd, covering paradigm, state machine, port contracts, state ownership, git checkpoint, HITL, stack, capability map, deferred/open
- (direction) Spec amended with CAP-12 (Epic Readiness Check), CAP-13 (Tier-2 Deep Code Review), CAP-14 (Correct-Course Escalation) -- architecture spine amended to match. AD IDs stay stable; amending AD-2/AD-3/AD-5 in place and adding AD-9 for the new HITL directive-routing rule, per Update flow
- (decision) New nodes: EpicReadinessCheckNode (core/nodes/epic-readiness-check.ts) reuses the planner LLM role (Claude Sonnet, no new alias needed) to sweep an epic's stories against the spine (Gate 1) and TARGET_REPO_PATH's actual state (Gate 3) via ExecPort reads, before Planner's first dispatch on that epic; sole writer of Epic.readinessReport/readinessCheckedAt, mirroring each worker's ownership of its own story's fields (AD-5). DeepCodeReviewNode (core/nodes/deep-code-review.ts) reuses the complex-worker LLM role (Gemini 3.1 Pro) for 3 parallel lens-calls (correctness, edge-case, acceptance-criteria coverage), gates GitCheckpoint, only runs on a Tier-1 APPROVE. CorrectCourseNode (core/nodes/correct-course.ts) makes no LLM call itself -- it forces EpicReadinessCheckNode to re-run against remaining stories, ignoring any cached report
- (decision) AD-2 amended: two verdict producers now exist (Tier-1 Reviewer, Tier-2 DeepCodeReview), both constrained to the same ReviewVerdict enum; Tier-2 only runs on a Tier-1 APPROVE and can downgrade it. AD-3 amended: MAX_AUTO_FIX_ATTEMPTS is a single shared budget across both tiers, not two independent ceilings. AD-5 amended: EpicReadinessCheckNode is the sole writer of Epic.readinessReport/readinessCheckedAt; its presence is what makes a later CAP-12 sweep on that epic optional (skipped by default); CAP-14's forced re-sweep bypasses that cache and overwrites the report
- (decision) New AD-9: HITL response directive routing -- the HITL node (not HITLPort) inspects the human's free-form response text for a correct-course: prefix after HITLPort.prompt() resolves. Unflagged text resumes at Planner unchanged. A flagged response routes to CorrectCourseNode, which forces EpicReadinessCheckNode to re-run against the epic's remaining not-yet-approved stories, seeded with the flagged text, ignoring Epic.readinessReport. Prevents two implementers disagreeing on where directive-parsing happens (port vs node) or on whether a flagged response silently no-ops
- (event) Targeted adversarial review of CAP-12/13/14 amendment: 9 findings (1 critical, 2 high, rest medium/low). All fixed at Finalize, none deferred: (1) AD-4 never amended for two-tier gating -- fixed to require both tiers APPROVE; (2) current_code semantics unpinned -- fixed as cumulative-per-story diff, Tier-2 consumes verbatim; (3) AD-3 increment-trigger loophole (tests-pass-but-reviewer-unhappy never incremented) -- fixed to increment on every applied AUTO_FIX patch regardless of the following Tester outcome; (4+5) no field carried reviewer rationale into the fix step, and AUTO_FIX had no file in Structural Seed -- resolved by removing AUTO_FIX as a separate node: whichever tier decides AUTO_FIX applies its own patch within the same node call, no cross-node handoff needed; (6) correct-course: prefix matching unspecified -- fixed to trim().toLowerCase().startsWith(); (7) sweep-mode signal missing -- resolved structurally (determined by which graph edge invoked the node, not stored state); (8) CLI flag unnamed -- named --recheck-readiness (boolean, default false); (9) partial-report crash edge case -- resolved by stating EpicReadinessCheckNode is a single atomic node covering both gates, so a crash leaves no partial write
- (direction) MAJOR PIVOT propagated from spec: standalone-sandbox retired, orchestrator is BMad-native. Real epics.md/story files/sprint-status.yaml are canonical (not SQLite); TARGET_REPO_PATH may be this repo; write ownership scoped per real artifact matching each real bmad-* skill's exact role, not a blanket Planner-writes-everything rule. Also finally applying the still-outstanding AD-4 fix from the prior adversarial review (checkpoint gating on both tiers), which had not yet been written to the file when this pivot interrupted that work
- (decision) AD-5 rewritten: real BMad artifacts (epics.md, story files, sprint-status.yaml, readiness reports) are the canonical durable state, not the SQLite checkpoint. SQLite shrinks to ephemeral run-state only: autoFixAttempts counters and which story was mid-flight, for crash-resume within a single run -- never epic/story content itself. Write ownership is scoped per real artifact and per which real bmad-* skill would perform that exact write (table in state-machines.md), enforced by the graph's sequential per-story/epic execution rather than a single blanket writer
- (decision) New AD-10: the orchestrator never autonomously writes a target project's PRD or architecture reference file. CorrectCourseNode and EpicReadinessCheckNode are constrained to epics.md, sprint-status.yaml, and readiness reports only -- any finding that implies a PRD/architecture-level change halts at HITL with a recommendation to run the real bmad-correct-course/bmad-prd/bmad-architecture skills, never an autonomous edit to those documents
- (decision) New: BMad artifact parsing/serialization (epics.md, story .md files with frontmatter+task-checklist+dev-notes sections, sprint-status.yaml) lives in core as pure functions (core/bmad-artifacts/), not in an adapter -- parsing/formatting is domain logic operating on strings ExecPort already fetched, not I/O itself, consistent with AD-1's port boundary (ports are for side effects, not for pure transformation logic)
- (version) yaml (npm) 2.9.0 -- chosen specifically over js-yaml because it supports a comment-preserving Document API; js-yaml does not preserve comments on round-trip (confirmed via its own open GitHub issue requesting the feature), which would silently destroy this repo's real inline sprint-status.yaml comments (e.g. the 2026-08-05 revision note on story 0-7)
- (decision) AD-4 fix applied (from the still-outstanding prior adversarial-review finding): checkpoint now fires only once BOTH review tiers return APPROVE, not on Tier-1 alone. Commit now includes code changes + the story's updated file + its sprint-status.yaml entry together, matching the BMad-native pivot's file set
- (event) lint_spine.py re-run after BMad-native rewrite: 1 low-severity finding, a false positive on the literal '{planning_artifacts}' runtime-path variable name (matches real bmad-epic-readiness-check's own documented variable convention) -- reviewed and accepted, not a real placeholder. Fixed one genuine stale reference caught by grep: Consistency Conventions table's State row still said SQLite checkpointer was the mutation source of truth, contradicting the rewritten AD-5 -- corrected
- (event) Second adversarial review (fidelity to real repo): CRITICAL config-path bug (should be _bmad/bmm/config.yaml not _bmad/core/config.yaml, confirmed against real bmad-epic-readiness-check SKILL.md), a direct AD-3 vs state-machines.md contradiction on the AUTO_FIX counter, a story-key format mismatch (epics.md dotted keys vs sprint-status.yaml/filename slugs, not equivalent), a readiness-report shape gap (real reports have addenda/stories_covered, AD-5 wrongly said forced re-sweep overwrites when real convention appends an addenda entry), an unspecified SQLite-loss-vs-pre-existing-real-status policy (confirmed non-hypothetical: this repo's real sprint-status.yaml already has 0-4/0-5/0-6/0-7 at review with zero orchestrator involvement), a CAP-1 Phase A autonomy gap vs AD-10's spirit (real bmad-create-epics-and-stories is explicitly interactive/WAIT-FOR-INPUT), and a real git-safety risk (AD-4's git add -A safety claim rests on the still-open dirty-tree precondition). All fixed at Finalize
- (decision) Fix: config path corrected to _bmad/bmm/config.yaml everywhere (planning_artifacts/implementation_artifacts live there, not _bmad/core/config.yaml which only has project_name/user_name/communication_language/document_output_language/output_folder)
- (decision) Fix: AD-3 rewritten to match state-machines.md exactly (increments on every applied AUTO_FIX patch, tier-agnostic, regardless of the following Tester outcome) rather than reverting state-machines.md to match AD-3's old Tester-failure-gated condition -- deliberately keeping the stricter rule since the Tester-failure-gated version reopens the original unbounded-cycling loophole a prior review found. Explicitly documented as an intentional tradeoff: a single successful fix can consume the whole shared budget before a second tier even gets a turn; MAX_AUTO_FIX_ATTEMPTS is user-tunable if that default proves too aggressive
- (decision) Fix: story-key format clarified -- epics.md section keys are dotted (0.7a), sprint-status.yaml keys and story filenames are dash-slugs (0-7a-nav-item-primitive) that are NOT a deterministic transform of the title. Existing stories are located by prefix-matching the dotted key's dash-equivalent (0-7a-*) against real filenames/sprint-status keys. A brand-new story's descriptive slug suffix is chosen by Planner at materialization time using the same judgment bmad-create-story applies, becoming the durable key from then on
- (decision) Fix: readiness_report shape corrected to include addenda (date/trigger/stories_corrected) and stories_covered, matching the real epic-1-readiness.md in this repo. CAP-14's forced re-sweep now APPENDS a new addenda entry to the existing report (preserving prior findings/date/stories_covered), never a wholesale overwrite -- matches the real convention this repo already uses for a correction to an already-swept epic
- (decision) New decision: foreign-work policy -- if a story is already at in-progress/review/done in real sprint-status.yaml with no SQLite checkpoint entry for the current run (confirmed non-hypothetical against this repo's real data), the orchestrator treats it as work it did not produce and leaves it untouched, skipping to the next backlog/ready-for-dev story in the epic by default. It is never silently resumed, reprocessed, or auto-reviewed. An explicit opt-in (a CLI flag to adopt a specific story) is required to have the orchestrator touch a story already in a non-backlog/ready-for-dev state it has no record of itself
- (decision) New decision: dirty-tree pre-flight gate -- resolves the previously-open uncommitted-work question. GitCheckpoint's git add -A safety claim depends on a clean starting tree; the orchestrator now checks git status --porcelain against TARGET_REPO_PATH at run start and REFUSES to start (hard error, not a warning) if there are any pre-existing uncommitted or untracked changes, telling the user to commit or stash first. This directly protects a live repo (this one included) from having unrelated human work silently folded into an autonomous done commit
- (decision) New decision: CAP-1 Phase A (Planner's first-ever coarse decomposition of a previously epic-less epics.md) requires a HITL confirmation pause before the draft is committed to epics.md -- extends CAP-7's HITL triggers. epics.md is itself a planning document, and the real bmad-create-epics-and-stories skill it mirrors is explicitly interactive (WAIT FOR INPUT on every menu) -- fully autonomous generation would sit uncomfortably close to AD-10's own rationale even though it does not technically violate AD-10's narrower PRD/architecture-file scope
- (note) Accepted, documented gap: sprint-status.yaml's review status cannot distinguish Tier-1-review-in-progress from Tier-2-review-in-progress from mid-AUTO_FIX-after-Tier-2-downgrade -- BMad's real 5-value enum has no tier concept and AD-5 forbids inventing a new status value, so this coarseness is accepted rather than worked around
- (event) All second-adversarial-review fixes applied to ARCHITECTURE-SPINE.md: AD-3 rewritten to match state-machines.md exactly (no more contradiction); AD-4 gained the dirty-tree pre-flight gate as its enabling precondition and disambiguated the epics.md-dotted-key vs sprint-status.yaml-slug-key commit message reference; AD-5 gained story-key-format clarification, addenda-append-not-overwrite, foreign-work policy, and the accepted review-substate gap note; AD-10 widened to bind CAP-1 and cover the Phase A HITL confirmation gate; Structural Seed's parse-readiness-report.ts type corrected; Deferred section pruned of now-decided items. lint_spine.py re-run clean (1 accepted false positive). spine finalized (second pass)
- (decision) Fix: Node.js pin corrected from 24 to 22 -- validated against this actual repo/machine: festgrid's own root package.json engines requires >=22.0.0, packageManager is pinned pnpm@9.15.4, and this dev machine has Node v22.13.1 installed (not 24). openai SDK 7.5.0 still supports Node 22 as its minimum (24 is only its recommended, not required, version), so no dependency conflict. Removes friction running the orchestrator on this machine or against this repo
- (decision) New: orchestrator adopts pnpm (matching this repo's pinned pnpm@9.15.4) as its own package manager for environment consistency, even though it is not a workspace member. New: orchestrator adopts ESLint 9.x (matching this repo's resolved 9.39.5) with its own standalone flat config for its own source -- not reusing @festgrid/eslint-config since the orchestrator is a standalone project, but matching the same major version for tooling consistency
- (event) Node/pnpm/ESLint fixes applied to ARCHITECTURE-SPINE.md Stack table and Structural Seed. lint_spine.py re-run clean (1 accepted false positive, unchanged). spine finalized (third pass)
- (decision) Fix (found during epics/stories cascading-failure elicitation): AD-4's git add -A justification (safe because the loop is serial) only accounted for the orchestrator's own changes across ONE story, not a human editing unrelated files elsewhere in TARGET_REPO_PATH between story commits during a long multi-story epic run -- the one-time start-of-run dirty-tree check doesn't protect the whole run's duration. Superseded: ExecPort.writeFile/writeIfUnchanged now track written paths per story (getWrittenPaths/resetWrittenPaths); GitCheckpoint stages only those tracked paths (git add <paths>), never git add -A. This removes the 'nothing else changes during the whole run' assumption entirely rather than just patching the timing gap -- a human editing anything the orchestrator didn't itself write is now structurally excluded from every commit, regardless of timing
- (event) Git-add scoping fix applied to AD-1 (new ExecPort methods) and AD-4 (scoped staging). lint_spine.py re-run clean (1 accepted false positive). spine finalized (fourth pass)
