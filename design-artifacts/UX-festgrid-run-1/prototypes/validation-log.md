# Prototype validation log

Automated Playwright screenshot-vs-source-PNG comparisons (see `.claude/skills/bmad-png-to-html/references/validation-script.md`). Human Artifact sign-off skipped for all seven per the run's instructions (no ambiguous comparisons surfaced).

All seven prototypes were re-rendered a second time at their card's actual real-world width (and, for the grid-item, real height) instead of an arbitrary demo size -- derived directly from `components.grid.masonry` (GridContainer baseCols/colsStep math), `WeeklyCalendarView.tsx`'s real wrapper padding, and `WeeklyCalendarView.tsx`'s `DAY_CELL_CLASS`/popover constants -- so a validated prototype also demonstrates the composition actually fits where it will render, not just that it matches the PNG in isolation. Each file's top comment shows the arithmetic. This surfaced one real finding: see the grid-item entries below.

- `event-card-masonry/default-with-thumbnail.html` -- validated 2026-09-14 -- structural/spacing match (TILL corner tag, large two-tier stacked date box, height-matched thumbnail, corner favorite pill, badge row, title/venue). Deviation: badge_row colors reuse established neutral/secondary tokens, not the PNG's literal green/maroon. Real-size check: renders cleanly at both 175px (mobile 2-col) and 269px (desktop xl:5-col) -- no overflow/truncation at either.
- `event-card-masonry/default-thumbnail-fallback.html` -- validated 2026-09-14 -- exact match (date box unchanged, reserved slot with bare centered heart+count). Same badge-color deviation as above. Real-size check: clean at 175px (mobile).
- `event-card-masonry/prominent-poster.html` -- validated 2026-09-14 -- structural match (overlay date box + TILL tag top-left, overlay favorite pill top-right, taller poster aspect, badges/title/venue below). Deviation: poster art is a flat placeholder, not the PNG's photographic scene -- only structure/text/color are in scope, not photographic content. Same badge-color deviation as above. Real-size check: clean at 175px (mobile).
- `event-card-calendar-row/with-thumbnail.html` -- validated 2026-09-14 -- structural match after one fix (initial render's placeholder poster text overlapped the 64x64 thumbnail's favorite pill illegibly; removed the text, kept the pill legible -- a mockup-art-only fix, not a structural one). Same badge-color deviation as above; card chrome uses the real shipped `EVENT_CARD_COMPACT_CLASS` (bg-violet-50/border-violet-200), not the PNG's plain background. Real-size check: clean at both 326px (narrow mobile) and 655px (widest mobile, just below the md: breakpoint where this surface stops existing) -- title truncates gracefully at 326px as expected, nothing overflows.
- `event-card-calendar-row/thumbnail-fallback.html` -- validated 2026-09-14 -- exact match (date box unchanged, reserved 64x64 slot, bare centered heart+count). Same deviations as the with-thumbnail state. Real-size check: clean at 326px (narrow mobile).
- `event-card-calendar-grid-item/with-thumbnail-multiple-days.html` -- validated 2026-09-14, revised same day after user feedback (twice). First validation: structural match for the elements the PNG shows (thumbnail, title/venue, favorite count, type badge), with a status badge and a "Day 2 of 5"/CalendarRange multi-day badge added for compositional completeness. **Real-size check surfaced a genuine finding**: rendered at the real 174x128px `day_cell` footprint (`WeeklyCalendarView.tsx` `DAY_CELL_CLASS`, 1280px viewport), the card does not fit -- `overflow-hidden` clips it, making the existing 5-events-per-day assumption physically impossible for any thumbnail-bearing card at that budget. Rendered instead at the real 200px inner width of the existing "+N more" popover, it fits comfortably -- this drove the attachment decision in EXPERIENCE.md (Component Patterns > Calendar View Cards: Attachment and Composition). **User feedback round 1**: image is conditional (`isMultiDay && !imgError`, no reserved slot), title wraps (no truncate), venue wraps up to 2 lines, category badge replaced by the nearby badge -- all corrected. **User feedback round 2** (after comparing both reference screenshots side by side): the favorite control was wrongly built as a small horizontal icon+text row in the with-image state; corrected to the same large, unpilled, icon-over-text shape (`event_card_favorite_count_badge_large`) both states actually show. Final file demonstrates both compositions side by side in one real 200px-wide popover: Row 1 (multi-day, image OK, 3-column) and Row 2 (single-day, no image, the structurally distinct 2-row composition) -- both render cleanly with no overflow.
- `event-card-calendar-grid-item/thumbnail-fallback.html` -- validated 2026-09-14, revised same day after user feedback (twice). First validation: exact match after correcting the favorite control to the vertical `event_card_favorite_count_badge_large` shape, reserved thumbnail slot stays blank. **User feedback round 1**: image should never reserve space at all for this card (only conditionally rendered for multi-day + valid image, per the with-thumbnail-multiple-days.html finding above) -- corrected to omit the image element entirely rather than leaving a blank reserved div; also applied title-wrap/venue-2-line-clamp/nearby-badge-replaces-category changes. **User feedback round 2**: user replaced the source PNG with a new composition -- two stacked rows (title+favorite on row 1, venue+nearby-badge on row 2) instead of the row-1-draft's "3-column row with the image column removed." Rebuilt to match exactly; re-validated against the new PNG at the real 200px popover width -- clean match, no overflow, title/venue wrapping behaves as directed.

## Round 3 revisions -- 2026-09-14, user's view-mode-organized rules pass

The user supplied a consolidated rules document organized by view-mode (calendar desktop/mobile, card view) plus two general rules (radius/distance badge replaces the category badge at `<8km`, "happeningNow" shortened to a single translatable word). All seven prototypes were revised again same-day; each is re-screenshotted and matches its real-grid-width validation from Round 2 (no new width/height regressions introduced by these content/behavior changes).

- `event-card-masonry/default-with-thumbnail.html`, `default-thumbnail-fallback.html`, `prominent-poster.html` -- title now wraps up to 2 lines at a smaller font (was single-line, larger); venue explicitly single-line (`truncate`); category badge replaced by the `<8km`-gated radius/distance badge; "happeningNow" label shortened to "Now". `prominent-poster.html` additionally: thumbnail aspect ratio changed from `aspect-[2/3]` to `1:1` (square), a real crop change the user's rules call out explicitly, overriding the reference PNG's own portrait crop. Masonry's reserved-space fallback convention is explicitly UNCHANGED -- the user's rules distinguish masonry (keeps it) from the row/grid-item cards (both drop it).
- `event-card-calendar-row/with-thumbnail.html` -- title now wraps up to 2 lines (was truncate); venue stays 1 line; same badge/label changes as masonry.
- `event-card-calendar-row/thumbnail-fallback.html` -- same title/venue/badge changes, plus a real reversal: "no area for image" per the user's explicit instruction -- the previously-reserved `w-16 h-16` slot is removed entirely (not just left blank); `content` now expands to fill the freed width and the large favorite control sits at the row's end with no reserved wrapper. This brings the row card in line with the grid-item card's own no-reserved-space rule, diverging from masonry.
- `event-card-calendar-grid-item/with-thumbnail-multiple-days.html` -- structural rework, not just styling: the "Day X of N" badge is removed entirely (user: implicit from the card's position in the calendar) and the multi-day composition is rebuilt to demonstrate a single card SPANNING multiple day-columns directly in the grid (3 adjacent 174px day-cell columns = a 522px-wide bar), rather than a fixed-width item in the "+N more" popover -- a real change to the attachment mechanism for the multi-day case specifically (single-day is unaffected, confirmed unchanged via the "Keep day_cell + popover for single-day" decision). A comparison panel shows the same composition constrained to a single day_cell's width for contrast. Category badge/label changes applied as above.
- `event-card-calendar-grid-item/thumbnail-fallback.html` -- confirms the single-day case is unchanged (day_cell + popover, no thumbnail, two-row composition); the radius/distance badge's `<8km` gating is now demonstrated with a second card showing the badge correctly omitted (not empty/placeholder) at `>=8km`.

## Round 4 revisions -- 2026-09-14, follow-up feedback on the round 3 prototypes

- General: every TILL/favorite/radius/"Now" badge across all 7 files bumped to a `>=11px` text-size floor (several narrow-panel instances had dropped to 9-10px). Applied to `event-card-masonry/{default-with-thumbnail,default-thumbnail-fallback,prominent-poster}.html` and `event-card-calendar-row/{with-thumbnail,thumbnail-fallback}.html`; the two grid-item files were already compliant (text-sm/text-xs throughout), no change needed there.
- `event-card-masonry/default-thumbnail-fallback.html` -- favorite icon within the reserved slot now scales up to occupy more of that fixed footprint (was a small w-5 h-5 icon with empty space around it); added a 269px desktop panel (new this round) specifically to demonstrate the icon scaling larger against the desktop card's taller reserved slot.
- `event-card-calendar-row/with-thumbnail.html` -- user replaced the reference PNG: the favorite heart+count moves from a small corner-overlay pill on the thumbnail to its own pill directly BELOW the thumbnail, the two forming a vertical stack centered within the row's full height. Pill drops its glassmorphism (solid `bg-background`/`shadow-sm` now, since it no longer sits over image content).
- `event-card-calendar-row/thumbnail-fallback.html` -- favorite control's own box now actively grows to occupy the space freed by the dropped image (was a fixed `shrink-0` small element) -- icon size scales with available space, demonstrated concretely at both real row widths (326px: w-7 icon; 655px: w-10 icon, more room freed).
- `event-card-calendar-grid-item/with-thumbnail-multiple-days.html` -- thumbnail is now explicit `aspect-square`, wrapped so it spans the full height of the title+venue column and centers within it (both axes) rather than a fixed size set independently; the "Now"/status badge is removed from this composition entirely (user: "no need the `now` related badge" on the desktop thumbnail/multiday view).
- `event-card-calendar-grid-item/thumbnail-fallback.html` -- no changes needed; already compliant with the `>=11px` floor and this round's feedback didn't touch the single-day/no-thumbnail composition.

All revised files re-screenshot-validated; no new overflow/clipping issues introduced by these changes.

## Coverage note added 2026-09-20 -- rounds 5-8 are not recorded in this file

This log covers rounds 1-4 (all 2026-09-14). `DESIGN.md` and `EXPERIENCE.md` cite further
user-directed revisions from the same pass -- "rounds 5-7" (date/favorite pill position made
conditional on the TILL badge, and the TILL tag's `-top-1.5` default / `-top-3` prominent-poster
exception), "round 6" (`favorite` badge font-size >= TILL badge font-size; `happeningNow`'s solid
emerald `happening_now` color), "round 7" (masonry card max-width capped at 230px across all three
states) and "round 8" (TILL and favorite badge font/padding harmonized across all three masonry
states, `text-[10px]`/`text-[11px]` replaced by the `text-xs`/`text-sm` pair). Some of those
`DESIGN.md` attributes to "git history on this file" (its own history) rather than to a named round.
No prototype-file revision or screenshot re-validation was recorded for those rounds, so the final
spec state has no fidelity record here. Flagged rather than reconstructed; see `backlog.yaml`
IDEA-046 (`_bmad-output/implementation-artifacts/backlog/IDEA-046-prototype-fidelity-residuals.md`,
renumbered from IDEA-043) for the rules and their implementation status.


