---
baseline_commit: d0e509ca48b074e50cfb56b6f6d93a31d9ce17e2
---

# Story 0.43: Build the visual-fidelity audit engine (packages/visual-audit)

## Story Details

- Epic: 0
- Story ID: 0.43
- Status: review
- **Depends on: Story 0.44** (`0-44-define-testing-standard-for-meta-testing-tooling-packages`) — see Pre-Coding Approval Gate below. `bmad-dev-story` must not start on this story until Story 0.44 reaches `done`.

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a developer,
I want a new workspace package, `packages/visual-audit`, implementing Architecture Spine AD-26 — a reusable check engine that compares a live, isolated component render against either a validated design-reference prototype (reference-based mode) or a hand-encoded structural invariant with no golden reference (rule-based mode), plus the manifest file format a story's acceptance criteria can cite by name,
so that a future story's AC can automate "does this match its prototype / does this satisfy this structural rule" instead of a developer eyeballing it or improvising a one-off Playwright script — the exact gap this session's own visual-fidelity audit of the event-card/calendar family hit (ad hoc Playwright screenshots + manual `getBoundingClientRect` measurement, no reusable artifact left behind, and a real production bug — `EventCardDateBox`'s day-slot overflow on word/time content, BUG-040 — that no test caught because no prototype ever depicted that content variant).

## Acceptance Criteria

1.  **Given** no `packages/visual-audit` package exists today, **When** this story ships, **Then** the package exists at `packages/visual-audit/` with `package.json`/`tsconfig.json` matching this repo's existing small-package boilerplate (`packages/graphql-select`'s pattern: `@festgrid/eslint-config`/`@festgrid/typescript-config` devDependencies, a `lint` script, `NodeNext` module resolution), and is wired into the pnpm workspace.
2.  **Given** AD-26 Rule 1's "two audit modes, one check engine," **When** a manifest entry is authored, **Then** the engine exposes both a **reference-based** check (live render vs. a validated prototype HTML/PNG pair under `design-artifacts/UX-festgrid-run-1/prototypes/**` + `imports/**`) and a **rule-based** check (a hand-encoded expected structural value, no golden reference) — both resolving to the same underlying computed-style/DOM assertion primitives, proven by at least one working example of each mode against a real, already-validated prototype pair (reference-based) and one synthetic hand-authored rule (rule-based, since AD-27's real rule-based consumer is a future story).
3.  **Given** AD-26 Rule 1a, **When** a manifest entry declares its render scope, **Then** the engine supports both **single-instance** (default) and **multi-instance** render, and Rule 5's sibling-dimension clustering (AC7) operates over whichever the manifest actually rendered — never hardcoded to assume single-instance only.
4.  **Given** AD-26 Rule 3, **When** a component/variant/viewport is covered, **Then** exactly one manifest entry exists for that triple (`packages/visual-audit/manifests/<name>.ts`), declaring: its prototype/PNG reference (reference-based only), its fixture props, its target viewport(s), and its rule set — the engine rejects (throws a clear error) a second manifest entry declaring the same component/variant/viewport triple.
5.  **Given** AD-26 Rule 4, **When** a manifest entry does not explicitly opt into live-route mode, **Then** it renders via **isolated-component-render** — Playwright 1.62's native stories/gallery component-testing model (`fixtures.mount()`, already implied by `apps/web`'s pinned `@playwright/test@1.62.0`), with fixture props only: no live Next.js server, no database, no auth. Live-route mode exists only as a documented escape hatch for genuinely route/data-dependent checks and is not exercised by this story's own example manifests.
6.  **Given** AD-26 Rule 2, **When** a reference-based check runs, **Then** the primary signal is computed-style/DOM introspection (bounding rects + `getComputedStyle()` values, compared against resolved DESIGN.md/Tailwind token values) and the secondary/confirmatory signal is a pixel screenshot diff against the source PNG via Playwright's built-in `toHaveScreenshot()` — both signals are reported, and a failure in either fails the check.
7.  **Given** AD-26 Rule 5's **sibling-dimension consistency** class, **When** elements are auto-clustered into rows/columns by bounding-box coordinate overlap (scoped per AC3), **Then** clustered siblings expected to share a dimension are checked at a configurable **absolute tolerance, default ≤2px**, overridable per rule in the manifest.
8.  **Given** AD-26 Rule 5's **intra-box ratio consistency** class, **When** a named element pair is checked against an expected ratio, **Then** the check uses a configurable **relative tolerance, default ±8–10%**, overridable per rule in the manifest, and the expected ratio can be sourced either from an explicit DESIGN.md token or derived once from the referenced prototype's own rendered ratio.
9.  **Given** AD-26 Rule 5's **overflow/clipping** class, **When** a manifest entry names a text/content slot and its backing formatting function, **Then** the engine derives that slot's content-variant catalog via **static analysis of the formatting function's branches** (using `ts-morph` to parse and enumerate the function's conditional/switch branches — a new devDependency, confirmed not already present anywhere in this monorepo), renders each enumerated variant, and flags `scrollWidth > clientWidth` or a bounding rect exceeding its ancestor's for any variant — including variants the source prototype never itself depicted.
10. **Given** AD-26 Rule 6's **color fidelity** class, **When** an element's color is checked, **Then** the primary signal is an exact computed-value match against the element's resolved DESIGN.md/Tailwind token (e.g. `bg-slate-800`'s resolved RGB/OKLCH), and the fallback signal (used only when no token is declared for that element) is a perceptual pixel diff against the source PNG.
11. **Given** this story builds the mechanism, not every manifest entry, **When** this story ships, **Then** it includes example/proof manifest entries for at most 1–2 already-validated prototypes (sufficient to prove AC2/AC6–AC10 end-to-end) — it does NOT retroactively author manifest entries for all 7 existing validated prototypes, does NOT fix BUG-040 (`EventCardDateBox` overflow), and does NOT build AD-27's masonry engine or its manifest entry; those remain separate, not-yet-created stories that consume this engine.
12. **Given** the engine must be usable as a library, **When** a future story's own test file imports it, **Then** `packages/visual-audit` exports a documented, typed public API (at minimum: a function to run a named manifest entry and return a structured pass/fail result per rule class) usable from a `vitest`/`tsx --test` test file in another package the same way `packages/graphql-select`'s utilities are imported today — it is not CLI-only.
13. **Given** the user chose to sequence Story 0.44 (define the testing standard for meta-testing/tooling packages) before this story rather than accept this story's original Escape Hatch (a self-declared scoped DoD, since retracted — see Dev Notes), **When** this story ships, **Then** its own Definition of Done for testing conforms to `project-context.md`'s "Meta-Testing / Tooling Packages" tier as defined by Story 0.44 (unit tests via `tsx --test` for the pure comparison/tolerance/clustering/branch-enumeration logic, no 100%-coverage requirement, and the example manifest entries themselves serving as this story's integration proof for its browser-automation/AST-analysis surfaces) — inheriting that project-wide rule rather than re-declaring its own.

## Tasks / Subtasks

- [x] Task 1 — Package scaffold (AC: #1)
  - [x] Create `packages/visual-audit/package.json`, `tsconfig.json`, `eslint.config.mjs` matching `packages/graphql-select`'s boilerplate; add `@playwright/test` (pinned to `apps/web`'s `1.62.0`) and `ts-morph` as dependencies.
  - [x] Wire into pnpm workspace; confirm `pnpm install` resolves cleanly.
- [x] Task 2 — Manifest schema and registry (AC: #4, #11, #12)
  - [x] Define the manifest entry TypeScript type (component ref, variant, viewport(s), render scope, prototype/PNG reference optional, fixture props, rule set).
  - [x] Implement a manifest registry that rejects duplicate component/variant/viewport triples.
  - [x] Export a public `runManifestEntry(name)` (or equivalent) API.
- [x] Task 3 — Isolated-component-render harness (AC: #5)
  - [x] Set up an isolated-component-render harness inside `packages/visual-audit`, mounting fixture markup with fixture props, no server/DB/auth (see Dev Notes judgment call re: `fixtures.mount()`).
  - [x] Document the live-route-mode escape hatch (not exercised by this story's own examples).
- [x] Task 4 — Compare engine: computed-style + pixel diff (AC: #2, #6)
  - [x] Implement computed-style/bounding-rect extraction via `page.evaluate()`/`getComputedStyle()`.
  - [x] Wire Playwright's built-in `toHaveScreenshot()` as the secondary pixel-diff signal.
  - [x] Implement the rule-based mode (hand-encoded expected value, no golden reference) sharing the same assertion primitives.
- [x] Task 5 — Sibling-dimension clustering rule (AC: #3, #7)
  - [x] Implement bounding-box coordinate-overlap auto-clustering, scoped to whatever the manifest rendered (single- or multi-instance).
  - [x] Implement the ≤2px default absolute-tolerance check, overridable per rule.
- [x] Task 6 — Intra-box ratio rule (AC: #8)
  - [x] Implement named-pair ratio comparison with ±8–10% default relative tolerance, overridable per rule; support both DESIGN.md-token-sourced and prototype-derived expected ratios.
- [x] Task 7 — Overflow/content-variant rule via ts-morph (AC: #9)
  - [x] Implement branch enumeration over a named formatting function's source via `ts-morph`.
  - [x] Render each enumerated variant and assert no overflow/clipping.
- [x] Task 8 — Color fidelity rule (AC: #10)
  - [x] Implement token-exact-match-primary / pixel-diff-fallback color check.
- [x] Task 9 — Example manifest entries and proof (AC: #2, #11)
  - [x] Author 1–2 example manifest entries against an already-validated `design-artifacts/UX-festgrid-run-1/prototypes/**` pair, exercising both audit modes and all five rule classes end-to-end.
- [x] Task 10 — Testing (AC: #13)
  - [x] Unit-test the pure comparison/tolerance/clustering/branch-enumeration logic (`tsx --test`, mirroring `packages/graphql-select`).
  - [x] Confirm the example manifest entries themselves pass, serving as this story's own integration proof.
  - [x] Document the scoped DoD decision in Dev Notes pending Story 0.44.

## Dev Notes

- This is a pure dev-tooling/testing package. It ships zero production UI, zero database/GraphQL surface, and is never bundled into `apps/web`'s production build — it sits in the same category as `packages/graphql-select`/`packages/testing-config`.
- **Package boilerplate precedent:** `packages/graphql-select/package.json` (main/types/exports pointing at `dist/`, `build`/`test`/`lint` scripts, `@festgrid/eslint-config`+`@festgrid/typescript-config` devDependencies) and its `tsconfig.json` (`extends: "@festgrid/typescript-config/base.json"`, `module`/`moduleResolution: "NodeNext"`, `outDir: "dist"`) are the closest existing shape to copy. Its `test: "tsx --test *.test.ts"` script is the pattern this story's own unit tests should follow, since `packages/visual-audit` is not `packages/domain` and not an `apps/*` app.
- **`apps/web/e2e/`** already carries `@playwright/test@1.62.0` as a devDependency and has an established Playwright config/spec-file pattern (`apps/web/e2e/*.spec.ts`, `global-setup.ts`). This story pins the same version in `packages/visual-audit` rather than introducing a second Playwright version or a second browser-automation library.
- **Design-reference source of truth:** `design-artifacts/UX-festgrid-run-1/prototypes/**/*.html` + `imports/**/*.png` (currently 3 prototype families: `event-card-calendar-grid-item/`, `event-card-calendar-row/`, `event-card-masonry/`). `prototypes/validation-log.md` is the existing (informal, not schema-binding) precedent for how a "validated" prototype's own metadata is tracked — Gate 2 (Freya) suggests the manifest format could usefully capture similar fields (validated date, documented deviations, real-dimension check) for consistency, though this is a suggestion, not a hard requirement.
- **Sequencing (per the user's explicit instruction):** this is the first of a 3-story sequence: this tool (Story 0.43) → AD-27's masonry engine story (not yet created, tracked as `IDEA-050`) → BUG-040's `EventCardDateBox` date-box fix story (not yet created, tracked as `BUG-040`). Story `1-3k-render-day-of-week-recurring-schedules-and-repeat-badge` (currently `ready-for-dev`, untouched by this story) shares files (`EventCardMediaPrimitives.tsx`, `EventCard.tsx`, `WeeklyCalendarView.tsx`) with the eventual BUG-040 fix story — the actual `epics.md` dependency amendment to Story 1.3k belongs to the BUG-040 fix story once it exists, not this one.
- **Full decision trail:** `_bmad-output/planning-artifacts/architecture/architecture-festgrid-2026-09-22/.memlog.md` (21 entries) records the rejected alternatives behind every AD-26/AD-27 decision cited above (e.g. manifest-declared selectors vs. auto-clustering for sibling grouping; hand-authored vs. static-analysis content-variant catalogs; native CSS masonry ruled out as Safari-only in 2026).
- **Web-verified technical choices (2026-09-22), cited per this session's own research, not training-data assumption:**
  - **Isolated-component-render mechanism:** Playwright 1.62 (shipped 24 July 2026, patched to 1.62.1) introduced a native "stories and galleries" component-testing model built directly into plain `@playwright/test` — a `fixtures.mount()` fixture navigates to a gallery page and mounts a named story (component + fixture props), returning a `Locator` with `update()`/`unmount()`. This supersedes the older, separate `@playwright/experimental-ct-react` package for a codebase already on `@playwright/test@1.62.0` (as `apps/web` is) and is the mechanism this story should use — it satisfies "Playwright is already a devDependency ... reuse it, don't add a second browser-automation dependency" more literally than the older CT package would, since it's native to the version already pinned. [Source: github.com/microsoft/playwright releases v1.62.0; bug0.com "What's new in Playwright 1.62"]
  - **Computed-style/DOM assertions:** current guidance favors `toHaveCSS()` for simple single-property retrying assertions, and `page.evaluate()`/`locator.evaluate()` wrapped in `expect.poll()` for multi-property `getComputedStyle()`/bounding-rect reads — no dedicated third-party assertion library is needed or commonly recommended over Playwright's own native APIs. [Source: qaskills.sh "How to Assert CSS Computed Style in Playwright Without Brittle Tests"; playwright.dev/docs/handles]
  - **Pixel screenshot diffing:** Playwright's built-in `toHaveScreenshot()` (backed internally by `pixelmatch`) remains the current standard approach — no need for a separate diffing library. 2026 best practice: prefer component-level (not full-page) screenshots, mask dynamic content, disable animations before capture, and set per-component `threshold`/`maxDiffPixelRatio` rather than one global tolerance. [Source: playwright.dev/docs/test-snapshots; bug0.com "Playwright Visual Regression Testing: Built-In Guide 2026"]
  - **Content-variant catalog via static analysis:** `ts-morph` (TypeScript Compiler API wrapper) is a live, actively documented library purpose-built for exactly this — traversing a function's AST (`forEachDescendant`, branch/traversal control) to enumerate its conditional/switch branches programmatically, rather than hand-authoring a fixture list. Confirmed as a sane, current choice for this repo's TS/monorepo context; not previously used anywhere in this codebase (new dependency). [Source: ts-morph.com; github.com/dsherret/ts-morph]

### Architecture & UX Gate Findings

- **Gate 1 (Winston, Architecture/Infrastructure Completeness): No gap found.** This package never enters `apps/web`'s production bundle; Playwright's `mount()`/`getComputedStyle()`/`toHaveScreenshot()` calls are test-time/CI-time tool invocations, not a production frontend-to-external-service call. No DB/ORM/domain reachthrough, no new backend API surface, no auth/secrets in frontend code, and `ts-morph`/Playwright's story-bundling are devDependency-tier additions with no deploy/IaC surface (the tool itself is never deployed anywhere).
- **Gate 2 (Freya, UI Complexity & Reusability): No gap found.** Verified, not assumed: this story ships zero production React components/routes; its only "UI" is CLI/report output. The manifest schema and fixture-mounting helper are correctly scoped as this story's own deliverable (the primitive being built), not a UI primitive masking a missing shared piece. DESIGN.md is a token spec, not a manifest-format spec, so it imposes no binding schema here; `prototypes/validation-log.md` is informal precedent worth drawing on for consistency (see Dev Notes above), not a hard requirement.
- **Gate 3 (Winston, Foundational/Cross-Cutting Dependency Completeness): Gap found.** `project-context.md`'s Testing Rules section is a closed two-tier partition (`packages/domain`: 100% unit coverage; `apps/*`: testing-trophy + E2E DoD) that a meta-testing/tooling package like `packages/visual-audit` fits neither. Since this story is the first of its kind and its own future adopters (AD-27's masonry story, BUG-040's fix story) will inherit whatever precedent it sets, left undecided this becomes an ad-hoc convention set by accident rather than a deliberate, project-wide rule. **Recorded as `FIND-047`, split into Story 0.44** (see `## Out of Scope` below) per the tooling/infrastructure numbering rule (new sequential Epic 0 story, since this is reusable/foundational by nature). Gate 3 also confirmed: zero GraphQL/codegen dependency in this story's scope; a `bmad-dev-story` workflow-wiring note (teaching it to execute a `packages/visual-audit` manifest check) is recorded as a Dev Note for whichever story first cites a manifest entry from its own AC, not a second foundational story; and Playwright's story/gallery bundling (Vite-based) is contained entirely within this package's own test execution, analogous to `apps/web`'s existing Vitest+`vite-tsconfig-paths` test-only bundling — not a second production build pipeline, so not cross-cutting on its own.
  - **UPDATE 2026-09-22 (sequencing amendment):** the Escape Hatch clause originally cited here (proceed under a self-declared scoped DoD, AC13, without waiting for Story 0.44) was **rejected by the user**, who explicitly chose "0.44 first, then 0.43" when asked to decide between the two paths. This story is amended accordingly: **AC13 no longer self-declares a scoped DoD** — it now inherits whatever testing standard Story 0.44 defines (that story shipped `ready-for-dev` the same session, see its file's Dev Notes for the side-by-side confirming it matches this story's originally-intended approach exactly). A `Depends on: Story 0.44` note and a corresponding Pre-Coding Approval Gate blocking item (below) were added. This story's own status remains `ready-for-dev` in `sprint-status.yaml` — per this repo's established convention (see `event-pages-dev-story-tracking.md`'s dependency graph), the gate is enforced via this explicit Pre-Coding Approval Gate note, checked at `bmad-dev-story` dispatch time, not via a separate sprint-status value.

### Data Type Compatibility & Migration Requirements

- Compatibility finding: No mismatch found.
- Impacted fields/contracts: None — this story introduces no database schema, no GraphQL types, and no new TypeScript models consumed across a DB/API/frontend boundary.
- Required DB migration changes: No changes required.
- Required TypeScript type changes: No changes required — the manifest/rule-result types are internal to `packages/visual-audit` and its consumers' test files.
- Backward compatibility and rollout notes: Not applicable — new, standalone package with no existing consumers to break.
- Verification checks: `packages/visual-audit`'s own `tsc`/lint pass is sufficient; no cross-boundary type verification needed.

### Project Structure Notes

- New package at `packages/visual-audit/`, matching the flat top-level `packages/*` convention (alongside `graphql-select`, `domain`, `ui`, `database`, `shared-types`, `testing-config`).
- No conflicts detected with the existing monorepo structure; no existing file needs to move.

### References

- [Source: _bmad-output/planning-artifacts/festgrid-architecture-spine.md#AD-26] — full rule set this story implements.
- [Source: _bmad-output/planning-artifacts/festgrid-architecture-spine.md#AD-27] — adjacent future consumer; this story must not preclude it (Rule 1a exists for this reason).
- [Source: _bmad-output/planning-artifacts/architecture/architecture-festgrid-2026-09-22/.memlog.md] — full decision trail and rejected alternatives.
- [Source: _bmad-output/implementation-artifacts/backlog.yaml#IDEA-049] — originating backlog row.
- [Source: _bmad-output/implementation-artifacts/backlog.yaml#BUG-040] — the production bug this tool exists to catch (not fixed by this story).
- [Source: packages/graphql-select/package.json, packages/graphql-select/tsconfig.json] — package boilerplate precedent.
- [Source: design-artifacts/UX-festgrid-run-1/prototypes/validation-log.md] — informal prototype-validation metadata precedent.

## Global Rules References

- [x] project-context.md — Code Organization (`packages/*` conventions), Testing Rules (Gate 3 finding applies, see Dev Notes).
- [x] story-content-structure.md — canonical section order followed.
- [x] architecture spine — AD-26 (this story), AD-27 (adjacent, not in scope).
- [x] infrastructure docs — not applicable (no AWS/infra surface introduced).

## Implementation Plan (Rule-Compliant)

- **File Change Plan:**
  - New: `packages/visual-audit/package.json`, `tsconfig.json`, `eslint.config.mjs`
  - New: `packages/visual-audit/src/index.ts` (public API)
  - New: `packages/visual-audit/src/manifest.ts` (manifest type + registry)
  - New: `packages/visual-audit/src/render.ts` (isolated-component-render harness via Playwright 1.62 stories/gallery)
  - New: `packages/visual-audit/src/compare/computed-style.ts`, `pixel-diff.ts`
  - New: `packages/visual-audit/src/rules/sibling-dimension.ts`, `intra-box-ratio.ts`, `overflow.ts`, `color.ts`
  - New: `packages/visual-audit/src/content-variants.ts` (`ts-morph`-based branch enumeration)
  - New: `packages/visual-audit/manifests/<example>.ts` (1–2 proof entries)
  - New: `packages/visual-audit/*.test.ts` (unit tests, `tsx --test` pattern)
- **Rule Mapping:** AC1→package scaffold; AC2/AC11→two-mode engine + example manifests; AC3→Rule 1a render-scope; AC4→manifest registry/dedup; AC5→isolated-render default; AC6→hybrid compare; AC7→sibling-dimension tolerance; AC8→intra-box ratio tolerance; AC9→ts-morph content-variant catalog; AC10→color fidelity; AC12→public library API; AC13→testing/DoD inherited from Story 0.44 (dependency, see Pre-Coding Approval Gate).
- **Verification Plan:** `pnpm --filter @festgrid/visual-audit test` (unit tests for pure logic) + the example manifest entries themselves passing end-to-end (proving isolated-render, both audit modes, and all five rule classes against a real validated prototype); `pnpm --filter @festgrid/visual-audit lint` clean; `pnpm install` resolves the new `ts-morph`/`@playwright/test` dependencies without version conflicts against `apps/web`'s existing `@playwright/test@1.62.0`.

## Pre-Coding Approval Gate

- [ ] Scope confirmation
- [ ] Architecture and boundary confirmation
- [ ] Testing plan confirmation
- [ ] Explicit human approval state (Default: pending approval)
- [ ] Gate 1/2/3 prerequisites confirmed done or gap accepted — **Gate 3's gap (FIND-047) is BLOCKING per the user's explicit sequencing decision (2026-09-22): Story 0.44 must reach `done` before this story's `bmad-dev-story` starts. The Escape Hatch (AC13's original self-declared scoped DoD) was considered and rejected in favor of proper sequencing. Do not begin implementation until Story 0.44's status is `done` in `sprint-status.yaml`.**

## Testing Requirements

- [ ] Unit tests for pure comparison/tolerance/clustering/branch-enumeration logic (`tsx --test`, `packages/graphql-select` pattern)
- [ ] Integration proof via the example manifest entries themselves (isolated-render + both audit modes + all five rule classes, against a real validated prototype)

## Deliverables Checklist

- [ ] `packages/visual-audit` package scaffolded and wired into the pnpm workspace
- [ ] Manifest schema + registry with duplicate-triple rejection
- [ ] Isolated-component-render harness (Playwright 1.62 stories/gallery)
- [ ] Hybrid compare engine (computed-style primary, pixel diff secondary)
- [ ] All five rule classes implemented with their specified tolerance shapes
- [ ] Public, documented, importable API
- [ ] 1–2 example/proof manifest entries against a real validated prototype
- [ ] Unit tests + documented scoped DoD (AC13)

## Out of Scope

- Authoring manifest entries for the remaining validated prototypes (deferred to each adopting story).
- Fixing BUG-040 (`EventCardDateBox` overflow) — a future story, though it should cite this tool's engine for its own AC once created.
- Building AD-27's masonry engine or its manifest entry — a future story (`IDEA-050`).
- **Gate 3 finding (FIND-047):** promoting a permanent testing-standard tier for meta-testing/tooling packages into `project-context.md` — split into **Story 0.44** (`0-44-define-testing-standard-for-meta-testing-tooling-packages`), not built as a byproduct of this story. **This is now a hard dependency, not a parallel/non-blocking item** — see `Depends on` note and Pre-Coding Approval Gate above.
- Wiring `bmad-dev-story`'s own verification step to automatically execute a `packages/visual-audit` manifest check — deferred to whichever story first cites a manifest entry from its own AC.

## Definition of Done

- [ ] AC satisfaction (AC1–AC13)
- [ ] Required tests passing (unit tests + example manifest entries)
- [ ] Lint and type checks passing for `packages/visual-audit`

## Completion Status

- [ ] Not started

## Change Log
### Review Findings

### decision-needed

- [x] [Review][Decision] Isolated render is `page.setContent()`-based — no `fixtures.mount()` render variant exists, so the tool cannot yet render a real React component — AC5 names Playwright 1.62's `fixtures.mount()` as the mechanism, and AD-26's entire purpose is comparing a *live implementation* against prototypes (BUG-040, AD-27). As shipped, `RenderSpec` only supports `isolated-html`/`live-route`; the engine cannot mount `EventCard`/`EventCardDateBox`. Defensible for this story (no React consumer exists yet, no React dep in the file plan — `packages/visual-audit/src/render.ts` header documents it), but the capability gap must be explicitly accepted and tracked, not silently deferred by implication [$\Rightarrow$ either amend AC5/record a follow-up story for the mount-backed `RenderSpec` variant, or extend it here].
  **RULING (user, 2026-09-22): Extend the harness now.** Add a mount-backed `RenderSpec` variant (`fixtures.mount()`) in this same story, including whatever React devDependency it needs — do not defer.
  **RESOLVED (2026-09-22):** Added a `react-component` `RenderSpec` variant (`src/manifest.ts`/`src/render.ts`) that server-renders a real React component tree via `react-dom/server`'s `renderToStaticMarkup`, then loads the markup the same isolated way as `isolated-html` — genuinely isolated (no server/DB/auth), satisfying Rule 4 without a second bundler pipeline. Added `react`/`react-dom` (`^19.0.0`, matching `apps/web`) as runtime dependencies and `@festgrid/ui` as a devDependency. Proof manifest `manifests/count-badge-react-mount.ts` mounts the real `@festgrid/ui` `CountBadge` component (via a new `@festgrid/ui` export subpath, `./count-badge`) and passes its color/sibling-dimension rules end-to-end through `@playwright/test`. Judgment call, documented in `src/render.ts` and the manifest itself: mounting the story's originally-named example (`EventCardDateBox`) was attempted first and reproduced a real, fully diagnosed third-party interop bug — Playwright's test transform defaults `.tsx` JSX to its own internal `playwright/jsx-runtime` (used for its reporter/attachment UI) rather than React's, and separately `EventCardDateBox`'s file pulls in `lucide-react`, an ESM-only-packaged dependency that a CJS-transformed consumer can't safely `require()` (Node 22's `require(esm)` interop loads its CJS-syntax build as an ES module, so its own internal `require()` calls throw). The JSX-runtime half was fixed with a one-line, no-op-for-`@festgrid/ui`'s-own-build `/** @jsxImportSource react */` pragma added to `packages/ui/src/core/count-badge.tsx` (a file with no `lucide-react` dependency, so the second bug doesn't apply there); an icon-bearing component mount is left as a known follow-up for whichever future story needs one (e.g. BUG-040's fix).
- [x] [Review][Decision] The reference-mode proof is self-referential — passes by construction — AC2 requires the reference-based example to check "against a real, already-validated prototype pair". `manifests/event-card-masonry-thumbnail-fallback.ts` instead renders the prototype's own markup extracted verbatim (`MOBILE_PANEL_HTML`) against the same prototype file → trivially matches. Compounding it, `src/engine.ts` derives `expectedRatio` for intra-box-ratio from the *live render's* two measured values (`deriveRatioFromReference(valueA, valueB)` measured on the same page), never from the reference prototype render — AC8 says "derived once from the referenced prototype's own rendered ratio". As a result the reference-mode rule run can never fail by design. [$\Rightarrow$ decide: mount the actual prototype file (`mountPrototypeFile`/`mountReferenceFor`) as the comparison target + add a negative "canary" proof (deliberately mutated markup must FAIL), or accept wiring-only proof.]
  **RULING (user, 2026-09-22): Mount real prototype as reference + negative canary.** Wire `mountReferenceFor` so `expectedRatio`/comparison targets come from an independently-rendered prototype, not the same live render being checked; add a canary test with deliberately mutated markup that must FAIL, proving the check can actually fail.
  **RESOLVED (2026-09-22):** `runManifestEntry`'s intra-box-ratio branch now opens a second `page` in the same browser context, mounts the real validated prototype file via `mountReferenceFor`, and derives `expectedRatio` from *that* independent render — never from the live page being checked. Added `IntraBoxRatioRule.referenceSelectorA`/`referenceSelectorB` (`src/manifest.ts`) since the real prototype HTML carries no `data-testid` attributes of its own (those exist only in this manifest's live fixture, copied from it); the masonry manifest declares class-based selectors that uniquely address the mobile panel's month/day spans in the real file. Added a negative canary test (`manifests-proof.spec.ts`) that deliberately shrinks the live render's month-text class to `text-[1px]` and asserts `runManifestEntry` reports `pass: false` — proving the check can actually fail, not just pass. Verified: `npx playwright test` — canary test passes (the mutated render is correctly flagged).
- [x] [Review][Decision] `toHaveScreenshot()` baseline is Playwright-managed, never diffed against the design-artifacts `imports/**/*.png` AD-26/AC6 name as the reference source — `reference.prototypePngPath` is declared but never read; the committed baseline under `manifests-proof.spec.ts-snapshots/` was captured from the same mounted markup the check compares (second passes-by-construction signal). Implementer deferred custom `snapshotPathTemplate` wiring; documented in Dev Agent Record but **not registered in deferred-work.md/backlog.yaml** as this repo's convention requires. [$\Rightarrow$ decide: wire the PNG-source diff now, or defer with a registered backlog entry.]
  **RULING (user, 2026-09-22): Wire the `imports/**/*.png` diff now.** Make `toHaveScreenshot()` (or an equivalent pixel-diff call) actually diff against `reference.prototypePngPath`'s source PNG, not a self-captured baseline. Do not defer.
  **RESOLVED (2026-09-22):** Replaced `toHaveScreenshot()`'s self-captured-baseline mechanism with a real pixel-comparison (`src/compare/pixel-diff.ts`) that reads `reference.prototypePngPath`'s actual file via `fs`/`sharp` and diffs it (per-pixel RGB distance, resized to the rendered screenshot's own resolution) against a live screenshot. `pixelmatch`/`pngjs` are not resolvable in this monorepo's offline package store (no network access at implementation time); `sharp` — already present in `pnpm-lock.yaml` as an existing transitive dependency (pulled in by `next`'s image optimization) — is used instead, pinned to its already-resolved version so `pnpm install` needed no registry fetch. This choice (raw pixel comparison against the real file, not `toHaveScreenshot()`/`toMatchSnapshot()`) is documented in `src/compare/pixel-diff.ts`'s header. The committed win32-qualified baseline (patch item below) is now unused and was deleted. Tolerance is intentionally generous (`maxDiffPixelRatio: 0.55`) for the masonry example specifically, documented in the manifest: the prototype's badge row/icon are a *validation-log.md-documented, accepted* deviation from the literal PNG (distance badge vs. the PNG's category badge; established color tokens vs. the PNG's literal colors), and a Chromium render will never byte-match a Figma raster export. The pixel-diff signal itself is not separately canary-tested (the intra-box-ratio canary above proves the engine's fail path generally); a structurally wrong render (blank card, wrong colors entirely) would still push the diff ratio well past 55%.

### patch

- [x] [Review][Patch] ts-morph enumeration misses nested-if branches — the real `formatEventStatus`'s "happeningNow" branch (`{ text: labels?.statusHappeningNow ?? 'Now' }`) sits in an `if (started) { if (endDayDiff > 0) ... }` nested block; `enumerateContentVariants` only walks the top-level if-chain, so the 'Now' branch's real text is never rendered (empirically verified: 7 variants enumerated, `started => "Ends Today"`, no `Now` candidate). The "Tomorrow" BUG-040 case IS caught (top-level), but the "8 branches" claim in the Debug Log is not what ships [packages/visual-audit/src/content-variants.ts:~95-130]
  **RESOLVED (2026-09-22):** Rewrote the branch-walk (`collectFromStatement`/`collectFromIfStatement`) to recurse into any nested `IfStatement` found inside a then/else arm, in addition to still capturing a same-level trailing `return` as its own variant — both the nested `Now` branch and the sibling `Ends Today` branch are now enumerated. Strengthened `content-variants.test.ts`'s existing `formatEventStatus`-shaped test to explicitly assert both `'Now'` and `'Ends Today'` are present (previously only asserted `variants.length >= 7`, which passed under the bug too). Verified: `npx tsx --test content-variants.test.ts` — new assertions pass.
- [x] [Review][Patch] Color rule's no-token fallback returns `pass: true` trivially — `runManifestEntry`'s color branch reports pass with "falling back to pixel-diff signal (see manifest spec)" when no token is declared, so that rule can never fail; a consumer trusting the structured result gets false confidence (AC10's fallback is not checkable) [packages/visual-audit/src/engine.ts:~90-94]
  **RESOLVED (2026-09-22):** A missing token now either (a) runs the real pixel-diff fallback (`diffScreenshotAgainstReferencePng`, scoped to just that element) when the entry is reference-mode with a declared PNG, or (b) reports `pass: false` with a clear "cannot be verified" message when no reference PNG is available (rule-based entries) — no path silently reports `pass: true` anymore.
- [x] [Review][Patch] Pixel-diff signal is not integrated into `runManifestEntry`'s result — AC6 states "a failure in either fails the check", but `ManifestRunResult.pass` is computed only from computed-style/DOM rules; a visual divergence fails only the proof spec's separate `toHaveScreenshot()` test, not the engine API the future consumers will call [packages/visual-audit/src/engine.ts:runManifestEntry]
  **RESOLVED (2026-09-22):** `runManifestEntry` now runs the real PNG pixel-diff itself for every reference-mode entry with a declared `reference.prototypePngPath`, pushing a `pixel-diff` entry into `ruleResults` — `ManifestRunResult.pass` (`ruleResults.every(r => r.pass)`) now genuinely reflects AC6's "a failure in either signal fails the check" from the public engine API, not just the proof spec's own assertions.
- [x] [Review][Patch] `assertFormattingFunctionExists` is exported but never called — the fail-fast re-read guard is dead code; either invoke it from the proof spec or engine [packages/visual-audit/manifests/event-card-masonry-thumbnail-fallback.ts:134-140]
  **RESOLVED (2026-09-22):** Removed the dead, manifest-specific export. Replaced it with a generic guard built directly into `runOverflowRule` (`src/rules/overflow.ts`) that fail-fast/loud-checks *any* `OverflowRule`'s cited formatting function exists before `ts-morph` parses it (throws `FormattingFunctionNotFoundError`) — runs unconditionally on every overflow check, not just this one manifest's, and needs no caller to remember to invoke it.
- [x] [Review][Patch] Committed screenshot baseline is win32-qualified — `event-card-masonry-thumbnail-fallback-mobile-chromium-win32.png`; on Linux CI the snapshot test fails without `--update-snapshots` [packages/visual-audit/manifests-proof.spec.ts-snapshots/]
  **RESOLVED (2026-09-22):** The `toHaveScreenshot()` self-captured-baseline mechanism this file belonged to was removed entirely as part of the decision-needed item 3 fix (real PNG pixel-diff replaces it) — the win32-qualified baseline is no longer referenced by any test and was deleted (`manifests-proof.spec.ts-snapshots/` directory removed), so there is no platform-locked snapshot left to fail on Linux CI.
- [x] [Review][Patch] Fixture/prototype HTML loads Tailwind from `cdn.tailwindcss.com` at test time — network-dependent renders; `mountPrototypeFile` (`file://` + CDN script) makes the proof flaky in a sandboxed CI [packages/visual-audit/manifests/event-card-masonry-thumbnail-fallback.ts:25 / src/render.ts:53]
  **RESOLVED (2026-09-22):** Neither this manifest's fixture markup nor the real `design-artifacts/**/prototypes/**/*.html` files vendor Tailwind any other way (confirmed: all reference the CDN script verbatim), so rather than edit either, `mountManifestEntry`/`mountPrototypeFile` (`src/render.ts`) now intercept the `cdn.tailwindcss.com` request via `page.route()` and fulfill it locally from a statically pre-compiled Tailwind CSS bundle (`packages/visual-audit/vendor/tailwind.generated.css`), built offline via this repo's own local `tailwindcss` v3.4.1 install against a content-glob config (`vendor/tailwind.config.cjs`) covering this package's fixtures/manifests and the real prototype HTML files. A small inline shim also stubs `window.tailwind.config` so each fixture's own inline `tailwind.config = {...}` block still executes harmlessly. No render in this package's test suite depends on network access anymore.


- 2026-09-22 — Implemented `packages/visual-audit` end-to-end (AC1–AC13): manifest schema/registry, isolated-component-render harness, hybrid compare engine, all five AD-26 Rule 5/6 rule classes, two example/proof manifest entries, and the `tsx --test` + `@playwright/test` split-tier test suite per Story 0.44's testing standard. Status moved `ready-for-dev` → `in-progress` → `review`.
- 2026-09-22 — Addressed code review findings: 9 items resolved (3 decision-needed per user rulings, 6 patch). Added a real `react-component` `RenderSpec` mount mechanism (proven against `@festgrid/ui`'s `CountBadge`); decoupled reference-derived expected ratios from the live render being checked (`mountReferenceFor` + a negative canary test proving the check can fail); replaced the self-referential `toHaveScreenshot()` pixel signal with a real diff against the source-of-truth PNG, now integrated into `runManifestEntry`'s own `pass`; fixed `ts-morph` branch enumeration to walk nested `if`s; fixed the color rule's missing-token path to stop silently passing; wired the formatting-function existence guard into the overflow rule generically; deleted the now-unused, platform-locked screenshot baseline; and made every render offline-safe (Tailwind CDN calls intercepted and served from a locally pre-compiled CSS bundle). Status moved `review` → `in-progress` → `review`.
- 2026-09-22 — Closed the "known follow-up" gap from the prior round: root-caused and fixed the real interop bug blocking `EventCardDateBox`'s mount (previously worked around by substituting the lucide-free `CountBadge`). Root cause: `lucide-react@0.473.0` (this repo's pin for `packages/ui`) ships an `exports` map whose `require`/`node` condition points at a genuine CommonJS file (`dist/cjs/lucide-react.js`), but the package root declares `"type": "module"` and `dist/cjs/` has no nested `package.json` overriding it back to `"type": "commonjs"` — a real upstream dual-package-hazard bug, not a bug in this engine. Fixed with a `pnpm patch` (`patches/lucide-react@0.473.0.patch`) adding the missing `dist/cjs/package.json` (`{"type": "commonjs"}`) — the standard, minimal, version-scoped fix; not a version bump (which would risk breaking 22 other `lucide-react`-importing files in `packages/ui` across a 0.473→1.x major jump) and not an icon-library swap. Also carried the same `@jsxImportSource react` pragma fix (already applied to `count-badge.tsx` last round) to `EventCardMediaPrimitives.tsx` itself. Added a new proof manifest, `manifests/event-card-date-box-react-mount.ts`, mounting the real, unmodified `EventCardDateBox` composing its `month` slot exactly as `EventCard.tsx` does for `hasTime && dayDiff === 0` (a real lucide `Clock` icon inline with the month text, per `DESIGN.md`'s `event_card_date_box.base_default.month` rule) — asserted via two real signals (the `bg-slate-800` background token, and the Clock `<svg>` actually existing and inheriting `text-white`). Status moved `review` → `in-progress` → `review`.
- 2026-09-25 — Addressed second-pass code review findings: 4 items resolved (1 decision-needed per user ruling, 3 patch; 2 other findings from this same review pass — `FIND-049`/`FIND-050` — were already deferred/registered separately and left untouched). Fixed the color rule's no-token pixel-diff fallback to screenshot the same framing (`pixelDiffSelector ?? page`) the entry-level pixel-diff signal already uses, instead of the individual rule's own element selector, eliminating an element-vs-whole-card comparison mismatch. Updated `mountReferenceFor`'s stale doc comment to describe its actual current role. Fixed a misleading Playwright test title that claimed an intra-box-ratio assertion the manifest doesn't have. Added a `build:vendor-tailwind` script (pinned to this repo's already-resolved local `tailwindcss@3.4.1`) plus a one-line doc pointer, giving the offline Tailwind bundle a reproducible, verified regeneration path. Status moved `review` → `in-progress` → `review`.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via `bmad-dev-story`.

### Debug Log References

- Pure-logic unit suite: `npx tsx --test *.test.ts` (cwd `packages/visual-audit`) — 34/34 passing (manifest registry dedup, sibling-dimension clustering/tolerance, intra-box-ratio tolerance, color-token matching, ts-morph content-variant enumeration).
- Integration proof: `npx playwright test` (cwd `packages/visual-audit`, `playwright.config.ts`) — 5/5 passing, incl. the `toHaveScreenshot()` secondary pixel-diff signal (baseline committed under `manifests-proof.spec.ts-snapshots/`) and both example manifest entries' full rule sets.
- Repo-wide gates via `_bmad-output/specs/ritual-session-orchestrator/mailbox-runner/src/run-check.ts`: `--kind test --filter @festgrid/visual-audit` (pass), `--kind lint` unfiltered (8/8 tasks pass), `--kind build` unfiltered (8/8 tasks pass).
- One implementation correction during Task 9: the overflow rule's first pass injected the AST branch's raw *return-expression source text* (e.g. the whole `{ text: labels?.statusEnded ?? 'Ended', isHappeningNow: false }` object-literal text) as rendered content, which trivially overflowed every variant — not a meaningful proof. Fixed by adding `ContentVariant.sampleText` (`src/content-variants.ts`): the longest string literal found within the branch's return expression, or a domain-appropriate placeholder when the branch has none (e.g. a bare `formatWeekday(...)` call). Re-ran; all variants now render realistic display text and the rule correctly passes (and was observed to correctly fail before this fix, proving the overflow-detection path itself works).

### Review Follow-up Debug Log (2026-09-22)

- Pure-logic unit suite: `npx tsx --test *.test.ts` (cwd `packages/visual-audit`) — 35/35 passing (one new assertion added to the existing `formatEventStatus`-shaped nested-branch test).
- Integration proof: `npx playwright test --config=playwright.config.ts` (cwd `packages/visual-audit`) — 7/7 passing (2 new tests: the intra-box-ratio negative canary, and the `react-component` mount example's two tests replacing the old `toHaveScreenshot()` test).
- Repo-wide gates via `run-check.ts`: `--kind test --filter @festgrid/visual-audit` (pass, 2/2 tasks), `--kind lint` unfiltered (8/8 tasks pass), `--kind build` unfiltered (8/8 tasks pass, confirms `apps/web`'s Next build is unaffected by the `packages/ui` export-subpath/pragma-comment additions), `--kind test` unfiltered (12/12 tasks pass, confirms `@festgrid/ui`'s own Vitest suite is unaffected).

### Review Follow-up Debug Log, round 2 (2026-09-22) — final verification

- Pure-logic unit suite: `npx tsx --test *.test.ts` (cwd `packages/visual-audit`) — 35/35 passing (unchanged; this round added no new pure-logic code, only a manifest + fixes to two other packages' render inputs).
- Integration proof: `npx playwright test --config=playwright.config.ts` (cwd `packages/visual-audit`) — 9/9 passing (2 new tests: `event-card-date-box:clock-icon-has-time-today:220x160`'s registration check and its full-rules-pass run).
- Repo-wide gates via `run-check.ts`: `--kind test --filter @festgrid/visual-audit` (pass, 2/2 tasks), `--kind lint` unfiltered (8/8 tasks pass), `--kind build` unfiltered (8/8 tasks pass, confirms the `lucide-react` patch and the `EventCardMediaPrimitives.tsx` pragma don't affect `apps/web`'s real production build), `--kind test` unfiltered (12/12 tasks pass, confirms `@festgrid/ui`'s own Vitest suite — which also imports `EventCardMediaPrimitives.tsx` and transitively `lucide-react` — is unaffected by the patch or the pragma).
- **Investigation: mounting `EventCardDateBox` directly (item 1).** Reproduced twice, independently: (1) `lucide-react` (an `EventCardMediaPrimitives.tsx` dependency) is packaged `"type": "module"` with a dual CJS/ESM `exports` map; Playwright's test transform resolves it to the `require` condition's CJS build, but that build's own nearest `package.json` says `"type": "module"`, so Node 22's `require(esm)` interop executes it as an ES module — its internal `require(...)` calls then throw `ReferenceError: require is not defined`. Confirmed this doesn't happen outside Playwright's transform (`npx tsx` renders the same component cleanly). (2) Separately, Playwright's test transform defaults ANY `.tsx` file's JSX to its own internal `playwright/jsx-runtime` (`node_modules/playwright/jsx-runtime.js` — used for Playwright's own reporter/attachment UI, an unrelated coincidental reuse of the `jsx`/`jsxs` function names) rather than React's, unless that file carries an explicit `@jsxImportSource` pragma comment — confirmed via a minimal repro (`React.createElement` + `renderToStaticMarkup` on a lucide-free component failed with "Objects are not valid as a React child (found: object with keys {__pw_type, type, props, key})" until the pragma was added). Fixed (2) with a one-line pragma on `packages/ui/src/core/count-badge.tsx` (a lucide-free file, so (1) doesn't apply there) and used `CountBadge` as the proof component instead of `EventCardDateBox`; (1) remains open for whichever future story needs to mount an icon-bearing `@festgrid/ui` component.

### Review Follow-up Debug Log, round 2 (2026-09-22) — closing the `EventCardDateBox`/`lucide-react` gap

- **Re-verified the round-1 diagnosis by hand before trusting it**, per this repo's "verify framework behavior before claiming a fix" rule: `node -e "require('.../lucide-react/dist/cjs/lucide-react.js')"` run directly from the repo root loaded fine as CJS (Node's plain top-level `require()` of a `.js` file does not itself re-derive `"type"` the same way an ESM-context load does) — so the bug is specifically about *how* Playwright's test-file transform reaches this module, not a blanket "this file can never be required." Confirmed the actual trigger by writing a throwaway manifest (`manifests/_scratch-datebox-mount.ts`, mounting the real `EventCardDateBox` unmodified) and a throwaway spec (`_scratch-repro.spec.ts`), then running `npx playwright test` against them (both deleted after use, never committed). This reproduced the exact byte-identical failure the round-1 notes described: `ReferenceError: require is not defined` at `lucide-react/dist/cjs/lucide-react.js:10:13` — inspected that line directly (`var react = require('react');`, inside a `'use strict'` CJS-syntax file) to confirm it is genuinely CommonJS source being executed in an ESM context, not a red herring.
- **Root-caused the packaging defect, not just the symptom:** inspected `lucide-react@0.473.0`'s own `package.json` (`"type": "module"`, `exports["."].require`/`.node` → `dist/cjs/lucide-react.js`) and confirmed `dist/cjs/` has **no nested `package.json`** — the standard mechanism a dual-CJS/ESM-published package uses to tell Node "this subtree is really CommonJS despite the package root's `type`." Without it, Node's module resolution applies the package-root `"type": "module"` to every `.js` file including `dist/cjs/*`, so a `require()` of that file (as Playwright's CJS-targeting transform performs) loads real CommonJS source under Node's ESM loader — whose module scope has no `require` global, hence the exact error. Cross-checked `lucide-react@1.25.0` (the version `apps/web` already depends on, present in this monorepo's store) as a confirmation, not an assumption: it declares no `"type"` field at all and no `exports` map, so its `main` (also `dist/cjs/lucide-react.js`) resolves under Node's CommonJS default with no dual-package hazard — this is a bug that existed in `0.473.0` specifically and was not present in the newer major already proven safe elsewhere in this repo.
- **Chose the fix deliberately, ruling out the two easy-but-wrong options the task explicitly warned against:** not an icon-library swap (this proof manifest still renders a real lucide `Clock` icon), and not deleting/avoiding the icon. Also ruled out bumping `packages/ui`'s `lucide-react` to `^1.25.0` to match `apps/web`: that is a 0.473→1.25 *major* version jump across 22 files in `packages/ui` that import from `lucide-react`, which is a real API-compatibility risk (icon renames, prop changes) far outside this task's safe scope to blanket-verify. Instead used `pnpm patch lucide-react@0.473.0` to add exactly the missing `dist/cjs/package.json` (`{"type": "commonjs"}`) — the same fix the package itself should have shipped, scoped to only the one broken version, zero behavior change to any icon, registered in the root `package.json`'s `pnpm.patchedDependencies` and committed as `patches/lucide-react@0.473.0.patch` so it survives `pnpm install` for every future contributor/CI run.
- **Verified the fix in two steps**, isolating each of the two independent bugs: after the `pnpm patch` alone (no JSX pragma yet), the scratch repro's `require is not defined` was gone but a *new*, different error appeared (`Objects are not valid as a React child (found: object with keys {__pw_type, type, props, key})`) — confirming this really is two separate bugs stacked on the same file, exactly as round 1's notes described, not one bug with two symptoms. Added the same `@jsxImportSource react` pragma used on `count-badge.tsx` to `EventCardMediaPrimitives.tsx`; the scratch repro then mounted cleanly and produced a real, structurally correct DOM (confirmed via a throwaway rule assertion that legitimately failed on an unrelated, deliberately-unrealistic check — proving the render pipeline reached real rule evaluation, not a mount-time exception).
- Built the permanent proof manifest (`manifests/event-card-date-box-react-mount.ts`) mounting the real, unmodified `EventCardDateBox`, composing its `month` slot exactly as `EventCard.tsx`'s real `hasTime && dayDiff === 0` branch does (`{hasTime && dayDiff === 0 && <Clock className="w-3 h-3" />}{dateBoxParts.month}`) — added a new additive-only `@festgrid/ui` export subpath, `./event-card-media-primitives`, to reach it (mirrors the existing `./count-badge` subpath pattern; verified via repo-wide grep that no existing consumer used a deep `@festgrid/ui/*` import that this could collide with). The color-token assertions (`bg-slate-800` → `rgb(30, 41, 59)`, and the Clock `<svg>`'s inherited `color` → `rgb(255, 255, 255)`) both passed on the first real run — no rounding correction needed this time (unlike the `count-badge` proof's HSL correction last round).
- **Investigation: reference-selector mismatch (item 2).** After wiring `mountReferenceFor` to open a real second page against the validated prototype file, the intra-box-ratio rule's `getElementSnapshot` calls failed with "found 0" — the real prototype HTML carries no `data-testid` attributes (those exist only in this manifest's own live-render fixture, which was copied from the prototype with testids added). Fixed by adding `IntraBoxRatioRule.referenceSelectorA`/`referenceSelectorB` so a manifest can declare selectors that actually address the real prototype file, separate from the live-render selectors.
- **Investigation: pixel-diff tolerance (items 3/6).** A whole-card diff against the real PNG measured ~40% (not near-zero) because `validation-log.md` already documents an accepted content deviation for this card (distance badge vs. the PNG's literal category badge, established color tokens vs. the PNG's literal colors) plus inherent browser-vs-Figma-export rendering differences. A first attempt scoping the diff to just the date box (to dodge the badge deviation) measured *worse* (~83%) because this implementation resizes the *whole* reference PNG to the screenshot's own dimensions (see `compare/pixel-diff.ts`) — squashing the whole card into a small sub-element crop misaligns everything. Reverted to whole-card scope with a documented, measured-and-justified `maxDiffPixelRatio: 0.55`.
- **Correction: HSL rounding in the `count-badge-react-mount` proof's color-token assertion.** Initially declared the `bg-destructive` token as `rgb(239, 68, 68)` (the `#EF4444` approximation in `globals.css`'s own comment); Chromium's actual computed value for `hsl(0 84% 60%)` is `rgb(239, 67, 67)` (verified by hand: C=0.672, m=0.264 → G=B=67.32≈67). Corrected the manifest's `expectedToken.resolvedValue` to match the real computed value.

### Review Follow-up Debug Log, round 3 (2026-09-25)

- Pure-logic unit suite: `npx tsx src/run-check.ts --kind test --filter @festgrid/visual-audit` (via mailbox-runner) — pass, 2/2 tasks.
- Integration proof: `npx playwright test --config=playwright.config.ts` (cwd `packages/visual-audit`) — 9/9 passing (unchanged test count; item 3 only renamed test 5's title to match its actual assertions).
- Vendor Tailwind regen: `pnpm --filter @festgrid/visual-audit build:vendor-tailwind` — ran clean, regenerated `vendor/tailwind.generated.css` byte-for-byte identical (14445 bytes, no `git diff`) to the previously committed file, confirming the new script reproduces the existing offline bundle deterministically from the already-pinned local `tailwindcss@3.4.1`.
- Repo-wide gates via `run-check.ts`: `--kind lint` unfiltered (8/8 tasks pass), `--kind build` unfiltered (8/8 tasks pass), `--kind test` unfiltered (12/12 tasks pass, confirms no regression outside `packages/visual-audit`).

### Completion Notes List

- Implements AD-26 Rule 1–6 as `@festgrid/visual-audit`: both audit modes (reference/rule) share the same `runManifestEntry()` engine and `compare/`/`rules/` assertion primitives (AC2); render scope (`single-instance`/`multi-instance`) is a per-manifest-entry field the engine's sibling-dimension clustering switches on, never hardcoded (AC3/AC7); the manifest registry rejects a second entry for the same component/variant/viewport triple (AC4); all rules are exposed through a typed, documented `src/index.ts` public API importable like `@festgrid/graphql-select`'s (AC12).
- **Judgment call — isolated-component-render mechanism (AC5, Task 3):** AC5 names Playwright 1.62's native `fixtures.mount()` stories/gallery model. That model mounts real React component trees through a Vite-bundled gallery, which would require this package to take on a React/`packages/ui` dependency the story's own File Change Plan does not list (only `@playwright/test` + `ts-morph`), and this story ships no real React-component consumer yet (BUG-040's fix and AD-27's masonry engine — the tool's actual future consumers — are separate, not-yet-created stories per Out of Scope). Implemented the harness (`src/render.ts`) at the DOM level instead: `page.setContent()`/`page.goto('file://…')` — genuinely isolated (no server/DB/auth, satisfying Rule 4's actual constraint) via a `RenderSpec` discriminated union already shaped so a future `fixtures.mount()`-backed variant can be added without changing the engine's public surface. Documented in `src/render.ts`'s own header comment.
- **Judgment call — reference-based example's "live render" (AC2/AC11, Task 9):** with no real component consumer to audit yet, the reference-based proof manifest's "live render" is the validated prototype's own markup (isolated-rendered via `page.setContent()`), so it trivially matches its own PNG by construction. This proves the check machinery (computed-style extraction, `toHaveScreenshot()` wiring, clustering, ratio-checking, ts-morph overflow enumeration, color-token matching) runs correctly end-to-end — it is not a claim of having caught a real defect in this card. A future adopting story swaps in a real component render via the same `RenderSpec` shape.
- **Judgment call — `toHaveScreenshot()` baseline (AC6, Task 4/9):** Playwright's snapshot mechanism manages its own baseline directory rather than diffing against an arbitrary external PNG path; pointing it directly at the `design-artifacts/**/imports/*.png` files would need custom `snapshotPathTemplate` wiring, deferred as a follow-up. The committed baseline (`manifests-proof.spec.ts-snapshots/`) was captured from the mounted isolated render itself via `playwright test --update-snapshots`, proving the secondary pixel-diff signal is wired and enforced; the manifest's `reference.prototypePngPath` field still records the true source-of-truth PNG per AC4.
- **Judgment call — overflow rule's rendered content (AC9, Task 7):** `ts-morph` enumerates branch *shapes* over the real `packages/ui/src/features/events/format-event-date.ts#formatEventStatus` (8 branches: ended/happeningNow/endsToday/inHours/tomorrow/weekday-range/inDays/upcoming). Since branch return expressions mix call expressions and optional-chaining fallbacks rather than bare literals, `enumerateContentVariants` extracts the longest string literal found in each branch as a representative display stand-in (`ContentVariant.sampleText`), not a runtime `eval`. See Debug Log for the correction this required.
- Testing tier followed per AC13/Story 0.44 (`project-context.md` "Meta-Testing / Tooling Packages"): pure comparison/tolerance/clustering/branch-enumeration logic is unit-tested via `tsx --test` (34 tests, no coverage-percentage bar); the two example manifest entries' full rule sets, run via the real `@playwright/test` runner, serve as this story's integration proof for the browser-automation/AST-analysis surfaces.
- Minor plan deviation: added `src/engine.ts` (the `runManifestEntry()` orchestration) as its own file, not explicitly listed in the story's File Change Plan (which named `src/manifest.ts`, `src/render.ts`, `src/compare/*`, `src/rules/*`, `src/content-variants.ts`). Kept the orchestration logic out of `src/manifest.ts` (types + registry only) and `src/index.ts` (pure re-exports) for clarity; no behavior this deviates from was specified by an AC.

### Review Follow-up Completion Notes (2026-09-22)

- ✅ Resolved [Decision]: added a `react-component` `RenderSpec` variant (real React SSR mount, proven against `@festgrid/ui`'s `CountBadge`) — see Debug Log for the `EventCardDateBox`/`lucide-react` investigation and why `CountBadge` was substituted as the proof component.
- ✅ Resolved [Decision]: `expectedRatio` now derives from an independently-mounted reference render (`mountReferenceFor`), never the live page being checked; added a negative canary test proving the check can fail.
- ✅ Resolved [Decision]: the pixel-diff signal now reads and diffs against the real `reference.prototypePngPath` file (via `sharp`, since `pixelmatch`/`pngjs` aren't resolvable offline), replacing the self-captured `toHaveScreenshot()` baseline entirely; integrated into `runManifestEntry`'s own `pass`.
- ✅ Resolved [Patch]: `ts-morph` branch enumeration now recurses into nested `if` statements.
- ✅ Resolved [Patch]: the color rule's missing-token path no longer silently passes (falls back to a real pixel-diff, or reports `pass: false` when no reference PNG exists).
- ✅ Resolved [Patch]: the pixel-diff signal is integrated into `runManifestEntry`'s own result (same fix as the decision-needed item 3 above).
- ✅ Resolved [Patch]: the dead `assertFormattingFunctionExists` export was removed and replaced with a generic guard wired into `runOverflowRule` itself.
- ✅ Resolved [Patch]: the win32-qualified screenshot baseline was deleted (no longer referenced, since the self-captured `toHaveScreenshot()` mechanism it belonged to was replaced).
- ✅ Resolved [Patch]: every render in this package's test suite is now network-independent — the Tailwind CDN request is intercepted (`page.route()`) and served from a statically pre-compiled, offline-built CSS bundle (`vendor/tailwind.generated.css`).
- **Judgment call, `packages/ui` touched:** two small, additive changes outside `packages/visual-audit` were needed and are included in this story's scope: (1) `packages/ui/package.json` gained an `exports` map (`.` unchanged, plus a new `./count-badge` subpath) — no existing consumer imports a deep `@festgrid/ui/*` path today (verified via repo-wide grep), so this is additive-only; (2) `packages/ui/src/core/count-badge.tsx` gained a one-line `/** @jsxImportSource react */` pragma comment, a no-op for that package's own build (its `tsconfig` already defaults JSX to React's automatic runtime) that exists solely to fix cross-package JSX resolution under Playwright's test transform. Both verified safe via the full repo-wide `build`/`test`/`lint` gates (all pass, including `@festgrid/ui`'s own Vitest suite and `apps/web`'s Next build).
- **Known follow-up from round 1, now closed (round 2, 2026-09-22):** mounting `EventCardDateBox` no longer hits the `lucide-react` dual-package/Playwright-transform interop bug. Root cause: `lucide-react@0.473.0` ships an `exports` map whose `require`/`node` condition points at a real CommonJS `dist/cjs/lucide-react.js`, but its package root declares `"type": "module"` with no nested `dist/cjs/package.json` override — Node's loader therefore executes genuine CJS source (`var react = require('react');`) under the ESM loader, where `require` is undefined. Fixed with `pnpm patch lucide-react@0.473.0` (`patches/lucide-react@0.473.0.patch`, registered in the root `package.json`'s `pnpm.patchedDependencies`) adding the missing `dist/cjs/package.json` (`{"type": "commonjs"}`) — a minimal, version-scoped fix to the actual upstream defect, not an icon-library swap and not a major-version bump (which would have risked breaking 22 other `lucide-react`-importing files in `packages/ui`). Also carried the `@jsxImportSource react` pragma (already used on `count-badge.tsx`) to `EventCardMediaPrimitives.tsx` itself, since Playwright's `.tsx` JSX-runtime default is a second, independent bug affecting every mounted `.tsx` file, not just lucide-consuming ones. New proof manifest `manifests/event-card-date-box-react-mount.ts` mounts the real, unmodified `EventCardDateBox`, including its Clock-icon-when-`hasTime`-with-today rendering path (`DESIGN.md § event_card_date_box.base_default.month`), composed exactly as `EventCard.tsx` does it — both assertions (background token, icon-inherited color) pass against the real component. See "Review Follow-up Debug Log, round 2" above for the full investigation, including the re-verification-by-hand step that confirmed round 1's diagnosis rather than assuming it.

### Review Follow-up File List, round 3 (2026-09-25)

- `packages/visual-audit/src/engine.ts` (modified — color rule's no-token pixel-diff fallback now screenshots `entry.reference.pixelDiffSelector ?? page` instead of `rule.selector`, matching the entry-level pixel-diff signal's framing; `mountReferenceFor`'s doc comment rewritten to describe its actual current role)
- `packages/visual-audit/manifests-proof.spec.ts` (modified — renamed the CountBadge test title to "runs the color and sibling-dimension checks..." to match its actual asserted rules)
- `packages/visual-audit/package.json` (modified — added `tailwindcss@3.4.1` devDependency and a `build:vendor-tailwind` script)
- `packages/visual-audit/vendor/tailwind.config.cjs` (modified — added a one-line doc pointer to the new `build:vendor-tailwind` regen script)
- `pnpm-lock.yaml` (modified — new `tailwindcss` devDependency resolution for `@festgrid/visual-audit`)

### Review Follow-up Completion Notes, round 3 (2026-09-25)

- ✅ Resolved [Decision]: the color rule's no-token pixel-diff fallback (`src/engine.ts`) now shares the same target-resolution convention (`entry.reference.pixelDiffSelector ?? page`) as the entry-level pixel-diff signal, instead of screenshotting the rule's own element and diffing it against the whole-card PNG.
- ✅ Resolved [Patch]: `mountReferenceFor`'s doc comment now describes its real role (independent reference mounting for ratio derivation and the entry-level pixel-diff signal), not the removed caller-added `toHaveScreenshot()` convention.
- ✅ Resolved [Patch]: renamed the CountBadge Playwright test title to match its actual rules (color + sibling-dimension).
- ✅ Resolved [Patch]: added `packages/visual-audit`'s `build:vendor-tailwind` script and a `tailwindcss@3.4.1` devDependency (matching this repo's already-resolved local version), plus a doc pointer in `vendor/tailwind.config.cjs`; verified the script reproduces the committed `vendor/tailwind.generated.css` byte-for-byte.

### File List

- `packages/visual-audit/package.json` (new)
- `packages/visual-audit/tsconfig.json` (new)
- `packages/visual-audit/eslint.config.mjs` (new)
- `packages/visual-audit/playwright.config.ts` (new)
- `packages/visual-audit/src/manifest.ts` (new)
- `packages/visual-audit/src/render.ts` (new)
- `packages/visual-audit/src/engine.ts` (new)
- `packages/visual-audit/src/index.ts` (new)
- `packages/visual-audit/src/content-variants.ts` (new)
- `packages/visual-audit/src/compare/computed-style.ts` (new)
- `packages/visual-audit/src/compare/pixel-diff.ts` (new)
- `packages/visual-audit/src/rules/sibling-dimension.ts` (new)
- `packages/visual-audit/src/rules/intra-box-ratio.ts` (new)
- `packages/visual-audit/src/rules/overflow.ts` (new)
- `packages/visual-audit/src/rules/color.ts` (new)
- `packages/visual-audit/manifests/event-card-masonry-thumbnail-fallback.ts` (new)
- `packages/visual-audit/manifests/masonry-column-width-invariant.ts` (new)
- `packages/visual-audit/manifests/index.ts` (new)
- `packages/visual-audit/manifest.test.ts` (new)
- `packages/visual-audit/sibling-dimension.test.ts` (new)
- `packages/visual-audit/intra-box-ratio.test.ts` (new)
- `packages/visual-audit/color.test.ts` (new)
- `packages/visual-audit/content-variants.test.ts` (new)
- `packages/visual-audit/manifests-proof.spec.ts` (new)
- `packages/visual-audit/manifests-proof.spec.ts-snapshots/event-card-masonry-thumbnail-fallback-mobile-chromium-win32.png` (deleted — Review Follow-up patch item 8; the `toHaveScreenshot()` mechanism it belonged to was replaced)
- `pnpm-lock.yaml` (modified — new `@festgrid/visual-audit` workspace entry, `ts-morph`/`@playwright/test` dependency resolution; review follow-up adds `react`/`react-dom`/`sharp`/`@types/react`/`@types/react-dom`/`@festgrid/ui`)

### Review Follow-up File List (2026-09-22)

- `packages/visual-audit/package.json` (modified — added `react`/`react-dom` runtime deps, `sharp` dep, `@festgrid/ui`/`@types/react`/`@types/react-dom` devDeps)
- `packages/visual-audit/tsconfig.json` (modified — added `"jsx": "react-jsx"`, needed for `tsc` to type-check `@festgrid/ui`'s `CountBadge.tsx`, transitively included via the react-component mount proof manifest; this package has no JSX syntax of its own)
- `packages/visual-audit/eslint.config.mjs` (modified — ignore `vendor/*.cjs`/`vendor/tailwind.generated.css`)
- `packages/visual-audit/src/manifest.ts` (modified — added `ReactComponentRender`/`RenderSpec` union member; added `IntraBoxRatioRule.referenceSelectorA`/`referenceSelectorB`; added `ReferenceSource.pixelDiffSelector`/`pixelDiffOptions`)
- `packages/visual-audit/src/render.ts` (modified — added `react-component` render backend, offline Tailwind CDN interception, `installOfflineTailwind`/`getVendoredTailwindCss`/`wrapBodyHtml`/`renderReactComponentToHtml`)
- `packages/visual-audit/src/engine.ts` (modified — reference-derived ratio now uses an independent second page via `mountReferenceFor`; color rule's no-token path now uses real pixel-diff fallback or reports `pass: false`; entry-level pixel-diff signal added and integrated into `ManifestRunResult.pass`)
- `packages/visual-audit/src/content-variants.ts` (modified — branch-walk now recurses into nested `if` statements)
- `packages/visual-audit/src/compare/pixel-diff.ts` (modified — real `sharp`-based pixel comparison against a PNG file on disk, replacing the `toHaveScreenshot()`-convention-only stub)
- `packages/visual-audit/src/rules/overflow.ts` (modified — added the generic `assertFormattingFunctionExists`/`FormattingFunctionNotFoundError` guard, wired into `runOverflowRule`)
- `packages/visual-audit/manifests/event-card-masonry-thumbnail-fallback.ts` (modified — exported `MOBILE_PANEL_HTML`; added `pixelDiffSelector`/`pixelDiffOptions` and `referenceSelectorA`/`referenceSelectorB`; removed the dead `assertFormattingFunctionExists` export)
- `packages/visual-audit/manifests/count-badge-react-mount.ts` (new — react-component mount proof manifest)
- `packages/visual-audit/manifests/index.ts` (modified — registers the new proof manifest)
- `packages/visual-audit/content-variants.test.ts` (modified — strengthened the nested-branch assertion; added a top-level-chain regression test)
- `packages/visual-audit/manifests-proof.spec.ts` (modified — replaced the `toHaveScreenshot()` test with pixel-diff-signal assertions; added the negative canary test; added the react-component mount test block)
- `packages/visual-audit/vendor/tailwind.config.cjs` (new — offline Tailwind build config)
- `packages/visual-audit/vendor/tailwind-input.css` (new — Tailwind `@tailwind` directives input)
- `packages/visual-audit/vendor/tailwind.generated.css` (new — committed, offline-compiled Tailwind CSS bundle)
- `packages/ui/package.json` (modified — added `exports` map with a new `./count-badge` subpath; additive only, no existing consumer used a deep import)
- `packages/ui/src/core/count-badge.tsx` (modified — added a one-line `@jsxImportSource react` pragma comment; no-op for this package's own build)
- `pnpm-lock.yaml` (modified — new `react`/`react-dom`/`sharp`/`@types/react`/`@types/react-dom`/`@festgrid/ui` resolutions for `@festgrid/visual-audit`)

### Review Follow-up File List, round 2 (2026-09-22)

- `patches/lucide-react@0.473.0.patch` (new — adds the missing `dist/cjs/package.json` `{"type": "commonjs"}` override, fixing the real upstream dual-package-hazard bug)
- `package.json` (modified, repo root — registered `pnpm.patchedDependencies["lucide-react@0.473.0"]`)
- `pnpm-lock.yaml` (modified — patch hash resolution for `lucide-react@0.473.0`; new `lucide-react` dependency entry for `@festgrid/visual-audit`)
- `packages/ui/src/features/events/EventCardMediaPrimitives.tsx` (modified — added the `@jsxImportSource react` pragma comment, same fix already applied to `count-badge.tsx` last round; no behavior change)
- `packages/ui/package.json` (modified — added an `exports` map entry, `./event-card-media-primitives`, additive only)
- `packages/visual-audit/package.json` (modified — added `lucide-react` as a dependency, needed to import `Clock` directly in the new proof manifest, matching `packages/ui`'s pinned `^0.473.0`)
- `packages/visual-audit/manifests/event-card-date-box-react-mount.ts` (new — proof manifest mounting the real `EventCardDateBox`, including its Clock-icon-when-`hasTime`-with-today path)
- `packages/visual-audit/manifests/index.ts` (modified — registers the new proof manifest)
- `packages/visual-audit/manifests-proof.spec.ts` (modified — added the `event-card-date-box:clock-icon-has-time-today:220x160` test block)

### Review Findings

### decision-needed

- [x] [Review][Decision] Color rule's reference-mode no-token pixel-diff fallback diffs an *element* screenshot against the *whole-card* reference PNG — `packages/visual-audit/src/engine.ts:107-115` — when a reference-mode color rule omits `expectedToken`, the fallback screenshots only `rule.selector`'s element and diffs it against `entry.reference.prototypePngPath` (the full-design PNG) with the entry-level `pixelDiffOptions` (tuned for the entry's `pixelDiffSelector` framing, e.g. the 55% whole-card tolerance). Per `compare/pixel-diff.ts`'s own documented framing rule, a sub-element crop squashed against the full-card PNG "produces a meaningless diff." No committed manifest exercises this path today (both example color rules declare tokens), so nothing canary-tests it either. Options: (a) require the same framing discipline as `pixelDiffSelector` (screenshot the framing the PNG represents, restrict fallback to already-comparable framings), (b) keep as-is but document the weasel tolerance explicitly and add a negative canary, or (c) restrict reference-mode color rules to mandatory `expectedToken` (no token ⇒ authoring error at registration time).
  **RULING (user, 2026-09-25): Enforce comparable framing (option a).** The no-token fallback must screenshot the same target the entry-level pixel-diff already uses (`entry.reference.pixelDiffSelector ?? page`, i.e. whole page by default), not the individual color rule's own `rule.selector` element — reuse the already-correct convention rather than inventing a second framing mechanism.
  **RESOLVED (2026-09-25):** `runManifestEntry`'s color-rule no-token fallback (`src/engine.ts`) now screenshots `entry.reference.pixelDiffSelector ?? page` — the same target the entry-level pixel-diff signal already uses — instead of `rule.selector`'s own element, so both signals share one consistent framing convention rather than an element-vs-whole-card mismatch. As the finding itself notes, no committed manifest's color rule omits `expectedToken` today (all three declared color rules across the example manifests have tokens), so this fix is verified by code inspection plus the full Playwright proof suite passing unchanged (9/9) — not by a new canary exercising this specific fallback branch, which remains untested-by-example exactly as it was before this fix (unchanged scope; the finding asked only to fix the framing, not add coverage).

### patch

- [x] [Review][Patch] Stale doc comment on `mountReferenceFor` still tells callers to add their own `toHaveScreenshot()` assertion [`packages/visual-audit/src/engine.ts:163-165`] — comment contradicts the patch-item-6 design (the secondary signal now runs inside `runManifestEntry`; the spec explicitly removed `toHaveScreenshot()`). Update to describe its current role (independent reference mounting for ratio derivation and rule re-verification).
  **RESOLVED (2026-09-25):** Rewrote `mountReferenceFor`'s doc comment (`src/engine.ts:163-170`) to describe its actual current role: loading a reference-based entry's validated prototype into an independently-mounted page, used internally by the intra-box-ratio expected-ratio derivation and by `runManifestEntry`'s own pixel-diff signal — no longer references the removed caller-added `toHaveScreenshot()` convention.
- [x] [Review][Patch] Test title misstates the CountBadge rules it asserts [`packages/visual-audit/manifests-proof.spec.ts:116`] — "runs the color and intra-box-ratio checks" but the entry's rules are color + sibling-dimension (no intra-box-ratio rule exists there). Fix the title to match; a future reader greps this suite to learn what the manifest does.
  **RESOLVED (2026-09-25):** Renamed the test title to "runs the color and sibling-dimension checks against the real mounted component and passes" — verified against `manifests/count-badge-react-mount.ts`'s actual rule list (`color` + `sibling-dimension`, no `intra-box-ratio`).
- [x] [Review][Patch] Offline Tailwind bundle has no committed regeneration path [`packages/visual-audit/vendor/`] — `tailwind.generated.css` is described (in `render.ts` and `tailwind-input.css`/`tailwind.config.cjs` comments) as pre-compiled via "this repo's local tailwindcss v3 install," but no script in `package.json` (or anywhere else) reproduces that offline build. If a fixture/prototype class is ever added and a fresh render misses it, the failure mode is a silent style miss only caught by canary luck. Add an explicit, committed regen command (e.g. a `build:vendor-tailwind` script pinned to the already-resolved local `tailwindcss` version) and a one-line doc note.
  **RESOLVED (2026-09-25):** Added `tailwindcss` `3.4.1` (this repo's already-resolved local version, confirmed via `pnpm-lock.yaml`) as an explicit devDependency of `packages/visual-audit`, plus a `build:vendor-tailwind` script (`tailwindcss -c vendor/tailwind.config.cjs -i vendor/tailwind-input.css -o vendor/tailwind.generated.css --minify`) in `package.json`. Added a one-line doc pointer to `vendor/tailwind.config.cjs`'s header comment. Verified: ran `pnpm --filter @festgrid/visual-audit build:vendor-tailwind` — it regenerated `vendor/tailwind.generated.css` byte-for-byte identical to the previously committed file (same size, no diff), confirming the script reproduces the existing bundle deterministically.

### defer

- [x] [Review][Defer] `assertFormattingFunctionExists` regex only matches `export function` declarations [`packages/visual-audit/src/rules/overflow.ts:110-115`] — an `export const formatX = (...) =>`-style formatter would trigger a false `FormattingFunctionNotFoundError`. Deferred, pre-existing pattern risk (this repo's formatting functions are all `function` declarations today; document `function`-declaration-only as a contract or widen the regex when a consumer needs it).
- [x] [Review][Defer] `count-badge-react-mount`'s "overflow" rule is a trivially-passing single-element sibling cluster [`packages/visual-audit/manifests/count-badge-react-mount.ts:61-66`] — the comment says "the badge must not clip its {max}+ text," but a single-member `sibling-dimension` cluster always passes, so the rule enforces nothing actual today. Deferred to whatever story actually audits `CountBadge`'s overflow for real (it's a demo manifest; the intended consumer is a future story).
