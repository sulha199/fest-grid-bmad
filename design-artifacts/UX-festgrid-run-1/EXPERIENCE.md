---
title: "EXPERIENCE.md: festgrid"
status: "draft"
created: "2026-07-20T10:59:00Z"
updated: "2026-09-17T00:00:00Z"
sources:
  - "design-artifacts/UX-festgrid-run-1/DESIGN.md"
  - "_bmad-output/planning-artifacts/prds/festgrid-prd-2026-07-10-2047/prd.md"
---

# EXPERIENCE.md: FestDaily

This document defines the information architecture, behavior, states, and interactions for the FestDaily application. It is the authority on *how the app works*.

## Foundation

*   **Form-factor:** The primary experience is mobile-first, but the application will be a responsive web app accessible on desktop.
*   **Visual Identity:** All visual styling is defined in `{design-artifacts/UX-festgrid-run-1/DESIGN.md}`.

## Information Architecture

The application's structure is as follows:

*   **/ (Event Discovery):** The main landing page for authenticated users.
*   **/favorites:** A dedicated page showing the user's favorited events.
*   **/my-calendar:** A dedicated page showing events the user has added to their calendar.
*   **/feed:** The page where a user views events from their subscribed accounts.
*   **/settings:** A parent page for all user settings.
*   **/settings/account:** *(rewritten 2026-08-25, `sprint-change-proposal-2026-08-24-ux-rework-batch.md` Section 4.2 — see Component Patterns § Account Settings & Moderator Tools Shells)* The "Account Settings" tabbed shell — collapses the four routes below into tabs of one page: **API Keys**, **Subscribed Accounts**, **Posts** (manual post selection, Story 5.1), **Notifications**. Replaces the four former standalone routes `/settings/api-keys`, `/settings/subscriptions`, `/settings/notifications`, and `/settings/queue-status` (dissolved — its pending-extraction count moves into the Subscribed Accounts tab, its API-key-health banner moves into the API Keys tab).
*   **/settings/locations:** The "Manage Locations" screen. Stays a standalone route — not part of the Account Settings shell.
*   **/settings/widgets:** The "Manage Widgets" (embeddable calendar widget) screen. Stays a standalone route — not part of the Account Settings shell. *(Added 2026-08-25 — this route was already shipped but missing from this list; a pre-existing documentation gap, unrelated to the shell rewrite, corrected while touching this section.)*
*   **/reports:** The "User Reports" page — status and history of the user's own submitted reports (PRD 3.9.3). Reachable only via the Profile menu, not part of the primary 5-item nav.
*   **/moderator/tools:** *(new 2026-08-25, Section 4.2)* The "Moderator Tools" tabbed shell — collapses the two routes below into tabs of one page: **Actor Runs**, **Unprocessed Payloads**. Visible only to `role === MODERATOR` (reuses Story 4.7a's route guard as-is). Replaces the two former standalone routes `/moderator/actor-runs` and `/moderator/unprocessed-payloads`.
*   **/moderator/items:** The "Moderator Items" page — visible only to users with `role === MODERATOR` (PRD 3.9.3, AD-7.4). Surfaces user reports and pending "Default Location" changes (see State Patterns § Default Location Pending Review) for a moderator to accept or revert. Reachable only via the Profile menu. **Deliberately stays a separate, standalone page — does not join the Moderator Tools shell** (user decision, Section 4.2).

### Global Navigation

The five top-level destinations, always reachable from the app shell (see Component Patterns and Responsive & Platform below):

| Label | Route | Icon (Lucide) |
|---|---|---|
| Discover | `/` | `Compass` |
| Feed | `/feed` | `Rss` |
| Favorites | `/favorites` | `Heart` |
| Calendar | `/my-calendar` | `CalendarDays` |
| Profile | see below (auth-dependent) | `UserCircle` / `LogIn` |

"Feed" (not "Following") to stay consistent with the `/feed` route name above.

#### Profile item — authentication states

**Ownership note:** Epic 0/Story 0.7 owns only the *trigger* — rendering this nav slot's icon/state and invoking whichever behavior is described below. The menu itself (its contents, ARIA pattern, and interaction spec below) is owned by **Story 2.8 "User Menu"** (Epic 2, `epics.md`, status `backlog`), which already exists as the story for this feature — Story 0.7 must not build or duplicate it. This correction was made 2026-08-05 after checking `epics.md` (not done before the first draft of this subsection — see `.memlog.md`).

The Profile item is the only nav entry with two distinct states:

- **Unauthenticated — "Log In":** icon `LogIn`, label "Log In". Tap/click **navigates to `/login`** (Story 1.7 "User Signup and Login with Google", Epic 1 — already builds this route, including its own `generateMetadata`). Not a modal — Story 1.7 establishes `/login` as a real page, and this nav item must route to it rather than reinventing an in-place login affordance.
- **Authenticated — avatar, opens a menu:** icon is the user's avatar (falls back to `UserCircle` if no avatar image). Tap/click opens the **User Menu** (Story 2.8) **anchored to the nav item** — a dropdown at the rail tiers (≥768px), a bottom sheet at the mobile tab-bar tier (<768px) — not a full-page navigation and not a blocking modal, so quick actions (especially Log Out) are reachable from anywhere without losing context. `/settings` remains a valid deep-link destination (e.g. from a notification), it just isn't the required first stop.

  **ARIA pattern:** this is a *disclosure of navigation links*, not an application command menu — do **not** use `role="menu"`/`"menuitem"` (WAI-ARIA APG reserves that pattern for desktop-style command menus; 10 of the 11 rows below are plain page links). The trigger is a `<button aria-haspopup="true" aria-expanded={open}>`; the popup content is a plain `<ul><li><a>…</a></li></ul>` list of links, with **Log Out** as a real `<button>` in that same list. If a component library's dropdown-menu primitive is used for convenience (e.g. Radix `DropdownMenu`), it must be swapped for a `Popover` + native list instead — `DropdownMenu` hard-codes menu/menuitem roles and command-menu keyboard behavior (Home/End/typeahead) that don't fit link navigation.

  **Focus behavior:** no focus trap (a focus trap is a dialog-only requirement — trapping here would fight normal Tab-order expectations for a set of nav links). Tabbing past the last item (Log Out) closes the menu and moves focus to the next focusable element after the nav in DOM order, same as any other disclosure. `Escape` explicitly closes it and returns focus to the Profile trigger. Focus-return on close has three distinct cases:
  - **Escape / outside dismiss:** focus returns to the Profile trigger.
  - **Activating a navigation item** (Profile, Locations, Subscribed Accounts, API Keys, Notifications, Reports, Moderator Items): the menu closes and focus follows the app's standard route-change focus management (moves to the destination page's main heading/landmark) — not back to the nav item, which is no longer relevant on the new page.
  - **Log Out:** focus returns to the trigger (deliberate exception — the same DOM node persists, now relabeled "Log In"). If Log Out ever redirects to a different route instead, apply the destination-page rule like other items.

  **Bottom sheet (mobile tab-bar tier, <768px):** in addition to outside-tap and `Escape`, the sheet has an explicit, always-reachable **Close** control (first or last focusable item in the sheet) — outside-tap/Escape alone aren't reliably discoverable/producible on pure-touch, no-keyboard devices. The sheet also honors the Android back gesture/button to close. Drag-down-to-dismiss is an optional touch enhancement, never the only non-Escape path.

  **Icon-only rail tier (768–1279px) interaction:** Profile is **excluded** from the generic "tap navigates and flashes label" rule that applies to the other 4 items at this tier (see Responsive & Platform) — since Profile's target is a disclosure trigger, not a navigation link, a tap/click here opens the menu directly, with no separate flash-then-second-tap step. Opening the menu (by click, tap, `Enter`, or `Space`) always dismisses Profile's *own* hover tooltip immediately, independent of hover/focus-out state (mouse `:hover` can persist after a click while the cursor stays over the icon).

  Menu contents, top to bottom *(rewritten 2026-08-25, `sprint-change-proposal-2026-08-24-ux-rework-batch.md` Section 4.2 — collapses the four-route settings block plus Queue Status into one Account Settings entry, adds Moderator Tools; see Component Patterns § Account Settings & Moderator Tools Shells)*:
  1. Header (non-interactive): avatar + display name.
  2. **Profile** → `/settings`
  3. **Locations** → `/settings/locations`
  4. **Widgets** → `/settings/widgets` *(added 2026-08-25 — pre-existing gap: this route was already shipped but never registered here; unrelated to the shell rewrite, corrected while touching this section)*
  5. **Account Settings** → `/settings/account` — the tabbed shell (API Keys / Subscribed Accounts / Posts / Notifications); replaces the four former separate entries below it in this list's pre-2026-08-25 version.
  6. **Reports** → `/reports`
  7. **Moderator Tools** → `/moderator/tools` — the tabbed shell (Actor Runs / Unprocessed Payloads), *preceded by a divider*, rendered only for `role === MODERATOR`.
  8. **Moderator Items** → `/moderator/items`, rendered only for `role === MODERATOR`, *followed by a divider* — stays separate from the Moderator Tools shell (Section 4.2 decision). Items 7-8 together form one moderator-only block: **both its surrounding dividers** (before item 7, after item 8) are absent entirely (not disabled/greyed) for regular users, so a non-moderator sees exactly one divider directly before Log Out, never two adjacent dividers — same rule as before, now spanning two items instead of one.
  9. **Log Out** — action, not a navigation.

  This formalizes Story 2.8's existing (loose) AC plus PRD 3.9.3's intent ("a dedicated 'Reports' page under their user menu"; "a 'Moderator Items' page will be available under the user menu") into a concrete interaction spec. It **supersedes** Story 2.8's original item list (`epics.md`, pre-2026-08-05: "My Favorites", "My Calendar", "My Locations", "Settings", "Logout") — "My Favorites"/"My Calendar" are dropped as redundant now that both are first-class items in the primary 5-item nav (Story 0.7); "My Locations"/"Settings" are expanded into the settings-registry set above. Reports and Moderator Items are registered by Stories 4.6 and 4.7 respectively (Epic 4, which already reference "the user menu" generically); Locations/Widgets are registered by Stories 2.3/6.5 respectively — Story 2.8 owns the menu *mechanism* and its registry, not the content of every entry. **Account Settings is registered by Story 3.12** (new, Epic 3, absorbs the former API Keys/Subscribed Accounts/Notifications entries — Stories 3.1b/3.2/2.9 — and Queue Status, Story 3.9a, now dissolved). **Moderator Tools is registered by Story 4.7b** (new, Epic 4, absorbs the former standalone Actor Runs/Unprocessed Payloads pages).

The main view is centered around a filterable, dynamic grid of events that can be viewed as either a card-grid or a weekly calendar. This provides flexibility for users to discover events in their preferred format.

- **Filter Hub**: Prominently displayed at the top of the discovery view, the Filter Hub contains compact dropdown triggers for filtering events by `EventType` and `EventCategory`. Each trigger opens a popover containing tap-to-toggle multi-selection controls, allowing users to combine filters (e.g., `FESTIVAL` + `MUSIC` + `FAMILY_AND_KIDS`).
- **Dynamic Event Grid**: The event grid (both card and calendar view) dynamically updates as filters are applied, showing only the events that match the user's criteria.
- **Calendar View**: The weekly calendar includes a header with previous/next week navigation and a "Today" button. Each schedule of an event is displayed as a separate compact card. The title of the card is formatted to distinguish between main and sub-schedules. Below `{components.calendar.mobile_day_list.breakpoint}` (768px), the 7-column grid becomes a vertical list of days, skipping days with no schedules — see Component Patterns § Mobile Multi-Day Calendar Spanning for how a multi-day schedule renders in that layout.

## Interaction Primitives

- **Filtering**: Users can open an `EventType` or `EventCategory` trigger in the Filter Hub, then tap buttons/tags in its popover. The event grid below will update in real-time with each selection. Selected filters are clearly indicated on the trigger.
- **Event Discovery**: Clicking on an event card in the main grid view opens a modal with the full event details, including a clear display of its types and categories as tags. Clicking on a specific schedule in the calendar view also opens the modal with the full event details. The URL is updated for deep-linking in both cases.
- **Swipe-to-delete:** On mobile touch interfaces, a swipe gesture on a list item can reveal a "Delete" button. This will trigger the "Soft Delete with Undo" state.

## User Flows

- **Default Location for a Subscribed Account:**
  1. User navigates to the "Manage Subscribed Accounts" page (`/settings/subscriptions`).
  2. Each subscribed account's row renders `{components.account_location_field}` (Component Patterns § Account Location Field), showing its current default location, if any, and a "Set/Edit Default Location" action, following the In-Table Add Form pattern.
  3. **First time set (no default location on the account yet):** clicking the action reveals a text input field; the user types a location (e.g., "Grand Indonesia Mall, Jakarta") and saves. The location is persisted immediately and associated with the *account* — not the individual subscription — so every other subscriber to that account inherits it too.
  4. **Editing an already-set default location:** the field shows the existing value, editable. Saving is never blocked — the change applies immediately — but it triggers the "Default Location Pending Review" state (see State Patterns), since an edit here silently changes what every other subscriber of the account sees. This is true even for a subscriber who also happens to hold the moderator role — this page always uses the standard review-required flow regardless of viewer role (Component Patterns § Account Location Field).
- **Correcting an Account's Default Location (Moderator, added 2026-08-24):** A moderator reviewing pending "Default Location" changes on Moderator Items (`/moderator/items`, Story 4.7) is not limited to Accept/Revert on a bad value — the same `{components.account_location_field}` used on the subscriber settings page also renders inline on each pending-request row, letting the moderator set the value directly to the actually-correct one. Unlike a subscriber's edit, a moderator's edit from this page applies with no further review and automatically clears any other still-pending request for that account (see State Patterns § Default Location Pending Review).

## Voice and Tone

The feeling of using FestDaily should be one of exciting discovery. Microcopy should be clear, concise, and helpful. Avoid technical jargon where possible. Provide immediate and clear feedback for user actions (e.g., "Event favorited", "API Key saved").

## Component Patterns

- **Notifications:** The system will use notifications to keep users informed about events and other relevant updates.
- **Accessibility:** All components will be designed and built to meet WCAG 2.1 AA standards, ensuring they are usable by people with a wide range of disabilities.
- **Platform Adaptability:** Components will be responsive and adapt their layout and density for optimal viewing on both mobile and desktop platforms.
- **Motion & Animation:** Subtle animations will be used on interactive elements (like button presses or card selection) to provide feedback and enhance the sense of discovery.
- **Content Density:** Components will adjust their information density based on the screen size, showing more detailed information on larger screens and a more concise version on smaller screens.
- **In-Table Add Form:** For managing lists of items (e.g., API Keys, Subscriptions), the mechanism for adding a new item is an always-present, editable row at the bottom of the table. This row contains the necessary input fields and an "Add" (`+`) action button.
- **Global Navigation:** A single, persistent nav element (never remounted across route changes, never duplicated in the DOM) renders the five items from Information Architecture § Global Navigation, laid out per the breakpoint rules in Responsive & Platform below.
  - **Active item:** indicated by `{colors.nav_active_indicator}` as a leading bar (vertical on the rail, top edge on the mobile tab bar) *and* a filled-vs-outline icon-style swap, so the cue survives even where the bar's contrast is hard to perceive. `aria-current="page"` is set on the active item independent of any visual styling.
  - **Icon-only rail (768–1279px):** every item carries an explicit `aria-label` matching its ≥1280px visible label — this is the accessible name, not the tooltip. The label tooltip appears on `:hover` **and** `:focus-visible` alike, stays visible while focus remains on the item, and dismisses on `Escape` or focus-out.
  - **Touch on the icon-only rail:** a single tap both navigates *and* flashes the item's label for ≥2000ms before it fades (never shortened, only extended, under `prefers-reduced-motion`) — not a two-step reveal-then-navigate.
  - **Hit areas:** every nav item's clickable/tappable region is at least `{components.nav.item_hit_area}`, not just the icon's visual bounds.
  - **Focus ring:** every item gets a visible focus indicator (`{components.nav.focus_ring}`) rendered in a color distinct from the active-bar accent, so "focused" and "active/current page" never collapse into one signal.
  - **Profile item (auth-dependent):** Story 0.7's shell renders this slot's trigger only (icon/state, `/login` navigation when unauthenticated, invoking Story 2.8's User Menu when authenticated) — the User Menu itself (ARIA pattern, focus behavior, dismiss/close affordances, icon-only-rail tap handling, item registry) is Story 2.8's scope; see Information Architecture § Profile item — authentication states.

### Mobile Multi-Day Calendar Spanning

*Added via a targeted `bmad-ux` pass, 2026-08-24 — resolves the deferred half of `ux-rework-2026-08-24.md` item #11 (`sprint-change-proposal-2026-08-24-ux-rework-batch.md` Section 4.8: vertical/skip-empty-days was already scoped for the mobile breakpoint; multi-day-span rendering inside that layout had no design until this pass). Applies to `WeeklyCalendarView` (Story 1.3g) at both its consumers (Discovery's Calendar View, Story 1.3f; "My Calendar", Story 2.6).*

**Breakpoint.** Below `{components.calendar.mobile_day_list.breakpoint}` (768px, matching Global Navigation's existing tier split above — the app's other precedent for a breakpoint-driven layout change), the 7-column week grid (`{components.calendar.grid_weekly}`) is replaced by a vertical list of days (`{components.calendar.mobile_day_list.container}`), one `{components.calendar.mobile_day_list.day_row}` per calendar day that has at least one schedule — days with none are omitted entirely, not rendered empty. Both layouts exist in the DOM simultaneously, toggled by Tailwind's `hidden`/`md:hidden` pairing; per the same rule as Global Navigation's own two-markup case (Accessibility Floor above), only the visually active variant is ever present in the accessibility tree, since `display:none` removes an element from it.

**Multi-day schedules repeat, once per spanned day.** A schedule whose `eventEndDate` differs from `eventStartDate` does **not** render once (on its start day) with a "spans N days" summary. It renders as a normal, fully-interactive compact card in **every** day-row it touches — the exact same per-day segment already computed for the desktop grid (`WeeklyCalendarView`'s `dayBuckets`/`isFirstSegment`/`isLastSegment`, AC4). This keeps one mental model across breakpoints: a schedule's presence on a given day is always its own independently focusable, independently clickable card, never a cross-day summary object with different interaction rules than a single-day card.

**Why no day ever gets "pulled back in" or jumped over.** The skip-empty-days rule is defined per day: a day is empty only if its schedule bucket has zero entries. A day inside a multi-day schedule's span always has at least one entry — its own segment of that schedule — so the rule never has occasion to skip it. There is no separate re-inclusion or "jump the gap" logic to design or build; it is a direct consequence of repeating the segment per day (previous point). This guarantee depends on the next rule holding:

**No cap, no "+N more" popover on the mobile list (SUPERSEDED 2026-09-17 — see Component Patterns § Calendar Overflow: Scalable Cap + Infinite-Scroll Popup below).** The desktop grid's `{components.calendar.event_rendering.more_link}` overflow-popover mechanism exists because `{components.calendar.day_cell}` is a fixed `h-32` box (see Story 1.3g Dev Notes § "Design Decisions Confirmed With User" for the original tradeoff). A vertical list row has no such fixed-height constraint — it grows to fit its content — so that *visual* constraint, and the mechanism it justified, do not carry over. This reasoning still holds on its own terms, but it only ever weighed layout, not scale: uncapped, always-render-everything does not survive a popular day accumulating hundreds of schedules — unbounded DOM size and an unbounded per-day fetch, not a layout problem. Mobile now gets a cap too, for that reason. The continuity guarantee this paragraph originally protected (a multi-day segment must never vanish behind "+N more" on one of its spanned days) is preserved, not dropped — see the superseding section for how.

**Visual treatment: a repeated "Day X of N" badge, not a connecting line.** The desktop grid signals continuity between adjacent segments by suppressing the shared border/corner rounding where two day *columns* touch (`multiDayRoundingClass` in `WeeklyCalendarView.tsx`) — it reads as one bar because the columns are physically adjacent. In the vertical list, each day's segment sits inside its own `{components.calendar.mobile_day_list.day_row}`, separated from the next by that row's own `{components.calendar.mobile_day_list.day_row_header}` — there is no shared edge to merge, and building a literal connecting line across non-adjacent, variable-height rows would need new absolute-positioning/measurement machinery this project has no primitive for (checked `packages/ui/src/core/`: `PageContainer`, `GridContainer`, `WeekPicker`, `BlockingLoader`, `MultiSelect` and the rest provide layout/composition, none provide timeline or connector rendering). Instead, every segment of a multi-day schedule keeps the existing `{components.calendar.event_rendering.multi_day_event}` violet background/border as its "this is multi-day" signal, plus a small `{components.calendar.mobile_day_list.multi_day_badge}` reading "Day *X* of *N*" (a small calendar-range icon + text, e.g. lucide-react's `CalendarRange` — confirm the exact icon name against the installed `lucide-react` version at implementation time). `X` and `N` are computed from the schedule's actual `eventStartDate`/`eventEndDate`, not clamped to the currently visible week — the same convention `isFirstSegment`/`isLastSegment` already use — so a schedule spanning a week boundary shows consistent numbering ("Day 3 of 10" on Saturday, "Day 4 of 10" on the following Sunday) as a user pages between weeks. The label is supplied by the caller as a resolver function, mirroring the existing `moreLabel` contract: `labels.multiDaySegmentLabel?: (dayNumber: number, totalDays: number) => string`, falling back to a plain `` `Day ${dayNumber} of ${totalDays}` `` if omitted — the count is only known per-segment inside the component, exactly like `moreLabel`'s existing rationale, so a static pre-resolved string can't work here either.

**Card content and line order.** A mobile list card stacks, top to bottom: (1) title, bold for a main schedule / normal for a sub-schedule (unchanged from desktop), plus the existing favorited/added-to-calendar icons; (2) the time range for that day's segment, rendered as always-visible inline text (`{components.calendar.mobile_day_list.time_range_inline}`) rather than the desktop's hover/focus tooltip — a necessary corollary, since the tooltip's existing show/hide handlers already skip touch pointers entirely (`pointerType !== 'touch'`), so a touch-only mobile user currently has no way to see a schedule's time at all; (3) the favorite count, when present, as its own line (`{components.calendar.mobile_day_list.favorite_count_line}`) — the "calendar items may be two lines tall" decision (`sprint-change-proposal-2026-08-24-ux-rework-batch.md` Section 4.5); (4) the "Day X of N" badge, multi-day segments only. None of these lines compete for space or force truncation, since list rows (unlike the desktop grid's `h-32` cell) size to their content.

**Keyboard/focus model.** The desktop grid's roving-tabindex arrow-key navigation (AC8) is a 2D-grid concern — rows and columns of day cells. The vertical list has no columns, so it does not adopt that model: cards are plain, linear Tab-order stops top to bottom, same as any other list in the app. Enter/Space activation (`onScheduleClick`) and the hover/focus-driven tooltip's `Escape`-to-dismiss behavior are unaffected on the desktop grid; on the mobile list, since time is always visible (no tooltip), there is nothing to dismiss.

**Interaction with the same-week-relative/`>`7-days-absolute date display rule.** No change needed. That rule (Section 4.9) is explicitly scoped to card/list *event* views, not the calendar — "calendar already positions items by absolute day column." The mobile list's day-row headers are themselves each day's absolute-date anchor (same `formatDayHeader` output the grid already uses, left-aligned per `{components.calendar.mobile_day_list.day_row_header}` instead of centered), so the relative-label rule simply does not apply inside the calendar at any breakpoint — confirmed non-conflicting, not extended here.

### Account Location Field

*Added via a targeted `bmad-ux` pass, 2026-08-24 — the shared component step of `sprint-change-proposal-2026-08-24-moderator-location-override.md`, following that proposal's PRD amendment (§3.7 "Moderator Override", §4.14 `changeSource: MODERATOR` / `status: SUPERSEDED`) and architecture amendment (AD-11). Extracted from `subscriptions-content.tsx`'s existing inline block — value, `{components.status_badge.pendingReview}` badge, and edit trigger — which today is hand-duplicated rather than shared, not a new interaction being invented.*

**One component, two call sites, no role-branching inside it.** `AccountLocationField` displays an account's current default location, its `pendingReview` status if one is open (`{components.status_badge}`, existing `pendingReview` variant — this closes a pre-existing gap where the subscriber page hand-rolled its own badge styling instead of using the shared primitive), and an edit trigger that opens the existing `SetDefaultLocationDialog` (already generic — `accountId`/`mode`/`initialLocation` props, unchanged by this pass). The component itself does not check who's viewing it or branch on role; it only renders what it's given and calls back on edit. It composes into two contexts:

- **Subscriber settings** (`/settings/subscriptions`, Story 3.2/3.3b): one instance per subscribed-account row, exactly as today's layout — this pass only extracts the existing inline markup into the shared component, no visual change.
- **Moderator review** (`/moderator/items`, Story 4.7, not yet built): one instance per pending-request list item, rendered *alongside* — not replacing — the existing Accept/Revert actions (Information Architecture § `/moderator/items`). Accept, Revert, and directly editing via `AccountLocationField`'s trigger are three additive options on the same row, matching how a moderator actually thinks about a bad value: agree with it, discard it with no opinion on the right answer, or supply the right answer directly (State Patterns § Default Location Pending Review covers the resulting states).

**Review-required vs. self-resolved is decided by the page, not the viewer's role.** A user who holds the moderator role and also personally subscribes to an account can reach `editAccountDefaultLocation` from either page. Resolved: mutation semantics follow which page is doing the calling, not an ambient check on the logged-in user's role. Editing from `/settings/subscriptions` always goes through the standard subscriber flow (`changeSource: USER`, creates a `PENDING_REVIEW` request) — even for a moderator — because that page's call site is deliberately wired to the ordinary subscriber path; only `/moderator/items`'s call site is wired to the moderator path (`changeSource: MODERATOR`, self-resolved, AD-11). This keeps "acting as moderator" a deliberate page you navigate to, not a silent capability upgrade on a page built for subscribers.

### Account Settings & Moderator Tools Shells

*Added 2026-08-25, `sprint-change-proposal-2026-08-24-ux-rework-batch.md` Section 4.2 — the authoritative source Stories 3.12 (Account Settings) and 4.7b (Moderator Tools) are built against. Both shells are instances of `TabbedShell` (`packages/ui/src/core/tabbed-shell/`, Story 0.29 — free navigation, no step-gating, unlike the wizard chrome it's a sibling of), each with its own local, hardcoded tabs array — there is no shared registry between the two shells, and neither shell is itself a new primitive.*

**Shell A — Account Settings (`/settings/account?tab=...`).** Tabs, left to right: **API Keys**, **Subscribed Accounts**, **Posts**, **Notifications**. Each tab's content is an existing, already-built page-content component remounted as a tab panel — `api-keys-content.tsx` (Story 3.1b, absorbs the API-key-health banner formerly on `/settings/queue-status`), `subscriptions-content.tsx` (Story 3.2, absorbs the pending-extraction-count badge formerly on `/settings/queue-status`), `posts-select-content.tsx` (Story 5.1, see the nesting note below), `notifications-content.tsx` (Story 2.9). `/settings/queue-status` is removed as a standalone route once both its pieces have moved. Active tab is tracked via a `?tab=` URL query param (`nuqs`, per `project-context.md`'s URL State rule) owned by the shell's own page component — `TabbedShell` itself has no `nuqs`/`next/navigation` dependency (Story 0.29 Dev Notes).

**Nested tabs inside the Posts tab.** `posts-select-content.tsx` (Story 5.1) already has its own internal per-account tab bar (PRD §3.10) — nesting it inside Shell A's own tabs is an accepted tabs-within-tabs situation, resolved (not deferred) directly against that one component, five changes, no new primitive:
1. **Shape differentiation:** inner row becomes rounded pill/chip buttons (filled background when active, no bottom border) instead of underline buttons, so it doesn't read as a continuation of Shell A's own tab strip.
2. **Avatars on the inner tabs:** each pill gets the account's `profileImageUrl` (already fetched in this component) as a small circular avatar — a real signal distinct from Shell A's plain-text outer tabs.
3. **Section label:** a "Posts from:" text heading above the inner row (previously absent).
4. **Visual containment:** the inner row + its content wrapped in the existing `card` token (`rounded-lg shadow-md p-4`, `DESIGN.md`) so it reads as a box inside the Posts tab panel.
5. **Mobile single-row scroll:** `flex-wrap` → `flex-nowrap overflow-x-auto` below the mobile breakpoint (`sm:flex-wrap sm:overflow-visible` above it).

**Shell B — Moderator Tools (`/moderator/tools?tab=...`).** Tabs, left to right: **Actor Runs**, **Unprocessed Payloads** — the existing `actor-runs-content.tsx`/`unprocessed-payloads-content.tsx` components remounted as tab panels, reusing Story 4.7a's route guard as-is (no new auth logic). `/moderator/items` (Information Architecture) is a deliberately separate page, not a third tab here.

**Chrome/tokens.** Both shells reuse `DESIGN.md`'s existing `nav.profile_menu`-adjacent chrome conventions and button primary/secondary tokens (`#1E293B`/`#6366F1`, `bg-violet-600 text-white` / `bg-gray-200 text-gray-800`) for the tab bar's active/inactive states — a 2-state model, not `WizardStepSummary`'s 3-state Completed/Current/Upcoming (free navigation has no linear-progress concept; see Story 0.29 Dev Notes). No new component tokens are introduced by this pass beyond what `TabbedShell` itself already ships with.

### Masonry EventCard: Date Box, TILL Badge, and Status/Nearby Badge Row

*Added via a targeted `bmad-ux` pass, 2026-09-04 — `sprint-change-proposal-2026-09-04.md` Section 4.3, feeding Story 1.3b's AC14-AC18 and Story 1.3d's AC16-AC19. Token source: `{design-artifacts/UX-festgrid-run-1/DESIGN.md}` `components.event_card_date_box` / `event_card_till_badge` / `event_card_status_badge` / `event_card_nearby_badge` / `event_card_masonry`. This entry supersedes the masonry-relevant portion of whatever informal behavior the original 2026-08-25 relative-day pill carried — there was no dedicated EXPERIENCE.md entry for it before this pass. **Revised 2026-09-11** (see the two subsections below marked accordingly) — reopened against new reference screenshots; Story 1.3b's already-shipped AC17 prominent-poster path is untouched, only the default (non-prominent) composition changes.*

**Composition, top to bottom — `prominentPoster=true` (opted-in accounts, `durableImageUrl` present, unchanged since 2026-09-04).** The masonry card's poster area keeps its two existing overlays unchanged in position — `{components.event_card_date_box.base}` top-left, the heart+favorite-count control top-right (confirmed visually unchanged by this pass) — and gains `{components.event_card_till_badge}` as a small tag anchored to the date box's top-left corner, conditional per Story 1.3b AC14. Below the poster, a new `{components.event_card_masonry.badge_row}` renders the status badge (`{components.event_card_status_badge}`, always present — one of its 8 states always applies) followed by the nearby badge (`{components.event_card_nearby_badge}`, conditional on `distanceKm <= 5`) when present. The existing eventName/locationName caption is unchanged and simply sits below this row. **Why this state alone keeps the full-width overlay poster (revised 2026-09-11, replacing this entry's original "both states" framing):** a `durableImageUrl` never expires, so there's no graceful-degrade concern to design around — it can safely be the card's large hero element, matching `{components.event_card_masonry.image_prominent}`'s taller aspect ratio.

**Date/favorite pill position is conditional on the TILL badge, revised 2026-09-14 (rounds 5-7, user-directed — corrects two intermediate wrong mechanisms before landing here).** `{components.event_card_date_box.base}` and `{components.event_card_date_box.favorite_pill}` (the poster's own heart+count overlay, newly tokenized this pass) both sit at their default `top-2` position when the event has no TILL badge (already ended, or ends today). When a TILL badge IS present (ends tomorrow or later), both pills move down together to `top-5`, freeing room for the tag to sit above the date pill's text without clipping against the poster's own edge. Two earlier fixes were tried and rejected by the user: first, giving the date pill asymmetric top/bottom padding so its text sat lower (rejected — "the till badge area can overlap with date badge, but should not overlap with the text," and padding must stay the same on all sides); the final mechanism is position-only — the TILL tag's own offset increases (see Component Patterns' badge-row accessibility notes below) and the pills move down to give it room, padding untouched. Screenshot-validated across all four combinations (has-till/no-till × mobile/230px-capped-desktop) in `prototypes/event-card-masonry/prominent-poster.html` (Panels A-D).

*Reference: `imports/event-card-masonry/prominent-poster.png`.*

**Composition, top to bottom — `prominentPoster=false` (the default, common case — a hotlinked/scraped image that can expire, PRD §3.16). Revised 2026-09-11, replacing this state's prior overlay treatment.** New reference screenshots showed the date box as a separate solid panel beside a *small* thumbnail, not overlaid on a full-width poster — but only for this state; re-reading the screenshots against `prominentPoster=true`'s own reference images (still overlay) resolved what first looked like a flat contradiction in EXPERIENCE.md line ~178 into two states that were never actually specified separately. Top row (`{components.event_card_masonry.top_row_default}`): `{components.event_card_date_box.base_default}` (solid navy fill, no longer glassmorphism — there is no poster directly behind it to justify a blur) sits beside `{components.event_card_masonry.thumbnail_default}`, a small thumbnail whose height matches the date box's own intrinsic height (`items-stretch`), not the reverse. `{components.event_card_till_badge}` still anchors to the date box's top-left corner exactly as in the prominent state. The thumbnail carries its own small heart+favorite-count pill in its corner (`thumbnail_default.favorite_badge`) when the image is present. Below this row, `{components.event_card_masonry.badge_row}` and the caption are unchanged from the prominent state's composition above. **Why binding the thumbnail's size to the date box (not to a full poster) matters — the actual reason this state differs from the opted-in one:** a hotlinked image's URL can expire at any time after the card has already rendered. Sizing the thumbnail off the date box's own height (a value that never depends on the image loading) means the row's layout is stable whether or not the image ever arrives — see the fallback state immediately below, which is the entire point of this composition.

*Reference: `imports/event-card-masonry/default-with-thumbnail.png`.*

**Date box size corrected 2026-09-14.** `{components.event_card_date_box.base_default}` is a large two-tier stacked box (a small uppercase month/weekday line over a large bold day number), not the single small `text-xs` line this section and DESIGN.md both described until now — the reference screenshot above has always shown the large stacked treatment; the mismatch went uncaught through two prior passes because both re-described the image in prose rather than reconstructing it. Caught this pass via a screenshot-validated HTML/Tailwind prototype (`prototypes/event-card-masonry/default-with-thumbnail.html`, `prototypes/validation-log.md`), checked at both the real mobile 2-col card width (175px) and the real desktop xl:5-col width (269px) — clean at both. See the token's own DESIGN.md comment for the full correction and the still-open implementation gap (the shipped `EventCardDateBox` primitive still renders the old single-line shape).

**Title/venue and prominent-poster aspect ratio revised 2026-09-14 (user's view-mode-organized rules pass), applies to both `prominentPoster` states.** `{components.event_card_masonry.title}` wraps up to 2 lines (`line-clamp-2`) at a font size deliberately smaller than the generic `{components.card.title}` token it previously fell back to — room for a 2-line title in a narrow 2-col mobile tile mattered more here than matching that generic size. `{components.event_card_masonry.venue}` stays a single line (`truncate`), now explicitly tokenized rather than left unstyled. Separately, `{components.event_card_masonry.image_prominent}` (the `prominentPoster=true` poster) changes from a tall `aspect-[2/3]` portrait to a `1:1` square — a real crop change, not just a doc correction; the reference screenshot's own portrait crop is superseded by this explicit instruction.

**Missing/expired image fallback — `prominentPoster=false` only (new 2026-09-11, folding in backlog FIND-023).** When the image is absent or its `onError` fires (the same detection `EventCard.tsx`'s existing AC3 standard-variant fallback already uses), `{components.event_card_masonry.thumbnail_default_fallback}` renders in the reserved thumbnail slot: **nothing** in the image's place — no icon, no "image not available" text, no fill distinct from the card's own background — per the user's explicit instruction that a broken/expired image should render nothing rather than a placeholder. In that same reserved slot, the small corner favorite pill is replaced by `{components.event_card_favorite_count_badge_large}`, centered rather than corner-anchored: the same favorite-toggle heart and count, just larger and with its pill background removed, so a viewer's eye lands on a legible, still-interactive control rather than a visibly broken image. The `prominentPoster=true` state has no equivalent fallback in scope here — a `durableImageUrl` is defined as non-expiring, so this failure mode doesn't apply to it (**deferred to implementation**: if a `durableImageUrl` load can still fail for other reasons — e.g. a transient network error — that is an existing, unrelated failure mode, not something this pass's expiry-driven reasoning covers; revisit only if it turns out to matter in practice).

**The favorite icon actively grows within its reserved slot, revised 2026-09-14 (user-directed).** Unlike the row card (which has no reserved slot to grow into), masonry's reserved slot itself stays a fixed size (matching the date box's own height, unchanged this round) — but within that fixed footprint, the favorite icon now scales up to occupy noticeably more of the available space rather than sitting small with empty room around it. Validated at both real masonry widths (175px mobile, 269px desktop): the icon is larger in the desktop card, which reserves a taller slot (a bigger date box) to scale into.

*Reference: `imports/event-card-masonry/default-thumbnail-fallback.png`.*

**Why the status badge doesn't use per-state color.** Also resolved with the user: one neutral style for all 8 states (`{components.event_card_status_badge}`), differentiated by text alone rather than a new 8-way (or tiered) color system. The existing `{components.status_badge}` primitive's positive/negative/pendingReview/superseded palette was deliberately not reused here — that palette's colors mean "review-outcome," and forcing this badge's time-urgency states through it (e.g. "Ended" as the red "negative" variant) would misread as an error rather than a neutral fact about elapsed time. **The `happeningNow` state's label is shortened to "Now" (2026-09-14, user-directed)** — a single translatable word, replacing "Happening Now"; the other 7 states' labels are unchanged.

**Exception: `happeningNow` gets a distinctly more noticeable color (revised 2026-09-14, round 6, user-directed: "`now` badge should have more noticeable color").** `{components.event_card_status_badge.happening_now}` is a solid emerald green, not the shared neutral `base` every other state uses. This is a scoped, deliberate exception for this one state's own visual weight — it does NOT reopen the "one neutral style for all 8 states" decision above; the other 7 states are unaffected. Applied everywhere this badge renders across masonry, the Calendar Row Card, and the Calendar Grid Item Card's no-image composition (the grid-item's with-image composition dropped this badge entirely, per Component Patterns § Calendar Grid Item Card below).

**Card max-width capped at 230px, revised 2026-09-14, round 7 (user-directed).** All three masonry states — this one, the default-with-thumbnail state below, and its missing-image fallback — never render wider than `{components.event_card_masonry.max_width}` (230px), regardless of how wide their grid slot is. At the mobile 2-col slot (175px) this has no visible effect; at the desktop xl:5-col slot (269px) the card renders at 230px, left-aligned within the wider cell by default CSS Grid item alignment, leaving the remainder as unused cell space rather than stretching the card to fill it.

**Distinguishing the two below-image badges without relying on color alone (WCAG 1.4.1).** The status and nearby badges sit adjacent in the same row and could be visually confused at a glance if color were their only distinguishing signal (this project's existing convention already requires a non-color cue — see State Patterns § Soft Delete with Undo below). Two independent cues separate them: the nearby badge carries its own icon (a `Navigation` glyph, deliberately distinct from the caption's `MapPin` icon so the two location-related glyphs never look identical in one dense card) that the status badge never has, and each badge's text content is always unique and self-describing regardless of styling. Color (`{components.event_card_nearby_badge}`'s secondary fill vs. the status badge's neutral muted fill) is present as a reinforcing cue only, not the sole differentiator.

**The nearby badge now replaces the category/type badge in this row entirely (revised 2026-09-14, user-directed).** Before this pass, `badge_row` could show a category/type badge (e.g. "Music") alongside the conditional nearby badge. Now the category/type badge is gone from this row altogether — the same slot shows only `{components.event_card_nearby_badge}`, and only when the event is **<8km** away (threshold changed from the original `<=5`; see that token's DESIGN.md comment), computed client-side against the active filter location if one is selected, else the viewer's current location coordinate. At `>=8km`, nothing renders in that slot — no placeholder, consistent with the badge's original omit-entirely behavior, just a wider gate on when it fires. This is scoped to masonry, the Calendar Row Card, and the Calendar Grid Item Card (all three redesigned this pass) — not asserted as a change to every other consumer of a category/type badge elsewhere in the app.

### Calendar Row Card: Thumbnail and Fallback

*Added via a targeted `bmad-ux` pass, 2026-09-11 — backlog IDEA-016/FIND-023, per two user reference screenshots of a wide-row calendar-view card (distinct from the masonry EventCard above). Token source: `{design-artifacts/UX-festgrid-run-1/DESIGN.md}` `components.event_card_compact` / `event_card_compact_thumbnail_fallback` / `event_card_favorite_count_badge_large`. Prior to this pass, neither `EXPERIENCE.md`'s Mobile Multi-Day Calendar Spanning section nor `WeeklyCalendarView.tsx` rendered any event image at all — this is new scope, not a correction.*

**Composition, left to right.** A date box (`{components.event_card_compact.date_box}`, same solid-navy, non-glassmorphism visual treatment as the masonry card's `prominentPoster=false` state — reused rather than inventing a third date-box style, for the same reason: no photo sits directly behind it), then a title/venue/status/nearby-badge column (`{components.event_card_compact.content}`, reusing `{components.event_card_status_badge}`/`{components.event_card_nearby_badge}` as-is — no new badge design needed), then a thumbnail-plus-favorite stack (`{components.event_card_compact.image_wrapper}`) sized to the row's own height rather than the reverse — the identical "chrome sizes the image, not the image sizing the chrome" principle as the masonry card's default-state thumbnail, and for the identical reason (see below).

**Favorite control moved below the thumbnail, revised 2026-09-14 (user replaced the reference screenshot).** `{components.event_card_compact.favorite_badge}` no longer overlays the thumbnail's own corner — it's now a separate pill sitting directly below the thumbnail, the two forming a vertical stack (`image_wrapper`) centered within the row's full stretched height. This reverses the original corner-overlay composition and drops that pill's glassmorphism (`bg-background/80 backdrop-blur-sm`), now a solid `bg-background`/`shadow-sm` pill instead, since it no longer sits over image content.

**Title/venue line rules and badge replacement, revised 2026-09-14 (user's view-mode-organized rules pass).** `{components.event_card_compact.title}` wraps up to 2 lines (was single-line truncate) and `{components.event_card_compact.venue}` stays 1 line (truncate, now explicitly tokenized) — the mobile calendar view's explicit rule: "event name max 2 lines, venue max 1 line." The category/type badge this content column could show is gone entirely, same as masonry: only `{components.event_card_nearby_badge}` (Navigation icon + distance) shows in that slot, gated at `<8km`, omitted otherwise — see Component Patterns § Masonry EventCard's own badge-replacement paragraph above for the shared rule and its rationale.

**Date box corrected 2026-09-14** — `{components.event_card_compact.date_box}` is the same large two-tier stacked chrome (a small uppercase month/weekday line over a large bold day number, plus the amber `till_label` corner tag) as the corrected `{components.event_card_date_box.base_default}`, not the single-line small text this section originally described. See that token's DESIGN.md comment for why the original description was wrong and how this pass caught it (a screenshot-validated HTML/Tailwind prototype built from `imports/event-card-calendar-row/with-thumbnail.png`, not a re-description of the image by eye). Screenshot-validated at this card's own real mobile widths — 326px (narrow phone) and 655px (widest mobile, just below the `md:` breakpoint where this surface stops existing) — both render cleanly (`prototypes/event-card-calendar-row/with-thumbnail.html`, `prototypes/validation-log.md`).

*Reference: `imports/event-card-calendar-row/with-thumbnail.png`.*

**One date box, showing till/end info — not a second start-date display plus a separate TILL badge (revised 2026-09-11, resolving this section's original open item about a redundant per-card date box).** Unlike the masonry card, this row never gets its own `{components.event_card_till_badge}` sub-badge: the calendar surface is already organized under a day/date container (`day_row_header` in the mobile day-list, the day-column header in the desktop grid) that tells the viewer when the schedule *starts* — repeating that same start date inside every card underneath it would be pure duplication. User-confirmed resolution: the one date box this card does have shows *end*/"till" information instead of the start date `{components.event_card_date_box}` shows on the masonry card — the box is reused for its date-chip chrome, but its content rule is till-shaped, not start-shaped. Unlike `event_card_till_badge`'s own AC14 gating (no badge until the event has started), this box cannot go empty pre-start — most calendar cards are for events that haven't started yet, and an empty box would defeat the point of keeping one — so its till content is shown unconditionally for the day's segment, not gated on "has it started." **Deferred to the amendment story:** the exact always-shown text rule (most likely that day-segment's end time, reusing `formatEventTime` and the per-day segment data `WeeklyCalendarView.tsx` already computes for multi-day spanning — `dayBuckets`/`isFirstSegment`/`isLastSegment` — rather than new formatting logic), since only the *gating* condition is decided here, not the exact string.

**Missing/expired image fallback — REVERSED 2026-09-14 (user-directed; no longer the same reserved-space convention as the masonry card).** Originally (2026-09-11, folding in backlog FIND-023): the thumbnail's reserved space stayed exactly the same size and rendered nothing where the image would have been, matching masonry's own fallback convention. That convention is now dropped for this card specifically: `{components.event_card_compact_thumbnail_fallback}` means **no area for image at all** — the image element is omitted from the DOM entirely (not left blank in its old spot), `{components.event_card_compact.content}` expands to fill the freed width, and `{components.event_card_favorite_count_badge_large}` sits directly at the row's end with no fixed-width wrapper reserved for it. Masonry's own fallback is unchanged — this reversal is specific to the row (and, per the next section, the grid-item) card, per the user's explicit three-way distinction between the card families this pass covers.

**The favorite control's own box actively grows to occupy the freed space (revised 2026-09-14, same round of user feedback).** Rather than staying a small fixed-size element regardless of how much room the dropped image left behind, the favorite control's icon scales up based on how much space is actually available — validated at both real row widths (326px narrow mobile, 655px widest mobile): the icon is noticeably larger in the wider row, where more space was freed. Icon and count stay vertically stacked in both cases (the existing `{components.event_card_favorite_count_badge_large}` shape), just at different concrete sizes per real width rather than one fixed size for all.

*Reference: `imports/event-card-calendar-row/thumbnail-fallback.png`.*

**Attachment — resolved 2026-09-14 (previously left as an open item).** See Component Patterns § Calendar View Cards: Attachment and Composition below, which resolves this card's attachment point together with the new desktop `event_card_calendar_grid_item` card rather than separately.

### Calendar Grid Item Card: Desktop Composition and Fallback

*New scope, `bmad-ux` pass 2026-09-14 — backlog IDEA-016/IDEA-017, the desktop counterpart to the Calendar Row Card above, per two new reference screenshots (`imports/event-card-calendar-grid-item/`). Token source: `{design-artifacts/UX-festgrid-run-1/DESIGN.md}` `components.event_card_calendar_grid_item` / `event_card_calendar_grid_item_thumbnail_fallback`. Grounded directly in `WeeklyCalendarView.tsx`'s actual `variant === 'grid'` render path (today: plain text-only, no thumbnail, no date box) rather than re-describing the reference screenshots by eye.*

**Composition, with-image state (multi-day, image present and not errored) — left to right.** A thumbnail (`{components.event_card_calendar_grid_item.image}`), then a title/venue column (`{components.event_card_calendar_grid_item.content}` — no badges here, unlike the masonry/row cards), then a right-side stack (`{components.event_card_calendar_grid_item.side_stack}`) holding the favorite heart+count and the nearby badge together. That grouping is a genuine layout difference from its sibling cards, observed directly in the reference screenshot and preserved rather than normalized: `event_card_masonry.badge_row` and `event_card_compact.content` both put badges inline under the title, but this card's badges sit beside the favorite control instead. This composition is multi-day only — see Attachment below for why, and for what changed about where/how it renders.

*Reference: `imports/event-card-calendar-grid-item/with-thumbnail-multiple-days.png`.*

**Thumbnail and favorite area are both 1:1/centered against the row's own height, revised 2026-09-14 (user-directed: "the thumbnail and its parent area are positioned center both vertically and horizontally" / "the favorite area should be positioned center both vertically and horizontally").** `{components.event_card_calendar_grid_item.image}` is explicitly square. Mechanism (simplified by the user's own later edit, superseding this pass's first draft): rather than each column stretching to the row's height and re-centering its own content in a nested wrapper, `{components.event_card_calendar_grid_item.base}` itself uses `items-center` (not `items-stretch`, unlike every other card family's row composition in this document) — every column then sizes to its own natural height and centers directly against the row's tallest sibling (normally the title/venue text). So a longer title wrapping to more lines leaves the image and the favorite/badge stack both centered in the extra room, not pinned to the top.

**No status ("Now") badge on this composition, revised 2026-09-14 (user-directed: "no need the `now` related badge").** `side_stack` here shows only the favorite control and the nearby/radius badge — `{components.event_card_status_badge}` is dropped entirely on the with-image/multi-day composition, unlike the masonry and row cards, which both keep it. The no-image (single-day) composition never had a status badge to begin with, so nothing changes there.

**The image is conditional, not a reserved slot (revised 2026-09-14 — user correction of this pass's first draft, which wrongly copied the masonry/row cards' reserved-space convention onto this card).** `{components.event_card_calendar_grid_item.image}` renders **only** when the schedule is multi-day **and** its image hasn't errored/expired. A single-day event never attempts an image at all — there is no blank/reserved slot standing in for it, and no space is set aside on the chance one might have loaded. When the condition is false, the card renders via an entirely different composition (see below), not the with-image row with its image column simply omitted.

**No-image composition — two stacked rows, not the with-image row minus its thumbnail.** When no image renders (single-day event, or a multi-day event whose image errored), the card is `{components.event_card_calendar_grid_item.no_image_base}`: row 1 (`title_row`) pairs the title with the favorite control, top-aligned so a wrapping multi-line title doesn't push the favorite down; row 2 (`location_row`) pairs the venue with the nearby badge. This is a genuinely different grid from the with-image state's single 3-column row — confirmed by the user replacing the reference screenshot with this exact two-row layout, not inferred from the with-image state by removing its image column.

*Reference: `imports/event-card-calendar-grid-item/thumbnail-fallback.png`.*

**No date box in either composition (unlike the Calendar Row Card, which keeps one for till/end info).** The desktop day-column header sits directly above the day's cell/popover trigger — even more immediately adjacent than the mobile `day_row_header` the Calendar Row Card's own till-content decision already reasoned about — so a per-card date reference here would be pure duplication with no compensating new information (there is no "till" content gap to fill the way there was on the row card, since this card has no day-boundary ambiguity to resolve). Neither reference screenshot shows any date chrome, confirming this reading rather than an omission.

**Title and venue wrap, they don't truncate (revised 2026-09-14 — this pass's first draft incorrectly single-line-truncated both, unlike its own reference screenshots).** `{components.event_card_calendar_grid_item.title}` wraps freely across as many lines as it needs; `{components.event_card_calendar_grid_item.venue}` wraps up to 2 lines. Both are shared between the with-image and no-image compositions. This is a deliberate difference from the masonry/row cards, which keep single-line truncation for both — not an oversight to reconcile toward consistency.

**The category/type badge is replaced by the nearby/radius badge, gated at <8km (revised 2026-09-14, threshold corrected same pass).** Unlike the masonry and row cards' original behavior, this card never shows a category/type badge ("Music," etc.) — that badge slot instead shows `{components.event_card_nearby_badge}` (`Navigation` icon + distance) only when the event is **<8km** away, omitted entirely otherwise, in both compositions. Not a new token; reuses the existing nearby-badge component, same threshold/role change as masonry and the Calendar Row Card (Component Patterns § Masonry EventCard's badge-replacement paragraph above has the shared rationale).

**The favorite control is always the "large" shape, regardless of image state (corrected 2026-09-14).** Both compositions use the identical large, unpilled, icon-over-text heart+count (`{components.event_card_favorite_count_badge_large}` — the same shared token the masonry and row cards' *missing-image fallbacks* use, but here it is simply the card's one and only favorite treatment, not a fallback swapped in for a smaller default). Unlike the masonry/row cards, this card has no small-corner-pill favorite variant at all, so there is nothing to gate between image success and failure — confirmed by comparing both reference screenshots side by side, which show the same shape in each.

**No "Day X of N" badge — REMOVED 2026-09-14 (reverses this pass's own earlier addition, user-directed).** This pass first added a `multi_day_badge` token extending the mobile day-list's "Day X of N" + `CalendarRange` convention to desktop, reasoning that an isolated card render can't show the desktop grid's own multi-day cue. That reasoning no longer applies: per the Attachment section below, a multi-day schedule now renders as ONE card spanning across its N day-columns, not a per-day popover-list item — its position in the calendar (which day-columns it visually spans) already conveys the same information a "Day X of N" label would, making the label pure duplication. The token is removed with no replacement.

### Calendar View Cards: Attachment and Composition

*New section, `bmad-ux` pass 2026-09-14, resolving the Calendar Row Card's own open item (above) together with the new Calendar Grid Item Card, per this run's instruction to decide both surfaces jointly rather than separately — deciding one in isolation risked re-litigating the same "which existing surface, if any, does this replace" question twice with inconsistent answers.*

**Mobile (Calendar Row Card): already resolved by shipped code, not actually still open.** Checking `WeeklyCalendarView.tsx` directly (rather than trusting this doc's own stale open item) shows the question already answered: its `variant === 'list'` render path — the mobile day-list's per-schedule card — already renders `EventCardDateBox` (via `computeCalendarSegmentTillText`) beside `EventCardMediaSlot`, i.e. exactly this card's date-box-plus-thumbnail shape. The Calendar Row Card **replaced the mobile day-list's per-schedule card outright** (one of this doc's own previously-listed options), not a distinct parallel surface — this doc was simply left stale after the code shipped, a recurring pattern on this project (dev-story bookkeeping lagging already-committed code). No attachment decision remains open on the mobile side; only the token-level corrections above (the two-tier date box) were still needed.

**Desktop (Calendar Grid Item Card): splits by single-day vs. multi-day (revised 2026-09-14, user-directed — supersedes this pass's own earlier single shared-mechanism resolution).**

*Single-day, unchanged from this pass's earlier finding: attaches to the "+N more" popover, not the always-visible `grid_weekly`/`day_cell` grid.* Resolved from actual dimensions, not the reference screenshots' arbitrary canvas size. `{components.calendar.day_cell}` is a fixed `h-32` (128px) with `overflow-hidden`, sized to hold up to `{components.calendar.event_rendering.discovery_view.max_events_per_day}` (5) events. Single-day events never show a thumbnail at all (Component Patterns § Calendar Grid Item Card), so the no-image two-row composition they use is compact enough it may fit directly in a day_cell for at least the first visible event; anything beyond what fits overflows to the grid's existing "+N more" popover disclosure (`w-56 max-h-56 overflow-y-auto`, already rendering every one of a day's schedules uncapped, exactly like the mobile day-list does) — screenshot-validated at that popover's real 200px inner content width (`prototypes/event-card-calendar-grid-item/thumbnail-fallback.html`): fits comfortably with room to scroll. This makes the popover desktop's functional counterpart to mobile's own uncapped day-list for the single-day case — both are the "show me everything for this day" surface, distinct from their respective space-constrained always-visible rows/cells — even though the popover is opened by a click rather than always rendered. The always-visible day_cell **keeps its current minimal text-only `CalendarCard` render** for whatever doesn't fit directly; exact `max_events_per_day` recalibration for the richer single-day card remains an implementation-story question (see Consequence below).

*Multi-day: a single card spanning across its N day-columns, directly in `grid_weekly` — NOT the popover (revised 2026-09-14, user-directed; this pass's first draft wrongly routed multi-day through the same popover mechanism as single-day).* A multi-day schedule renders as ONE spanning bar across the day-columns it covers, extending the same mechanism `{components.calendar.event_rendering.multi_day_event}`/`WeeklyCalendarView.tsx`'s `multiDayRoundingClass` already uses for today's plain-text multi-day bar — not a fixed-width item in a per-day popover list. Because its width is `N * day_cell_width` (e.g. a 3-day span at the 1280px-viewport day_cell width of 174px = 522px) rather than a fixed ~200px, the with-image 3-column composition (Component Patterns § Calendar Grid Item Card) fits comfortably even with a thumbnail — screenshot-validated at that real spanning width (`prototypes/event-card-calendar-grid-item/with-thumbnail-multiple-days.html`). This also drops the need for `multiDayRoundingClass`'s own suppressed-rounding-at-touching-edges trick for this card specifically: since it's one spanning element rather than repeated per-column segments, ordinary uniform rounding on both ends is enough — there's nothing to suppress. The multi-day card's position (which columns it visually spans) is also why no "Day X of N" badge is needed (Component Patterns § Calendar Grid Item Card) — the single-day case's popover-list context, where that reasoning originally applied, no longer applies to multi-day at all.

**Consequence for `event_rendering.discovery_view.max_events_per_day` (flagged, not resolved here).** This pass does not change that token's value (5) or the single-day always-visible grid's own rendering — only the popover's contents (single-day overflow) gain the richer no-image card, and multi-day bypasses the cap/popover entirely via its own spanning mechanism. Whether 5 compact text-only single-day events per cell remains the right always-visible cap, now that popover overflow shows a materially richer card than before, is an architecture/product question for the amendment story, not a UX-doc decision.

### Day-of-Week Recurring Schedules

*Added via a targeted `bmad-ux` pass, 2026-09-16 — extends the Mobile Multi-Day Calendar Spanning section above (IDEA-003) to cover `Schedule.applicableDaysOfWeek?: DayOfWeek[]` (PRD Section 4.4, added 2026-09-11 via BUG-026's PRD amendment; reuses the `DayOfWeek` enum already defined for `EventFilterInput.dayOfWeek`, PRD Section 4.18). BUG-026 itself remains the tracking item for that feature's still-open backend/extraction halves (the extraction prompt actually populating the field, and `buildEventsQueryCondition.ts`/`drizzle-where.ts`'s own weekday-aware filter-matching correctness fix) — this section covers only how a schedule that already carries `applicableDaysOfWeek` renders across the app's calendar and card surfaces, a piece BUG-026's own note never called out.*

**Expansion happens client-side, not via a backend contract change (user-directed: "the get-events endpoint should be optimized").** `getEvents`/`eventBySlug` already return a schedule's full `eventStartDate`/`eventEndDate`/`applicableDaysOfWeek` as-is; every surface that renders individual calendar days derives which specific dates a schedule actually occupies from that raw data itself — the same way `WeeklyCalendarView.tsx`'s existing `dayBuckets` computation already derives per-day occupancy from a plain `eventStartDate`/`eventEndDate` range today. Chosen over server-side pre-expansion specifically to avoid `Query.events`/`Query.eventBySlug` (project-context.md's highest- and second-highest-traffic endpoints) having to return a variable, potentially large number of synthetic per-occurrence rows.

**A day only counts as an occurrence when both conditions hold.** A calendar day belongs to a schedule's occurrence set when it falls inside `[eventStartDate, eventEndDate]` (today's existing rule, unchanged) AND — only when `applicableDaysOfWeek` is set and non-empty — its weekday is included in that list. A schedule with `applicableDaysOfWeek` unset behaves exactly as today: every day in the range is an occurrence, matching the PRD's stated default/legacy-data behavior (BUG-026's PRD decision record).

**Consecutive matching days collapse into one run; gaps start a new one (user-directed: "detect consecutive-days occurrence... n-consecutive days is treated as single n-days range schedule").** `isFirstSegment`/`isLastSegment` generalize from "equals the schedule's overall `eventStartDate`/`eventEndDate`" to "the immediately adjacent calendar day (±1) is not also an occurrence of this schedule" — the same day-by-day adjacency check `dayBuckets` already performs, now evaluated against the narrowed occurrence set instead of the raw range. A day-of-week pattern whose matching days happen to be calendar-adjacent (e.g. Mon+Tue back to back) therefore renders and behaves exactly like today's plain multi-day span — same rounding, same "Day X of N" badge — with one difference: **N is the run's own length, not the schedule's overall date-range span** (e.g. "Day 1 of 2" / "Day 2 of 2" for a Mon+Tue run inside a Sept 1–30 schedule, never "Day 1 of 30") — the full-range number would misstate how long any single visit actually lasts. A pattern whose matching days are never calendar-adjacent (e.g. a single weekly Tuesday) renders every occurrence as its own isolated single-day card (user-confirmed default), identical in shape to a genuine one-off single-day event.

**New: `{components.event_card_repeat_badge}` marks any occurrence — isolated or collapsed run — that originates from a day-of-week pattern.** Once collapsed, a day-of-week run is visually indistinguishable from a genuine one-off multi-day span, and an isolated single occurrence is visually indistinguishable from a genuine one-off single-day event — position and span alone no longer tell the two apart the way they used to before this pattern existed. This icon-only badge (no text label, `Repeat` glyph, DESIGN.md `components.event_card_repeat_badge`) closes that gap: reuses this project's existing hover+focus tooltip pattern (`WeeklyCalendarView.tsx`'s own time-range tooltip mechanism — non-touch-gated, `Escape`-dismiss) for its content, and an `aria-label` covering the same content for screen-reader/touch users who can't hover — both carrying the schedule's actual matching weekdays, translated through the project's existing enum-translation convention (project-context.md's Locale-Sensitive Data Rendering rule, same as any other `DayOfWeek`/`EventCategory` value), never a raw enum string.

**Placement is per card family, not one shared position (user-directed).**
- **Masonry EventCard** (`{components.event_card_masonry.badge_row}`): inserted immediately after `{components.event_card_status_badge}` ("the upcoming/now variant badge"), before `{components.event_card_nearby_badge}` — badge_row's existing status-then-nearby order becomes status → repeat → nearby.
- **Calendar Row Card** (`{components.event_card_compact}`) and **Calendar Grid Item Card** (`{components.event_card_calendar_grid_item}`): extends the leading-icon-then-title row both card families already share with `WeeklyCalendarView.tsx`'s existing `isFavorited`/`isAddedToCalendar` conditional icons — identical markup today in both the shipped `variant === 'list'` render and the `variant === 'grid'` render, unaffected by whatever differs on each card's own right-side content (user: "it works identically across both card layouts regardless of their right-side components"). The repeat icon joins that same leading row, before the title text — never truncated/clipped by the title's own `line-clamp`/`truncate`, since it lives outside that span, not inside it.
- Applies uniformly across all three families because `applicableDaysOfWeek` is a `Schedule`-level field and every consumer here (`EventCard`, `WeeklyCalendarView`'s list and grid variants) already renders one specific, single schedule per card — confirmed against `EventCard.tsx`'s own flat `startDate`/`startTime`/`endDate`/`endTime` props, not an aggregated multi-schedule shape — so there is no cross-schedule ambiguity (e.g. "which of this event's schedules is recurring") to resolve per card.

**Flagged for `bmad-architecture`, not decided here.** Whether the client-side weekday-match check this section relies on (a calendar day's weekday ∈ `applicableDaysOfWeek`) should reuse/extend the weekday-enumeration utility BUG-026 already found living in `packages/domain/src/events/buildEventsQueryCondition.ts` (`getDays`, lines 73–83) rather than a third independent reimplementation in `packages/ui`'s `WeeklyCalendarView.tsx`. BUG-026's own note already flags the extraction layer and the backend filter layer as two inconsistent implementations of this same weekday-matching concept — adding this frontend occurrence-expansion as a third makes that worth resolving deliberately rather than by further accretion.

### Temporal Filter: Today / Upcoming / All (Card View Only)

*Added via a targeted `bmad-ux` pass, 2026-09-17 — IDEA-019, a coaching-path session grounded directly in `EventDiscoveryPanel.tsx`/`FilterHub.tsx`/`format-event-date.ts`'s actual current code rather than the backlog note's own description alone. **Amended same day, user-directed:** the first option was originally labeled and defined as "Happening now" (`started && !ended`, clock-time-strict); renamed to **"Today"** and redefined to cover anything scheduled today regardless of whether it's started yet, so a user planning their day sees a later-today event (e.g. an 8pm concert at 2pm) under this option instead of missing it. Reasoning: "Happening now," read strictly, answers "is this in progress this exact second" — useful, but not what someone planning their day is actually asking, which is closer to "what's on today."*

**Format: a 3-way mutually exclusive segmented control (`{components.temporal_filter}`), not a dropdown or a 4th FilterHub facet — confirming the backlog note's own reasoning.** `role="radiogroup"` of three `role="radio"` options, single-select with roving tabindex — deliberately **not** `EventDiscoveryPanel`'s own `role="tablist"`/`"tab"` view-switcher pattern (`EventDiscoveryPanel.tsx` lines 111–134): that control switches which content panel is displayed, while this one filters data within whichever panel is already showing — different APG roles for a reason, not a styling choice.

**Position: `EventDiscoveryPanel`'s own new row, between `SearchBar` and `FilterHub` — and the backlog note's own "implementation wrinkle" turns out not to be one.** The note worried that `activeView`/`currentViewId` (`EventDiscoveryPanel.tsx` lines 41–48) aren't exposed outside the component, but the toggle renders inside this same component's own JSX (a sibling of `SearchBar`/`FilterHub`, not something a parent renders) — gating it on `currentViewId === 'card'` is a plain local conditional, no state-lifting required. Full-width on mobile (its own row, same width as `SearchBar`), collapsing to its natural inline width as the leading control in the `FilterHub` facet row on desktop where width allows (matching the note's own suggested behavior).

**Data flow: a new top-level `EventDiscoveryPanel` prop pair (e.g. `temporalFilter`/`onTemporalFilterChange`), not folded into `FilterHub`'s own `onChange`.** `FilterHub`'s `onChange` signature is `(types: string[], categories: string[]) => void` (`FilterHub.tsx:30`) — adding a third parameter there would conflate a genuinely separate control with `FilterHub`'s own facets. Each consuming page (`home-content.tsx`, `feed-content.tsx`, `favorites-content.tsx`) owns the committed value via `useListPaginationController`, per AD-18's rule that every filter's committed value is owned by that controller rather than a hand-rolled `useState` cursor.

**Semantics, grounded in `formatEventStatus`'s existing `started`/`ended` computation (`format-event-date.ts:138–193`) — no new date logic, just a different combination of the same two booleans (amended 2026-09-17):**
- **Today**: `todayISO` falls within `[eventStartDate, eventEndDate ?? eventStartDate]` **AND** `!ended`. Deliberately does **not** require `started` — a single-day event later today (badge: `In N hour(s)`), one starting exactly at midnight, and a multi-day event already in progress (badge: `Happening Now`/`Ends Today`) all qualify identically; only an event that has fully finished (badge: `Ended`, or one that started and also ended earlier today) is excluded. This intentionally covers more badge states than the superseded "Happening now" definition did (which required `started`, so it missed later-today events entirely) — the filter's job here is "what's on today," not "what's in progress this instant."
- **Upcoming** (amended 2026-09-17, was `!started`): `eventStartDate > todayISO` — starts on a genuinely future day, not merely "not started yet." This is the change that keeps **Today** and **Upcoming** a clean, non-overlapping partition: an event starting later today now falls only under **Today**, never both. Still covers the badge's `Tomorrow`/a weekday name/`In N days`/`Upcoming` (14+ days out) states, just no longer `In N hour(s)` (today's own not-yet-started case, now exclusively **Today**'s). **Known, accepted label overlap unchanged from the original pass:** the badge separately uses "Upcoming" for only its own narrower 14+-day sub-state — low-stakes, the two serve different jobs.
- **All** (default): no additional temporal narrowing beyond the app's existing default-hide-past-events rule (Story 2.7, `buildDefaultEventVisibilityConditions`, grace window configurable via `mySettings.hidePastEventsAfterDays`, default 7 days) — preserves current behavior for existing users/links, per the backlog note's own requirement. An event that ended before today but is still inside that grace window is visible under "All" but under neither **Today** nor **Upcoming** — a real third bucket that doesn't need its own filter label, since "All" already covers it.

**The per-card status badge (`{components.event_card_status_badge}`) is explicitly unchanged by this amendment.** Its own 8-state model (`Ended`/`Happening Now`/`Ends Today`/`In N hour(s)`/`Tomorrow`/a weekday name/`In N days`/`Upcoming`) keeps its existing, more granular meanings on every card regardless of which filter bucket is active — the filter's **Today**/**Upcoming** labels are deliberately independent bucket names, not a promise that every card under a bucket shares that bucket's exact label.

**Card view only (backlog note's own rule, confirmed unchanged).** `WeeklyCalendarView` already expresses time structurally via its own day grid/day grouping — this toggle would be redundant there. It must not render, and must not apply, when `currentViewId === 'calendar'`.

**Flagged for `bmad-architecture`, not decided here.** This needs to be a real backend `WHERE`-clause condition — matching every other `FilterHub` facet's convention (location/type/category are all server-side filters, never client-side post-filtering of an already-paginated page) — via a new field on `EventFilterInput`, most likely a `started`/`ended` boundary check against `NOW()` similar in shape to Story 2.7's existing default-visibility past-event condition, not new date-math. Whether this needs its own DB index (mirroring the `hashtags`/`favorites` precedents project-context.md already calls out for other high-traffic `Query.events` filters) is also an architecture-pass question, not decided here. *(Originally resolved by Architecture Spine AD-20; the 2026-09-17 Today/Upcoming amendment above simplified AD-20's own mechanism — see AD-20's amendment in `festgrid-architecture-spine.md` for the current, smaller SQL surface.)*

### Calendar Overflow: Scalable Cap + Infinite-Scroll Popup

*Added via a targeted `bmad-ux` pass, 2026-09-17 — resolves FIND-026 (desktop's `max_events_per_day=5` reconsideration) and supersedes Component Patterns § Mobile Multi-Day Calendar Spanning's "No cap, no '+N more' popover on the mobile list" rule above. User-directed: "always renders a day's full event_stack will cause issue if our apps scales and has so many users/event. So we need to apply the caps for scalability" — the superseded rule's reasoning only ever weighed per-row layout, never data/DOM scale, and does not survive a popular day accumulating hundreds of schedules.*

**Both surfaces now cap, and both now open into the same infinite-scroll overflow surface.** Desktop's existing `{components.calendar.event_rendering.more_link}` ("+N more") disclosure and mobile's day-row both apply an inline cap; exceeding it opens **one shared, responsive dialog/sheet component** (full-screen sheet on mobile, a larger centered dialog on desktop — not two independently-built surfaces) showing that day's remaining schedules with real infinite scroll, replacing desktop's current small, non-paginated `max-h-56 overflow-y-auto` popover entirely — that popover renders its entire (already client-side-loaded) bucket at once today, which does not scale either.

**Multi-day segments are exempt from the cap, mirroring a rule desktop already has.** Desktop's multi-day schedules never go through `day_cell`'s per-day bucket-and-cap logic at all today — they render as a single spanning bar directly in `{components.calendar.grid_weekly}` (Component Patterns § Calendar View Cards: Attachment and Composition), bypassing the cap by construction. This pass extends the identical principle to the mobile list explicitly: **the cap counts only single-day and isolated day-of-week occurrences** (Component Patterns § Day-of-Week Recurring Schedules) — a multi-day segment (or a collapsed day-of-week run, per that same section) always renders inline regardless of count, on both surfaces. This is what preserves the superseded rule's original continuity guarantee (a multi-day segment must never vanish behind "+N more" on one of its spanned days) without needing mobile to stay fully uncapped to get it. The exemption applies at the data layer too, not just render-time: a multi-day segment touching a given day must never be dropped by whatever page-size limit bounds that day's *single-day* schedule fetch — dropping one at the fetch layer would reintroduce the exact discontinuity this rule exists to prevent, just one layer lower. The concrete fetch shape for this (a separate always-included multi-day set vs. a paginated single-day set) is a `bmad-architecture` question, not decided here.

**The inline cap is sized from real dimensions, not a fixed magic number — on both surfaces.** User-directed: estimate how many cards fit from the designed card's actual rendered size against the available space, rather than picking an arbitrary constant. Desktop has a genuine fixed reference to measure against (`{components.calendar.day_cell}`'s `h-32`); mobile's per-day row has no fixed height of its own (it grows to fit content), so its available-space reference is necessarily a different, viewport-relative heuristic — the exact reference and measurement technique (most likely `ResizeObserver` comparing the real card's rendered height against the available space, since a pure-CSS overflow clip cannot produce the exact hidden count "+N more" needs to display) is left to the implementation story, not fixed here. **Practical fallback, user-confirmed:** if precise measurement proves too costly to build now, over-fetch a generous flat bound (20 single-day/isolated occurrences per day) from the backend regardless of surface, and let the frontend decide the actual inline-vs-overflow split from that bound — one mechanism (over-fetch, then measure-and-cut client-side) for both surfaces either way, only the "available space" reference differs per breakpoint.

**Flagged for `bmad-architecture`, not decided here:** the actual data-fetching contract for per-day pagination. Today `WeeklyCalendarView` receives one already-fully-loaded batch of schedules for the entire visible week and buckets them client-side — there is no existing mechanism to fetch "the next page of single-day schedules for date X" at all. This pass states the UX/behavioral contract (capped inline count sized from real dimensions; multi-day-exempt; "+N more" opens a shared infinite-scroll dialog/sheet; a 20-per-day flat bound as an acceptable fallback shape) — the GraphQL query shape, cursor design, and whether the always-included multi-day set and the paginated single-day set are one query or two is an architecture decision, not a UX one.

### PWA Install Prompt

*Added via a targeted `bmad-ux` pass, 2026-09-17 — IDEA-020's UX half, explicitly excluded from Architecture Spine AD-21 (which only decided `embed.js` caching + `preconnect` hints + a locale-scoped dedicated service worker). No `manifest.json` exists today, so the app is not installable as a PWA at all yet.*

**Not part of onboarding — web-verified reason.** Chrome will not even fire `beforeinstallprompt` until the user has clicked/tapped the page at least once *and* spent 30+ seconds on it (across any visit), over HTTPS, with a service worker exposing a `fetch` handler already registered (AD-21's caching worker satisfies this precondition) — [web.dev, "What does it take to be installable?"](https://web.dev/articles/install-criteria); [Chrome for Developers, "Revisiting Chrome's installability criteria"](https://developer.chrome.com/blog/update-install-criteria). The existing `OnboardingNotificationStep` pattern (a wizard step with a toggle, asking the user to grant a capability) does not transfer here — the browser's own engagement gate would not be satisfied that early, independent of general "don't ask before value is shown" UX guidance pointing the same direction. This is main-app-shell scope (`design-artifacts/UX-festgrid-run-1`), not the separate wizard-page UX workspace.

**Placement: a persistent, dismissible banner at the top of `<main>`, below the global nav, across every route** (`packages/ui/src/core/app-shell/AppShell.tsx`) — shown once the platform's own eligibility signal is available (the captured `beforeinstallprompt` event on Android/Chrome; a comparable client-tracked engagement heuristic on iOS, mirroring Chrome's spirit since no equivalent browser-provided signal exists there — exact threshold left to the implementation story) and not already dismissed/cooling down.

**Two distinct dismiss actions, user-directed:** "Not now" commits permanent suppression (never shown again on that browser); a second, separate action — "Remind me in 2 weeks" — starts a 2-week cooldown before the banner can resurface. Both are real, visually distinct buttons on the banner, not one dismiss control with an implied meaning.

**Dismiss/cooldown state lives in browser `localStorage`, not a backend `mySettings` field.** Installability is inherently per-device — a user could reasonably want the banner suppressed on a phone they've already installed to but still see it on a laptop they haven't. Syncing dismissal account-wide via a backend setting (the pattern `pushNotificationsEnabled` and similar settings already use) would silently suppress the prompt cross-device for a capability that is itself never cross-device.

**Settings fallback, independent of banner state:** a permanent "Install App" action added to the existing Notifications tab (`/settings/account?tab=Notifications`) — the same class of concern as the push-notification toggle already there (a device/browser capability action), not a new tab. Available regardless of whether the banner was ever shown, dismissed, or is cooling down.

**Two different primary actions, by platform:**
- **Android/Chrome:** the banner's (and Settings action's) primary button calls `.prompt()` on the captured `beforeinstallprompt` event directly — the browser's own native install flow.
- **iOS Safari (no native install prompt exists at all):** the primary action opens a dedicated, richer step-by-step modal — numbered instructions with the Share icon, then "Add to Home Screen" — rather than cramming instructions into the banner itself. The banner's own placement, copy-independent mechanics (two dismiss actions, `localStorage` state, Settings fallback) are otherwise identical to the Android/Chrome path; only what the primary action opens differs.

## State Patterns

### Soft Delete with Undo

This pattern is used for any destructive action on a user-created item (e.g., deleting a saved location, an API key, or a subscription).

**Event-list exception:** unfavoriting an event from the Discover/Favorites list already follows an immediate-commit variant of this pattern today (the toggle call fires at Trigger, and "Undo" re-invokes the same toggle rather than a dedicated restore call) — that existing behavior is the precedent this pattern generalizes to every other surface, not a divergent case to reconcile.

*   **Initial State:** The item is visible in a list, in its normal (not-pending) appearance.
*   **Trigger:** User clicks a "Delete" button (or confirms one revealed via the Swipe-to-delete primitive). Focus is not force-moved anywhere by Trigger itself (see Accessibility Floor § Soft Delete with Undo below) — the triggering control either stays focusable in an updated (e.g., disabled/relabeled) state, or, if this pattern's consuming component removes it from the DOM as part of the pending-state change, focus deliberately moves to the item's own container rather than being left to fall through to the browser default.
*   **Intermediate State (Pending):**
    *   The delete commits to the backend immediately, at the moment of Trigger — a real backend delete, not a deferred/pending call. (Revised 2026-08-06 — see below.)
    *   In that same moment, without waiting for the call's response, the item optimistically switches to its "marked for deletion" appearance — greyed out and/or struck through; at least one non-color cue is required (WCAG 1.4.1) — and stays visible in the list, not removed from view yet.
    *   A toast notification appears with an "Undo" action (e.g., "Item deleted" / "Undo"), rendered per Accessibility Floor § Soft Delete with Undo (live-region announcement, timing, focus, and tab-order rules). The toast auto-dismisses after `{components.notification.undo_duration_ms}`.
*   **"Undo" Action:** If the user clicks "Undo" before the toast dismisses, a restore call reverses the already-committed delete (the same record is restored, not recreated). On success the item returns to its Initial State appearance and the toast dismisses.
*   **Final State (Commit confirmed — timeout path):** If the toast times out without "Undo" being clicked, the item is removed from the visible list by filtering it out of the client's already-loaded list data — no further network call is made, since the delete already committed at Trigger. It does not reappear on the next visit. No separate announcement is required for this removal beyond the toast's own dismissal (a deliberate decision, not an oversight).
*   **Failure path (Trigger-time delete call fails):** If the delete call itself fails (network/server error), the item reverts from its greyed-out-and/or-struck-through appearance back to Initial State, the Undo toast/timer is cancelled, and a distinct error toast is shown instead (e.g., "Couldn't delete — try again"), auto-dismissing after `{components.notification.error_duration_ms}` with a visible close control, and given its own accessibility treatment per Accessibility Floor § Soft Delete with Undo — it is not a lesser-specified variant of the undo toast. Nothing was committed, so there is nothing to undo.
*   **Concurrent deletes:** Each pending item gets its own independent toast (per `useSoftDeleteWithUndo`'s multi-id support). Stacked toasts' live-region announcements queue in trigger order rather than interrupting each other, and their Undo buttons are reachable via Tab in the same order the toasts appeared, so a user who fires several quick deletes can still identify and reach each Undo unambiguously.

**Why revised (2026-08-06):** the prior version of this pattern deferred the backend commit until the user navigated away from the page (component unmount). That silently broke if the user closed the browser tab/window instead of navigating within the app — unmount never fired, so the delete was never committed even though the toast and greyed-out item had already told the user it was. Immediate-commit-at-Trigger removes that failure mode entirely; the trade-off is that "Undo" now must reverse a completed delete rather than simply cancel a pending one, which is why every surface using this pattern needs a real restore/undo delete path on its backend mutation (tracked separately as an architecture item, not specified here).

### Default Location Pending Review

This pattern applies only when a user edits an *already-set* default location on a shared subscribed account — not the first-time-set case, which has no prior value to protect and needs no review.

*   **Initial State:** The account row shows its current default location value.
*   **Trigger:** User edits the value and saves.
*   **Immediate State:**
    *   The new value saves and displays right away — the save is never blocked waiting on a moderator.
    *   A confirmation message acknowledges the save and briefly notes that the change affects every subscriber to this account and has been sent for moderator review — informative, not alarming (Voice and Tone).
    *   The row shows a "Pending Review" badge until a moderator resolves it.
*   **Resolution (external):** A moderator accepts, reverts, or directly corrects the change from Moderator Tools (Epic 4) via `{components.account_location_field}` (Component Patterns § Account Location Field). On revert, the row's value reverts to the prior location and the badge clears; on accept, the badge simply clears; on a direct correction, the row's value updates to the moderator's input immediately and the badge clears with no further review needed — the moderator's edit *is* the review (added 2026-08-24, AD-11).
*   **Superseded (added 2026-08-24):** If a *later* edit — by any subscriber or a moderator — completes while this request is still pending, this request is marked superseded rather than staying in the queue: its badge and any action affordance disappear from view (it is not actionable and not shown as a separate history item in this pattern's scope). The account row simply reflects whatever the newer edit produced. This prevents a moderator from acting on a stale before/after snapshot that a subsequent edit has already overtaken.

## Responsive & Platform

Global Navigation is the app's original 3-tier responsive component (Tailwind default breakpoints, matching the existing app-shell breakpoint usage); the Calendar View's grid-to-vertical-list swap (Component Patterns § Mobile Multi-Day Calendar Spanning) is a second, simpler 2-tier component using the same `md:` breakpoint:

| Breakpoint | Layout | Label visibility |
|---|---|---|
| `< 768px` | Fixed bottom tab bar, all 5 items | Icon + label always visible |
| `768–1279px` | Left sidenav rail, icon-only | Label on hover/focus tooltip; tap-to-flash on touch |
| `≥ 1280px` | Same rail, permanently expanded | Icon + label always visible |

At the rail tiers (≥768px), the app logo is pinned to the top of the rail; Profile is pinned to the bottom (`mt-auto`); Discover/Feed/Favorites/Calendar sit between them in that order. There is no manual collapse/expand control — the tier is purely a function of viewport width. Only one layout variant is ever present in the accessibility tree at a time (see Accessibility Floor).

## Accessibility Floor

*Scoped to Global Navigation for this pass — the rest of the app's accessibility floor is documented per-component in Component Patterns and should be consolidated here in a future UX pass.*

- **Landmarks:** one persistent `<nav aria-label="Main">` across all breakpoints — CSS drives the layout change, not swapped markup. If mobile/desktop ever require structurally different markup, only one variant may be in the DOM/accessibility tree at a time (`hidden`/`inert` on the other), both sharing the identical `aria-label`, so a screen reader user resizing the viewport never sees two "navigation" regions.
- **Accessible names:** every nav item has an `aria-label` matching its visible label text, present at every breakpoint regardless of whether the label is visually shown.
- **Current page:** `aria-current="page"` on the active item, independent of the visual active-bar/icon-style cue.
- **Focus:** every item has a visible focus ring (`{components.nav.focus_ring}`) distinct in color from the active-bar indicator (`{colors.nav_active_indicator}`), so focus and active-page never read as the same signal.
- **Hover/focus parity:** the icon-only rail's label tooltip triggers on `:focus-visible` as well as `:hover`, persists while focused, and dismisses on `Escape` or focus-out (WCAG 1.4.13).
- **Hit area:** minimum `{components.nav.item_hit_area}` per item, independent of the icon's visual size.
- **Motion:** the touch tap-to-flash label and any rail/tab-bar transition respect `prefers-reduced-motion` (instant show/hide fallback, never a shorter animated fade).

### Soft Delete with Undo (Notification Toast)

*Scoped to the Soft Delete with Undo pattern (State Patterns above) — added 2026-08-06 alongside its immediate-commit revision, following an ad-hoc accessibility review (`review-accessibility-soft-delete-undo.md`).*

- **Live region:** the undo toast renders inside a `role="status" aria-live="polite" aria-atomic="true"` region, so screen reader users hear its content (e.g., "Item deleted. Undo.") as it appears without needing focus to be on the deleted item. The failure-path toast instead uses `role="alert"` / `aria-live="assertive"`, since an error interrupting an in-progress action warrants immediate announcement rather than a polite queue.
- **Timing (WCAG 2.2.1):** the toast's countdown pauses on mouse hover or keyboard focus (already stated in State Patterns), and additionally on assistive-technology virtual-cursor entry into the live region — an AT user "visiting" the toast via swipe navigation (mobile VoiceOver/TalkBack) counts as pausing it, the same as a sighted keyboard user tabbing to it. Regardless of input modality, the Undo button itself (see Hit area/tab order below) is always reachable and clickable/activatable for the toast's full open duration, giving touch-only and AT users a functional equivalent to "extend the timer" — they can always still act, even without a hover-pause gesture available to them.
- **Focus:** the toast does not steal focus when it appears (non-modal — matches APG guidance for status messages); see Trigger's focus-management rule in State Patterns above for what happens to the item's own triggering control.
- **Hit area / tab order:** the Undo action (and the failure toast's close control) is a real, tab-reachable `<button>` inside the live region, meeting `{components.notification.action_hit_area}`. It is reached by continuing to Tab forward from wherever focus currently is — not assumed to be the very next stop after the triggering item, since the toast is portal-rendered outside normal document flow.
- **Non-color cue:** the pending item's "marked for deletion" appearance always includes a non-color signal (greyed out and/or struck through — WCAG 1.4.1), never opacity/color alone.

### Masonry EventCard Badge Row

*Scoped to Component Patterns § Masonry EventCard: Date Box, TILL Badge, and Status/Nearby Badge Row above — added 2026-09-04 alongside that pattern. Extended 2026-09-11 for the `prominentPoster=false` default-state thumbnail and its fallback, and for the new Calendar Row Card pattern, which shares the same conventions. Extended again 2026-09-14 for the new Calendar Grid Item Card's favorite-control and reading-order conventions — but see the Reserved space bullet below, which only masonry still follows as of 2026-09-14 (the Calendar Row Card's own reserved-space convention was reversed the same pass) — a deliberate divergence, not an inconsistency to fix.*

- **Minimum badge text size (new 2026-09-14, user-directed general rule):** the TILL badge, favorite badge/count, radius/nearby badge, and status ("Now") badge never render below **11px** text, across all three card families and every real card width validated this pass. Several of this pass's own narrower real-width panels (masonry's 175px mobile card, the calendar-row's 326px narrow-mobile row) had dropped to 9-10px before this rule was applied — all corrected. `{components.event_card_till_badge.base}` and `{components.event_card_status_badge.base}`/`{components.event_card_nearby_badge.base}` reflect the floor directly in their token classes (`text-[11px]`/`text-xs` at 12px); the same floor applies to `{components.event_card_favorite_count_badge_large}`'s count text at every width it's rendered.
- **Favorite badge font-size >= TILL badge font-size (new 2026-09-14, round 6, user-directed general rule).** Verified at every real width across all three card families: wherever both badges appear on the same card, the favorite badge's text is never smaller than the TILL badge's. The one violation this rule caught (masonry's mobile favorite count at 11px against TILL's round-7 bump to 12px) was fixed by raising the favorite badge, never by lowering the TILL badge.
- **TILL and favorite badge font/padding harmonized across all three masonry states (new 2026-09-14, round 8, user-directed: "the till badge has font and padding size uniform across different variant" / "the favorite badge has font and padding size uniform on prominent-card & card-with-small-thumbnail").** Previously the prominent-poster state pinned its TILL and favorite badges at a fixed `text-[11px]` regardless of viewport, while the default-with-thumbnail/fallback states already followed a responsive `text-xs` (12px) mobile / `text-sm` (14px) desktop pair (Component Patterns § Masonry EventCard, "all non-prominent card" rule). That exception is now removed: all three masonry states use the identical 12px-mobile/14px-desktop pair for both badges, and the favorite badge's padding (`px-1.5 py-1`) is now identical between the prominent poster's own overlay (`{components.event_card_date_box.favorite_pill}`) and the default-with-thumbnail state's corner pill (`{components.event_card_masonry.thumbnail_default.favorite_badge}`) — previously the desktop instance of the latter used a looser `px-2.5 py-1.5`.
- **Non-color cue (WCAG 1.4.1):** the status badge (`{components.event_card_status_badge}`) and nearby badge (`{components.event_card_nearby_badge}`) are never distinguished by color alone — each has unique, self-describing text, and the nearby badge additionally carries its own icon. A viewer with color-vision deficiency or a grayscale display can still tell them apart. This holds for the Calendar Grid Item Card's badges too, unchanged — only their position (side stack or `location_row`, not inline under the title) differs from their siblings.
- **Reading order:** the date box, TILL badge, status badge, and nearby badge are all supplementary metadata about an already-labeled event card (the card's own accessible name comes from `eventName`/`imageAlt`, unchanged by this pass) — none of them are interactive on their own, so they need no independent focus stop; they follow normal DOM order (date box, then its corner-anchored TILL tag, then the badge row) inside the card's single existing focusable/clickable region. This holds for both `prominentPoster` states and for the new Calendar Row Card — only the date box's own position (overlay vs. in-flow) changes between them, not its place in reading order. The Calendar Grid Item Card has no date box to place in this order (Component Patterns § Calendar Grid Item Card above) — its with-image composition follows normal DOM order (thumbnail, then title/venue, then side-stack), and its no-image composition likewise (title+favorite row, then venue+nearby-badge row), both within the same single focusable region.
- **Favorite control stays a real, reachable control regardless of shape (new 2026-09-11, extended 2026-09-14 to the grid item card):** `{components.event_card_favorite_count_badge_large}` — whether it's replacing the small corner favorite pill on a missing-image fallback (masonry default-state, Calendar Row Card) or serving as the Calendar Grid Item Card's *only* favorite shape in both its compositions (Component Patterns § Calendar Grid Item Card — not fallback-triggered there, just how that card always renders it) — is still the same live favorite-toggle button in every case, not a decorative label. It keeps a real `min-h-11 min-w-11` tap target (`{components.nav.item_hit_area}`'s existing convention, reused here) even though visually it now reads as bare icon+text rather than a pill-bounded button.
- **Reserved space, not reflow (WCAG 2.4.3 predictability, new 2026-09-11) — masonry ONLY as of 2026-09-14 (the Calendar Row Card's own reserved-space convention was reversed the same pass, and the Calendar Grid Item Card never had one).** In the masonry default state, a missing/expired image never changes the size or position of anything else in the card — the thumbnail's slot dimensions come from the date box's own height, never from the image itself, so a user tabbing or reading through a grid of cards doesn't have card boundaries shifting under them as images individually succeed or fail to load. **Neither the Calendar Row Card nor the Calendar Grid Item Card follows this rule**, per direct user direction: both omit the image element from the DOM entirely (not a blank reserved slot) whenever it doesn't apply, and both reflow their remaining content to fill the freed space — the row card expands its content column and drops the favorite control's fixed-width wrapper; the grid-item card switches to a structurally different two-row composition (Component Patterns § Calendar Grid Item Card). This is a real, acknowledged tradeoff against WCAG 2.4.3's general reflow-predictability intent on both cards, not an oversight: for the Calendar Row Card, "no area for image" is an explicit product decision independent of error timing — the image was never going to occupy that space regardless of when/whether it fails. For the Calendar Grid Item Card, its image is absent for a reason knowable at render time in the common case (the event isn't multi-day) — only the async-error sub-case (a multi-day event's image failing after paint) still carries the same late-shift risk masonry's reserved-space rule exists to prevent, and is accepted here as a known, narrower exception rather than solved.
