# generated: 2026-07-29T02:58:30Z
# last_updated: 2026-08-29T18:00:00Z
# project: festgrid
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-29T02:58:30Z
last_updated: 2026-09-03T00:00:00Z
project: festgrid
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-0: in-progress
  0-1-initialize-pnpm-monorepo: done
  0-2-configure-typescript-and-eslint-for-the-monorepo: done
  0-3-set-up-shadcn-ui-and-configure-themes: done
  0-4-set-up-drizzle-orm-for-schema-migrations: review
  0-5-set-up-ci-cd-pipeline-with-github-actions: review
  0-6-set-up-i18n-foundation-next-intl: review
  # reset from review to ready-for-dev 2026-08-05 — revised AC/Tasks against a new
  # UX spec supersede the original implementation; see the story file's Revision note.
  0-7-build-the-global-app-shell-and-navigation-layout: review
  # added 2026-08-05 via bmad-create-story Gate 2 finding while revising 0.7's nav
  # pattern against the new UX spec (design-artifacts/UX-festgrid-run-1) — see
  # 0.7's Architecture & UX Gate Findings for detail.
  0-7a-nav-item-primitive: review
  0-8-set-up-graphql-server-scaffold-code-generator-pipeline-and-the-optimized-select-query-utility: review
  0-9-set-up-state-management-foundation: review
  0-10-set-up-testing-frameworks-foundation: review
  0-11-set-up-runtime-schema-validation: review
  0-12-set-up-firebase-cloud-messaging-foundation: review
  # Reopened 2026-08-24 via bmad-correct-course: selectApiKey gains a new final-
  # fallback tier (platform-funded system key, env-var-stored) for Story 3.4m's
  # AI-assisted default-location inference only -- not a general Phase 2 managed-
  # key pool. See sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.1.
  0-13-set-up-ai-gateway-adapter-layer-for-gemini: review
  0-14-set-up-aws-iac-for-lambda-sqs-eventbridge-and-kms: review
  0-15-set-up-outbound-email-adapter: done
  # added 2026-08-08 via bmad-create-story: user-requested dev-experience addendum
  # to already-done Story 0.15 (not a Gate 1/2/3 finding) — single-story split per
  # story-split-gate.md's numbering rule. See epics.md Story 0.15a.
  0-15a-local-dev-email-adapter-stub: review
  # regenerated 2026-08-03 via bmad-create-story against Geoapify-based epics.md text
  # (Sprint Change Proposal 2026-08-03); Postgres-backed no-TTL cache and the API-key
  # restriction MVP limitation were confirmed with the user during story creation.
  0-16-set-up-geolocation-adapter-with-caching-layer: review
  0-17-set-up-graphql-authenticated-context-layer: review
  # Reopened 2026-08-06 via bmad-correct-course: AC2-4/hook contract revised
  # (commit-on-unmount -> commit-at-trigger/undo-reverses/onExpire-on-timeout).
  # See sprint-change-proposal-2026-08-06.md.
  0-18-build-the-reusable-soft-delete-with-undo-ui-primitive: review
  0-19-build-the-reusable-swipe-to-reveal-action-ui-primitive: review
  # SUPERSEDED 2026-08-04 via bmad-create-story: fully absorbed by Story 0.16's
  # regenerated Task 3 (Geoapify provider swap, Sprint Change Proposal 2026-08-03),
  # which now creates this exact geolocation_cache table. Not built independently;
  # tracked to completion via 0-16 instead. See epics.md Story 0.20 note.
  0-20-create-geolocation-cache-database-table: superseded-by-0.16
  0-21-set-up-fcm-device-token-registry: review
  # New 2026-08-06 via bmad-correct-course: AD-8 rule 2 ("enforced once, never
  # per-resolver") reviewer-gate finding -- 6 hand-written isNull() call sites,
  # no shared enforcement point. See sprint-change-proposal-2026-08-06.md.
  0-22-build-the-shared-active-rows-query-filter-helper-for-ad-8: review
  # added 2026-08-07 via bmad-create-story Gate 1/Gate 3 finding while drafting
  # Story 2.10: Story 0.15 (email adapter) is unimplemented and has no generic
  # system/developer-error template, and the reportSystemError mechanism itself
  # is cross-cutting (future stories will also want to alert on client failures).
  # See epics.md Story 0.23 note.
  0-23-build-the-system-error-reporting-and-alerting-foundation: review
  # added 2026-08-07 via bmad-create-story Gate 3 (lightweight guard) finding while
  # drafting Story 3.1: design-artifacts/UX-wizard-page-run-1 specs a generic /wizard
  # page (EXPERIENCE.md status: final) with a confirmed second, cross-epic consumer
  # (Story 5.5), but no story owns building it; the epic-3-readiness.md sweep didn't
  # evaluate this UI-shaped gap. Registry design revised from URL-query-param steps
  # to a code-defined wizard registry per user decision, for i18n/generateMetadata
  # compatibility. See epics.md Story 0.24 note.
  # Reopened 2026-08-24 via bmad-create-story: added AC12 -- wizard chrome
  # (WizardNavigation bottom bar) visually collides with AppShell's fixed
  # sidenav rail (ux-rework-2026-08-24.md item #9). Static review found no
  # obvious defect (<main>'s md:ps-16/xl:ps-56 should already offset correctly;
  # neither WizardNavigation nor WizardStepSummary sets fixed/sticky/z-index)
  # -- this story's own original Dev Notes admit the wizard chrome was never
  # tested composed inside the app shell at any breakpoint. AC1-AC11 unaffected.
  # UPDATE 2026-08-24 (same day): live-verified via Playwright against the real
  # app at 500/768/1024/1440px -- NOT reproduced at any width, no collision
  # with the sidenav rail or mobile bottom tab bar. Not blocked, but needs more
  # specific repro info (authenticated state? which step? mid-transition?)
  # before a fix can be written against a bug that doesn't currently reproduce.
  # See sprint-change-proposal-2026-08-24-ux-rework-batch.md.
  0-24-build-the-reusable-wizard-page-primitive: ready-for-dev
  # added 2026-08-07 via bmad-create-story Gate 1 finding while drafting Story 0.23:
  # no backend env var (not just 0.23's new SYSTEM_ERROR_ALERT_EMAIL) is wired into a
  # deployed Lambda's environment config, since Story 0.14's L_API Lambda resource
  # doesn't exist in IaC yet. User confirmed via AskUserQuestion to split this off
  # rather than accept it as an implicit Story 0.14 forward dependency. See epics.md
  # Story 0.25 note and Story 0.23's Architecture & UX Gate Findings.
  0-25-wire-backend-environment-variables-into-the-deployed-api-lambda-s-iac-configuration: review
  # added 2026-08-07 via bmad-correct-course: every route-page's Suspense boundary
  # (fallback-less since Story 1.9) needs a shared, on-brand loading fallback instead
  # of a blank flash, including the event-detail modal's bounded dialog content.
  # See sprint-change-proposal-2026-08-07.md.
  0-26-build-the-reusable-routeloader-component: review
  # added 2026-08-11 via bmad-create-story Gate 1 finding while drafting Story 3.10:
  # a new scheduled Lambda needs a real SES send grant, but no story provisions one --
  # Story 0.14 predates this need and Story 0.25's SES reconciliation is scoped only
  # to L_API. User confirmed via AskUserQuestion to split this off narrowly rather
  # than fold it into Story 3.10. See epics.md Story 0.27 note.
  0-27-provision-the-notifier-lambda-s-infrastructure-and-ses-send-permission: review
  # added 2026-08-15 via bmad-create-story Gate 3 finding while reopening Story 1.3g
  # for AC13 (sprint-change-proposal-2026-08-13-discovery-detail-calendar-ux.md
  # Section 4.4, AD-9): packages/ui has no shadcn/Radix setup at all, but AD-9
  # mandates packages/ui/src/core/WeekPicker.tsx wrap shadcn Popover+Calendar --
  # packages/ui cannot depend on apps/web's own shadcn output. Same gap blocks
  # Story 1.5's FilterHub popover too. User confirmed via AskUserQuestion to split
  # into a new Epic 0 story rather than absorb into either 1.3g or 1.5. See
  # epics.md Story 0.28 note.
  0-28-set-up-shadcn-ui-component-generation-for-packages-ui: review
  # added 2026-08-24 via bmad-correct-course: user wants API Keys/Subscribed
  # Accounts/Posts/Notifications collapsed into one tabbed page (like Story 0.24's
  # wizard) and moderator's actor-runs/unprocessed-payloads into a second tabbed
  # shell. Free-navigation tabs (no isStepCompleted gating, unlike the wizard) --
  # a new primitive, not a reuse of wizardRegistry. See sprint-change-proposal-
  # 2026-08-24-ux-rework-batch.md Section 4.2.
  # Story drafted 2026-08-25 via bmad-create-story: TabbedShell is scoped as a
  # props-driven packages/ui primitive only (no apps/web registry, unlike
  # Story 0.24's wizardRegistry -- its two consumers are fixed, not dynamic).
  # Gate 1/2/3 run fresh; Gate 3's initial NuqsAdapter concern re-verified
  # resolved (already wired in apps/web/src/app/[locale]/layout.tsx). Found
  # and flagged (not fixed here) a real doc-drift gap: EXPERIENCE.md/DESIGN.md
  # were never actually rewritten for the two-shell IA despite the 2026-08-24
  # correct-course proposal's approved log claiming it was done -- see the
  # story's Dev Notes. See 0-29-build-the-reusable-tabbedshell-primitive.md.
  0-29-build-the-reusable-tabbedshell-primitive: review
  # added 2026-08-24 via bmad-correct-course: the same p-4 sm:p-8 space-y-8
  # max-w-7xl mx-auto container className found copy-pasted across 7 pages
  # (home, favorites, feed, my-calendar, archive, account-content, posts-
  # select) -- expanding ux-rework-2026-08-24.md item #1 from a one-file fix
  # into a shared primitive. Full-width + responsive min-w floor per
  # project-context.md's new "Grid/Calendar Page Containers" rule. Blocks
  # Stories 1.3/1.3d/2.2/2.6/3.7/3.11/4.8/5.1's own PageContainer-adoption
  # ACs. See sprint-change-proposal-2026-08-24-ux-rework-batch.md.
  # DONE 2026-08-24: implemented, merged (c44ba15). Verification note: the
  # dev-agent report claimed tests/lint/build/code-review all passed in the
  # isolated worktree, but the worktree's node_modules symlink was actually
  # broken (module-not-found on vitest.setup.ts) -- files were also never
  # committed to the branch. Re-verified independently after a clean
  # `pnpm install`: 3/3 tests pass, lint clean, matches spec. Code itself was
  # correct; the reported verification process was not accurate as described.
  # Reopened 2026-08-24 (later same day) via bmad-correct-course: added AC7 --
  # fullWidth?: boolean prop (default true, unchanged for existing 8
  # consumers). fullWidth=false: "w-full max-w-5xl mx-auto lg:min-w-[768px]"
  # for settings/table pages (never embedded, don't need the 5-step
  # narrow-host-defense schedule), replacing their inconsistent max-w-3xl/
  # max-w-4xl split. User confirmed a real max-width cap is wanted here
  # (reversing the earlier "no cap" framing) -- max-w-5xl chosen as the common
  # value, numerically aligned with the lg breakpoint where the floor kicks in.
  # added 2026-08-24 via bmad-correct-course: new Story 0.32 PageHeader
  # primitive -- h1+optional-action row, found duplicated across 18 files
  # (drifting heading styles, inconsistent mobile-label hiding, no flex-wrap
  # safety on some). See 0-32 below.
  # DONE 2026-08-24: AC7 merged (be20e31 range, story/0-30-fullwidth-prop,
  # 1db6b2c). Independently re-verified: 5/5 tests pass, lint clean.
  0-30-build-the-reusable-pagecontainer-primitive: done
  # added 2026-08-24 via bmad-correct-course, same day as 0-30: user asked for
  # a reusable grid-column-count component (baseCols/colsStep props, e.g.
  # baseCols=1 colsStep=1 for the standard grid, baseCols=2 colsStep=1 for the
  # not-yet-built masonry mode) instead of Story 1.3d/5.1 each hand-widening
  # their own literal grid-cols-* className string. Uses a per-breakpoint
  # literal-string lookup table internally (Tailwind can't see a dynamically
  # interpolated class name). Supersedes the literal-string versions of Story
  # 1.3d's AC14 and Story 5.1's AC12 committed in fe8a1af -- no code existed
  # against those yet, documentation correction only. Blocks both stories'
  # revised ACs. See sprint-change-proposal-2026-08-24-ux-rework-batch.md.
  # DONE 2026-08-24: implemented, merged (10274c3). Verification note: the
  # worktree had NO node_modules installed at all -- the reported "7 unit
  # tests passed" could not have run there as described. Re-verified
  # independently after `pnpm install`: 7/7 tests pass, lint clean, matches
  # spec exactly (lookup-table approach, no runtime string interpolation).
  # Code itself was correct; the reported verification process was not.
  0-31-build-the-reusable-gridcontainer-primitive: done
  # added 2026-08-24 via bmad-correct-course: PageHeader primitive (title +
  # optional { label, icon, onClick, disabled? } action, flex-wrap row,
  # standardized text-3xl font-bold h1, action label hidden below sm:).
  # Blocks Stories 3.1b/2.3/2.9/3.2/6.5's own adoption ACs. Adoption scoped to
  # the 6 settings pages this pass (queue-status excluded -- see 3-9a note,
  # dissolved per Story 3.12, not worth adopting into a page being removed);
  # the other ~12 pages using the old pattern are a cataloged follow-up, not
  # dropped. See project-context.md "Page Headers".
  # DONE 2026-08-24: merged (story/0-32-page-header, be20e31). Independently
  # re-verified: 4/4 tests pass, lint clean.
  0-32-build-the-reusable-pageheader-primitive: done
  # New 2026-08-25 via bmad-correct-course: first object-storage/CDN resource
  # in the project. CDK construct for a private S3 bucket (Origin Access
  # Control, no public bucket access) fronted by a CloudFront distribution
  # (Cache-Control: immutable on served objects), plus IAM permissions for
  # the AI-extraction Lambda to write to it. Feeds Stories 3.6's re-hosting
  # amendment and the Event resolver's original-vs-durable serving logic.
  # See sprint-change-proposal-2026-08-25-video-priority-display.md
  # (Architecture Spine AD-12).
  # DONE 2026-08-26: merged to master (5b049f7). CDK-construct-only (no
  # apps/backend code touched). Independently re-verified after merge:
  # `pnpm exec tsx --test lib/**/*.test.ts` in apps/infrastructure -- 1/1
  # pass, directly on master (not just the worktree). `cdk synth` succeeded
  # for all 3 stages; inspected the synthesized dev template directly --
  # bucket policy grants s3:GetObject only to the CloudFront distribution
  # (OAC SourceArn-scoped), only aiProcessorLambda's IAM policy references
  # s3:PutObject, PublicAccessBlockConfiguration fully blocked. No cdk
  # deploy performed (explicit task boundary). Two gemini-cli delegation
  # attempts stalled/hung (wrong model, then a missing-ripgrep-driven slow
  # fallback, then a hang mid-verification with only 3 import lines written)
  # before implementation was completed directly; see story file's Dev
  # Agent Record for full detail.
  0-33-provision-s3-cloudfront-infrastructure-for-post-media: review
  epic-0-retrospective: optional
  epic-1: in-progress
  1-1-create-initial-database-tables: done
  1-2-seed-database-with-mock-data: done
  1-2a-create-posts-table-and-link-seeded-events-to-their-source-post: done
  1-3a-build-the-events-backend-graphql-api-layer: done
  # Reopened 2026-08-24 via bmad-correct-course: new `variant="masonry"` prop
  # (2-col Pinterest-style layout, native poster aspect ratio), relative-day badge,
  # and favoriteCount badge. See sprint-change-proposal-2026-08-24-ux-rework-
  # batch.md Sections 4.4/4.5/4.9.
  # Full AC11-13 backfilled 2026-08-25 via bmad-create-story (this reopening note
  # existed but no real ACs/tasks were ever drafted for it -- same tracking gap
  # found across this batch). AC1-10 confirmed already implemented, unaffected.
  # depends on Story 2.1a's new favoriteCount GraphQL field for real data.
  # AC11-13 implemented 2026-08-26 via cline: 288/288 packages/ui tests pass.
  1-3b-build-the-reusable-eventcard-component: review
  1-3c-build-the-reusable-infinite-scroll-hook: done
  # added 2026-08-05 via bmad-create-story at user's request: retroactive extraction —
  # home-content.tsx (this story's sibling 1.3) and favorites-content.tsx (Story 2.2) were
  # found to duplicate an identical loading/error/empty/grid/infinite-scroll shell below
  # their SearchBar/FilterHub row. Positioned as a 1.3-family lettered suffix per user
  # decision (see story Dev Notes → Retroactive Extraction Rationale).
  # Reopened 2026-08-24 via bmad-correct-course: new view-mode switcher (1-col /
  # masonry), following the same pattern as Story 1.3f's card/calendar switcher.
  # See sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.4.
  # Reopened 2026-08-24 via bmad-correct-course: added AC14 -- default grid
  # className widened from lg:grid-cols-3-capped to
  # md:2/lg:3/xl:4/2xl:5 (ux-rework-2026-08-24.md item #1, expanded scope
  # after a wider sweep found the same max-w-7xl container copy-pasted across
  # 7 pages). Masonry mode's own progression (2/3/4/5/6) locked in
  # project-context.md for whenever that AC is built, not built here.
  # See sprint-change-proposal-2026-08-24-ux-rework-batch.md.
  # UPDATE 2026-08-24 (same day): AC14 revised to compose the new
  # GridContainer primitive (Story 0.31, baseCols=1 colsStep=1) instead of a
  # hand-written literal className. Blocked on Story 0.31.
  # DONE 2026-08-24: AC14 merged (Wave 2, story/wave2-page-grid-adoption).
  # Reopened 2026-08-25 via bmad-create-story: AC15 masonry viewMode switcher
  # backfilled with real AC/task (only a forward-note existed). Depends on
  # Story 1.3b's variant="masonry" prop. Does not build the toggle-button UI
  # itself -- that's the apps/web call site's job. See sprint-change-proposal-
  # 2026-08-24-ux-rework-batch.md Section 4.4.
  1-3d-build-the-reusable-eventlistview-component: review
  # added 2026-08-05 via bmad-create-story at user's request: retroactive extraction —
  # home-content.tsx and favorites-content.tsx still duplicate the SearchBar/FilterHub
  # row above the shell Story 1.3d already extracted. Positioned as a 1.3-family
  # lettered suffix per the 1.3d precedent; see epics.md Story 1.3e note.
  1-3e-build-the-reusable-eventdiscoverypanel-component: review
  # added 2026-08-05 via bmad-create-story Gate 2 finding while drafting Story 1.3f —
  # DESIGN.md's calendar tokens already encode a shared discovery_view/personal_view
  # component split, and Story 2.6 is a confirmed second consumer. See epics.md
  # Story 1.3g note.
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.4: added AC13 (manual week-picker,
  # AD-9). A prior partial bmad-quick-dev attempt (519f822) added a functional but
  # AD-9-non-compliant native-input stopgap -- Tasks 13-16 replace it with the
  # sanctioned WeekPicker.tsx (Button+Popover+Calendar) and fix a divergent
  # Sunday-start boundary calc flagged during Story 3.7a's own reopening. Task 13/14
  # are BLOCKED on new prerequisite Story 0.28 (packages/ui shadcn/Radix setup, a
  # Gate 3 finding also needed by Story 1.5 -- not absorbed into this story per
  # AskUserQuestion). Depends on updated to add Story 3.7a, Story 0.28.
  # Reopened 2026-08-24 via bmad-correct-course: favoriteCount rendered on calendar
  # items (confirmed OK to grow to two lines); mobile breakpoint gets a vertical
  # day list skipping empty days -- multi-day-span rendering still undecided,
  # deferred to a scoped bmad-ux pass. See sprint-change-proposal-2026-08-24-ux-
  # rework-batch.md Sections 4.5/4.8.
  # Full AC14 (favoriteCount, desktop grid) backfilled 2026-08-25 via
  # bmad-create-story -- only a reopening comment existed before, no real AC.
  # Also found AC13 (WeekPicker) was already implemented and unblocked, despite
  # this file's stale "blocked on Story 0.28" header. Depends on Story 2.1a's
  # favoriteCount GraphQL field.
  # Reopened 2026-08-26 via bmad-create-story for AC15 (mobile vertical
  # day-list + multi-day-span badge, ux-rework-batch Section 4.8 item #11) --
  # design already resolved by an earlier bmad-ux pass, just never turned into
  # story scope until now. See 1-3g-build-the-reusable-weeklycalendarview-
  # component.md.
  1-3g-build-the-reusable-weeklycalendarview-component: review
  # added 2026-08-05 via bmad-create-story Gate 1 finding while drafting Story 1.3f —
  # the events resolver's main-schedule-only join can't correctly date-range-filter
  # sub-schedules; needs its own EXISTS-subquery DSL field + GiST index migration.
  # See epics.md Story 1.3h note.
  1-3h-extend-the-events-graphql-api-with-schedule-level-date-range-query-support: review
  # added 2026-08-05 via bmad-create-story: Gate 3-shaped gap surfaced while drafting
  # Story 1.3e — Story 1.3d's Out-of-Scope note incorrectly assumed "Calendar View is
  # Epic 2 Story 2.6"; 2.6 is actually the separate /my-calendar personal page. The
  # Discovery-page Card/Calendar view-switcher named in 01-event-list-view.md has no
  # owning story. See epics.md Story 1.3f note. Narrowed to integration-only scope
  # 2026-08-05 after Gate 2 (-> 1.3g) and Gate 1 (-> 1.3h) findings split off the
  # primitive and backend query capability respectively.
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.5: added AC8 (week-picker pass-through).
  # Verified via direct code inspection that Tasks 1-9 are already implemented despite
  # this story file's own stale Completion Status/Dev Agent Record (never filled in by
  # whatever process actually built it) -- onSelectWeek/label pass-through is already
  # shipped in CalendarView.tsx; only a new getWeekRange prop is outstanding (Task 11,
  # blocked on Story 1.3g's Task 14-15).
  1-3f-build-the-discovery-weekly-calendar-view-and-view-switcher: review
  # added 2026-08-27 via bmad-create-story at user's request, surfaced during a
  # bmad-help session: Stories 1.3b/1.3d both already ship masonry variant/viewMode
  # support but each deliberately declined to build the toggle-button UI itself --
  # confirmed via grep that all 5 apps/web call sites omit `viewMode`, making masonry
  # mode unreachable. New standalone `ViewModeToggle` component + per-page `layout`
  # nuqs param. Gate 2 (Freya lens) resolved visual treatment (icon-pair, not a
  # tab-strip) and component boundary (standalone, not baked into EventDiscoveryPanel
  # -- archive-content.tsx has no EventDiscoveryPanel, tiebreaker). Persistence
  # (per-page URL state, not global) and default ('list') confirmed with user via
  # AskUserQuestion. See epics.md Story 1.3i.
  # Implemented 2026-08-28 via cline (vertex:gemini-3.5-flash) in an isolated worktree.
  # Independently verified by Claude before merge: found and reverted an out-of-scope
  # regression to FilterHub.tsx's aria-label (unrelated to this story, from a separate
  # already-merged fix); diagnosed an apparent nearby.test.tsx failure as the worktree
  # being branched from a stale master predating that same fix, not a real regression
  # (confirmed via merge-base ancestry + swap testing). Merged onto current master
  # (4448d61). Post-merge: packages/ui 309/309 tests pass, all 5 touched pages' tests
  # + nearby.test.tsx pass (29/29), web lint clean (only pre-existing warnings).
  1-3i-wire-the-list-masonry-view-mode-toggle-into-apps-web: done
  # Reopened 2026-08-24 via bmad-create-story: added AC9/Task 7 -- root
  # container drops its max-w-7xl cap in favor of full width
  # (ux-rework-2026-08-24.md item #1, missed by the original bmad-correct-
  # course triage pass). AC1-AC8 unaffected. Status reset to ready-for-dev for
  # this one new AC. See sprint-change-proposal-2026-08-24-ux-rework-batch.md.
  # UPDATE 2026-08-24 (same day, bmad-correct-course): AC9 revised again -- a
  # wider sweep found the same max-w-7xl container duplicated across 7 pages,
  # so this now adopts a shared PageContainer primitive (new Story 0.30)
  # instead of a one-file w-full change. Blocked on Story 0.30. The original
  # narrow implementation (story/1-3-ac9-full-width worktree, already coded
  # and tested) is superseded -- do not merge that branch.
  # DONE 2026-08-24: AC9 merged (Wave 2, story/wave2-page-grid-adoption).
  1-3-display-a-list-of-events-on-the-main-page: done
  # Extended 2026-08-29 (commit a0fc985, sprint-change-proposal-2026-08-28.md Item 3,
  # FR97): a #-prefixed search phrase now routes to an exact array-containment
  # match against the new posts.hashtags column (GIN index, migration 0039) via
  # buildEventsQueryCondition's shared code path -- same fix also covers the Feed
  # search bar (FR30, Epic 3), no separate implementation. Direct implementation,
  # not routed through bmad-create-story -- epics.md/FR backfill only.
  1-4-search-for-events: done
  1-5a-build-the-reusable-multiselect-component: done
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.1: added AC9 (compact dropdown
  # presentation) and AC10 (per-facet clear + row layout). Confirmed via git show
  # --stat that no prior partial bmad-quick-dev attempt touched FilterHub.tsx --
  # genuine clean slate, unlike the calendar-chain stories. Tasks 7-9 blocked on
  # Story 0.28 (packages/ui shadcn/Radix setup, shared with Story 1.3g's WeekPicker,
  # not re-split here) plus this story's own shadcn badge addition (single-consumer,
  # added directly in this story rather than expanding 0.28's shared scope).
  1-5-filter-events-by-type-and-category: review
  # Reopened 2026-08-25 via bmad-correct-course: EventDetailViewProps gains
  # videoUrl/videoAlt; EventImage gains a video-capable sibling/variant
  # (<video autoPlay muted loop playsInline>) rendered in place of the image
  # when videoUrl is present; on playback failure, falls back to EventImage
  # and surfaces the existing originalPostUrl/sourcePostUrl attribution link.
  # See sprint-change-proposal-2026-08-25-video-priority-display.md.
  # DONE 2026-08-26: AC17-19 implemented in an isolated worktree (cline-cli,
  # story/1-6a-video-component) -- EventImage.tsx extended in place with
  # video autoplay/muted/loop/playsInline, videoReady/videoError state,
  # imageFallbackUrl onError retry, and video-failure attribution note.
  # Independently re-verified: 303/303 packages/ui tests pass (incl. 3 new
  # video/fallback cases), lint clean, no new TS errors in any touched file
  # (checked via apps/web's own tsc --noEmit as a real consumer). One real
  # issue found and fixed during verification: the failure-note link text
  # was hardcoded English instead of reusing labels.viewOriginalPostLabel/
  # viewSourceLabel (i18n-rule violation, same category as Story 3.3b's
  # AccountLocationField fix) -- corrected before merge.
  1-6a-build-the-reusable-event-detail-view-component: done
  1-6b-build-the-context-aware-list-navigation-hook: done
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.2: added AC13 (Carousel-chrome
  # navigation) and AC14 (icon-only modal close). Verified via git show that AC14
  # is already correctly implemented (failed bmad-quick-dev run's final commit,
  # c0c4912) but AC13's Carousel chrome is not -- scoped as Task 15. Unlike
  # Stories 1.3g/1.5, NOT blocked on Story 0.28: EventDetailWrapper.tsx is an
  # apps/web file with its own already-existing shadcn setup.
  # 2026-08-16: Task 15/AC13 implemented (Carousel chrome), then amended at user
  # request to a 3-slide peek-preview window + real swipe gesture (beyond the
  # original single-slide text) -- also fixed a duplicate modal close button found
  # during that work. Same-day bmad-correct-course pass
  # (sprint-change-proposal-2026-08-16-detail-carousel-ux-fixes.md) fixed two UX
  # regressions from manual testing: peek/active slide size mismatch (EventPreviewCard
  # restructured to mirror EventDetailView's own loading-skeleton shape) and the
  # route-level RouteLoader incorrectly showing on in-app Next/Previous navigation
  # (adjacent slugs now prefetched via router.prefetch()).
  # 2026-08-20: Verification pass completed; all 15 tasks confirmed complete; AC1-14
  # satisfied; test files verified; Dev Agent Record filled in; ready for code review.
  # Reopened 2026-08-25 via bmad-correct-course: wires the new videoUrl field
  # from the GraphQL query through to EventDetailView (Story 1.6a's amendment).
  # See sprint-change-proposal-2026-08-25-video-priority-display.md.
  # Story drafted 2026-08-26/27 via bmad-create-story: added AC15/Task 16 --
  # pure wiring against two already-merged endpoints (Story 3.3c's
  # Event.videoUrl, Story 1.6a's EventDetailViewProps.videoUrl/videoAlt). No
  # Gate 1/2/3 subagent dispatch (epic-1-readiness.md already swept and covers
  # 1.6; change is mechanical, same shape as this story's own AC11). Scoped to
  # apps/web only (queries.graphql, generated codegen, mapper.ts,
  # EventDetailWrapper.test.tsx) -- does not touch apps/backend/packages/
  # database, where sibling Story 3-6e is concurrently working.
  # AC15 implemented and merged 2026-08-27 (commit f27ee0e, isolated worktree
  # .claude/worktrees/1-6, cline-cli): getEventBySlug now selects videoUrl,
  # apps/web/src/generated/graphql.ts regenerated via `pnpm --filter web
  # codegen` (real regen diff, not hand-edited), mapper.ts maps
  # videoUrl/videoAlt (videoAlt = event.eventName, same convention as
  # imageAlt), imageFallbackUrl left unset per scope. Independently
  # re-verified after merge, directly on master: 19/19
  # EventDetailWrapper.test.tsx tests pass (18 pre-existing + 1 new
  # video-rendering case asserting a real <video src=...> element via
  # EventImage's existing data-testid="event-video", Story 1.6a's markup);
  # `pnpm --filter web lint` clean (warnings only, none new); `tsc --noEmit`
  # shows zero new errors in any touched file (pre-existing unrelated errors
  # in other test files untouched by this change). Diff scoped to exactly 4
  # files under apps/web, no apps/backend/packages/database/packages/ui
  # changes -- sibling Story 3-6e's concurrent packages/database/schema.ts
  # WIP in the main repo's working tree confirmed untouched throughout.
  1-6-view-event-details: review
  1-7a-build-the-reusable-blockingloader-component: review
  1-7-user-signup-and-login-with-google: review
  1-8-setup-posthog-analytics: review
  1-9-dynamic-browser-title-and-meta-tags: review
  epic-1-retrospective: optional
  epic-2: in-progress
  # Reopened 2026-08-24 via bmad-correct-course: new favoriteCount field, a
  # correlated-subquery COUNT sibling to the existing isFavorited EXISTS pattern
  # (same activeOnly(favorites) scoping). See sprint-change-proposal-2026-08-24-
  # ux-rework-batch.md Section 4.5.
  # Reopened 2026-08-25 via bmad-correct-course/bmad-create-story: added AC5 --
  # Event.favoriteCount computed field, exact isFavorited pattern (COUNT not
  # EXISTS), no auth scoping (public aggregate). AC1-4 confirmed already
  # implemented via direct code inspection during this amendment -- the story
  # file's own header previously said "Not started," now corrected. See
  # sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.5.
  # AC5 (favoriteCount) implemented and verified 2026-08-26.
  2-1a-build-the-favorites-and-calendar-additions-backend-graphql-api-layer: review
  2-1-favorite-an-event: review
  2-1b-build-the-ics-route-handler-and-generator-utility: review
  # Reopened 2026-08-24 via bmad-correct-course: added AC14 -- adopt shared
  # PageContainer primitive (Story 0.30), full-width instead of max-w-7xl.
  # Blocked on Story 0.30 landing first. See sprint-change-proposal-2026-08-24-
  # ux-rework-batch.md.
  # DONE 2026-08-24: AC14 merged (Wave 2, story/wave2-page-grid-adoption).
  2-2-view-favorited-events: done
  # Reopened 2026-08-06 via bmad-correct-course: AC5/AC6 revised (hard delete ->
  # AD-8 soft delete via new SoftDeleteAction-param mutation contract), Task 1b
  # (deletedAt migration) added. See sprint-change-proposal-2026-08-06.md.
  2-3a-build-the-saved-locations-backend-graphql-api-layer: review
  2-3b-extend-the-geolocation-adapter-and-saved-locations-api-with-address-autocomplete-support: review
  # regenerated 2026-08-04 via bmad-create-story at user's request: re-verified against
  # current codebase state (Story 2.3b now fully drafted with a matching contract minus a
  # dropped sessionToken arg; Story 0.16 progressed to review/implemented, narrowing the
  # dependency chain to 2.3a/2.3b only). Status unchanged (already ready-for-dev).
  # AC9-11 revised 2026-08-06 via bmad-correct-course: delete now commits
  # immediately (DELETE action) instead of on unmount; Undo now calls a real
  # RESTORE mutation; timeout splices from local list, no unmount trigger.
  # New AC11a (delete-call failure path). See sprint-change-proposal-2026-08-06.md.
  # Reopened 2026-08-24 via bmad-correct-course: added AC14 -- adopt
  # PageContainer(fullWidth=false) + PageHeader (icon+label action,
  # conditional on locations.length > 0, preserved exactly). Blocked on
  # Stories 0.30 (AC7)/0.32.
  # Wave 4 (2026-08-24): AC14 delivered via Cline, independently verified
  # (diff-scoped, lint clean, action prop preserved correctly) and merged.
  2-3-manage-saved-locations: done
  # Reopened 2026-08-06 via bmad-correct-course: AC2/AC5-AC8 revised, AC18-AC20 added
  # (shared map-picker continuity state across search/current-location/map-pick, an
  # embedded search inside the map sheet, zoom control + contextual initial zoom).
  # See sprint-change-proposal-2026-08-06-map-picker-continuity.md.
  2-4-set-location-by-current-location-or-map: review
  # regenerated 2026-08-04 via bmad-create-story: raw maplibre-gl (not
  # @vis.gl/react-maplibre) and a pointer-only, documented a11y limitation for
  # marker placement were confirmed with the user during story creation.
  # AC12/AC13 added 2026-08-06 via bmad-correct-course: showZoomControl and
  # onViewStateChange props, needed by Story 2.4's map-picker-continuity amendment.
  # See sprint-change-proposal-2026-08-06-map-picker-continuity.md.
  2-4a-set-up-frontend-map-integration-and-reusable-map-component: review
  # drafted 2026-08-04 via bmad-create-story: Gate 1 gap surfaced during Story 2.4's
  # creation (see epics.md Story 2.4b note) — no query exposes reverse-geocoding as
  # a standalone read; Story 2.4's address-preview flow needs one. No dependency on
  # Story 2.3a/2.3b (Story 0.16/0.17, this story's only real deps, are both `review`).
  # AC8/AC9 added 2026-08-06 via bmad-correct-course: previewLocation now also accepts
  # a placeId as an alternative to latitude/longitude. See
  # sprint-change-proposal-2026-08-06-map-picker-continuity.md.
  2-4b-extend-the-geolocation-adapter-with-a-reverse-geocode-preview-query: review
  # regenerated 2026-08-05 via bmad-create-story: full context engine pass with two
  # user-confirmed design decisions (Haversine+scalar-column geo storage; full recursive
  # multi-locationPreferenceId DSL support). Gate 1/3 cited from swept epic-2-readiness.md
  # (this story is itself that report's identified gap-fill). Gate 2 found no in-scope UI
  # gap; left a forward note for Story 2.5's own creation (Discovery radius-control design).
  # AC1a added 2026-08-06 via bmad-create-story while drafting Story 2.5: broadened
  # withinRadius to accept ad-hoc { latitude, longitude, radiusKm } coordinates (not
  # just a saved locationPreferenceId), needed by 2.5's no-saved-location fallback.
  # Not yet implemented, so amended in place rather than splitting a new story.
  2-5a-extend-the-events-graphql-api-with-geo-distance-query-support: review
  # Reopened 2026-08-24 via bmad-correct-course: compact/inline presentation
  # variant for FilterHub's location filter, reusing the collapsed-trigger-plus-
  # popover pattern from Story 1.5's AC9/AC10. See sprint-change-proposal-2026-
  # 08-24-ux-rework-batch.md Section 4.7.
  # Full AC13 backfilled 2026-08-25 via bmad-create-story -- only a reopening
  # comment existed before (the story file's own AC1-12 are a DIFFERENT,
  # already-implemented feature: the original nearby-filter build itself, not
  # the compact-presentation ask). Confirmed via direct code inspection: AC13
  # mirrors FilterHub.tsx's existing Type/Category renderFacet pattern exactly.
  2-5-find-nearby-events: review
  # added 2026-08-06 via bmad-create-story Gate 2/escape-hatch finding while drafting
  # Story 2.6 — no story wires the "Add to Calendar" trigger UI (flagged but deferred
  # by epic-2-readiness.md's "Anticipated Gate 2 note" and Stories 2.1/2.1b's own
  # Out-of-Scope notes). See 2.6's Architecture & UX Gate Findings for detail.
  2-6b-wire-the-add-to-calendar-trigger-dialog-and-ics-export: review
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.5: added AC9 (week-picker pass-through).
  # Verified via direct code inspection that Tasks 1-8 are genuinely already
  # implemented (this story's own Completion Notes/File List, unlike 1.3f's, were
  # accurately filled in) -- onSelectWeek/label pass-through is already shipped in
  # my-calendar-content.tsx; only a new getWeekRange prop is outstanding (Task 9,
  # blocked on Story 1.3g's Task 14-15), identical to Story 1.3f's Task 11.
  # Reopened 2026-08-24 via bmad-correct-course: added AC10 -- adopt shared
  # PageContainer primitive (Story 0.30), full-width instead of max-w-7xl;
  # column-count rule N/A (WeeklyCalendarView is a fixed 7-col days-of-week
  # grid). Blocked on Story 0.30. See sprint-change-proposal-2026-08-24-ux-
  # rework-batch.md.
  # DONE 2026-08-24: AC10 merged (Wave 2, story/wave2-page-grid-adoption).
  2-6-view-and-manage-events-on-a-calendar: done
  2-6a-create-user-settings-table-and-settings-query-mutation-resolvers: review
  # regenerated 2026-08-06 via bmad-create-story: user broadened scope from
  # personal-lists-only to a global default across every event view, requested a
  # reusable/extensible hiding mechanism (Story 1.3h's overlaps operator extended
  # with an open-ended to:null), and requested a new Archive page escape hatch
  # (Story 4.8, Epic 4). All confirmed via AskUserQuestion; epics.md AC revised.
  2-7-automatically-hide-past-events: review
  # Reopened 2026-08-24 via bmad-correct-course: registry rewritten for a two-
  # shell settings IA -- Locations/Widgets stay standalone menu items; API Keys/
  # Subscribed Accounts/Posts/Notifications collapse into one tabbed shell
  # (Story 0.29); Queue Status entry removed (Story 3.9a dissolved). Moderator
  # Items stays a separate, standalone entry (user decision) -- does not join the
  # new moderator actor-runs/unprocessed-payloads tabbed shell. EXPERIENCE.md's
  # User Menu registry (also stale on queue-status/widgets independent of this
  # change) amended alongside. See sprint-change-proposal-2026-08-24-ux-rework-
  # batch.md Section 4.2.
  2-8-user-menu: done
  # Reopened 2026-08-24 via bmad-correct-course: added AC10 -- adopt
  # PageContainer(fullWidth=false) + PageHeader (title-only, no action; drops
  # the border-b pb-4 divider for visual consistency). Blocked on Stories
  # 0.30 (AC7)/0.32.
  # Wave 4 (2026-08-24): AC10 delivered via Cline, independently verified
  # (diff-scoped, lint clean) and merged.
  2-9-manage-push-notification-settings: done
  2-10-service-worker-lifecycle-updates-and-database-self-healing: review
  epic-2-retrospective: optional
  epic-3: in-progress
  3-1a-create-social-media-account-profiles-table: done
  # regenerated 2026-08-07 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md; lightweight guard surfaced two real gaps the
  # sweep couldn't anticipate (both UI/design-artifact-shaped) -- split new Story 0.24
  # (generic /wizard primitive, confirmed cross-epic reuse via Story 5.5) and added a
  # forward-note to Story 3.2 (/settings/subscriptions page gap). User confirmed via
  # AskUserQuestion: this story owns the subscribeToAccount mutation (3.2 will reuse
  # it), the subscribe step is manual-entry only (no live scrape validation -- Story
  # 3.4 forward-noted), and the wizard registry is code-defined (not URL-query-param)
  # for i18n/generateMetadata compatibility. Depends on: 3.1a, 3.1b, 0.24 (none done
  # yet -- sequencing confirmation required before implementation, see story's
  # Pre-Coding Approval Gate).
  3-1-onboarding-wizard-for-api-key-and-subscriptions: review
  # regenerated 2026-08-07 via bmad-create-story: scope expanded from view+revoke-only
  # to also own key creation (createApiKey), folded in at user's request after the UX
  # scenario doc (04.1-manage-api-keys.md) showed an add-flow on this same page. Story
  # 3.1's onboarding wizard now reuses this story's createApiKey mutation instead of
  # building its own. Depends-on updated: adds Story 0.13/0.14, drops Story 3.1 (no
  # longer needed — this story is now self-sufficient for create+revoke). Cross-amended
  # Story 0.13's fetchCandidateKeys task with an activeOnly(apiKeys) filter requirement.
  # See epics.md Story 3.1b's Amendment note.
  # Reopened 2026-08-24 via bmad-correct-course: becomes Account Settings tab
  # content (Story 0.29 shell); absorbs Story 3.9a's API-key-health banner.
  # See sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.2.
  # Reopened 2026-08-24 via bmad-correct-course: added AC12 -- adopt
  # PageContainer(fullWidth=false) + PageHeader (title-only, no header
  # action -- add-key is via the in-table row). Blocked on Stories 0.30
  # (AC7)/0.32.
  # Wave 4 (2026-08-24): AC12 delivered via Cline, but the "no header action"
  # premise above was wrong -- no in-table add-row exists or ever existed;
  # that header button was the only trigger for the Add Key dialog, and
  # removing it broke the flow (caught by this story's own existing test,
  # api-keys-content.test.tsx, once actually run -- Cline's report never
  # mentioned running tests). Fixed directly during verification: PageHeader
  # now carries the action prop, same pattern as 2-3/3-2/6-5. AC12 text in
  # the story file corrected to match. Verified (diff-scoped, lint clean,
  # 4/4 tests passing) and merged.
  3-1b-manage-and-revoke-api-keys: done
  # regenerated 2026-08-07 via bmad-create-story: full context engine pass. Scope
  # expanded from add-only to also own the full /settings/subscriptions list+remove
  # view (mySubscriptions query, removeSubscription mutation), resolving this
  # story's own Forward note (added while drafting Story 3.1) that flagged Story
  # 5.1a's original ownership of those two as a backward Epic-3-on-Epic-5 dependency.
  # Confirmed via AskUserQuestion. Story 5.1a's epics.md entry amended to extend
  # this story's mySubscriptions with isInactive instead of rebuilding it, and to
  # reuse removeSubscription as-is. Verified subscriptions.is_newly_added already
  # exists in the DB (Story 3.1a's actual migration 0013) despite epics.md's AC
  # text for 3.1a never listing it -- a doc-only gap, not a schema gap.
  # Reopened 2026-08-24 via bmad-correct-course: (1) remove 'twitter' from
  # SUPPORTED_PLATFORMS (packages/domain/src/subscriptions/platforms.ts) --
  # blocks both new subscriptions and new account votes, register-adapters.ts
  # untouched so existing Twitter subscriptions keep scraping; (2) becomes
  # Account Settings tab content (Story 0.29 shell), absorbs Story 3.9a's
  # pending-extraction-count badge. See sprint-change-proposal-2026-08-24-ux-
  # rework-batch.md Sections 4.2/4.3.
  # Reopened 2026-08-24 (later same day) via bmad-correct-course: added AC14
  # -- adopt PageContainer(fullWidth=false) + PageHeader (icon+label action,
  # conditional on hasApiKey && subscriptions.length > 0, preserved exactly)
  # -- this is the exact header the user reviewed to request the whole
  # PageHeader pattern. Blocked on Stories 0.30 (AC7)/0.32.
  # Wave 4 (2026-08-24): AC14 delivered via Cline, independently verified
  # (diff-scoped, lint clean, action prop preserved correctly, existing
  # tests passing) and merged.
  3-2-subscribe-to-a-social-media-account: done
  # added 2026-08-08 via bmad-create-story Gate 2 finding while drafting Story 3.3:
  # the user confirmed (AskUserQuestion) reusing the full Saved-Locations-style
  # address-autocomplete/current-location/map-pick experience for the default-
  # location field, which is currently embedded, unextracted, inside
  # location-form-dialog.tsx (Stories 2.3/2.4). See epics.md Story 3.3d note.
  # Reopened 2026-08-24 via bmad-create-story: added AC10 -- dropdown gains a
  # real isDismissed state (survives selection, closable via click-outside/
  # Escape). Root cause traced directly: handleSelect's setIsDropdownOpen(false)
  # was clobbered on the next render by the [addressSearch, resolvedPreview]
  # effect re-firing (ux-rework-2026-08-24.md items #7/#8). AC1-AC9 unaffected.
  # See sprint-change-proposal-2026-08-24-ux-rework-batch.md.
  # Corrected 2026-08-25 (status audit): stale entry -- AC1-9 and the AC10
  # amendment are both real, shipped, and independently verified (packages/ui
  # LocationPickerField/LocationPickerMapPanel/map.tsx tests, apps/web's
  # consuming location-form-dialog/locations-content tests, all green). The
  # story file's own Completion Status already said "review"; only this
  # tracking entry and the story file's top-of-file header/checklists lagged.
  3-3d-build-the-reusable-locationpickerfield-component: review
  3-3-set-a-default-location-for-a-subscription: review
  # regenerated 2026-08-08 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap); Gate 2 determined directly (zero UI
  # scope). Data Type Compatibility audit found posts.post_url was nullable/non-unique
  # despite the PRD/shared-types Post interface requiring it, and packages/shared-types'
  # Post interface was missing accountId/publishedAt -- both fixed in this story. A
  # dedup design decision (unique constraint + select-or-insert vs. blind insert
  # accepting duplicates) was confirmed with the user via AskUserQuestion: dedupe on
  # post_url, mirroring Story 3.1a's subscribeToAccount() precedent.
  3-3a-create-posts-table-and-persist-scraped-posts: review
  # regenerated 2026-08-08 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap; explicitly validates this story's new
  # default_location_change_requests table against Story 4.7's forward dependency);
  # Gate 2 run fresh, no gap. Lightweight guard surfaced three items resolved with the
  # user via AskUserQuestion: (1) extend Story 3.3's existing SetDefaultLocationDialog
  # with an edit mode rather than build EXPERIENCE.md's literal in-table form; (2)
  # absorb the "Pending Review" badge (EXPERIENCE.md state pattern) into this story
  # rather than leave it a gap; (3) allow stacked concurrent edits (new
  # PENDING_REVIEW row per edit, no server-side block) with a Forward Note binding
  # Story 4.7 to dedupe by accountId to the latest row when it's built. No new
  # prerequisite stories split off.
  # Reopened 2026-08-24 via bmad-correct-course (sprint-change-proposal-2026-08-24-
  # moderator-location-override.md, AD-11): confirmed this story's own (3) forward
  # note above was never actually implemented in Story 4.7 -- moderator-items-content.tsx
  # has no dedupe logic at all, so stacked PENDING_REVIEW rows for one account really
  # do all surface today. New AC: extend editAccountDefaultLocation's auth to accept
  # requireModerator OR the existing active-subscription check (additive, subscriber
  # path unchanged); a moderator-sourced call writes changeSource: MODERATOR and
  # inserts the request already resolved (no PENDING_REVIEW); any successful call
  # (subscriber or moderator) marks every other still-PENDING_REVIEW request for that
  # accountId as SUPERSEDED -- this both fixes the never-built dedupe note and enables
  # the new moderator-override capability in one mechanism. Extract the account-row
  # value+badge+edit-trigger block into shared AccountLocationField (packages/ui/src/
  # features/locations/, see design-artifacts/UX-festgrid-run-1/EXPERIENCE.md
  # Component Patterns > Account Location Field) for reuse by Story 4.7.
  # Reopened 2026-08-24 via bmad-create-story: added AC15-19 (the AD-11 amendment
  # above). Gate 1/3 sourced from the still-valid swept epic-3-readiness.md (no fresh
  # gap); Gate 2 run fresh via subagent against the AccountLocationField extraction --
  # no gap, proceeds as a task (Task 10) rather than a split story. Verified directly
  # against current code (not assumed): changeSource does not exist anywhere in the
  # codebase yet -- Story 3.4m (PRD's originally-intended owner of USER/AI_INFERENCE)
  # is still ready-for-dev, unimplemented, so this amendment is the one that actually
  # introduces the changeSource enum/column first (all three ratified values included
  # so 3.4m won't need to re-touch it). Two items flagged as escape-hatch notes rather
  # than silently absorbed: AC7's INVALID_STATE_TRANSITION guard is not relaxed for
  # moderators; AC9's moderator-alert email is not suppressed for self-resolved writes.
  # AC1-14 unaffected, subscriber-path regression required (AC15). See
  # sprint-change-proposal-2026-08-24-moderator-location-override.md.
  # Approved 2026-08-25: AC15-19 amendment Pre-Coding Approval Gate signed off by
  # user (all confirmation items + escape-hatch acknowledgment). bmad-dev-story
  # (run via gemini-cli, isolated worktree) implemented Tasks 1-2, 10 (the AD-11
  # amendment scope) same day -- backend 263/263, packages/ui 275/275, apps/web
  # 266/266 tests green, full repo build/lint clean. Independent verification
  # found and fixed two minor issues before merge: AccountLocationField's labels
  # prop was optional with hardcoded English fallback strings (i18n-rule
  # violation, tightened to required); a debug console.log and one test's
  # non-deterministic role assignment were also cleaned up. Story 4.7's own
  # AC10-15 amendment stays hard-blocked on this landing, now satisfied.
  3-3b-edit-an-accounts-default-location: review
  # regenerated 2026-08-08 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (this story is itself that sweep's own Gate 3
  # finding); Gate 2 run fresh, no gap (zero UI surface). User confirmed via
  # AskUserQuestion: new packages/domain/src/scraper/ folder (not folded into
  # subscriptions/); Twitter's platform slug is 'x'; and the registry also owns a
  # getPlatformDisplayName map, consolidating three duplicated inline platform-name
  # ternaries in apps/web (onboarding-subscribe-step.tsx, subscribe-account-dialog.tsx,
  # subscriptions-content.tsx) -- an initial Grep-tool read of those files looked like
  # a "Twitter\X" escape-sequence bug, corrected after a direct Read confirmed no bug,
  # only duplication. No new prerequisite stories split off.
  # Reopened 2026-08-24 via bmad-correct-course: normalized post shape gains
  # optional locationName/ownerDisplayName/ownerUsername fields -- present in
  # the raw Apify payload today (apify-runs/run-fKvCBvXjZ7w9R9nFN...md:52-55) but
  # currently discarded before reaching Post/SocialMediaAccountProfile. Feeds
  # Story 3.4m. See sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.1.
  # Reopened again 2026-08-25 via bmad-correct-course: normalized post shape
  # gains optional videoUrl, sourced from Apify's raw videoUrl field
  # (confirmed present for Reel/clip posts, apify-runs/run-fKvCBvXjZ7w9R9nFN...
  # md:146,154) but currently discarded, same pattern as the locationName/
  # ownerDisplayName/ownerUsername fields above. Also needs a new posts.
  # video_url DB column/migration and a videoUrl GraphQL field (fold into this
  # story's consumer or a small new story, PO's call at story-creation time).
  # See sprint-change-proposal-2026-08-25-video-priority-display.md.
  # Amended 2026-08-26 via bmad-create-story: the DB/GraphQL work above was
  # resolved as "fold in" -- new AC9-12 + Tasks 8-13 added directly to this
  # story file (not split into a separate story), absorbing ScrapedPost's
  # videoUrl mapping (AC9), the AJV schema update (AC10), the posts.video_url
  # column/migration + persist-scraped-post.ts threading (AC11), and the
  # events.graphql Event.videoUrl field + resolver flattening across 5 select
  # sites + codegen (AC12) into one end-to-end plumbing change per the
  # proposal's own framing. AC1-8 remain delivered/unaffected. Gate 1/2/3: no
  # gap on any gate (lightweight guard -- every change mirrors an
  # already-shipped pattern 1:1, no new architecture/infra/foundational
  # dependency). Pre-Coding Approval Gate marked approved (inherited from the
  # already-approved Sprint Change Proposal).
  # AC9-12 implemented and merged 2026-08-26: gemini-cli was tried first
  # (project convention) but failed immediately with a ModelNotFoundError
  # (GCP project/model access issue, unrelated to this story); fell back to
  # cline-cli in an isolated worktree (.claude/worktrees/3-3c, branch
  # 3-3c-video-url) per this task's documented fallback option. Independently
  # verified before merge: full diff read against the actual branch-point
  # commit (not a moved master), full repo build/lint/test re-run clean
  # (267/267 backend tests), a live-DB column-existence check, and a real
  # migration-ledger/physical-schema inconsistency caught and fixed (the
  # shared local dev Postgres -- also used by concurrent sibling worktrees
  # 0-33/1-6a -- already had video_url added out-of-band by the dispatched
  # agent when drizzle's own migrate command hit a sequencing conflict;
  # reconciled by inserting the correctly-hashed drizzle.__drizzle_migrations
  # row so `pnpm migrate` is now a clean idempotent no-op). Merged to master
  # (--no-ff); worktree and branch cleaned up. Status left at "review" (not
  # "done") pending this project's separate code-review workflow step,
  # matching this file's own STATUS DEFINITIONS and the precedent of other
  # implemented-but-not-yet-code-reviewed entries (e.g. 3-3b). See the story
  # file's own Completion Status/Dev Agent Record for full detail.
  3-3c-define-the-scraper-adapter-interface-and-platform-slug-registry: review
  3-4-scrape-new-posts-from-subscribed-accounts: review
  # added 2026-08-08 via bmad-create-story Gate 1 split while drafting Story 3.4:
  # making Bright Data the scheduled-batch's priority vendor (cheaper at scale,
  # async-only) needs a new job-tracking table, a public webhook endpoint, and a
  # stale-job fallback sweep -- a genuinely new architectural layer, not an
  # incremental adapter addition. See epics.md Story 3.4a note.
  # WARNING 2026-08-10 (RESOLVED same day): initial Bright Data reply said they
  # "no longer provide IPs for Social media account management usecase" --
  # risked invalidating this story's premise. Follow-up clarified this meant
  # their Proxy/IP product, not the Web Scraper/Dataset API -- Bright Data
  # confirmed "we can support you collecting Social media data from our
  # Webscraper APIs and Filter APIs... post URL or Profile URL as input."
  # Story is UNBLOCKED for bmad-create-story 3-4a. Remaining open question
  # (capability, not policy): confirm profile-URL input returns recent POSTS
  # (discovery) vs only profile metadata/single-post lookup -- verify with a
  # real test call before committing to the async job-tracking/webhook design.
  # UPDATE 2026-08-15 (bmad-create-story): a full story file already existed
  # (drafted 2026-08-09, one day before the ON HOLD note froze it) but this
  # status key was never synced to match its own ready-for-dev header. User
  # gave the new explicit decision to reopen the Bright Data avenue (epics.md
  # Story 3.4a REOPENED note); re-verified the story's technical premises
  # directly against current code (usage-store.ts, instagram-adapter.ts,
  # get-scrape-targets.ts, subscribe-to-account.ts, scraper.ts lambda, CDK
  # stack) -- no drift found, content unchanged. Status synced to ready-for-dev.
  # The profile-URL discovery-vs-metadata capability question above remains an
  # accepted, tracked gap to close during implementation (needs a real
  # authenticated Bright Data call, not resolvable via static research).
  3-4a-add-brightdata-as-the-priority-scraping-vendor-for-the-scheduled-batch: review
  # added 2026-08-08 via bmad-create-story while drafting Story 3.4: BYOK-pooled
  # scraper-vendor keys, researched and found legally ambiguous for both Apify
  # (anti-sublicensing clause) and Bright Data (KYC/declared-use-case vetting).
  # LEGALLY GATED -- do not move to ready-for-dev until written vendor
  # confirmation is obtained (see the story file's outreach email drafts).
  # UPDATE 2026-08-10: Bright Data replied -- KYC is for registered business
  # entities only, not individual freelancers, which appears to rule out
  # individual end-users contributing a Bright Data key regardless of the
  # sublicensing question. Bright Data BYOK looks closed; Apify still pending.
  3-4b-byok-pooled-scraper-vendor-keys: backlog
  # added 2026-08-09 via bmad-create-story: exploratory outreach to Instagram-
  # viewer sites (storiesig.info explicitly invites API-access contact; imginn.com
  # is the right shape but has no advertised partnership channel, cold outreach
  # only; mollygram has no scraping/API prohibition but is likely the wrong
  # shape). EXPLORATORY -- outreach only, no adapter code until a concrete offer
  # exists.
  3-4c-explore-whitelisted-access-with-instagram-viewer-sites: backlog
  # added 2026-08-14 via bmad-help/architect analysis of docs/assets/Apify actor
  # costing and facts.md; corrected same day. None of the four actors evaluated
  # publish a run-duration SLA, and two synchronous call sites (getPostByUrl,
  # lookupAccountProfile) exist today with only one (getPostByUrl) having a local
  # timeout. Recommends picking the sync-path actor empirically (cost + latency +
  # success rate across all four candidates) and adding the missing timeout to
  # lookupAccountProfile; batch path (Story 3.4a) unaffected. CORRECTION: the
  # app-funded key is the project owner's own account -- owner is open to
  # third-party/community actors there for best outcome (accepts Apify Actor
  # Terms Sec 4.4 Creator-access exposure knowingly). Inverse of Story 3.4b's
  # future BYOK path, which is restricted to Apify-maintained actors only since
  # contributed keys would belong to individual community members, not the owner.
  # UPDATE 2026-08-14: user added real per-actor run durations to the costing doc.
  # Single-sample results: apify/instagram-api-scraper 31s, apify/instagram-post-
  # scraper 1m15s (worst!), sones/instagram-posts-scraper-lowcost 5s (best), fast-
  # instagram-post-scraper 7s. Both Apify-maintained durations already EXCEED the
  # current 20s getPostByUrl timeout -- likely a live production issue today, not
  # just a future risk. Priority raised accordingly; still backlog pending a fuller
  # sample (Task 1b) and a real bmad-create-story pass.
  # UPDATE 2026-08-14 (user request): scope broadened to getNewestPosts too, since
  # all 3 adapter methods share one callApifyActor call today -- a sync-path-only
  # swap would silently change the batch actor as well. Confirmed duplication is
  # already DB-safe (persist-scraped-post.ts onConflictDoNothing on postUrl)
  # regardless of actor, so the real batch question is filter cost-efficiency: does
  # a cutoff matching zero new posts actually bill zero items (Story 3.4 AC4), or
  # does the actor charge for filtered-out items like fast-instagram-post-scraper's
  # sample hinted at? New Task 1c tests this across all four actors before
  # getNewestPosts gets its own actor constant, decided independently of Task 1b.
  # UPDATE 2026-08-14: Task 1c fully complete (all 12 real Apify test runs against
  # pakuwonmall.jogja, including a re-run of Run 6 which initially used the wrong
  # cutoff by mistake). Two confirmed bugs disqualify two of the four candidates
  # for getNewestPosts: apify/instagram-api-scraper (current prod actor) leaks all
  # 3 pinned posts past its onlyPostsNewerThan cutoff, even at a true live
  # zero-boundary cutoff; sones/instagram-posts-scraper-lowcost never filters
  # server-side, returns is_newer_than_cutoff:false items regardless of cutoff.
  # apify/instagram-post-scraper passed every scenario cleanly and is the FINAL
  # CONFIRMED getNewestPosts actor pick. fast-instagram-post-scraper remains a
  # validated fallback (correct data, but bills a nonzero fee even on a correct
  # 0-item result). Still backlog pending a real bmad-create-story pass to turn
  # this into an implementation-ready story.
  # UPDATE 2026-08-15: Task 1b fully complete too (all 8 runs, real data). Found a
  # MAJOR actor-agnostic correctness bug: an invalid post URL/handle returns a
  # truthy 1-item array {"error":"not_found",...} from BOTH apify/instagram-api-
  # scraper and apify/instagram-post-scraper -- not an empty array. The existing
  # items.length===0 check never catches this. getPostByUrl silently returns an
  # empty-content post instead of SCRAPE_FAILED; lookupAccountProfile is worse --
  # its fallback chain lands on item.username (the invalid handle itself echoed
  # back), so castVote silently inserts a fake SocialMediaAccountProfile row for a
  # nonexistent account and reports success. See story Dev Notes "Task 1b
  # Conclusions" and 3-4d-task1b-runs/.
  # UPDATE 2026-08-15 (Sprint Change Proposal, sprint-change-proposal-2026-08-15-
  # not-found-detection-bug.md): the fix (originally AC6/Task 6 here) is SPLIT OUT
  # to new Story 3.4e, since it's a confirmed live data-integrity bug (Story 6.1a
  # is already `review` status and ships the fabrication bug) versus this story's
  # remaining scope being a non-urgent cost/reliability optimization. AC6/Task 6
  # removed from this story's active scope -- see 3-4e.
  # UPDATE 2026-08-15 (bmad-create-story full pass): Tasks 2-6 (actor-constant
  # extraction, lookupAccountProfile timeout via new ApifyRequestTimeoutError,
  # castVote resolver wiring, test fixes) now have full task-level detail --
  # ready for bmad-dev-story or bmad-quick-dev.
  3-4d-per-use-case-actor-selection-and-sync-path-timeout: review
  # NEW 2026-08-15, split off 3-4d via Sprint Change Proposal (see that story's
  # Amendment 4 and epics.md Story 3.4e). Fixes a confirmed, already-live
  # data-integrity bug: getPostByUrl/lookupAccountProfile treat Apify's truthy
  # {"error":"not_found",...} response as valid data instead of null, causing
  # castVote (Story 6.1a, already `review` status) to silently insert a fabricated
  # SocialMediaAccountProfile row for a garbled/nonexistent handle -- a direct
  # violation of Story 6.1's own written AC ("never from placeholder handle text")
  # and PRD SS3.13. Small, fully-specified fix from real test evidence
  # (3-4d-task1b-runs/run-02/-04/-06/-08) -- elevated priority, candidate for
  # bmad-quick-dev ahead of the normal backlog->create-story->dev-story queue.
  # 2026-08-15: bmad-create-story pass complete -- full story file created
  # (3-4e-fix-not-found-detection-in-the-instagram-scraper-adapter.md), ready
  # for bmad-dev-story or bmad-quick-dev per the story's own priority note.
  # 2026-08-18: bmad-dev-story complete -- fixed isNotFoundItem helper to check
  # item.error === "not_found" (string, not object.message), added kind parameter
  # for field-absence fallback logic, wired into both getPostByUrl and
  # lookupAccountProfile. Added 4 new test cases (AC1/AC2 error-shape + AC3
  # fallback), all passing. Pre-existing failing test (Story 3.4d scope) left
  # unchanged per Dev Notes.
  3-4e-fix-not-found-detection-in-the-instagram-scraper-adapter: review
  # NEW 2026-08-17, added via bmad-correct-course at the user's explicit request
  # ("Add as a 3rd split-off story now"), folded into the same sprint-change-
  # proposal-2026-08-15-not-found-detection-bug.md document as 3-4d/3-4e (that
  # file is being retitled/extended to cover all three). Two findings: (1)
  # confirmed via direct read of apps/infrastructure/lib/festgrid-backend-
  # stack.ts that sharedLambdaProps (and every individual Lambda) sets no
  # `timeout` -- apiLambda/scraperLambda/aiProcessorLambda/ingestorLambda all
  # silently inherit AWS's 3-second default today, an already-live bug
  # independent of the async-handling question below; (2) user asked whether
  # scheduled-batch scraping using Apify (which can run minutes to ~1hr per
  # Apify's own actor page) was anticipated, then clarified the intended
  # multi-vendor split: brightdata only for scheduled batch, apify for
  # scheduled batch (fallback) + vote-time profile check + subscribe-time --
  # "for scheduled batch we would have same long process but different
  # mechanism." Confirmed directly against Apify's own API docs that ad-hoc
  # per-run webhooks (base64 `webhooks` query param) and GET
  # /v2/actor-runs/{runId} status polling are both available, so the
  # getNewestPosts role can get the same async job+webhook+sweep treatment
  # Story 3.4a already built for Bright Data. getPostByUrl/lookupAccountProfile
  # (Story 3.4d's sync paths) are explicitly unaffected.
  # 2026-08-18: bmad-create-story full pass complete -- full story file created
  # (3-4f-fix-missing-lambda-timeouts-and-extend-async-job-handling-to-apifys-long-running-scrape-paths.md).
  # Re-verified live: sharedLambdaProps already has a blanket 30s timeout (dd01ac5),
  # not the "no timeout" premise this draft AC originally assumed -- story redrafted
  # around the actual current state (per-Lambda explicit overrides needed instead).
  # Story 3.4a found in-progress with a partially inconsistent implementation
  # (unwired /webhooks/brightdata route, orphaned duplicate Lambda constructs) --
  # this story's own dependency scoped to only the parts of 3.4a confirmed real/working
  # (attemptBrightDataTrigger, runStaleJobSweep, get-scrape-targets.ts exclusion); the
  # rest flagged as 3.4a's own open gap in the story's Pre-Coding Approval Gate, not
  # fixed here. New apify_pending_jobs table kept separate from brightdata_pending_jobs
  # per explicit user decision (avoids touching 3.4a's actively-changing code). Gate 1/2/3
  # all run fresh (3.4f postdates epic-3-readiness.md's sweep) -- all three: no gap.
  # Ready for bmad-dev-story or bmad-quick-dev.
  3-4f-fix-missing-lambda-timeouts-and-extend-async-job-handling-to-apifys-long-running-scrape-paths: review
  # added 2026-08-18 via bmad-create-story: user-identified compliance gap against
  # project-context.md's Runtime Schema Validation rule (neither scraper vendor path --
  # Apify nor Bright Data -- got AJV validation, only the Gemini path did). Scope
  # expanded from Apify-only to both vendors during story creation once the same gap
  # was confirmed on the Bright Data path (process-brightdata-result.ts), which also
  # surfaced two pre-existing bugs this story's work directly fixes: a broken
  # persistScrapedPost import (confirmed via tsc TS2307) in two files, and a
  # Date-vs-string publishedAt mismatch. Gate 1/2/3: no gap (reuses Story 0.11's
  # compileValidator/AJV foundation as-is; epic-3-readiness.md already swept
  # 2026-08-09; no UI surface).
  3-4g-validate-scraped-posts-from-apify-and-brightdata-with-ajv-schema: review
  # added 2026-08-18 via bmad-create-story: captures AJV validation failures from scrapers,
  # persists raw payloads with full context, tracks parser versions, enables moderator-driven
  # re-processing with version selection. Depends on: 3-4g (detection), 0.15 (email),
  # 3.5 (enqueue), 4.7a (auth). Followed by: 3-4i (moderator UI page).
  3-4h-capture-and-surface-data-format-anomalies: review
  # added 2026-08-18 via bmad-create-story: new moderator page (/moderator/unprocessed-payloads)
  # to browse unprocessed payloads, select parser version from all historical versions, and
  # trigger re-processing. Separate from Story 4.7 (moderator-items-page). Depends on: 3-4h.
  # 2026-08-18: full story file created (comprehensive UI story, 844 lines) -- ready for
  # bmad-dev-story once Story 3-4h ships (backend contracts this story consumes).
  3-4i-moderator-unprocessed-payload-browser-and-reprocessing: review
  # added 2026-08-19 via bmad-create-story: user-identified follow-up during a /bmad-help
  # investigation of a "no posts after scrape" report. Root cause (posts.platform NOT NULL
  # never set by persist-scraped-post.ts) is tracked separately as a bmad-quick-dev fix, not
  # part of this story. Investigation also found no scraper actor run (Apify or Bright Data,
  # sync or async) is recorded anywhere beyond a bare runId/snapshotId in the narrowly-scoped
  # apifyPendingJobs/brightdataPendingJobs tables. New unified scraper_actor_runs table chosen
  # over extending those two tables (user decision) since the sync Apify tier -- the one
  # actually implicated in the bug -- never creates a pendingJobs row at all. Followed by 3-4k
  # (moderator UI). See story file Dev Notes for full evidence trail.
  3-4j-capture-scraper-actor-run-audit-trail-and-replay-by-run-id: review
  last_updated: 2026-09-03T00:00:00Z
  # added 2026-08-19 via bmad-create-story: split from 3-4j following the 3-4h/3-4i backend/
  # frontend precedent (Gate 2 finding). Extracts a new RawJsonViewer packages/ui component --
  # genuine reuse evidence vs. Story 3-4i's own inline (never extracted) raw-JSON display.
  # Blocked on 3-4j shipping its GraphQL contracts.  3-4k-moderator-actor-run-browser-and-replay-ui: in-progress
  # New 2026-09-02 via bmad-correct-course, sprint-change-proposal-2026-09-02.md:
  # account-type scraping filter, closes minimization-doc §1.3/§2.8 gap. See
  # epics.md Story 3.4n note. Amended 2026-09-03 via bmad-create-story: resolved
  # an AC2/AC3 contradiction (classify pre-scrape, bio/username/businessCategoryName
  # only), changed CURATOR_GUIDE from permanently excluded to temporarily excluded
  # pending new Story 3.4o, and split moderator review of AWAITING_APPROVAL accounts
  # into new Story 4.7c. Legacy accounts explicitly NOT backfilled (user's choice) --
  # see story file Dev Notes "Legacy Accounts: Explicit Accepted Gap".
  3-4n-filter-scraped-accounts-by-type-organizer-venue-event-only: review
  # Added 2026-09-03 via bmad-create-story, split off Story 3-4n's own creation
  # (Gate 1/3 finding): the minimization pipeline (no durable image storage, no
  # displayed/retained captions) CURATOR_GUIDE-classified accounts need before
  # 3-4n's scrape-gate can allow them through. See epics.md Story 3.4o.
  # Regenerated 2026-09-03 via bmad-create-story: full context-engine pass. Epic-3
  # readiness sweep (epic-3-readiness.md, swept 2026-08-09) predates this story, so
  # Gate 1/2/3 re-run fresh -- no gap requiring a new prerequisite story, but Gate 3
  # found a genuine new dependency on Story 3-6g (isImageStorageOptedIn column,
  # ready-for-dev, not yet built) for AC1/AC5's opt-in-override sub-task -- see the
  # story file's Pre-Coding Approval Gate for the scoped blocker. Own Data Type
  # Compatibility audit found posts.content's NOT NULL constraint and
  # extraction.graphql's Post.content: String! both need to become nullable, neither
  # anticipated by the original epics.md draft. Three tradeoffs resolved via
  # AskUserQuestion: opt-in overrides the CURATOR_GUIDE image-storage default
  # (image-storage-scoped only); caption-clearing triggers only at the pipeline's 2
  # existing markPostExtractedSeam terminal points (parse/validation-failure path is
  # an accepted, documented gap); the AI-assisted-correction re-extraction conflict
  # is accepted as a known limitation, enforced at both UI and API layers.
  3-4o-minimize-stored-displayed-data-for-curator-guide-sourced-posts: review
  # Added 2026-09-03 via bmad-create-story during a live production incident
  # investigation (/bmad-help): root-caused directly against prod CloudWatch logs +
  # DB. Fix chosen with the user via AskUserQuestion: lower SCRAPE_SKIP_RECENT_HOURS
  # to 12h (real margin below the ~24h daily schedule cadence) rather than a
  # structural per-day-bucket redesign, which was considered and explicitly
  # deferred. See epics.md Story 3.4p note and
  # 3-4p-fix-daily-batch-scrape-scheduling-race-that-silently-skips-accounts.md.
  3-4p-fix-daily-batch-scrape-scheduling-race-that-silently-skips-accounts: ready-for-dev
  # Added 2026-09-03 via bmad-create-story during the same live production incident
  # investigation as 3-4p. Root-caused directly against prod CloudWatch logs + DB:
  # every Bright Data trigger attempt has failed 401 Unauthorized since at least
  # 2026-08-31, zero alerting existed. Alert design chosen with the user via
  # AskUserQuestion: extend the existing send-dangerous-report-moderator-alerts.ts
  # pattern (email all role='moderator' users) rather than new CloudWatch Alarm/SNS
  # infra. Includes an external, non-code Definition-of-Done item (rotating the
  # Bright Data API token in Secrets Manager). See epics.md Story 3.4q note and
  # 3-4q-fix-brightdata-auth-failure-and-alert-moderators-when-a-scraper-provider-goes-down.md.
  3-4q-fix-brightdata-auth-failure-and-alert-moderators-when-a-scraper-provider-goes-down: ready-for-dev
  # added 2026-08-19 via bmad-create-story: user-identified linking gap during /bmad-help
  # investigation. Story 3-4j captures scraper_actor_runs table but doesn't thread the run ID
  # to posts/unprocessed_payloads, leaving moderator UI (3-4k) unable to display "posts from
  # this run" view. This story adds nullable FKs and threads the run ID through 4 call sites
  # (sync Apify, async Apify, async Bright Data, stale-job-sweep). Depends on 3-4j
  # (apifyAuditContext infrastructure, recordActorRunResult return value). Does not block 3-4k
  # (UI can ship anytime, but will show incomplete data until this story ships).
  3-4l-link-scraper-actor-runs-to-posts-and-unprocessed-payloads: review
  # added 2026-08-24 via bmad-correct-course: PRD Section 5's "Location Data
  # Reuse Boundary" forbids using a subscriber's own saved location to label an
  # unrelated account -- user confirmed Gemini is fed only the post/account's own
  # scraped metadata (locationName/ownerFullName/ownerUsername), never
  # UserLocationPreference, so no PRD conflict. Persists to defaultLocation
  # (triggers the existing DefaultLocationChangeRequest moderator flow, PRD
  # Section 3.7) -- open schema question: DefaultLocationChangeRequest.
  # changedByUserId has no slot for an AI-driven change; add a nullable
  # changedByUserId + new changeSource: 'user' | 'ai_inference' discriminator
  # (user-approved direction, needs formal PRD Section 4.14 amendment before
  # dev-story). Depends on Story 3.3c (new fields), Story 0.13 (system-key
  # fallback tier), Story 0.16/2.4b (geocode Gemini's place description into
  # real LocationDetails -- never trust an LLM for coordinates directly). See
  # sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.1.
  # UPDATE 2026-08-24 (bmad-prd): PRD amended -- Section 3.7 AI-Assisted
  # Location Inference/Account Profile Backfill, Section 4.14 changedByUserId
  # now optional + new changeSource enum, Section 4.5 cross-reference. PRD is
  # no longer a blocker. Still blocked on the architecture-spine addendum
  # (system-key storage/scope, Track A's second half, not yet done) before
  # bmad-create-story. epics.md Requirements Inventory backfilled with
  # FR89-FR91 (this story) and FR92 (Epic 4, Moderator Tools display).
  # UPDATE 2026-08-24 (bmad-architecture): AD-10 added to festgrid-architecture-
  # spine.md -- SYSTEM_GEMINI_API_KEY (Secrets Manager credential, matching
  # GEOAPIFY_API_KEY's classification), new sibling function
  # callGeminiForLocationInference() (not a callGemini modification -- keeps
  # the system key structurally unreachable from Story 3.6's general
  # extraction path), no additional rate-limiting (call frequency already
  # bounded by persisting the result to defaultLocation). Track A is now
  # fully done (PRD 0f48abe, FR89-92 backfill 68c99c4, architecture AD-10).
  # Ready for bmad-create-story -- still just a backlog placeholder, no full
  # story file yet.
  # UPDATE 2026-08-24 (bmad-create-story): full story file written. Note:
  # the 0-13 entry's reopened comment above (line ~70-73, "selectApiKey gains
  # a new final-fallback tier") predates and is superseded by AD-10's later,
  # ratified sibling-function design (callGeminiForLocationInference, NOT a
  # selectApiKey modification) -- this story follows AD-10, not that stale
  # phrasing. Gate 1 (run fresh, Epic 3's readiness sweep predates this story
  # by two weeks and doesn't cover it) raised a real finding -- routing the
  # Gemini/geolocation calls through the existing AIProcessingQueue/AI
  # Processor Lambda instead of the Scraper Lambda -- rejected with direct
  # evidence: that queue only fires on Story 5.1a's manual post-selection
  # mutation (still backlog, PRD SS3.10), not automatically per scrape, so it
  # cannot satisfy FR90's "automatically" requirement; the Scraper Lambda
  # (processScrapeJob) remains the only viable "on every scrape" call site.
  # Also found and folded into this story's own scope (not split out, both
  # story-local and required for its own ACs): default_location_change_-
  # requests.changedByUserId is NOT NULL today with no changeSource column
  # (schema/GraphQL amendment per PRD S4.14, ratified but not yet
  # implemented), and resolveDefaultLocationChange's REVERT branch throws on
  # a null previousLocation, which this story's AI-inference path makes
  # newly reachable. See the story file's Architecture & UX Gate Findings for
  # full detail. changeSource enum values confirmed USER/AI_INFERENCE
  # (uppercase, matching the ratified PRD text and the existing
  # DefaultLocationChangeStatus convention).
  # Approved 2026-08-25: Pre-Coding Approval Gate signed off by user, including
  # the three flagged decisions (ScraperLambda placement + new IAM/env wiring,
  # moderator-flow REVERT parity for null-previousLocation AI-inferred rows,
  # editAccountDefaultLocation's applyDefaultLocationChange extraction).
  # bmad-dev-story cleared to proceed.
  # Corrected 2026-08-25: gemini-cli's dev-story run (commit 98d2757, merged
  # 5accdde) left this at in-progress despite its own completion summary
  # claiming sprint-status.yaml was updated to review -- the story file's own
  # Status header WAS correctly set to review, only this tracking file lagged.
  # Independently verified merged and complete: backend 266/266, packages/domain
  # 147/147 (100% coverage), apps/web 268/268, infra 1/1, build/lint clean.
  # Extended 2026-08-29 (commit a0fc985, sprint-change-proposal-2026-08-28.md Item 1,
  # FR94): location-inference call now returns a confidence score; below
  # LOCATION_INFERENCE_CONFIDENCE_THRESHOLD (default 0.5) the change lands as
  # AWAITING_APPROVAL instead of applying immediately. See Story 4.7's amendment
  # (epics.md) for the moderator approve/reject side. Direct implementation, not
  # routed through bmad-create-story -- epics.md/FR backfill only.
  3-4m-backfill-account-profile-and-ai-infer-default-location-from-scrape-metadata: review
  3-5-add-new-posts-to-a-processing-queue: review
  # regenerated 2026-08-10 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap); Gate 2 run fresh (no gap, zero UI
  # surface). Lightweight guard surfaced three real gaps the sweep couldn't have
  # anticipated, all resolved with the user via AskUserQuestion: (1) PRD FR33
  # (timezone inference) had no owning story anywhere in Epic 3 -- Tier 1
  # (location-based, via the existing Geolocation adapter) absorbed into this
  # story's own AC6, Tiers 2-3 split into new Story 3.6a; (2) aiProcessorLambda's
  # CDK environment was missing DATABASE_URL/DATA_INGESTION_QUEUE_URL/
  # GEOAPIFY_API_KEY and its SqsEventSource had no reportBatchItemFailures --
  # wired in-scope since this story is the first real caller inside that Lambda
  # (Story 3.4 precedent); (3) apiLambda was separately missing GEOAPIFY_API_KEY
  # despite being a documented Geoapify caller since the 2.4b range -- fixed as a
  # user-confirmed one-line drive-by in the same file/variable. Also decided:
  # reportBatchItemFailures: true (diverging from scraper.ts's no-partial-retry
  # precedent) so a quota-exhausted post's message isn't silently lost forever;
  # and the EventInfo.location-required vs. AC5's "left absent" conflict is kept
  # decoupled (ExtractedEventMessage.location stays optional) and forward-noted to
  # Story 3.6b rather than resolved now. See epics.md Story 3.6a note.
  3-6-process-posts-from-the-queue-and-extract-event-information: review
  # added 2026-08-10 via bmad-create-story Gate 3-shaped lightweight-guard finding
  # while drafting Story 3.6: PRD FR33's Tier 2 (subscribing user's own timezone)
  # and Tier 3 (flag for manual clarification) have no owning story or data-model
  # concept anywhere in the codebase. See epics.md Story 3.6a note.
  3-6a-infer-event-timezone-from-subscriber-context-and-flag-ambiguous-cases-for-clarification: review
  # regenerated 2026-08-10 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (3.6b explicitly in stories_covered, no gap);
  # lightweight guard confirmed Story 3.6a's post-sweep schedules.timezone/timezoneStatus
  # dependency raises no new Gate 1/3 gap. Gate 2 run fresh via subagent (no gap, zero UI
  # surface). Two real tradeoffs resolved with the user via AskUserQuestion: (1) the
  # absent-location conflict Story 3.6 forward-noted here -- resolved by inserting a
  # fixed "Location not specified" placeholder into events.location rather than
  # migrating the column to nullable or skipping the event; (2) Story 3.6a was still
  # ready-for-dev/not-started when this story's creation began -- user initially chose
  # to hold off, then confirmed 3.6a's real implementation had landed (commit 3cf81df,
  # now review) mid-session, so creation proceeded against verified real code. Also
  # decided: idempotent insert via a new events.postId unique constraint +
  # onConflictDoNothing (not a select-then-insert check), and
  # reportBatchItemFailures: true on ingestorLambda's SqsEventSource, mirroring
  # aiProcessorLambda's own precedent. See epics.md Story 3.6b Dev Notes.
  3-6b-ingest-processed-events-into-the-database: review
  # added 2026-08-10 via bmad-create-story Gate 3 finding while drafting Story 3.6a:
  # users.timezone (added by 3.6a to match PRD 4.8's User.timezone) has no capture
  # mechanism anywhere in the codebase -- without this story FR33 Tier 2 can never
  # fire in practice. See epics.md Story 3.6c note.
  3-6c-capture-and-store-the-subscribing-users-timezone: review
  # added 2026-08-10 via bmad-create-story Gate 3 finding while drafting Story 3.6a:
  # schedules.timezoneStatus = 'NEEDS_CLARIFICATION' (3.6a) is written but nothing
  # displays/resolves it yet. Depends on 3.6b and 3.7 both existing first. See
  # epics.md Story 3.6d note.
  3-6d-surface-schedules-flagged-as-needing-timezone-clarification: review
  # New 2026-08-25 via bmad-correct-course: posts.durableImageUrl/
  # posts.imageUrlExpiresAt columns + migration. On successful extraction
  # (this story's own pipeline), upload the image bytes already fetched for
  # Gemini's vision call (build-gemini-request.ts) to the new S3 bucket
  # (Story 0.33) and write durableImageUrl; on scrape, parse and store
  # imageUrlExpiresAt from the raw URL's own embedded expiry (adapter-
  # specific; null if unparseable, treated as already-expired). posts.imageUrl
  # itself is never modified. Depends on Story 0.33. See sprint-change-
  # proposal-2026-08-25-video-priority-display.md (Architecture Spine AD-12).
  # Status corrected 2026-09-02 via bmad-correct-course: this row was stale --
  # verified via `git show 610cd9b` that this story is genuinely merged to
  # master (commit message confirms independent verification before merge).
  # Found live-unconditional (no consent gate) during this session's impact
  # analysis; see sprint-change-proposal-2026-09-02.md and Story 3-6h below,
  # which closes that gap without reverting this story's own mechanics.
  3-6e-re-host-extracted-event-images-to-durable-storage: done
  # New 2026-08-25 via bmad-correct-course: Event GraphQL resolver computes
  # the served imageUrl per AD-12 Rule 3 -- original while
  # now < imageUrlExpiresAt, else durableImageUrl, else original as a last
  # resort if durableImageUrl isn't populated yet. durableImageUrl exposed as
  # a secondary GraphQL field; EventDetailView/EventImage gain an optional
  # fallback-URL prop so the existing onError handler can retry it. Depends
  # on Story 3-6e. See sprint-change-proposal-2026-08-25-video-priority-
  # display.md.
  # Status corrected 2026-09-02 via bmad-correct-course: this row was stale
  # AND this story was never given an epics.md heading at all (only existed
  # here + its own implementation-artifact file) -- verified via
  # `git log --grep "3-6f"` that Story 3-6f is genuinely merged to master
  # (e872e27, independently verified per its own docs commit 10e3c12).
  # sprint-change-proposal-2026-09-02.md's impact analysis found this
  # resolver's fallback-to-durableImageUrl behavior is unconditional today --
  # a live display-side consent gap, not just 3-6e's storage-side one. See
  # Story 3-6h below.
  3-6f-serve-original-vs-durable-image-per-request: done
  # New 2026-09-02 via bmad-correct-course, sprint-change-proposal-2026-09-02.md:
  # closes the 3-6e/3-6f consent gap. See epics.md Stories 3.6g-3.6k and 3.7c/3.7d
  # notes for full provenance -- all six backlog, in dependency order below.
  # regenerated 2026-09-03 via bmad-create-story: full context engine pass. Epic-3
  # readiness sweep (epic-3-readiness.md, swept 2026-08-09) predates this story
  # (added 2026-09-02), so Gate 1/3 were re-run fresh (Winston persona, no gap) rather
  # than cited stale; Gate 2 (Freya persona) found no UI-complexity gap but did find
  # epics.md's AC4 references a "Moderator Tools account-management surface (Story
  # 3.11/4.7 family)" that does not actually exist -- 3.11 built a PUBLIC
  # unauthenticated account page, and /moderator/items is reactive queues, not an
  # account directory. User confirmed via AskUserQuestion: build a new "Accounts" tab
  # in /moderator/tools (TabbedShell, matching ActorRunsContent's shape) rather than
  # extend Actor Runs or ship the mutation with no UI. Also decided: reuse the
  # existing packages/ui Checkbox for the toggle rather than add a new Radix Switch
  # dependency (no existing precedent for one); new imageStorageOptInSourceEnum
  # pgEnum mirrors defaultLocationChangeSourceEnum exactly; new
  # queryModeratorAccountProfiles list query reuses decodeActorRunCursor and the
  # votedAccountSuggestions ilike-search pattern. See epics.md Story 3.6g note (AC4
  # correction recorded in the story file itself, not epics.md).
  3-6g-add-image-storage-opt-in-flag-and-moderator-only-mutation: review
  # regenerated 2026-09-03 via bmad-create-story: full context engine pass. Epic-3
  # readiness sweep predates this story (added 2026-09-02), so Gate 1/2/3 were re-run
  # fresh (all via runSubagent -- Winston/Freya personas), no gap found on any. Backend-
  # only story: generalizes process-ai-job.ts's existing CURATOR_GUIDE-only rehost gate
  # (Story 3.4o) to all accounts, and adds a required isImageStorageOptedIn param to
  # packages/domain's resolveServedImageUrl (Story 3.6f) wired through 5 resolvers.ts
  # join sites. Flagged (not blocking) sequencing risk: prerequisite Story 3.6g is
  # still tracked in-progress here and its code is uncommitted in the working tree as
  # of this story's creation, despite its own file's Completion Status being fully
  # checked off -- see this story's Pre-Coding Approval Gate. No apps/web/packages/ui
  # change, no DB migration, no new GraphQL API surface.
  3-6h-gate-image-re-hosting-and-serving-on-account-opt-in: review
  # regenerated 2026-09-03 via bmad-create-story: full context engine pass. Epic-3
  # readiness sweep (epic-3-readiness.md, swept 2026-08-09) predates this story (added
  # 2026-09-02), so Gate 1/2/3 were re-run fresh (Winston/Freya personas via
  # runSubagent), no gap found on any. Gate 2 also resolved AC3's scope: EventDetailView
  # never rendered contactInfo at all (even the business case) before this story, so the
  # story now builds both the normal-contact display and the private-contact fallback
  # message together (AC3 revised in place, not absorbed elsewhere). Tracing every
  # consumer of the shared Gemini schema surfaced a second leak: the AI-assisted
  # correction preview (extractEventDataFromUrl/Story 4.2a) can write an undiscarded
  # private contact into events.contactInfo via submitCorrection. User confirmed via
  # AskUserQuestion to extend this story's scope to close it (new AC5) rather than defer
  # it, but declined threading hasPrivateContact through the corrections/submitCorrection
  # round-trip itself (unnecessary to close the leak). See story file's Dev Notes ->
  # Scope Extension Decision.
  3-6i-classify-and-discard-private-contact-info-during-ai-extraction: ready-for-dev
  3-6j-verify-and-guard-against-performer-contact-photo-leakage-in-extraction: ready-for-dev
  3-6k-childrens-data-keyword-filter-tier-1-pre-ingestion-suppression: backlog
  # added 2026-08-10 via bmad-create-story Gate 2 finding (Freya persona, run fresh
  # since epic-3-readiness.md only swept Gate 1/3) while drafting Story 3.7: Feed's
  # planned calendar view would be a third near-byte-for-byte duplicate of
  # week-navigation/status-mapping/schedule-flattening logic already copy-pasted
  # between Discovery's CalendarView.tsx and My Calendar's my-calendar-content.tsx.
  # User confirmed via AskUserQuestion to extract a shared useWeeklyCalendarController
  # hook into packages/ui/src/hooks/ as its own prerequisite story rather than accept
  # a third duplicate. See epics.md Story 3.7a note.
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.3: added AC6 (Monday-start boundary,
  # user-confirmed) and AC7 (arbitrary-week selection, feeds Story 1.3g's week-picker).
  # AC6/AC7 were already correctly implemented once (commit 519f822, 2026-08-14) then
  # silently regressed by a later commit (c0c4912, 2026-08-15, HEAD) that reverted the
  # boundary math back to Sunday-start while keeping the renamed function signatures --
  # remaining work is a restoration, not new implementation (Tasks 7-9 in the story
  # file). Fresh Gate check found WeeklyCalendarView.tsx (Story 1.3g's file) has its
  # own divergent Sunday-start calc, untouched by either commit; user confirmed via
  # AskUserQuestion to keep this story hook-only and flag it as a pre-condition for
  # 1.3g's own reopening rather than expand this story's scope. NOTE: 519f822 also
  # substantially touched files belonging to Stories 1.3f, 2.6, and 1.3g (pass-through
  # wiring and partial week-picker UI) -- their own bmad-create-story passes should
  # verify actual current file state rather than assume ground-up implementation.
  3-7a-extract-shared-weekly-calendar-controller-hook: review
  # regenerated 2026-08-10 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap); Gate 2 run fresh via Freya subagent
  # (real gap: planned FeedCalendarView.tsx would be a third near-duplicate of
  # week-navigation/status-mapping/schedule-flattening logic already copy-pasted
  # between Discovery's CalendarView.tsx and My Calendar's my-calendar-content.tsx --
  # split into prerequisite Story 3.7a rather than landing a third copy; this story's
  # calendar-view task is blocked on 3.7a, card view/backend/i18n are not). Two real
  # tradeoffs resolved with the user via AskUserQuestion: (1) the UX scenario doc's
  # soft-optional per-subscription filter deferred to new Story 3.7b, since epics.md's
  # AC never required it; (2) "events from my subscribed accounts" expressed as a new
  # server-side isFromSubscribedAccount DSL field (mirroring isFavorited/isAddedToCalendar)
  # over a client-composed IN-list, with the user correcting the join path to
  # events.postId -> posts.accountId -> subscriptions (not the fragile
  # sourceSocialMediaAccountId text-match path initially considered). See epics.md
  # Story 3.7 Amendment.
  # Reopened 2026-08-24 via bmad-correct-course: added AC11 -- adopt shared
  # PageContainer primitive (Story 0.30), full-width instead of max-w-7xl.
  # Blocked on Story 0.30. See sprint-change-proposal-2026-08-24-ux-rework-
  # batch.md.
  # DONE 2026-08-24: AC11 merged (Wave 2, story/wave2-page-grid-adoption).
  3-7-display-extracted-events-to-the-user: done
  # added 2026-08-10 via bmad-create-story while drafting Story 3.7: the UX scenario
  # doc (design-artifacts/C-UX-Scenarios/03-alex-discovers-his-feed/03.4-viewing-the-feed.md)
  # softly describes an optional per-subscription filter on the Feed page, but
  # epics.md's original Story 3.7 AC never required it. User confirmed via
  # AskUserQuestion to defer rather than build now or silently drop -- a new
  # reusable subscription-picker component with no second consumer yet. See
  # epics.md Story 3.7b note.
  3-7b-filter-the-feed-page-by-specific-subscribed-account: review
  # New 2026-09-02 via bmad-correct-course, sprint-change-proposal-2026-09-02.md:
  # display-side implementation of Stories 3.6g/3.6h's opt-in gate. Depends on
  # 3.6h shipping first. See epics.md Story 3.7c/3.7d notes. 3.7c's prominent-
  # card visual spec awaits reference card-layout images from the product
  # owner (not yet provided) before bmad-create-story can fully draft it.
  3-7c-event-list-image-display-hotlink-only-with-graceful-degrade-and-opted-in-prominent-card: backlog
  3-7d-event-detail-image-display-oembed-transition-with-fallback: backlog
  3-8-push-notifications-for-extracted-events: review
  3-9-implement-api-key-quota-management: done
  # added 2026-08-11 via bmad-create-story Gate 2 finding while drafting Story 3.9:
  # FR23 (in-app queue status) maps to Epic 3 in the PRD but no story implements it --
  # Story 0.13's Out of Scope anticipated "Story 3.9 (UI-facing aspects)" would own it,
  # but 3.9's AC was later narrowed to backend-only verification tests by the Epic 3
  # readiness sweep's Gate 1 correction, orphaning FR23. User confirmed via
  # AskUserQuestion to split it off rather than re-absorb or silently drop. See
  # epics.md Story 3.9a note.
  # DISSOLVED 2026-08-24 via bmad-correct-course: this standalone page/route is
  # removed. Its API-key-health banner moves into Story 3.1b's tab content and
  # its per-subscription pending-extraction-count badge moves into Story 3.2's
  # tab content, inside the new Account Settings tabbed shell (Story 0.29) --
  # queue-status had no content of its own beyond those two data sources. Status
  # left 'done' (shipped code being relocated, not reverted). See sprint-change-
  # proposal-2026-08-24-ux-rework-batch.md Section 4.2.
  # NOTE 2026-08-24: deliberately excluded from the PageContainer(fullWidth=
  # false)/PageHeader adoption pass (0-30/0-32) despite matching the same
  # max-w-3xl pattern -- this page is already DISSOLVED (see the block above,
  # folded into Story 3.12's tabbed shell). Not worth adopting new primitives
  # into a page being removed; still lives on max-w-3xl mx-auto until 3.12
  # ships.
  3-9a-display-in-app-queue-status-and-api-key-health: done
  # created 2026-08-11 via bmad-create-story: full context engine pass. Gate 1
  # (fresh, since epic-3-readiness.md predates this story) found a real gap --
  # this story needs a new scheduled Lambda with a real SES send grant, but no
  # story provisions one (Story 0.14 predates the need; Story 0.25's SES
  # reconciliation is scoped only to L_API). User confirmed via AskUserQuestion
  # to split it off narrowly as new Story 0.27 rather than build the Lambda/
  # EventBridge/IAM wiring ad hoc inside this story. Gate 2 run fresh via Freya
  # subagent (no gap -- zero UI surface, email copy already shipped by Story
  # 0.15's QUOTA_EXHAUSTION_WARNING template). Gate 3: no gap (Story 0.15 is
  # this story's reserved first caller). De-dup design (new
  # users.lastQuotaWarningEmailSentAt cooldown column) and no-opt-out scope
  # also confirmed via AskUserQuestion. See epics.md Story 3.10 Amendment and
  # Story 0.27 note.
  3-10-email-notifications-for-queued-posts: review
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap, 3.11 explicitly covered); Gate 2 run
  # fresh via Freya subagent (no gap -- all reusable UI, e.g. EventDiscoveryPanel,
  # EventListView, WeeklyCalendarView/useWeeklyCalendarController, already built and
  # already reused >=2 places). Lightweight guard surfaced one real gap: Story 1.6a's
  # EventDetailView account-attribution props (accountName/accountPlatformIconUrl/
  # accountHref) were never wired to real data -- the Event GraphQL type exposed no
  # field carrying the account's platform/displayName/profileImageUrl. User confirmed
  # via AskUserQuestion to absorb the fix into this story (new Event.
  # sourceSocialMediaAccountProfile field resolver, mirroring the existing Event.
  # schedules resolver pattern) rather than split into a new story or leave deferred.
  # No new prerequisite stories split off.
  # Reopened 2026-08-24 via bmad-correct-course: added AC10 -- adopt shared
  # PageContainer primitive (Story 0.30), full-width instead of max-w-7xl.
  # Blocked on Story 0.30. See sprint-change-proposal-2026-08-24-ux-rework-
  # batch.md.
  # DONE 2026-08-24: AC10 merged (Wave 2, story/wave2-page-grid-adoption).
  3-11-view-events-for-a-social-media-account: done
  # added 2026-08-24 via bmad-correct-course: wraps Stories 3.1b (API Keys),
  # 3.2 (Subscribed Accounts), 5.1 (Posts), 2.9 (Notifications) as tab content
  # in Story 0.29's TabbedShell primitive. The Posts tab's content (Story 5.1)
  # already has its own internal per-account tab bar (PRD Section 3.10), so this
  # nests tabs-within-tabs -- user confirmed 2026-08-24 that's acceptable as
  # long as the two levels are visually distinct; resolved via Story 5.1's own
  # reopening (pill-style inner tabs, avatars, label, card containment, mobile
  # horizontal scroll) rather than a separate bmad-ux pass. See sprint-change-
  # proposal-2026-08-24-ux-rework-batch.md Section 4.2.
  # Story drafted 2026-08-25 via bmad-create-story: consolidates API Keys/
  # Subscribed Accounts/Posts/Notifications into one TabbedShell (Story 0.29)
  # instance at /settings/account, absorbs queue-status's two pieces (API-key-
  # health banner, pending-extraction count), fully dissolves Story 3.9a.
  # Extends TabbedShell with a new optional keepMounted-per-tab prop (backward
  # compatible) to fix two real tab-switch state-loss hazards found during
  # creation (Notifications' FCM-registration effect, Posts' inner-account-tab
  # selection) -- resolved via AskUserQuestion with the user before drafting,
  # not a default the dev agent picked unilaterally. EXPERIENCE.md's two-shell
  # IA rewrite (a Gate 2 prerequisite) was applied immediately before this
  # story's creation in the same session. See 3-12-consolidate-account-
  # settings-into-a-tabbed-shell.md.
  3-12-consolidate-account-settings-into-a-tabbed-shell: review
  epic-3-retrospective: optional
  epic-4: in-progress
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.1a explicitly in stories_covered, no gap);
  # Gate 2 run fresh via Freya subagent (no gap, zero UI surface -- Story 4.1 owns the
  # correction form UI). Three real design tradeoffs resolved with the user via
  # AskUserQuestion: (1) proposedData models the full EventInfo.schedules array with
  # id-based update/insert reconciliation (never auto-deleting omitted rows), not a
  # flat single-main-schedule shape; (2) the PRD's schedule/event location consistency
  # check uses case-insensitive substring containment, not exact match; (3)
  # submitCorrection's proposedData argument is a strict ProposedEventCorrectionInput
  # GraphQL input type (reusing the existing EventType/EventCategory enums) rather than
  # a raw JSON scalar, to preserve GraphQL Code Generator's end-to-end type safety for
  # Story 4.1's future frontend -- AJV (Story 0.11) narrows to the residual structural
  # constraints GraphQL's SDL can't express, while the PRD's actual inconsistency
  # checks (date ordering, one-main-schedule, location consistency) are implemented as
  # a pure, 100%-covered packages/domain function. No new prerequisite stories split
  # off (validationErrors is a request-time-computed GraphQL field, not a persisted
  # column, per AC1's exhaustive column list).
  4-1a-build-the-corrections-backend-graphql-api-layer: review
  # added 2026-08-11 via bmad-create-story Gate 2 finding while drafting Story 4.1:
  # the correction form is a confirmed 2-consumer reusable component (Story 4.1's
  # manual entry, Story 4.2's AI-assisted pre-fill extending the same form instance
  # per UX scenario 06.6) with non-trivial states (pre-fill, per-field validation
  # errors, an extension slot for 4.2). User confirmed via AskUserQuestion to split
  # it off rather than build inline in 4.1. See epics.md Story 4.1b note.
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # no gap (lightweight guard -- pure packages/ui component, zero infra footprint,
  # consistent with swept epic-4-readiness.md's 4.1a coverage). Gate 2 run fresh via
  # Freya subagent: no gap (multi-field-type form inline in one component matches
  # LocationRadiusFilter/EventDetailView precedent; no further split warranted). One
  # real design tradeoff resolved with the user via AskUserQuestion: CorrectionForm
  # imports ProposedEventCorrection/ProposedScheduleCorrection type-only from
  # @festgrid/domain/events (first-ever packages/ui -> packages/domain dependency
  # edge) rather than locally mirroring the shape, per the AC's literal type naming
  # and zero-runtime-cost of type-only imports. No new prerequisite stories split off.
  4-1b-build-the-reusable-event-correction-form-component: review
  4-1-manually-correct-event-data: review
  # added 2026-08-11 via bmad-create-story Gate 1 finding (run fresh, since the
  # implementation-detail-level gap postdates epic-4-readiness.md) while drafting
  # Story 4.2: no synchronous, single-URL, correction-shaped extraction capability
  # exists anywhere -- the AI Gateway adapter (0.13) and ScraperAdapter (3.3c) are
  # always invoked today only from the async, queue-driven Story 3.6 pipeline. User
  # confirmed via AskUserQuestion (four rounds) to split this off rather than build
  # inline in 4.2: reuse-first design (dual postUrl/originalPostUrl lookup against
  # posts, mirroring persistScrapedPost's existing dedup logic, before ever
  # attempting a live scrape), auto-detect platform from the URL domain, and run
  # synchronously with a hard timeout rather than async job+polling. See epics.md
  # Story 4.2a note.
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1
  # cited from Story 4.2's own creation (real gap already found and resolved via
  # four AskUserQuestion rounds there); Gate 2 run fresh via Freya subagent (no gap
  # -- zero UI surface, the adjacent trigger UI is fully owned by Story 4.2); Gate 3
  # cited from swept epic-4-readiness.md (4.2 in stories_covered, no gap). Confirmed
  # by direct code read that every reused piece (persistScrapedPost's dedup query,
  # callGemini/AiGatewayExhaustedError, ScraperAdapter/platform-registry, Story
  # 4.1a's ProposedEventCorrection domain interfaces, Story 4.2's own frozen
  # ExtractEventDataFromUrlResult/ExtractionErrorCode contract) exists with a
  # compatible signature. No new prerequisite stories split off.
  4-2a-build-the-on-demand-ai-assisted-correction-extraction-api-layer: done
  4-2-ai-assisted-event-data-correction: review
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.3a explicitly in stories_covered, no gap);
  # Gate 2 run fresh via Freya subagent (no gap, zero UI surface -- reportedEvents given
  # optional status/reason filter args per a UX-doc forward-signal, informing Story 4.7's
  # future filterable Moderator Items list). Two real schema/behavior tradeoffs resolved
  # with the user via AskUserQuestion: (1) reports.status expanded from epics.md's literal
  # 2-value [pending, resolved] to PRD 4.12-aligned [pending, upheld, dismissed] plus a new
  # resolvedByModeratorId FK, mirroring defaultLocationChangeRequests' reviewedByModeratorId
  # audit-trail precedent; (2) isHiddenForCurrentUser broadened from personal-reason-only to
  # any report (any reason), matching PRD 3.9.2's literal per-reason "immediately no longer
  # see the event" text and the dangerous-reason's "remains hidden even after dismissal"
  # clause -- Story 4.8's epics.md note corrected to match. Also corrected epics.md's
  # "joined via buildOptimizedDrizzleSelect" AC text (that utility only selects real table
  # columns, no joins/subqueries possible) to the codebase's actual Event.isFavorited-style
  # field-resolver pattern. No new prerequisite stories split off.
  # Reopened 2026-08-12 via bmad-correct-course: submitReport needs a personal-reason
  # auto-resolve branch (new auto_resolved ReportStatus enum value + migration).
  # Status stays 'review' (additive amendment). See sprint-change-proposal-2026-08-12.md.
  4-3a-build-the-reports-backend-graphql-api-layer-and-personal-visibility-filtering: review
  4-3-report-an-event: review
  # added 2026-08-11 via bmad-create-story Gate 2 finding while drafting Story 4.3:
  # PRD 3.9.2 requires the Report trigger in both list-view and detail-view, but
  # EventCard.tsx has no overflow-menu pattern (unlike EventDetailView, which Story
  # 4.1 already built one into) and no UX artifact specifies the card-level design.
  # User confirmed via AskUserQuestion to scope Story 4.3 to detail-view only and
  # split this off as its own design/implementation pass. See epics.md Story 4.3b note.
  # regenerated 2026-08-15 via bmad-create-story: full context engine pass. Gate 1/3
  # run fresh (this story postdates epic-4-readiness.md's stories_covered list, which
  # does not include 4.3b) -- no gap, Story 4.3/4.3a verified already implemented in
  # the codebase (reports table, submitReport resolver, report-dialog.tsx all exist
  # on disk). Gate 2 run fresh via a one-shot Freya-persona subagent for the overflow-
  # menu placement decision epics.md flagged as this story's own call to make:
  # recommended top-left/always-visible/opposite-corner-from-Favorite placement,
  # adopted directly. Three real tradeoffs resolved with the user via AskUserQuestion:
  # (1) unauthenticated list-view Report clicks reuse the existing login modal
  # (Discovery/Account pages) rather than epics.md's literal /login-redirect text,
  # for consistency with Favorite's established pattern at those exact call sites;
  # (2) post-report list behavior uses immediate, non-reversible removal via a new
  # shared useLocallyHiddenIds hook (packages/ui/src/hooks) rather than a toast-only/
  # wait-for-refetch approach or per-page duplicated state -- deliberately not built
  # on the existing useSoftDeleteWithUndo hook, since that hook's Undo affordance is
  # wrong for a non-reversible action; (3) page-wiring scope is Discovery/Favorites/
  # Feed/Account (4 pages) -- Archive deferred (real EventCard consumer, out of scope
  # by choice), My Calendar out of scope by construction (no EventCard/EventListView
  # usage at all, confirmed via grep). Also confirmed report-dialog.tsx (Story 4.3)
  # is already reusable as-is with no extraction needed, despite epics.md's own
  # speculative "likely requires extracting" framing. No new prerequisite stories
  # split off.
  # cancelled 2026-08-29 via bmad-correct-course: user decision reverses this story's own premise
  # -- reporting now requires viewing an event's detail view first, so a card-level report trigger
  # is deliberately not wanted. PRD 3.9.2's "list-view or detailed view" clause is narrowed to
  # detail-view only. No code was written (was ready-for-dev, unstarted) -- nothing rolled back.
  # Stories 4.3/4.3a/4.3c unaffected. See sprint-change-proposal-2026-08-29.md.
  4-3b-add-a-report-trigger-to-eventcard-list-view: wont-do
  # added 2026-08-12 via bmad-correct-course: wires Story 2.7's default-visibility
  # rule chain to exclude self-reported events from list views (Discovery/Feed/
  # Favorites/My Calendar/search), closing the gap where isHiddenForCurrentUser
  # was only consumed by the event detail view. See sprint-change-proposal-2026-08-12.md.
  # regenerated 2026-08-12 via bmad-create-story: full context engine pass. This story
  # postdates epic-4-readiness.md's 2026-08-11 sweep (not in its stories_covered), so
  # all three gates were run fresh via subagent -- Gate 1 PASS (reuses the existing
  # isFavorited/isAddedToCalendar/isFromSubscribedAccount fieldMap exists() pattern,
  # no new DSL operator/endpoint), Gate 2 PASS (zero UI surface, confirmed against
  # design-artifacts), Gate 3 PASS (reports/buildDefaultEventVisibilityConditions
  # already Epic-4-scoped, no cross-epic promotion needed). No gap found, no new
  # prerequisite story split off. Dependencies (Story 2.7, Story 4.3a) confirmed
  # real shipped code via git log, not just planned.
  4-3c-extend-default-event-visibility-rules-to-exclude-self-reported-events-from-list-views: review
  # created 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.4a explicitly in stories_covered; the
  # sweep's own status-enum->deletedAt and deleteEventPermanently-hard-delete-
  # exception AC corrections are already folded into this story's AC text). Gate 2
  # run fresh via Freya subagent (no gap -- zero UI surface; EXPERIENCE.md's "Soft
  # Delete with Undo" pattern is scoped to user-owned-item deletes, not moderator/
  # automatic soft-delete triggers). Three real implementation-level tradeoffs
  # resolved with the user via AskUserQuestion: (1) proceed with story creation now
  # despite Story 4.3a (reports table/submitReport) still being ready-for-dev/not
  # implemented, gating actual *coding* on 4.3a's completion via a Pre-Coding
  # Approval Gate item rather than delaying story creation; (2) retrofit all 5
  # existing events indexes to partial WHERE deleted_at IS NULL (AD-8 rule 3 /
  # project-context indexing rule) rather than skip indexing or add one low-value
  # standalone index; (3) includeSoftDeleted uses bypass-the-filter semantics
  # (returns both active+soft-deleted, undifferentiated) rather than only-soft-
  # deleted semantics, deferring Story 4.7's exact moderator-list shape to that
  # story's own future creation. No new prerequisite stories split off.
  4-4a-add-soft-delete-to-the-events-table-and-extend-the-events-resolver-and-moderator-mutations: review
  # created 2026-08-11 via bmad-create-story: full context engine pass found this
  # story's epics.md ACs already fully implemented by Stories 4.3/4.3a/4.4a (4.4a's
  # own Dev Notes explicitly flag this). Gate 1/3 cited from swept epic-4-readiness.md
  # (4.4 in stories_covered, no gap); Gate 2 run fresh via Freya subagent (no gap --
  # existing generic Not Found state already covers the only relevant UI surface).
  # User confirmed via AskUserQuestion: story rescoped to verification-only -- a new
  # backend integration test (3-reporter threshold/visibility/restore round trip) and
  # a new E2E test (real Report-dialog flow crossing the threshold), no new production
  # code. Blocked on Stories 4.3/4.3a/4.4a reaching a testable implementation state
  # (Pre-Coding Approval Gate item). No new prerequisite stories split off.
  4-4-handle-event-cancelled-reports: review
  # created 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.5 explicitly in stories_covered, no gap).
  # Gate 2 run fresh via Freya subagent (no gap -- zero UI surface; the "ignore
  # subsequent reports" moderator action UI is Story 4.7's scope per the sole UX
  # artifact 06.7 and Story 4.3a's own Out of Scope note). One real reliability
  # tradeoff resolved with the user via AskUserQuestion: the moderator-alert email
  # dispatch is awaited inside submitReport (errors swallowed internally by a new
  # extracted, deps-injectable send-dangerous-report-moderator-alerts.ts function)
  # rather than matching editAccountDefaultLocation's existing fire-and-forget
  # pattern, to avoid AWS Lambda's execution-context-freeze drop risk on unawaited
  # promises -- editAccountDefaultLocation's own existing fire-and-forget code has
  # the same latent gap, flagged for awareness only, not fixed by this story. No new
  # prerequisite stories split off.
  4-5-handle-dangerous-event-reports: review
  # created 2026-08-12 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.6 explicitly in stories_covered, no gap).
  # Gate 2 run fresh via Freya subagent: no split required (Story 2.2's "author
  # missing empty/loading/error states fresh in-story" precedent applies, matching
  # the same UX-doc-sparseness situation); report list item scoped to apps/web, not
  # packages/ui (Story 4.7's reuse need is speculative -- still backlog, different
  # data/actions -- per the no-premature-abstraction/Story 3.7 precedent). Three real
  # design tradeoffs resolved with the user via AskUserQuestion: (1) myReports is
  # unpaginated (no limit/offset/cursor) -- render the full list directly rather than
  # reopening Story 4.3a's reviewed contract to add pagination for the List
  # Navigation rule; (2) extend packages/ui's existing StatusBadge variant union
  # (active/invalid) additively with pending/upheld/dismissed rather than building a
  # separate report-scoped badge; (3) skip Next/Prev list-context wiring into a
  # report's event detail view (Context-Aware Detail Views rule) since a user's
  # reports reference unrelated events with no meaningful browsing sequence, unlike
  # Favorites/Discovery. No new prerequisite stories split off.
  # Reopened 2026-08-12 via bmad-correct-course: StatusBadge variant union needs
  # the new auto_resolved status (Story 4.3a's 2026-08-12 correction). Status stays
  # 'review' (additive amendment). See sprint-change-proposal-2026-08-12.md.
  4-6-users-reports-page: review
  # added 2026-08-12 via bmad-create-story Gate 2 finding (Freya subagent) while
  # drafting Story 4.7: no page in the codebase handles an authenticated-but-
  # wrong-role visitor (only isAuthenticated->/login exists today); no second
  # moderator-gated page exists anywhere in epics.md yet, so the user was asked
  # via AskUserQuestion whether to split this off now or build it inline in 4.7
  # per the no-premature-abstraction precedent -- user chose to split it off.
  # See epics.md Story 4.7a note.
  4-7a-moderator-route-guard: review
  # regenerated 2026-08-12 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.7 explicitly in stories_covered; only a
  # lightweight guard finding, Event.deletedAt missing from the public schema despite
  # existing since 4.4a -- fixed in-story, Task 1). Gate 2 run fresh via Freya
  # subagent: found a real gap (moderator route-guard, no second consumer anywhere in
  # epics.md yet) -- user chose to split it off despite the sub-2-place reuse bar not
  # being literally met, prioritizing a tested guard for this security boundary. See
  # new Story 4.7a above. Three further real tradeoffs resolved directly with the user
  # via conversation (not a single AskUserQuestion batch): (1) pendingDefaultLocationChanges
  # additionally exposes the linked account's displayName/platform/username/profileImageUrl,
  # not just the AC's literal accountId; (2) the reported-events list was reconsidered
  # mid-session from a flat per-Report list to event-grouped display, where one moderator
  # action (a new resolveReportsForEvent(eventId) mutation) resolves every pending report
  # on that event atomically -- verified against Story 4.4a's actual code that
  # deleteEventPermanently's reports.eventId cascade already removes report rows, so no
  # third "upheld" outcome/argument was needed; (3) confirmed deleteEventPermanently
  # cannot be circumvented via a future re-extraction of the same source post, since
  # posts.isExtracted is a write-once invariant already enforced server-side by the
  # existing extraction pipeline -- no new code needed, but a Forward note was added to
  # Story 5.1a (epics.md) flagging its own future submission mutation must apply the
  # same server-side guard. No DB migration required anywhere in this story.
  # AC amended 2026-08-12 via bmad-correct-course: default reportedEvents query
  # must scope to status: PENDING so auto-resolved personal reports (Story 4.3a's
  # 2026-08-12 correction) never appear in the attention queue. Story is unstarted,
  # so no rework caused. See sprint-change-proposal-2026-08-12.md.
  # Approved 2026-08-25: AC10-15 amendment Pre-Coding Approval Gate signed off by
  # user (all confirmation items + escape-hatch acknowledgment). Hard dependency on
  # Story 3.3b's AC15-19 amendment is cleared -- 3.3b merged to master (c134f0f),
  # independently verified. bmad-dev-story cleared to proceed on Tasks 13-17.
  # Extended 2026-08-29 (commit a0fc985, sprint-change-proposal-2026-08-28.md Items
  # 1-2, FR94-96): pending-change list now shows AWAITING_APPROVAL items (new
  # approve/reject actions) alongside existing PENDING_REVIEW accept/revert; new
  # moderatorPendingItemCount-driven badge (CountBadge, 60s poll) next to this
  # page's user-menu entry and on the navbar avatar. Direct implementation, not
  # routed through bmad-create-story -- epics.md/FR backfill only.
  4-7-moderator-items-page: review
  # added 2026-08-24 via bmad-correct-course: new tabbed shell (Story 0.29)
  # wrapping Stories 3-4k (actor-runs) and 3-4i (unprocessed-payloads), reusing
  # Story 4.7a's route guard as-is. /moderator/items (this story) stays a
  # separate, standalone page -- user decision, does not join this shell. See
  # sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.2.
  # Story drafted 2026-08-25 via bmad-create-story: consolidates Actor Runs/
  # Unprocessed Payloads into one TabbedShell (Story 0.29) instance at
  # /moderator/tools, reusing Story 4.7a's useRequireModerator() guard as-is
  # (no new shared layout). /moderator/items stays untouched, separate. No
  # AskUserQuestion needed (unlike sibling 3.12) -- Gate 3 confirmed the only
  # candidate tab-switch state-loss tradeoff here is lower-stakes than 3.12's
  # Notifications hazard, so it's documented as accepted rather than escalated.
  # See 4-7b-moderator-tools-tabbed-shell.md.
  4-7b-moderator-tools-tabbed-shell: review
  # Reopened 2026-08-24 via bmad-correct-course (sprint-change-proposal-2026-08-24-
  # moderator-location-override.md, AD-11): moderator-items-content.tsx's pending-
  # location-changes list gets AccountLocationField (see 3-3b's reopened note and
  # design-artifacts/UX-festgrid-run-1/EXPERIENCE.md Component Patterns > Account
  # Location Field) rendered per row alongside the existing handleResolveLocationChange
  # ACCEPT/REVERT actions -- a third, additive "correct it directly" action, not a
  # replacement. Also fixes a real live gap: this story's own original design assumed
  # 3-3b's stacked-PENDING_REVIEW-rows forward note would be deduped here, but no
  # dedupe logic was ever actually built (confirmed by reading the file directly) --
  # AD-11's supersede-on-any-write rule fixes this at the source instead. Depends on
  # 3-3b's amendment landing first (shared editAccountDefaultLocation auth/logic).
  # bmad-create-story (2026-08-24): amended with AC10-15 (AccountLocationField +
  # SetDefaultLocationDialog wired into pending-location-change-row.tsx/moderator-
  # items-content.tsx as a third additive action alongside Accept/Revert; mutual-
  # exclusion guard; distinguishable per-row accessible names). Hard implementation
  # dependency on 3-3b's AC15-19 landing first, confirmed by direct code search
  # (changeSource/SUPERSEDED/AccountLocationField do not exist in code yet). A real
  # discrepancy was found in 3-3b's own spec during drafting (AC19 claims mutation
  # semantics are decided by page, but its Task 2 code checks role alone) -- flagged
  # as 3-3b's own item to fix, not silently absorbed here; does not affect this
  # story's own correctness since /moderator/items is fully role-gated already.
  # 4-7-moderator-items-page scope extended to include DefaultLocationChangeRequest review (see epics.md)
  # added 2026-08-06 via bmad-create-story at user's request while drafting Story 2.7:
  # 2.7 broadens past-event hiding to a global default and needs an escape hatch so users
  # don't lose access to their own favorited/calendar/subscribed events. Depends on
  # Story 2.7, Story 3.1a, Story 4.3a, Story 4.4a. See epics.md Story 4.8 note.
  # regenerated 2026-08-12 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.8 explicitly in stories_covered, no gap --
  # resolver-level owner-scoped bypass built in-scope, mirroring Story 3.7's
  # isFromSubscribedAccount precedent). Gate 2 run fresh via Freya subagent: no split
  # required (single new consumer of already-built EventListView/EventCard/StatusBadge
  # patterns). Lightweight guard surfaced two real gaps the epic-wide sweep predated:
  # (1) AC1(a)'s premise -- isHiddenForCurrentUser was never wired into Query.events'
  # default filtering -- was already independently caught and split off via
  # bmad-correct-course as Story 4-3c (in-progress) before this pass completed; 4.8 now
  # depends on 4-3c and reuses its isReportedByCurrentUser fieldMap entry rather than
  # duplicating the fix; (2) Query.event/eventBySlug unconditionally exclude
  # soft-deleted events with no owner bypass, which would 404 an Archive card's detail
  # link even for the owner -- user confirmed via AskUserQuestion to fix in-story with a
  # strictly per-row personal-connection-verified bypass (never a blanket bypass). A
  # third tradeoff (whether to add an unhide/withdraw-report action for the
  # "hidden by me" reason) was resolved via AskUserQuestion: deferred to post-MVP: this
  # story ships Archive as read-only for all three hide reasons. Pre-Coding Approval
  # Gate item added confirming Story 4-3c reaches real implemented code (not just
  # planned) before this story's Task 1 reuse step begins, and confirming no collision
  # with Story 4.7's own in-flight Event.deletedAt schema addition.
  # Reopened 2026-08-24 via bmad-correct-course: added AC8 -- adopt shared
  # PageContainer primitive (Story 0.30), full-width instead of max-w-7xl.
  # Blocked on Story 0.30. See sprint-change-proposal-2026-08-24-ux-rework-
  # batch.md.
  # DONE 2026-08-24: AC8 merged (Wave 2, story/wave2-page-grid-adoption).
  # Added 2026-09-03 via bmad-create-story, split off Story 3-4n's own creation
  # (Gate 2/UX finding): /moderator/items (Story 4.7) hard-codes exactly two list
  # types today (reportedEvents, pendingDefaultLocationChanges) with no generic
  # plug-in mechanism -- AWAITING_APPROVAL account-type classifications (3-4n) need
  # a third, structurally distinct list/resolve shape. Lettered off Story 4.7 (the
  # page's owner), not off 3-4n. See epics.md Story 4.7c.
  4-7c-review-queue-for-pending-account-type-classifications: backlog
  4-8-view-archived-hidden-personal-events: done
  epic-4-retrospective: optional
  epic-5: in-progress
  5-1a-build-the-manual-post-selection-and-extraction-graphql-api-layer: review
  5-1b-build-the-reusable-postcard-component: review
  # Reopened 2026-08-24 via bmad-correct-course: resolves Story 3.12's Posts-tab
  # nesting question (nested tabs OK per user, but need clear visual separation
  # from Shell A's outer tabs). posts-select-content.tsx's account-tab row
  # (currently `border-b-2` underline buttons, same visual language as a
  # top-level Tabs bar) changes to: (1) rounded pill/chip style instead of
  # underline, (2) each pill gets the account's profileImageUrl as a small
  # avatar (already fetched for PostCard's publisher prop, just not rendered on
  # the tab itself), (3) a text label ("Posts from:") added above the row --
  # currently no heading there at all, (4) the row + its content wrapped in the
  # existing `card` token (rounded-lg shadow-md p-4, DESIGN.md) so it reads as a
  # box inside the Posts tab, (5) mobile-only: `flex-wrap` -> `flex-nowrap
  # overflow-x-auto` (single scrolling row instead of wrapping to multiple rows,
  # `sm:flex-wrap sm:overflow-visible` above the mobile breakpoint). No new
  # primitives needed. See sprint-change-proposal-2026-08-24-ux-rework-batch.md
  # Section 4.2.
  # UPDATE 2026-08-24 (same day): the above is now written into the story file
  # as real AC11 (superseding this comment-only description) plus new AC12 --
  # adopt PageContainer (Story 0.30), unify the 3 inline PostCard grids onto
  # the standard column progression (drops the sm: step). Both blocked on
  # their respective prerequisite stories landing first.
  # UPDATE 2026-08-24 (later same day): AC12's grid-unification half now
  # composes GridContainer (Story 0.31, baseCols=1 colsStep=1) instead of a
  # hand-written literal className. Also blocked on Story 0.31.
  # DONE 2026-08-24: AC12 merged (Wave 2, story/wave2-page-grid-adoption).
  # CORRECTED 2026-08-29: the "AC11 NOT part of this wave -- still open" note
  # above was stale. AC11 (pill-style per-account tabs, avatars, "Posts from:"
  # label) was implemented and merged 2026-08-26 (commit 9868049) -- the
  # story file's own Task 6 checklist and Completion Notes already reflect
  # this, only this tracking line lagged. A separate, earlier, independent
  # attempt at the same AC11 (branch story/5-1-ac11-nested-tabs, worktree
  # gemini-story-5-1) was left unmerged and stale; confirmed superseded by
  # 9868049 (not an ancestor of it) and cleaned up rather than merged.
  5-1-manual-post-selection-screen: review
  5-2-select-posts-for-extraction: review
  # Reopened 2026-08-24 via bmad-correct-course: added AC6 -- SummaryBar's
  # fixed bottom-0 z-50 shares the exact screen position (and a higher
  # z-index) of AppShell's mobile bottom tab bar, and renders unconditionally
  # on /posts/select -- traps mobile users, they cannot tap Discover/Feed/
  # Favorites/Calendar/Log In to leave the page. Confirmed via sweep as the
  # ONLY fixed+bottom-0 offender in the app (AppShell's own nav bar and
  # UserMenu's mobile sheet are legitimate). Fix: bottom-14 md:bottom-0
  # instead of bottom-0 (ux-rework-2026-08-24.md item #15, a second note
  # appended after the original file was archived). AC1-AC5 unaffected.
  # DONE 2026-08-24: AC6 merged (Wave 2, story/wave2-page-grid-adoption).
  5-3-display-and-enforce-api-quota: done
  5-4-inactive-account-warning: review
  # Reopened 2026-08-24 via bmad-correct-course: wizard steps extended with a
  # push-notification opt-in step (reusing Story 2.9's toggle), exitPath changed
  # to /feed. See sprint-change-proposal-2026-08-24-ux-rework-batch.md Section 4.6.
  # Full AC6-7 backfilled 2026-08-25 via bmad-create-story -- only a reopening
  # comment existed before. Also found AC1-5 (this story's ENTIRE original
  # scope) already implemented despite the story file's own header saying "Not
  # started" -- confirmed via direct code inspection (wizard-registry.ts,
  # posts-select-content.tsx both already correct).
  5-5-integrate-manual-post-selection-into-the-getting-started-wizard: review
  # added 2026-08-19 via bmad-create-story at user request: "Scrape Posts" button on
  # /posts/select for both the zero-posts (reuses Story 3.1/3.2's initial-scrape
  # cascade) and has-posts (newerThan = MAX(posts.publishedAt)) cases. Postdates
  # epic-5-readiness.md's 2026-08-12 sweep (not in stories_covered) -- Gate 1/2/3 run
  # fresh via subagent per the lightweight escape-hatch guard, all three: no gap.
  # Extracts the trigger cascade currently inlined once in subscribeToAccount
  # (Story 3.1a) into a shared triggerScrapeForAccount function. Verified live via
  # direct code reads that Story 3.4a's Bright Data CDK wiring (webhook route,
  # consolidated Lambda) landed this morning (commit 6411e8c, 2026-08-19) -- the
  # sprint-status note below on 3-4a describing an unwired route is now stale.
  # See epics.md Story 5.6.
  5-6-on-demand-scraping-trigger-for-manual-post-selection: review
  epic-5-retrospective: optional
  # Epic 6 added 2026-08-08 via bmad-prd (Sections 3.13/3.14, FR69-FR86) + direct
  # epics.md append (bmad-create-epics-and-stories skipped -- its Step 1 overwrites
  # the whole file from template, unsuitable for appending one epic to an
  # already-detailed backlog; same reasoning as the 2026-08-02 3.3b amendment).
  # Story ordering and the 6.1a/6.6a/6.7a backend/middleware prerequisites came from
  # an epic-readiness sweep (Gate 1 + Gate 3, epic-readiness/epic-6-readiness.md),
  # which also amended Story 3.3c to add ScraperAdapter.lookupAccountProfile --
  # a capability Story 3.4's 2026-08-07 Forward note had already flagged as needed
  # by Story 3.1/3.2 but left unbuilt; Story 6.1 is its second consumer.
  epic-6: in-progress
  6-1a-build-the-account-vote-backend-graphql-api-layer: review
  6-1-vote-for-a-social-media-account: review
  6-2-view-the-ranked-vote-list: review
  6-3-withdraw-a-vote: review
  6-4-autocomplete-voted-accounts-when-subscribing: review
  # added 2026-08-08 during PRD/epics.md revision: widget model changed from
  # stateless URL query params to a persisted entity (own id, editable post-
  # creation, embeds auto-update). See epics.md Story 6.5a note.
  6-5a-build-the-widget-config-backend-graphql-api-layer: review
  # Reopened 2026-08-24 via bmad-correct-course: added AC8 -- adopt
  # PageContainer(fullWidth=false) + PageHeader (icon+label action, plus a
  # description subtitle -- this page is the only one with one today). Also
  # surfaced a pre-existing i18n gap: this page's title/description/button
  # label are hardcoded English, now must move to a WidgetsSettingsPage
  # locale namespace (en/id). Blocked on Stories 0.30 (AC7)/0.32.
  # Wave 4 (2026-08-24): AC8 delivered via Cline, independently verified
  # (diff-scoped, lint clean, i18n keys correctly added to both en/id.json,
  # action + description preserved). No dedicated test file for this page
  # (pre-existing gap, not introduced here). Merged.
  6-5-configure-and-generate-a-widget-embed: done
  6-6a-build-the-embed-domain-backend-graphql-api-layer: review
  6-6-register-and-manage-embed-domains: review
  6-7a-build-the-widgets-dynamic-frame-ancestors-csp-middleware: review
  6-7-public-widget-rendering-page: review
  epic-6-retrospective: optional
  # Epic 7 added 2026-08-29: PRD Section 3.15 (AI Prompt-Based Custom Event Filter,
  # EventFilterInput/AIEventFilter interfaces 4.18/4.19) was specced via bmad-spec/
  # bmad-prd on 2026-08-28 but never backfilled into epics.md -- flagged as a pending
  # follow-up in the PRD's own .memlog.md. Backfilled directly (FR98-103, epics.md
  # Story 7.1a-7.5) per user's explicit request to skip bmad-create-epics-and-stories
  # (its Step 1 overwrites epics.md wholesale), matching the established precedent for
  # Epic 6's own direct append. Nothing implemented yet -- all five stories backlog.
  epic-7: done
  7-1a-build-the-shared-eventfilterinput-query-layer: done
  7-2a-build-the-ai-event-filter-backend-graphql-api-layer: done
  7-3-build-the-reusable-ai-filter-summary-renderer: done
  7-4-add-the-ai-filter-prompt-entry-point-to-filterhub: done
  7-5-my-ai-filters-list-page: done
  epic-7-retrospective: done
  # New 2026-09-02 via bmad-correct-course, sprint-change-proposal-2026-09-02.md:
  # placeholder only, per explicit user direction -- no stories drafted yet.
  # See epics.md Epic 8 entry for what this covers and why it's separate from
  # Epic 3's Stories 3.6g/3.6h (moderator-only opt-in, shipping now).
  epic-8: backlog

action_items:
  - id: correct-course-2026-09-02-1
    description: "Vendor confirmation: get Apify and Bright Data to confirm in writing that their Instagram scraping actors/datasets operate logged-out against public data only -- the favorable Meta v. Bright Data legal comparison the product currently relies on depends on this being true, not just structurally plausible from the adapter code. See monetization-plans/scraping-extraction-display-rules-2026-09-02.md §1.1/§5 item 8."
    owner: shulha
    status: open
    created_at: "2026-09-02"
  - id: retro-7-1
    description: "Refine AI Prompts: Monitor user behavior with AI filters to tune the system prompt for better date/location extraction accuracy."
    owner: Alice
    status: open
    created_at: "2026-08-29"
  - id: retro-7-2
    description: "Performance Monitoring: Keep an eye on Gemini API response times during FilterHub prompt resolutions."
    owner: Charlie
    status: open
    created_at: "2026-08-29"
