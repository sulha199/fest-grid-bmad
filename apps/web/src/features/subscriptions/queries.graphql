query getMySubscriptions {
  mySubscriptions {
    id
    accountId
    isNewlyAdded
    createdAt
    pendingExtractionCount
    account {
      id
      platform
      displayName
      username
      profileImageUrl
      defaultLocation {
        coordinates {
          lat
          lng
        }
        formattedAddress
        placeName
      }
      hasPendingDefaultLocationReview
    }
  }
}
