query getMySubscriptions {
  mySubscriptions {
    id
    accountId
    isNewlyAdded
    createdAt
    account {
      id
      platform
      displayName
      username
      profileImageUrl
      defaultLocation {
        coordinates {
          lat
          lng
        }
        formattedAddress
        placeName
      }
    }
  }
}
