# UX Rework Punch List — 2026-08-24 (source notes)

Raw intake notes, originally dropped at the project root as `ux-rework.md`, that seeded a `bmad-help` review and then `bmad-correct-course` triage on the same day. Moved here per `_bmad-output/`'s designated-location convention (`CLAUDE.md` §1) now that triage is complete — kept as the primary source rather than deleted, since the full change proposal paraphrases/expands these but this is the original wording.

Item numbers below match the line numbers of the original file and are the same numbers used throughout the conversation and in [`sprint-change-proposal-2026-08-24-ux-rework-batch.md`](sprint-change-proposal-2026-08-24-ux-rework-batch.md) — kept stable rather than renumbered, so existing cross-references (that proposal, `sprint-status.yaml`'s reopened-comment trail) still resolve.

| # | Original note | Disposition | Tracked in |
|---|---|---|---|
| 1 | "root main page container should be wide consuming the space" | ⚠️ **Not yet triaged** — fell through the initial analysis pass, caught only while archiving this file. Reads as a layout/CSS fix (the main page container's `max-width` is likely too narrow). | — none yet |
| 2 | "location filter in filter-hub should be more compact and suitable for inline" | Resolved — compact/inline `FilterHub` variant | Proposal §4.7 → Story 2.5 (reopened) |
| 3 | "wizard on subscription should include the post selection and enabling post notification. End of the route will return to feed" | Resolved — wizard steps extended, `exitPath` → `/feed` | Proposal §4.6 → Story 5.5 (reopened) |
| 4 | "rather than having separate menu for api keys, subscribed-accounts, posts, push notification -> tabbed-page like wizard page" | Resolved — two-shell tabbed IA | Proposal §4.2 → Story 0.29 (new primitive), 3.12 (new) |
| 5 | "scrapper runs and unprocessed payloads should also be in the tabbed page" | Resolved — separate moderator-tools shell | Proposal §4.2 → Story 4.7b (new) |
| 6 | "when adding a location, user is mandatory to set default location if they are not set" | **Dropped** — user chose to prioritize #14's AI-inference approach instead of a mandatory UI step | — superseded by #14 |
| 7 | "when autocomplete popup returns empty row, user cannot close the autocomplete popup" | Plain bug — recommended for direct `bmad-create-story`, **not yet created** | `LocationPickerField` (Story 3-3d) |
| 8 | "in location input(non-map-mode), selecting address from autocomplete doesnt select the location" | Plain bug, same component as #7 — recommended for direct `bmad-create-story`, **not yet created** | `LocationPickerField` (Story 3-3d) |
| 9 | "in the wizard page, the bottom bar is rendered on top of sidebar" | Plain bug — recommended for direct `bmad-create-story`, **not yet created** | Wizard primitive (Story 0-24) |
| 10 | "event-list card-mode: current card (1 col mobile) + pinterest-like one (2 cols mobile)" | Resolved — masonry view-mode, reference screenshot reviewed | Proposal §4.4 → Stories 1.3b/1.3d (reopened) |
| 11 | "calendar-view in mobile: display days vertically, dont show empty days. Questions for multi-days-spanning schedule" | Partially resolved — vertical/skip-empty-days is scoped; multi-day-span rendering still has no design | Proposal §4.8 → Story 1.3g (reopened, partial) |
| 12 | "disable twitter source" | Resolved — hidden from new subscriptions and votes, existing subscriptions unaffected | Proposal §4.3 → Story 3.2 (reopened) |
| 14 | "every time a post is scrapped... use it to update the account data. If no default location is set, use the subscriber's gemini api key to find the location detail based on the user profile" | Resolved — backfill + AI inference from scraped post/account metadata (not the subscriber's saved location, per PRD §5's Location Data Reuse Boundary), with a narrow platform-funded system-key fallback | Proposal §4.1 → Story 3.4m (new), 3.3c/0.13 (reopened) |

**Also surfaced during triage, not in the original list:** aggregate favorite/like count on cards and calendar (Proposal §4.5 → Story 2.1a/1.3b/1.3g), the same-week-relative/>7-days-absolute date display rule (Proposal §4.9 → Story 1.3b), and the Posts-tab nested-tabs styling resolution (Proposal §4.2 → Story 5.1, reopened).
