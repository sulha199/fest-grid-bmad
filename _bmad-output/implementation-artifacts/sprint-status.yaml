# generated: 2026-07-29T02:58:30Z
# last_updated: 2026-08-12T18:52:00Z
# project: festgrid
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-29T02:58:30Z
last_updated: 2026-08-16T21:45:00Z
project: festgrid
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-0: in-progress
  0-1-initialize-pnpm-monorepo: done
  0-2-configure-typescript-and-eslint-for-the-monorepo: done
  0-3-set-up-shadcn-ui-and-configure-themes: done
  0-4-set-up-drizzle-orm-for-schema-migrations: review
  0-5-set-up-ci-cd-pipeline-with-github-actions: review
  0-6-set-up-i18n-foundation-next-intl: review
  # reset from review to ready-for-dev 2026-08-05 — revised AC/Tasks against a new
  # UX spec supersede the original implementation; see the story file's Revision note.
  0-7-build-the-global-app-shell-and-navigation-layout: review
  # added 2026-08-05 via bmad-create-story Gate 2 finding while revising 0.7's nav
  # pattern against the new UX spec (design-artifacts/UX-festgrid-run-1) — see
  # 0.7's Architecture & UX Gate Findings for detail.
  0-7a-nav-item-primitive: review
  0-8-set-up-graphql-server-scaffold-code-generator-pipeline-and-the-optimized-select-query-utility: review
  0-9-set-up-state-management-foundation: review
  0-10-set-up-testing-frameworks-foundation: review
  0-11-set-up-runtime-schema-validation: review
  0-12-set-up-firebase-cloud-messaging-foundation: review
  0-13-set-up-ai-gateway-adapter-layer-for-gemini: review
  0-14-set-up-aws-iac-for-lambda-sqs-eventbridge-and-kms: review
  0-15-set-up-outbound-email-adapter: done
  # added 2026-08-08 via bmad-create-story: user-requested dev-experience addendum
  # to already-done Story 0.15 (not a Gate 1/2/3 finding) — single-story split per
  # story-split-gate.md's numbering rule. See epics.md Story 0.15a.
  0-15a-local-dev-email-adapter-stub: review
  # regenerated 2026-08-03 via bmad-create-story against Geoapify-based epics.md text
  # (Sprint Change Proposal 2026-08-03); Postgres-backed no-TTL cache and the API-key
  # restriction MVP limitation were confirmed with the user during story creation.
  0-16-set-up-geolocation-adapter-with-caching-layer: review
  0-17-set-up-graphql-authenticated-context-layer: review
  # Reopened 2026-08-06 via bmad-correct-course: AC2-4/hook contract revised
  # (commit-on-unmount -> commit-at-trigger/undo-reverses/onExpire-on-timeout).
  # See sprint-change-proposal-2026-08-06.md.
  0-18-build-the-reusable-soft-delete-with-undo-ui-primitive: review
  0-19-build-the-reusable-swipe-to-reveal-action-ui-primitive: review
  # SUPERSEDED 2026-08-04 via bmad-create-story: fully absorbed by Story 0.16's
  # regenerated Task 3 (Geoapify provider swap, Sprint Change Proposal 2026-08-03),
  # which now creates this exact geolocation_cache table. Not built independently;
  # tracked to completion via 0-16 instead. See epics.md Story 0.20 note.
  0-20-create-geolocation-cache-database-table: superseded-by-0.16
  0-21-set-up-fcm-device-token-registry: review
  # New 2026-08-06 via bmad-correct-course: AD-8 rule 2 ("enforced once, never
  # per-resolver") reviewer-gate finding -- 6 hand-written isNull() call sites,
  # no shared enforcement point. See sprint-change-proposal-2026-08-06.md.
  0-22-build-the-shared-active-rows-query-filter-helper-for-ad-8: review
  # added 2026-08-07 via bmad-create-story Gate 1/Gate 3 finding while drafting
  # Story 2.10: Story 0.15 (email adapter) is unimplemented and has no generic
  # system/developer-error template, and the reportSystemError mechanism itself
  # is cross-cutting (future stories will also want to alert on client failures).
  # See epics.md Story 0.23 note.
  0-23-build-the-system-error-reporting-and-alerting-foundation: review
  # added 2026-08-07 via bmad-create-story Gate 3 (lightweight guard) finding while
  # drafting Story 3.1: design-artifacts/UX-wizard-page-run-1 specs a generic /wizard
  # page (EXPERIENCE.md status: final) with a confirmed second, cross-epic consumer
  # (Story 5.5), but no story owns building it; the epic-3-readiness.md sweep didn't
  # evaluate this UI-shaped gap. Registry design revised from URL-query-param steps
  # to a code-defined wizard registry per user decision, for i18n/generateMetadata
  # compatibility. See epics.md Story 0.24 note.
  0-24-build-the-reusable-wizard-page-primitive: review
  # added 2026-08-07 via bmad-create-story Gate 1 finding while drafting Story 0.23:
  # no backend env var (not just 0.23's new SYSTEM_ERROR_ALERT_EMAIL) is wired into a
  # deployed Lambda's environment config, since Story 0.14's L_API Lambda resource
  # doesn't exist in IaC yet. User confirmed via AskUserQuestion to split this off
  # rather than accept it as an implicit Story 0.14 forward dependency. See epics.md
  # Story 0.25 note and Story 0.23's Architecture & UX Gate Findings.
  0-25-wire-backend-environment-variables-into-the-deployed-api-lambda-s-iac-configuration: ready-for-dev
  # added 2026-08-07 via bmad-correct-course: every route-page's Suspense boundary
  # (fallback-less since Story 1.9) needs a shared, on-brand loading fallback instead
  # of a blank flash, including the event-detail modal's bounded dialog content.
  # See sprint-change-proposal-2026-08-07.md.
  0-26-build-the-reusable-routeloader-component: review
  # added 2026-08-11 via bmad-create-story Gate 1 finding while drafting Story 3.10:
  # a new scheduled Lambda needs a real SES send grant, but no story provisions one --
  # Story 0.14 predates this need and Story 0.25's SES reconciliation is scoped only
  # to L_API. User confirmed via AskUserQuestion to split this off narrowly rather
  # than fold it into Story 3.10. See epics.md Story 0.27 note.
  0-27-provision-the-notifier-lambda-s-infrastructure-and-ses-send-permission: backlog
  # added 2026-08-15 via bmad-create-story Gate 3 finding while reopening Story 1.3g
  # for AC13 (sprint-change-proposal-2026-08-13-discovery-detail-calendar-ux.md
  # Section 4.4, AD-9): packages/ui has no shadcn/Radix setup at all, but AD-9
  # mandates packages/ui/src/core/WeekPicker.tsx wrap shadcn Popover+Calendar --
  # packages/ui cannot depend on apps/web's own shadcn output. Same gap blocks
  # Story 1.5's FilterHub popover too. User confirmed via AskUserQuestion to split
  # into a new Epic 0 story rather than absorb into either 1.3g or 1.5. See
  # epics.md Story 0.28 note.
  0-28-set-up-shadcn-ui-component-generation-for-packages-ui: review
  epic-0-retrospective: optional
  epic-1: in-progress
  1-1-create-initial-database-tables: done
  1-2-seed-database-with-mock-data: done
  1-2a-create-posts-table-and-link-seeded-events-to-their-source-post: done
  1-3a-build-the-events-backend-graphql-api-layer: done
  1-3b-build-the-reusable-eventcard-component: done
  1-3c-build-the-reusable-infinite-scroll-hook: done
  # added 2026-08-05 via bmad-create-story at user's request: retroactive extraction —
  # home-content.tsx (this story's sibling 1.3) and favorites-content.tsx (Story 2.2) were
  # found to duplicate an identical loading/error/empty/grid/infinite-scroll shell below
  # their SearchBar/FilterHub row. Positioned as a 1.3-family lettered suffix per user
  # decision (see story Dev Notes → Retroactive Extraction Rationale).
  1-3d-build-the-reusable-eventlistview-component: review
  # added 2026-08-05 via bmad-create-story at user's request: retroactive extraction —
  # home-content.tsx and favorites-content.tsx still duplicate the SearchBar/FilterHub
  # row above the shell Story 1.3d already extracted. Positioned as a 1.3-family
  # lettered suffix per the 1.3d precedent; see epics.md Story 1.3e note.
  1-3e-build-the-reusable-eventdiscoverypanel-component: review
  # added 2026-08-05 via bmad-create-story Gate 2 finding while drafting Story 1.3f —
  # DESIGN.md's calendar tokens already encode a shared discovery_view/personal_view
  # component split, and Story 2.6 is a confirmed second consumer. See epics.md
  # Story 1.3g note.
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.4: added AC13 (manual week-picker,
  # AD-9). A prior partial bmad-quick-dev attempt (519f822) added a functional but
  # AD-9-non-compliant native-input stopgap -- Tasks 13-16 replace it with the
  # sanctioned WeekPicker.tsx (Button+Popover+Calendar) and fix a divergent
  # Sunday-start boundary calc flagged during Story 3.7a's own reopening. Task 13/14
  # are BLOCKED on new prerequisite Story 0.28 (packages/ui shadcn/Radix setup, a
  # Gate 3 finding also needed by Story 1.5 -- not absorbed into this story per
  # AskUserQuestion). Depends on updated to add Story 3.7a, Story 0.28.
  1-3g-build-the-reusable-weeklycalendarview-component: review
  # added 2026-08-05 via bmad-create-story Gate 1 finding while drafting Story 1.3f —
  # the events resolver's main-schedule-only join can't correctly date-range-filter
  # sub-schedules; needs its own EXISTS-subquery DSL field + GiST index migration.
  # See epics.md Story 1.3h note.
  1-3h-extend-the-events-graphql-api-with-schedule-level-date-range-query-support: review
  # added 2026-08-05 via bmad-create-story: Gate 3-shaped gap surfaced while drafting
  # Story 1.3e — Story 1.3d's Out-of-Scope note incorrectly assumed "Calendar View is
  # Epic 2 Story 2.6"; 2.6 is actually the separate /my-calendar personal page. The
  # Discovery-page Card/Calendar view-switcher named in 01-event-list-view.md has no
  # owning story. See epics.md Story 1.3f note. Narrowed to integration-only scope
  # 2026-08-05 after Gate 2 (-> 1.3g) and Gate 1 (-> 1.3h) findings split off the
  # primitive and backend query capability respectively.
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.5: added AC8 (week-picker pass-through).
  # Verified via direct code inspection that Tasks 1-9 are already implemented despite
  # this story file's own stale Completion Status/Dev Agent Record (never filled in by
  # whatever process actually built it) -- onSelectWeek/label pass-through is already
  # shipped in CalendarView.tsx; only a new getWeekRange prop is outstanding (Task 11,
  # blocked on Story 1.3g's Task 14-15).
  1-3f-build-the-discovery-weekly-calendar-view-and-view-switcher: review
  1-3-display-a-list-of-events-on-the-main-page: done
  1-4-search-for-events: done
  1-5a-build-the-reusable-multiselect-component: done
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.1: added AC9 (compact dropdown
  # presentation) and AC10 (per-facet clear + row layout). Confirmed via git show
  # --stat that no prior partial bmad-quick-dev attempt touched FilterHub.tsx --
  # genuine clean slate, unlike the calendar-chain stories. Tasks 7-9 blocked on
  # Story 0.28 (packages/ui shadcn/Radix setup, shared with Story 1.3g's WeekPicker,
  # not re-split here) plus this story's own shadcn badge addition (single-consumer,
  # added directly in this story rather than expanding 0.28's shared scope).
  1-5-filter-events-by-type-and-category: review
  1-6a-build-the-reusable-event-detail-view-component: done
  1-6b-build-the-context-aware-list-navigation-hook: done
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.2: added AC13 (Carousel-chrome
  # navigation) and AC14 (icon-only modal close). Verified via git show that AC14
  # is already correctly implemented (failed bmad-quick-dev run's final commit,
  # c0c4912) but AC13's Carousel chrome is not -- scoped as Task 15. Unlike
  # Stories 1.3g/1.5, NOT blocked on Story 0.28: EventDetailWrapper.tsx is an
  # apps/web file with its own already-existing shadcn setup.
  # 2026-08-16: Task 15/AC13 implemented (Carousel chrome), then amended at user
  # request to a 3-slide peek-preview window + real swipe gesture (beyond the
  # original single-slide text) -- also fixed a duplicate modal close button found
  # during that work. Same-day bmad-correct-course pass
  # (sprint-change-proposal-2026-08-16-detail-carousel-ux-fixes.md) fixed two UX
  # regressions from manual testing: peek/active slide size mismatch (EventPreviewCard
  # restructured to mirror EventDetailView's own loading-skeleton shape) and the
  # route-level RouteLoader incorrectly showing on in-app Next/Previous navigation
  # (adjacent slugs now prefetched via router.prefetch()). Status stays in-progress
  # pending the remaining Definition of Done items (backend/E2E test pass, final
  # Dev Agent Record fill-in).
  1-6-view-event-details: in-progress
  1-7a-build-the-reusable-blockingloader-component: review
  1-7-user-signup-and-login-with-google: review
  1-8-setup-posthog-analytics: review
  1-9-dynamic-browser-title-and-meta-tags: review
  epic-1-retrospective: optional
  epic-2: in-progress
  2-1a-build-the-favorites-and-calendar-additions-backend-graphql-api-layer: review
  2-1-favorite-an-event: review
  2-1b-build-the-ics-route-handler-and-generator-utility: review
  2-2-view-favorited-events: review
  # Reopened 2026-08-06 via bmad-correct-course: AC5/AC6 revised (hard delete ->
  # AD-8 soft delete via new SoftDeleteAction-param mutation contract), Task 1b
  # (deletedAt migration) added. See sprint-change-proposal-2026-08-06.md.
  2-3a-build-the-saved-locations-backend-graphql-api-layer: review
  2-3b-extend-the-geolocation-adapter-and-saved-locations-api-with-address-autocomplete-support: review
  # regenerated 2026-08-04 via bmad-create-story at user's request: re-verified against
  # current codebase state (Story 2.3b now fully drafted with a matching contract minus a
  # dropped sessionToken arg; Story 0.16 progressed to review/implemented, narrowing the
  # dependency chain to 2.3a/2.3b only). Status unchanged (already ready-for-dev).
  # AC9-11 revised 2026-08-06 via bmad-correct-course: delete now commits
  # immediately (DELETE action) instead of on unmount; Undo now calls a real
  # RESTORE mutation; timeout splices from local list, no unmount trigger.
  # New AC11a (delete-call failure path). See sprint-change-proposal-2026-08-06.md.
  2-3-manage-saved-locations: review
  # Reopened 2026-08-06 via bmad-correct-course: AC2/AC5-AC8 revised, AC18-AC20 added
  # (shared map-picker continuity state across search/current-location/map-pick, an
  # embedded search inside the map sheet, zoom control + contextual initial zoom).
  # See sprint-change-proposal-2026-08-06-map-picker-continuity.md.
  2-4-set-location-by-current-location-or-map: review
  # regenerated 2026-08-04 via bmad-create-story: raw maplibre-gl (not
  # @vis.gl/react-maplibre) and a pointer-only, documented a11y limitation for
  # marker placement were confirmed with the user during story creation.
  # AC12/AC13 added 2026-08-06 via bmad-correct-course: showZoomControl and
  # onViewStateChange props, needed by Story 2.4's map-picker-continuity amendment.
  # See sprint-change-proposal-2026-08-06-map-picker-continuity.md.
  2-4a-set-up-frontend-map-integration-and-reusable-map-component: review
  # drafted 2026-08-04 via bmad-create-story: Gate 1 gap surfaced during Story 2.4's
  # creation (see epics.md Story 2.4b note) — no query exposes reverse-geocoding as
  # a standalone read; Story 2.4's address-preview flow needs one. No dependency on
  # Story 2.3a/2.3b (Story 0.16/0.17, this story's only real deps, are both `review`).
  # AC8/AC9 added 2026-08-06 via bmad-correct-course: previewLocation now also accepts
  # a placeId as an alternative to latitude/longitude. See
  # sprint-change-proposal-2026-08-06-map-picker-continuity.md.
  2-4b-extend-the-geolocation-adapter-with-a-reverse-geocode-preview-query: review
  # regenerated 2026-08-05 via bmad-create-story: full context engine pass with two
  # user-confirmed design decisions (Haversine+scalar-column geo storage; full recursive
  # multi-locationPreferenceId DSL support). Gate 1/3 cited from swept epic-2-readiness.md
  # (this story is itself that report's identified gap-fill). Gate 2 found no in-scope UI
  # gap; left a forward note for Story 2.5's own creation (Discovery radius-control design).
  # AC1a added 2026-08-06 via bmad-create-story while drafting Story 2.5: broadened
  # withinRadius to accept ad-hoc { latitude, longitude, radiusKm } coordinates (not
  # just a saved locationPreferenceId), needed by 2.5's no-saved-location fallback.
  # Not yet implemented, so amended in place rather than splitting a new story.
  2-5a-extend-the-events-graphql-api-with-geo-distance-query-support: review
  2-5-find-nearby-events: review
  # added 2026-08-06 via bmad-create-story Gate 2/escape-hatch finding while drafting
  # Story 2.6 — no story wires the "Add to Calendar" trigger UI (flagged but deferred
  # by epic-2-readiness.md's "Anticipated Gate 2 note" and Stories 2.1/2.1b's own
  # Out-of-Scope notes). See 2.6's Architecture & UX Gate Findings for detail.
  2-6b-wire-the-add-to-calendar-trigger-dialog-and-ics-export: review
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.5: added AC9 (week-picker pass-through).
  # Verified via direct code inspection that Tasks 1-8 are genuinely already
  # implemented (this story's own Completion Notes/File List, unlike 1.3f's, were
  # accurately filled in) -- onSelectWeek/label pass-through is already shipped in
  # my-calendar-content.tsx; only a new getWeekRange prop is outstanding (Task 9,
  # blocked on Story 1.3g's Task 14-15), identical to Story 1.3f's Task 11.
  2-6-view-and-manage-events-on-a-calendar: review
  2-6a-create-user-settings-table-and-settings-query-mutation-resolvers: review
  # regenerated 2026-08-06 via bmad-create-story: user broadened scope from
  # personal-lists-only to a global default across every event view, requested a
  # reusable/extensible hiding mechanism (Story 1.3h's overlaps operator extended
  # with an open-ended to:null), and requested a new Archive page escape hatch
  # (Story 4.8, Epic 4). All confirmed via AskUserQuestion; epics.md AC revised.
  2-7-automatically-hide-past-events: review
  2-8-user-menu: done
  2-9-manage-push-notification-settings: review
  2-10-service-worker-lifecycle-updates-and-database-self-healing: review
  epic-2-retrospective: optional
  epic-3: in-progress
  3-1a-create-social-media-account-profiles-table: done
  # regenerated 2026-08-07 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md; lightweight guard surfaced two real gaps the
  # sweep couldn't anticipate (both UI/design-artifact-shaped) -- split new Story 0.24
  # (generic /wizard primitive, confirmed cross-epic reuse via Story 5.5) and added a
  # forward-note to Story 3.2 (/settings/subscriptions page gap). User confirmed via
  # AskUserQuestion: this story owns the subscribeToAccount mutation (3.2 will reuse
  # it), the subscribe step is manual-entry only (no live scrape validation -- Story
  # 3.4 forward-noted), and the wizard registry is code-defined (not URL-query-param)
  # for i18n/generateMetadata compatibility. Depends on: 3.1a, 3.1b, 0.24 (none done
  # yet -- sequencing confirmation required before implementation, see story's
  # Pre-Coding Approval Gate).
  3-1-onboarding-wizard-for-api-key-and-subscriptions: review
  # regenerated 2026-08-07 via bmad-create-story: scope expanded from view+revoke-only
  # to also own key creation (createApiKey), folded in at user's request after the UX
  # scenario doc (04.1-manage-api-keys.md) showed an add-flow on this same page. Story
  # 3.1's onboarding wizard now reuses this story's createApiKey mutation instead of
  # building its own. Depends-on updated: adds Story 0.13/0.14, drops Story 3.1 (no
  # longer needed — this story is now self-sufficient for create+revoke). Cross-amended
  # Story 0.13's fetchCandidateKeys task with an activeOnly(apiKeys) filter requirement.
  # See epics.md Story 3.1b's Amendment note.
  3-1b-manage-and-revoke-api-keys: review
  # regenerated 2026-08-07 via bmad-create-story: full context engine pass. Scope
  # expanded from add-only to also own the full /settings/subscriptions list+remove
  # view (mySubscriptions query, removeSubscription mutation), resolving this
  # story's own Forward note (added while drafting Story 3.1) that flagged Story
  # 5.1a's original ownership of those two as a backward Epic-3-on-Epic-5 dependency.
  # Confirmed via AskUserQuestion. Story 5.1a's epics.md entry amended to extend
  # this story's mySubscriptions with isInactive instead of rebuilding it, and to
  # reuse removeSubscription as-is. Verified subscriptions.is_newly_added already
  # exists in the DB (Story 3.1a's actual migration 0013) despite epics.md's AC
  # text for 3.1a never listing it -- a doc-only gap, not a schema gap.
  3-2-subscribe-to-a-social-media-account: review
  # added 2026-08-08 via bmad-create-story Gate 2 finding while drafting Story 3.3:
  # the user confirmed (AskUserQuestion) reusing the full Saved-Locations-style
  # address-autocomplete/current-location/map-pick experience for the default-
  # location field, which is currently embedded, unextracted, inside
  # location-form-dialog.tsx (Stories 2.3/2.4). See epics.md Story 3.3d note.
  3-3d-build-the-reusable-locationpickerfield-component: review
  3-3-set-a-default-location-for-a-subscription: review
  # regenerated 2026-08-08 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap); Gate 2 determined directly (zero UI
  # scope). Data Type Compatibility audit found posts.post_url was nullable/non-unique
  # despite the PRD/shared-types Post interface requiring it, and packages/shared-types'
  # Post interface was missing accountId/publishedAt -- both fixed in this story. A
  # dedup design decision (unique constraint + select-or-insert vs. blind insert
  # accepting duplicates) was confirmed with the user via AskUserQuestion: dedupe on
  # post_url, mirroring Story 3.1a's subscribeToAccount() precedent.
  3-3a-create-posts-table-and-persist-scraped-posts: review
  # regenerated 2026-08-08 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap; explicitly validates this story's new
  # default_location_change_requests table against Story 4.7's forward dependency);
  # Gate 2 run fresh, no gap. Lightweight guard surfaced three items resolved with the
  # user via AskUserQuestion: (1) extend Story 3.3's existing SetDefaultLocationDialog
  # with an edit mode rather than build EXPERIENCE.md's literal in-table form; (2)
  # absorb the "Pending Review" badge (EXPERIENCE.md state pattern) into this story
  # rather than leave it a gap; (3) allow stacked concurrent edits (new
  # PENDING_REVIEW row per edit, no server-side block) with a Forward Note binding
  # Story 4.7 to dedupe by accountId to the latest row when it's built. No new
  # prerequisite stories split off.
  3-3b-edit-an-accounts-default-location: review
  # regenerated 2026-08-08 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (this story is itself that sweep's own Gate 3
  # finding); Gate 2 run fresh, no gap (zero UI surface). User confirmed via
  # AskUserQuestion: new packages/domain/src/scraper/ folder (not folded into
  # subscriptions/); Twitter's platform slug is 'x'; and the registry also owns a
  # getPlatformDisplayName map, consolidating three duplicated inline platform-name
  # ternaries in apps/web (onboarding-subscribe-step.tsx, subscribe-account-dialog.tsx,
  # subscriptions-content.tsx) -- an initial Grep-tool read of those files looked like
  # a "Twitter\X" escape-sequence bug, corrected after a direct Read confirmed no bug,
  # only duplication. No new prerequisite stories split off.
  3-3c-define-the-scraper-adapter-interface-and-platform-slug-registry: review
  3-4-scrape-new-posts-from-subscribed-accounts: review
  # added 2026-08-08 via bmad-create-story Gate 1 split while drafting Story 3.4:
  # making Bright Data the scheduled-batch's priority vendor (cheaper at scale,
  # async-only) needs a new job-tracking table, a public webhook endpoint, and a
  # stale-job fallback sweep -- a genuinely new architectural layer, not an
  # incremental adapter addition. See epics.md Story 3.4a note.
  # WARNING 2026-08-10 (RESOLVED same day): initial Bright Data reply said they
  # "no longer provide IPs for Social media account management usecase" --
  # risked invalidating this story's premise. Follow-up clarified this meant
  # their Proxy/IP product, not the Web Scraper/Dataset API -- Bright Data
  # confirmed "we can support you collecting Social media data from our
  # Webscraper APIs and Filter APIs... post URL or Profile URL as input."
  # Story is UNBLOCKED for bmad-create-story 3-4a. Remaining open question
  # (capability, not policy): confirm profile-URL input returns recent POSTS
  # (discovery) vs only profile metadata/single-post lookup -- verify with a
  # real test call before committing to the async job-tracking/webhook design.
  # UPDATE 2026-08-15 (bmad-create-story): a full story file already existed
  # (drafted 2026-08-09, one day before the ON HOLD note froze it) but this
  # status key was never synced to match its own ready-for-dev header. User
  # gave the new explicit decision to reopen the Bright Data avenue (epics.md
  # Story 3.4a REOPENED note); re-verified the story's technical premises
  # directly against current code (usage-store.ts, instagram-adapter.ts,
  # get-scrape-targets.ts, subscribe-to-account.ts, scraper.ts lambda, CDK
  # stack) -- no drift found, content unchanged. Status synced to ready-for-dev.
  # The profile-URL discovery-vs-metadata capability question above remains an
  # accepted, tracked gap to close during implementation (needs a real
  # authenticated Bright Data call, not resolvable via static research).
  3-4a-add-brightdata-as-the-priority-scraping-vendor-for-the-scheduled-batch: ready-for-dev
  # added 2026-08-08 via bmad-create-story while drafting Story 3.4: BYOK-pooled
  # scraper-vendor keys, researched and found legally ambiguous for both Apify
  # (anti-sublicensing clause) and Bright Data (KYC/declared-use-case vetting).
  # LEGALLY GATED -- do not move to ready-for-dev until written vendor
  # confirmation is obtained (see the story file's outreach email drafts).
  # UPDATE 2026-08-10: Bright Data replied -- KYC is for registered business
  # entities only, not individual freelancers, which appears to rule out
  # individual end-users contributing a Bright Data key regardless of the
  # sublicensing question. Bright Data BYOK looks closed; Apify still pending.
  3-4b-byok-pooled-scraper-vendor-keys: backlog
  # added 2026-08-09 via bmad-create-story: exploratory outreach to Instagram-
  # viewer sites (storiesig.info explicitly invites API-access contact; imginn.com
  # is the right shape but has no advertised partnership channel, cold outreach
  # only; mollygram has no scraping/API prohibition but is likely the wrong
  # shape). EXPLORATORY -- outreach only, no adapter code until a concrete offer
  # exists.
  3-4c-explore-whitelisted-access-with-instagram-viewer-sites: backlog
  # added 2026-08-14 via bmad-help/architect analysis of docs/assets/Apify actor
  # costing and facts.md; corrected same day. None of the four actors evaluated
  # publish a run-duration SLA, and two synchronous call sites (getPostByUrl,
  # lookupAccountProfile) exist today with only one (getPostByUrl) having a local
  # timeout. Recommends picking the sync-path actor empirically (cost + latency +
  # success rate across all four candidates) and adding the missing timeout to
  # lookupAccountProfile; batch path (Story 3.4a) unaffected. CORRECTION: the
  # app-funded key is the project owner's own account -- owner is open to
  # third-party/community actors there for best outcome (accepts Apify Actor
  # Terms Sec 4.4 Creator-access exposure knowingly). Inverse of Story 3.4b's
  # future BYOK path, which is restricted to Apify-maintained actors only since
  # contributed keys would belong to individual community members, not the owner.
  # UPDATE 2026-08-14: user added real per-actor run durations to the costing doc.
  # Single-sample results: apify/instagram-api-scraper 31s, apify/instagram-post-
  # scraper 1m15s (worst!), sones/instagram-posts-scraper-lowcost 5s (best), fast-
  # instagram-post-scraper 7s. Both Apify-maintained durations already EXCEED the
  # current 20s getPostByUrl timeout -- likely a live production issue today, not
  # just a future risk. Priority raised accordingly; still backlog pending a fuller
  # sample (Task 1b) and a real bmad-create-story pass.
  # UPDATE 2026-08-14 (user request): scope broadened to getNewestPosts too, since
  # all 3 adapter methods share one callApifyActor call today -- a sync-path-only
  # swap would silently change the batch actor as well. Confirmed duplication is
  # already DB-safe (persist-scraped-post.ts onConflictDoNothing on postUrl)
  # regardless of actor, so the real batch question is filter cost-efficiency: does
  # a cutoff matching zero new posts actually bill zero items (Story 3.4 AC4), or
  # does the actor charge for filtered-out items like fast-instagram-post-scraper's
  # sample hinted at? New Task 1c tests this across all four actors before
  # getNewestPosts gets its own actor constant, decided independently of Task 1b.
  # UPDATE 2026-08-14: Task 1c fully complete (all 12 real Apify test runs against
  # pakuwonmall.jogja, including a re-run of Run 6 which initially used the wrong
  # cutoff by mistake). Two confirmed bugs disqualify two of the four candidates
  # for getNewestPosts: apify/instagram-api-scraper (current prod actor) leaks all
  # 3 pinned posts past its onlyPostsNewerThan cutoff, even at a true live
  # zero-boundary cutoff; sones/instagram-posts-scraper-lowcost never filters
  # server-side, returns is_newer_than_cutoff:false items regardless of cutoff.
  # apify/instagram-post-scraper passed every scenario cleanly and is the FINAL
  # CONFIRMED getNewestPosts actor pick. fast-instagram-post-scraper remains a
  # validated fallback (correct data, but bills a nonzero fee even on a correct
  # 0-item result). Still backlog pending a real bmad-create-story pass to turn
  # this into an implementation-ready story.
  # UPDATE 2026-08-15: Task 1b fully complete too (all 8 runs, real data). Found a
  # MAJOR actor-agnostic correctness bug: an invalid post URL/handle returns a
  # truthy 1-item array {"error":"not_found",...} from BOTH apify/instagram-api-
  # scraper and apify/instagram-post-scraper -- not an empty array. The existing
  # items.length===0 check never catches this. getPostByUrl silently returns an
  # empty-content post instead of SCRAPE_FAILED; lookupAccountProfile is worse --
  # its fallback chain lands on item.username (the invalid handle itself echoed
  # back), so castVote silently inserts a fake SocialMediaAccountProfile row for a
  # nonexistent account and reports success. See story Dev Notes "Task 1b
  # Conclusions" and 3-4d-task1b-runs/.
  # UPDATE 2026-08-15 (Sprint Change Proposal, sprint-change-proposal-2026-08-15-
  # not-found-detection-bug.md): the fix (originally AC6/Task 6 here) is SPLIT OUT
  # to new Story 3.4e, since it's a confirmed live data-integrity bug (Story 6.1a
  # is already `review` status and ships the fabrication bug) versus this story's
  # remaining scope being a non-urgent cost/reliability optimization. AC6/Task 6
  # removed from this story's active scope -- see 3-4e.
  # UPDATE 2026-08-15 (bmad-create-story full pass): Tasks 2-6 (actor-constant
  # extraction, lookupAccountProfile timeout via new ApifyRequestTimeoutError,
  # castVote resolver wiring, test fixes) now have full task-level detail --
  # ready for bmad-dev-story or bmad-quick-dev.
  3-4d-per-use-case-actor-selection-and-sync-path-timeout: ready-for-dev
  # NEW 2026-08-15, split off 3-4d via Sprint Change Proposal (see that story's
  # Amendment 4 and epics.md Story 3.4e). Fixes a confirmed, already-live
  # data-integrity bug: getPostByUrl/lookupAccountProfile treat Apify's truthy
  # {"error":"not_found",...} response as valid data instead of null, causing
  # castVote (Story 6.1a, already `review` status) to silently insert a fabricated
  # SocialMediaAccountProfile row for a garbled/nonexistent handle -- a direct
  # violation of Story 6.1's own written AC ("never from placeholder handle text")
  # and PRD SS3.13. Small, fully-specified fix from real test evidence
  # (3-4d-task1b-runs/run-02/-04/-06/-08) -- elevated priority, candidate for
  # bmad-quick-dev ahead of the normal backlog->create-story->dev-story queue.
  # 2026-08-15: bmad-create-story pass complete -- full story file created
  # (3-4e-fix-not-found-detection-in-the-instagram-scraper-adapter.md), ready
  # for bmad-dev-story or bmad-quick-dev per the story's own priority note.
  3-4e-fix-not-found-detection-in-the-instagram-scraper-adapter: ready-for-dev
  # NEW 2026-08-17, added via bmad-correct-course at the user's explicit request
  # ("Add as a 3rd split-off story now"), folded into the same sprint-change-
  # proposal-2026-08-15-not-found-detection-bug.md document as 3-4d/3-4e (that
  # file is being retitled/extended to cover all three). Two findings: (1)
  # confirmed via direct read of apps/infrastructure/lib/festgrid-backend-
  # stack.ts that sharedLambdaProps (and every individual Lambda) sets no
  # `timeout` -- apiLambda/scraperLambda/aiProcessorLambda/ingestorLambda all
  # silently inherit AWS's 3-second default today, an already-live bug
  # independent of the async-handling question below; (2) user asked whether
  # scheduled-batch scraping using Apify (which can run minutes to ~1hr per
  # Apify's own actor page) was anticipated, then clarified the intended
  # multi-vendor split: brightdata only for scheduled batch, apify for
  # scheduled batch (fallback) + vote-time profile check + subscribe-time --
  # "for scheduled batch we would have same long process but different
  # mechanism." Confirmed directly against Apify's own API docs that ad-hoc
  # per-run webhooks (base64 `webhooks` query param) and GET
  # /v2/actor-runs/{runId} status polling are both available, so the
  # getNewestPosts role can get the same async job+webhook+sweep treatment
  # Story 3.4a already built for Bright Data. getPostByUrl/lookupAccountProfile
  # (Story 3.4d's sync paths) are explicitly unaffected. Not yet run through
  # bmad-create-story -- backlog until that pass and until the consolidated
  # Sprint Change Proposal (now covering 3.4d/3.4e/3.4f) receives final user
  # approval.
  3-4f-fix-missing-lambda-timeouts-and-extend-async-job-handling-to-apifys-long-running-scrape-paths: backlog
  3-5-add-new-posts-to-a-processing-queue: review
  # regenerated 2026-08-10 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap); Gate 2 run fresh (no gap, zero UI
  # surface). Lightweight guard surfaced three real gaps the sweep couldn't have
  # anticipated, all resolved with the user via AskUserQuestion: (1) PRD FR33
  # (timezone inference) had no owning story anywhere in Epic 3 -- Tier 1
  # (location-based, via the existing Geolocation adapter) absorbed into this
  # story's own AC6, Tiers 2-3 split into new Story 3.6a; (2) aiProcessorLambda's
  # CDK environment was missing DATABASE_URL/DATA_INGESTION_QUEUE_URL/
  # GEOAPIFY_API_KEY and its SqsEventSource had no reportBatchItemFailures --
  # wired in-scope since this story is the first real caller inside that Lambda
  # (Story 3.4 precedent); (3) apiLambda was separately missing GEOAPIFY_API_KEY
  # despite being a documented Geoapify caller since the 2.4b range -- fixed as a
  # user-confirmed one-line drive-by in the same file/variable. Also decided:
  # reportBatchItemFailures: true (diverging from scraper.ts's no-partial-retry
  # precedent) so a quota-exhausted post's message isn't silently lost forever;
  # and the EventInfo.location-required vs. AC5's "left absent" conflict is kept
  # decoupled (ExtractedEventMessage.location stays optional) and forward-noted to
  # Story 3.6b rather than resolved now. See epics.md Story 3.6a note.
  3-6-process-posts-from-the-queue-and-extract-event-information: review
  # added 2026-08-10 via bmad-create-story Gate 3-shaped lightweight-guard finding
  # while drafting Story 3.6: PRD FR33's Tier 2 (subscribing user's own timezone)
  # and Tier 3 (flag for manual clarification) have no owning story or data-model
  # concept anywhere in the codebase. See epics.md Story 3.6a note.
  3-6a-infer-event-timezone-from-subscriber-context-and-flag-ambiguous-cases-for-clarification: review
  # regenerated 2026-08-10 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (3.6b explicitly in stories_covered, no gap);
  # lightweight guard confirmed Story 3.6a's post-sweep schedules.timezone/timezoneStatus
  # dependency raises no new Gate 1/3 gap. Gate 2 run fresh via subagent (no gap, zero UI
  # surface). Two real tradeoffs resolved with the user via AskUserQuestion: (1) the
  # absent-location conflict Story 3.6 forward-noted here -- resolved by inserting a
  # fixed "Location not specified" placeholder into events.location rather than
  # migrating the column to nullable or skipping the event; (2) Story 3.6a was still
  # ready-for-dev/not-started when this story's creation began -- user initially chose
  # to hold off, then confirmed 3.6a's real implementation had landed (commit 3cf81df,
  # now review) mid-session, so creation proceeded against verified real code. Also
  # decided: idempotent insert via a new events.postId unique constraint +
  # onConflictDoNothing (not a select-then-insert check), and
  # reportBatchItemFailures: true on ingestorLambda's SqsEventSource, mirroring
  # aiProcessorLambda's own precedent. See epics.md Story 3.6b Dev Notes.
  3-6b-ingest-processed-events-into-the-database: review
  # added 2026-08-10 via bmad-create-story Gate 3 finding while drafting Story 3.6a:
  # users.timezone (added by 3.6a to match PRD 4.8's User.timezone) has no capture
  # mechanism anywhere in the codebase -- without this story FR33 Tier 2 can never
  # fire in practice. See epics.md Story 3.6c note.
  3-6c-capture-and-store-the-subscribing-users-timezone: ready-for-dev
  # added 2026-08-10 via bmad-create-story Gate 3 finding while drafting Story 3.6a:
  # schedules.timezoneStatus = 'NEEDS_CLARIFICATION' (3.6a) is written but nothing
  # displays/resolves it yet. Depends on 3.6b and 3.7 both existing first. See
  # epics.md Story 3.6d note.
  3-6d-surface-schedules-flagged-as-needing-timezone-clarification: ready-for-dev
  # added 2026-08-10 via bmad-create-story Gate 2 finding (Freya persona, run fresh
  # since epic-3-readiness.md only swept Gate 1/3) while drafting Story 3.7: Feed's
  # planned calendar view would be a third near-byte-for-byte duplicate of
  # week-navigation/status-mapping/schedule-flattening logic already copy-pasted
  # between Discovery's CalendarView.tsx and My Calendar's my-calendar-content.tsx.
  # User confirmed via AskUserQuestion to extract a shared useWeeklyCalendarController
  # hook into packages/ui/src/hooks/ as its own prerequisite story rather than accept
  # a third duplicate. See epics.md Story 3.7a note.
  # reopened 2026-08-15 via bmad-create-story per sprint-change-proposal-2026-08-13-
  # discovery-detail-calendar-ux.md Section 4.3: added AC6 (Monday-start boundary,
  # user-confirmed) and AC7 (arbitrary-week selection, feeds Story 1.3g's week-picker).
  # AC6/AC7 were already correctly implemented once (commit 519f822, 2026-08-14) then
  # silently regressed by a later commit (c0c4912, 2026-08-15, HEAD) that reverted the
  # boundary math back to Sunday-start while keeping the renamed function signatures --
  # remaining work is a restoration, not new implementation (Tasks 7-9 in the story
  # file). Fresh Gate check found WeeklyCalendarView.tsx (Story 1.3g's file) has its
  # own divergent Sunday-start calc, untouched by either commit; user confirmed via
  # AskUserQuestion to keep this story hook-only and flag it as a pre-condition for
  # 1.3g's own reopening rather than expand this story's scope. NOTE: 519f822 also
  # substantially touched files belonging to Stories 1.3f, 2.6, and 1.3g (pass-through
  # wiring and partial week-picker UI) -- their own bmad-create-story passes should
  # verify actual current file state rather than assume ground-up implementation.
  3-7a-extract-shared-weekly-calendar-controller-hook: review
  # regenerated 2026-08-10 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap); Gate 2 run fresh via Freya subagent
  # (real gap: planned FeedCalendarView.tsx would be a third near-duplicate of
  # week-navigation/status-mapping/schedule-flattening logic already copy-pasted
  # between Discovery's CalendarView.tsx and My Calendar's my-calendar-content.tsx --
  # split into prerequisite Story 3.7a rather than landing a third copy; this story's
  # calendar-view task is blocked on 3.7a, card view/backend/i18n are not). Two real
  # tradeoffs resolved with the user via AskUserQuestion: (1) the UX scenario doc's
  # soft-optional per-subscription filter deferred to new Story 3.7b, since epics.md's
  # AC never required it; (2) "events from my subscribed accounts" expressed as a new
  # server-side isFromSubscribedAccount DSL field (mirroring isFavorited/isAddedToCalendar)
  # over a client-composed IN-list, with the user correcting the join path to
  # events.postId -> posts.accountId -> subscriptions (not the fragile
  # sourceSocialMediaAccountId text-match path initially considered). See epics.md
  # Story 3.7 Amendment.
  3-7-display-extracted-events-to-the-user: review
  # added 2026-08-10 via bmad-create-story while drafting Story 3.7: the UX scenario
  # doc (design-artifacts/C-UX-Scenarios/03-alex-discovers-his-feed/03.4-viewing-the-feed.md)
  # softly describes an optional per-subscription filter on the Feed page, but
  # epics.md's original Story 3.7 AC never required it. User confirmed via
  # AskUserQuestion to defer rather than build now or silently drop -- a new
  # reusable subscription-picker component with no second consumer yet. See
  # epics.md Story 3.7b note.
  3-7b-filter-the-feed-page-by-specific-subscribed-account: review
  3-8-push-notifications-for-extracted-events: review
  3-9-implement-api-key-quota-management: done
  # added 2026-08-11 via bmad-create-story Gate 2 finding while drafting Story 3.9:
  # FR23 (in-app queue status) maps to Epic 3 in the PRD but no story implements it --
  # Story 0.13's Out of Scope anticipated "Story 3.9 (UI-facing aspects)" would own it,
  # but 3.9's AC was later narrowed to backend-only verification tests by the Epic 3
  # readiness sweep's Gate 1 correction, orphaning FR23. User confirmed via
  # AskUserQuestion to split it off rather than re-absorb or silently drop. See
  # epics.md Story 3.9a note.
  3-9a-display-in-app-queue-status-and-api-key-health: done
  # created 2026-08-11 via bmad-create-story: full context engine pass. Gate 1
  # (fresh, since epic-3-readiness.md predates this story) found a real gap --
  # this story needs a new scheduled Lambda with a real SES send grant, but no
  # story provisions one (Story 0.14 predates the need; Story 0.25's SES
  # reconciliation is scoped only to L_API). User confirmed via AskUserQuestion
  # to split it off narrowly as new Story 0.27 rather than build the Lambda/
  # EventBridge/IAM wiring ad hoc inside this story. Gate 2 run fresh via Freya
  # subagent (no gap -- zero UI surface, email copy already shipped by Story
  # 0.15's QUOTA_EXHAUSTION_WARNING template). Gate 3: no gap (Story 0.15 is
  # this story's reserved first caller). De-dup design (new
  # users.lastQuotaWarningEmailSentAt cooldown column) and no-opt-out scope
  # also confirmed via AskUserQuestion. See epics.md Story 3.10 Amendment and
  # Story 0.27 note.
  3-10-email-notifications-for-queued-posts: review
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-3-readiness.md (no gap, 3.11 explicitly covered); Gate 2 run
  # fresh via Freya subagent (no gap -- all reusable UI, e.g. EventDiscoveryPanel,
  # EventListView, WeeklyCalendarView/useWeeklyCalendarController, already built and
  # already reused >=2 places). Lightweight guard surfaced one real gap: Story 1.6a's
  # EventDetailView account-attribution props (accountName/accountPlatformIconUrl/
  # accountHref) were never wired to real data -- the Event GraphQL type exposed no
  # field carrying the account's platform/displayName/profileImageUrl. User confirmed
  # via AskUserQuestion to absorb the fix into this story (new Event.
  # sourceSocialMediaAccountProfile field resolver, mirroring the existing Event.
  # schedules resolver pattern) rather than split into a new story or leave deferred.
  # No new prerequisite stories split off.
  3-11-view-events-for-a-social-media-account: review
  epic-3-retrospective: optional
  epic-4: in-progress
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.1a explicitly in stories_covered, no gap);
  # Gate 2 run fresh via Freya subagent (no gap, zero UI surface -- Story 4.1 owns the
  # correction form UI). Three real design tradeoffs resolved with the user via
  # AskUserQuestion: (1) proposedData models the full EventInfo.schedules array with
  # id-based update/insert reconciliation (never auto-deleting omitted rows), not a
  # flat single-main-schedule shape; (2) the PRD's schedule/event location consistency
  # check uses case-insensitive substring containment, not exact match; (3)
  # submitCorrection's proposedData argument is a strict ProposedEventCorrectionInput
  # GraphQL input type (reusing the existing EventType/EventCategory enums) rather than
  # a raw JSON scalar, to preserve GraphQL Code Generator's end-to-end type safety for
  # Story 4.1's future frontend -- AJV (Story 0.11) narrows to the residual structural
  # constraints GraphQL's SDL can't express, while the PRD's actual inconsistency
  # checks (date ordering, one-main-schedule, location consistency) are implemented as
  # a pure, 100%-covered packages/domain function. No new prerequisite stories split
  # off (validationErrors is a request-time-computed GraphQL field, not a persisted
  # column, per AC1's exhaustive column list).
  4-1a-build-the-corrections-backend-graphql-api-layer: review
  # added 2026-08-11 via bmad-create-story Gate 2 finding while drafting Story 4.1:
  # the correction form is a confirmed 2-consumer reusable component (Story 4.1's
  # manual entry, Story 4.2's AI-assisted pre-fill extending the same form instance
  # per UX scenario 06.6) with non-trivial states (pre-fill, per-field validation
  # errors, an extension slot for 4.2). User confirmed via AskUserQuestion to split
  # it off rather than build inline in 4.1. See epics.md Story 4.1b note.
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # no gap (lightweight guard -- pure packages/ui component, zero infra footprint,
  # consistent with swept epic-4-readiness.md's 4.1a coverage). Gate 2 run fresh via
  # Freya subagent: no gap (multi-field-type form inline in one component matches
  # LocationRadiusFilter/EventDetailView precedent; no further split warranted). One
  # real design tradeoff resolved with the user via AskUserQuestion: CorrectionForm
  # imports ProposedEventCorrection/ProposedScheduleCorrection type-only from
  # @festgrid/domain/events (first-ever packages/ui -> packages/domain dependency
  # edge) rather than locally mirroring the shape, per the AC's literal type naming
  # and zero-runtime-cost of type-only imports. No new prerequisite stories split off.
  4-1b-build-the-reusable-event-correction-form-component: review
  4-1-manually-correct-event-data: review
  # added 2026-08-11 via bmad-create-story Gate 1 finding (run fresh, since the
  # implementation-detail-level gap postdates epic-4-readiness.md) while drafting
  # Story 4.2: no synchronous, single-URL, correction-shaped extraction capability
  # exists anywhere -- the AI Gateway adapter (0.13) and ScraperAdapter (3.3c) are
  # always invoked today only from the async, queue-driven Story 3.6 pipeline. User
  # confirmed via AskUserQuestion (four rounds) to split this off rather than build
  # inline in 4.2: reuse-first design (dual postUrl/originalPostUrl lookup against
  # posts, mirroring persistScrapedPost's existing dedup logic, before ever
  # attempting a live scrape), auto-detect platform from the URL domain, and run
  # synchronously with a hard timeout rather than async job+polling. See epics.md
  # Story 4.2a note.
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1
  # cited from Story 4.2's own creation (real gap already found and resolved via
  # four AskUserQuestion rounds there); Gate 2 run fresh via Freya subagent (no gap
  # -- zero UI surface, the adjacent trigger UI is fully owned by Story 4.2); Gate 3
  # cited from swept epic-4-readiness.md (4.2 in stories_covered, no gap). Confirmed
  # by direct code read that every reused piece (persistScrapedPost's dedup query,
  # callGemini/AiGatewayExhaustedError, ScraperAdapter/platform-registry, Story
  # 4.1a's ProposedEventCorrection domain interfaces, Story 4.2's own frozen
  # ExtractEventDataFromUrlResult/ExtractionErrorCode contract) exists with a
  # compatible signature. No new prerequisite stories split off.
  4-2a-build-the-on-demand-ai-assisted-correction-extraction-api-layer: done
  4-2-ai-assisted-event-data-correction: review
  # regenerated 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.3a explicitly in stories_covered, no gap);
  # Gate 2 run fresh via Freya subagent (no gap, zero UI surface -- reportedEvents given
  # optional status/reason filter args per a UX-doc forward-signal, informing Story 4.7's
  # future filterable Moderator Items list). Two real schema/behavior tradeoffs resolved
  # with the user via AskUserQuestion: (1) reports.status expanded from epics.md's literal
  # 2-value [pending, resolved] to PRD 4.12-aligned [pending, upheld, dismissed] plus a new
  # resolvedByModeratorId FK, mirroring defaultLocationChangeRequests' reviewedByModeratorId
  # audit-trail precedent; (2) isHiddenForCurrentUser broadened from personal-reason-only to
  # any report (any reason), matching PRD 3.9.2's literal per-reason "immediately no longer
  # see the event" text and the dangerous-reason's "remains hidden even after dismissal"
  # clause -- Story 4.8's epics.md note corrected to match. Also corrected epics.md's
  # "joined via buildOptimizedDrizzleSelect" AC text (that utility only selects real table
  # columns, no joins/subqueries possible) to the codebase's actual Event.isFavorited-style
  # field-resolver pattern. No new prerequisite stories split off.
  # Reopened 2026-08-12 via bmad-correct-course: submitReport needs a personal-reason
  # auto-resolve branch (new auto_resolved ReportStatus enum value + migration).
  # Status stays 'review' (additive amendment). See sprint-change-proposal-2026-08-12.md.
  4-3a-build-the-reports-backend-graphql-api-layer-and-personal-visibility-filtering: review
  4-3-report-an-event: review
  # added 2026-08-11 via bmad-create-story Gate 2 finding while drafting Story 4.3:
  # PRD 3.9.2 requires the Report trigger in both list-view and detail-view, but
  # EventCard.tsx has no overflow-menu pattern (unlike EventDetailView, which Story
  # 4.1 already built one into) and no UX artifact specifies the card-level design.
  # User confirmed via AskUserQuestion to scope Story 4.3 to detail-view only and
  # split this off as its own design/implementation pass. See epics.md Story 4.3b note.
  # regenerated 2026-08-15 via bmad-create-story: full context engine pass. Gate 1/3
  # run fresh (this story postdates epic-4-readiness.md's stories_covered list, which
  # does not include 4.3b) -- no gap, Story 4.3/4.3a verified already implemented in
  # the codebase (reports table, submitReport resolver, report-dialog.tsx all exist
  # on disk). Gate 2 run fresh via a one-shot Freya-persona subagent for the overflow-
  # menu placement decision epics.md flagged as this story's own call to make:
  # recommended top-left/always-visible/opposite-corner-from-Favorite placement,
  # adopted directly. Three real tradeoffs resolved with the user via AskUserQuestion:
  # (1) unauthenticated list-view Report clicks reuse the existing login modal
  # (Discovery/Account pages) rather than epics.md's literal /login-redirect text,
  # for consistency with Favorite's established pattern at those exact call sites;
  # (2) post-report list behavior uses immediate, non-reversible removal via a new
  # shared useLocallyHiddenIds hook (packages/ui/src/hooks) rather than a toast-only/
  # wait-for-refetch approach or per-page duplicated state -- deliberately not built
  # on the existing useSoftDeleteWithUndo hook, since that hook's Undo affordance is
  # wrong for a non-reversible action; (3) page-wiring scope is Discovery/Favorites/
  # Feed/Account (4 pages) -- Archive deferred (real EventCard consumer, out of scope
  # by choice), My Calendar out of scope by construction (no EventCard/EventListView
  # usage at all, confirmed via grep). Also confirmed report-dialog.tsx (Story 4.3)
  # is already reusable as-is with no extraction needed, despite epics.md's own
  # speculative "likely requires extracting" framing. No new prerequisite stories
  # split off.
  4-3b-add-a-report-trigger-to-eventcard-list-view: ready-for-dev
  # added 2026-08-12 via bmad-correct-course: wires Story 2.7's default-visibility
  # rule chain to exclude self-reported events from list views (Discovery/Feed/
  # Favorites/My Calendar/search), closing the gap where isHiddenForCurrentUser
  # was only consumed by the event detail view. See sprint-change-proposal-2026-08-12.md.
  # regenerated 2026-08-12 via bmad-create-story: full context engine pass. This story
  # postdates epic-4-readiness.md's 2026-08-11 sweep (not in its stories_covered), so
  # all three gates were run fresh via subagent -- Gate 1 PASS (reuses the existing
  # isFavorited/isAddedToCalendar/isFromSubscribedAccount fieldMap exists() pattern,
  # no new DSL operator/endpoint), Gate 2 PASS (zero UI surface, confirmed against
  # design-artifacts), Gate 3 PASS (reports/buildDefaultEventVisibilityConditions
  # already Epic-4-scoped, no cross-epic promotion needed). No gap found, no new
  # prerequisite story split off. Dependencies (Story 2.7, Story 4.3a) confirmed
  # real shipped code via git log, not just planned.
  4-3c-extend-default-event-visibility-rules-to-exclude-self-reported-events-from-list-views: review
  # created 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.4a explicitly in stories_covered; the
  # sweep's own status-enum->deletedAt and deleteEventPermanently-hard-delete-
  # exception AC corrections are already folded into this story's AC text). Gate 2
  # run fresh via Freya subagent (no gap -- zero UI surface; EXPERIENCE.md's "Soft
  # Delete with Undo" pattern is scoped to user-owned-item deletes, not moderator/
  # automatic soft-delete triggers). Three real implementation-level tradeoffs
  # resolved with the user via AskUserQuestion: (1) proceed with story creation now
  # despite Story 4.3a (reports table/submitReport) still being ready-for-dev/not
  # implemented, gating actual *coding* on 4.3a's completion via a Pre-Coding
  # Approval Gate item rather than delaying story creation; (2) retrofit all 5
  # existing events indexes to partial WHERE deleted_at IS NULL (AD-8 rule 3 /
  # project-context indexing rule) rather than skip indexing or add one low-value
  # standalone index; (3) includeSoftDeleted uses bypass-the-filter semantics
  # (returns both active+soft-deleted, undifferentiated) rather than only-soft-
  # deleted semantics, deferring Story 4.7's exact moderator-list shape to that
  # story's own future creation. No new prerequisite stories split off.
  4-4a-add-soft-delete-to-the-events-table-and-extend-the-events-resolver-and-moderator-mutations: review
  # created 2026-08-11 via bmad-create-story: full context engine pass found this
  # story's epics.md ACs already fully implemented by Stories 4.3/4.3a/4.4a (4.4a's
  # own Dev Notes explicitly flag this). Gate 1/3 cited from swept epic-4-readiness.md
  # (4.4 in stories_covered, no gap); Gate 2 run fresh via Freya subagent (no gap --
  # existing generic Not Found state already covers the only relevant UI surface).
  # User confirmed via AskUserQuestion: story rescoped to verification-only -- a new
  # backend integration test (3-reporter threshold/visibility/restore round trip) and
  # a new E2E test (real Report-dialog flow crossing the threshold), no new production
  # code. Blocked on Stories 4.3/4.3a/4.4a reaching a testable implementation state
  # (Pre-Coding Approval Gate item). No new prerequisite stories split off.
  4-4-handle-event-cancelled-reports: review
  # created 2026-08-11 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.5 explicitly in stories_covered, no gap).
  # Gate 2 run fresh via Freya subagent (no gap -- zero UI surface; the "ignore
  # subsequent reports" moderator action UI is Story 4.7's scope per the sole UX
  # artifact 06.7 and Story 4.3a's own Out of Scope note). One real reliability
  # tradeoff resolved with the user via AskUserQuestion: the moderator-alert email
  # dispatch is awaited inside submitReport (errors swallowed internally by a new
  # extracted, deps-injectable send-dangerous-report-moderator-alerts.ts function)
  # rather than matching editAccountDefaultLocation's existing fire-and-forget
  # pattern, to avoid AWS Lambda's execution-context-freeze drop risk on unawaited
  # promises -- editAccountDefaultLocation's own existing fire-and-forget code has
  # the same latent gap, flagged for awareness only, not fixed by this story. No new
  # prerequisite stories split off.
  4-5-handle-dangerous-event-reports: review
  # created 2026-08-12 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.6 explicitly in stories_covered, no gap).
  # Gate 2 run fresh via Freya subagent: no split required (Story 2.2's "author
  # missing empty/loading/error states fresh in-story" precedent applies, matching
  # the same UX-doc-sparseness situation); report list item scoped to apps/web, not
  # packages/ui (Story 4.7's reuse need is speculative -- still backlog, different
  # data/actions -- per the no-premature-abstraction/Story 3.7 precedent). Three real
  # design tradeoffs resolved with the user via AskUserQuestion: (1) myReports is
  # unpaginated (no limit/offset/cursor) -- render the full list directly rather than
  # reopening Story 4.3a's reviewed contract to add pagination for the List
  # Navigation rule; (2) extend packages/ui's existing StatusBadge variant union
  # (active/invalid) additively with pending/upheld/dismissed rather than building a
  # separate report-scoped badge; (3) skip Next/Prev list-context wiring into a
  # report's event detail view (Context-Aware Detail Views rule) since a user's
  # reports reference unrelated events with no meaningful browsing sequence, unlike
  # Favorites/Discovery. No new prerequisite stories split off.
  # Reopened 2026-08-12 via bmad-correct-course: StatusBadge variant union needs
  # the new auto_resolved status (Story 4.3a's 2026-08-12 correction). Status stays
  # 'review' (additive amendment). See sprint-change-proposal-2026-08-12.md.
  4-6-users-reports-page: review
  # added 2026-08-12 via bmad-create-story Gate 2 finding (Freya subagent) while
  # drafting Story 4.7: no page in the codebase handles an authenticated-but-
  # wrong-role visitor (only isAuthenticated->/login exists today); no second
  # moderator-gated page exists anywhere in epics.md yet, so the user was asked
  # via AskUserQuestion whether to split this off now or build it inline in 4.7
  # per the no-premature-abstraction precedent -- user chose to split it off.
  # See epics.md Story 4.7a note.
  4-7a-moderator-route-guard: review
  # regenerated 2026-08-12 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.7 explicitly in stories_covered; only a
  # lightweight guard finding, Event.deletedAt missing from the public schema despite
  # existing since 4.4a -- fixed in-story, Task 1). Gate 2 run fresh via Freya
  # subagent: found a real gap (moderator route-guard, no second consumer anywhere in
  # epics.md yet) -- user chose to split it off despite the sub-2-place reuse bar not
  # being literally met, prioritizing a tested guard for this security boundary. See
  # new Story 4.7a above. Three further real tradeoffs resolved directly with the user
  # via conversation (not a single AskUserQuestion batch): (1) pendingDefaultLocationChanges
  # additionally exposes the linked account's displayName/platform/username/profileImageUrl,
  # not just the AC's literal accountId; (2) the reported-events list was reconsidered
  # mid-session from a flat per-Report list to event-grouped display, where one moderator
  # action (a new resolveReportsForEvent(eventId) mutation) resolves every pending report
  # on that event atomically -- verified against Story 4.4a's actual code that
  # deleteEventPermanently's reports.eventId cascade already removes report rows, so no
  # third "upheld" outcome/argument was needed; (3) confirmed deleteEventPermanently
  # cannot be circumvented via a future re-extraction of the same source post, since
  # posts.isExtracted is a write-once invariant already enforced server-side by the
  # existing extraction pipeline -- no new code needed, but a Forward note was added to
  # Story 5.1a (epics.md) flagging its own future submission mutation must apply the
  # same server-side guard. No DB migration required anywhere in this story.
  # AC amended 2026-08-12 via bmad-correct-course: default reportedEvents query
  # must scope to status: PENDING so auto-resolved personal reports (Story 4.3a's
  # 2026-08-12 correction) never appear in the attention queue. Story is unstarted,
  # so no rework caused. See sprint-change-proposal-2026-08-12.md.
  4-7-moderator-items-page: done
  # 4-7-moderator-items-page scope extended to include DefaultLocationChangeRequest review (see epics.md)
  # added 2026-08-06 via bmad-create-story at user's request while drafting Story 2.7:
  # 2.7 broadens past-event hiding to a global default and needs an escape hatch so users
  # don't lose access to their own favorited/calendar/subscribed events. Depends on
  # Story 2.7, Story 3.1a, Story 4.3a, Story 4.4a. See epics.md Story 4.8 note.
  # regenerated 2026-08-12 via bmad-create-story: full context engine pass. Gate 1/3
  # cited from swept epic-4-readiness.md (4.8 explicitly in stories_covered, no gap --
  # resolver-level owner-scoped bypass built in-scope, mirroring Story 3.7's
  # isFromSubscribedAccount precedent). Gate 2 run fresh via Freya subagent: no split
  # required (single new consumer of already-built EventListView/EventCard/StatusBadge
  # patterns). Lightweight guard surfaced two real gaps the epic-wide sweep predated:
  # (1) AC1(a)'s premise -- isHiddenForCurrentUser was never wired into Query.events'
  # default filtering -- was already independently caught and split off via
  # bmad-correct-course as Story 4-3c (in-progress) before this pass completed; 4.8 now
  # depends on 4-3c and reuses its isReportedByCurrentUser fieldMap entry rather than
  # duplicating the fix; (2) Query.event/eventBySlug unconditionally exclude
  # soft-deleted events with no owner bypass, which would 404 an Archive card's detail
  # link even for the owner -- user confirmed via AskUserQuestion to fix in-story with a
  # strictly per-row personal-connection-verified bypass (never a blanket bypass). A
  # third tradeoff (whether to add an unhide/withdraw-report action for the
  # "hidden by me" reason) was resolved via AskUserQuestion: deferred to post-MVP: this
  # story ships Archive as read-only for all three hide reasons. Pre-Coding Approval
  # Gate item added confirming Story 4-3c reaches real implemented code (not just
  # planned) before this story's Task 1 reuse step begins, and confirming no collision
  # with Story 4.7's own in-flight Event.deletedAt schema addition.
  4-8-view-archived-hidden-personal-events: review
  epic-4-retrospective: optional
  epic-5: in-progress
  5-1a-build-the-manual-post-selection-and-extraction-graphql-api-layer: review
  5-1b-build-the-reusable-postcard-component: review
  5-1-manual-post-selection-screen: review
  5-2-select-posts-for-extraction: review
  5-3-display-and-enforce-api-quota: review
  5-4-inactive-account-warning: review
  5-5-integrate-manual-post-selection-into-the-getting-started-wizard: review
  epic-5-retrospective: optional
  # Epic 6 added 2026-08-08 via bmad-prd (Sections 3.13/3.14, FR69-FR86) + direct
  # epics.md append (bmad-create-epics-and-stories skipped -- its Step 1 overwrites
  # the whole file from template, unsuitable for appending one epic to an
  # already-detailed backlog; same reasoning as the 2026-08-02 3.3b amendment).
  # Story ordering and the 6.1a/6.6a/6.7a backend/middleware prerequisites came from
  # an epic-readiness sweep (Gate 1 + Gate 3, epic-readiness/epic-6-readiness.md),
  # which also amended Story 3.3c to add ScraperAdapter.lookupAccountProfile --
  # a capability Story 3.4's 2026-08-07 Forward note had already flagged as needed
  # by Story 3.1/3.2 but left unbuilt; Story 6.1 is its second consumer.
  epic-6: in-progress
  6-1a-build-the-account-vote-backend-graphql-api-layer: review
  6-1-vote-for-a-social-media-account: review
  6-2-view-the-ranked-vote-list: review
  6-3-withdraw-a-vote: review
  6-4-autocomplete-voted-accounts-when-subscribing: review
  # added 2026-08-08 during PRD/epics.md revision: widget model changed from
  # stateless URL query params to a persisted entity (own id, editable post-
  # creation, embeds auto-update). See epics.md Story 6.5a note.
  6-5a-build-the-widget-config-backend-graphql-api-layer: review
  6-5-configure-and-generate-a-widget-embed: review
  6-6a-build-the-embed-domain-backend-graphql-api-layer: review
  6-6-register-and-manage-embed-domains: review
  6-7a-build-the-widgets-dynamic-frame-ancestors-csp-middleware: review
  6-7-public-widget-rendering-page: review
  epic-6-retrospective: optional
