---
idea: FIND-022 coauthor/repost attribution, account associations, event UI, and subscription toggle
goal: Pressure-test the expanded finding into concrete buildable scope, split out-of-scope work, update the finding note and backlog row, and choose the correct next BMad workflow.
status: complete
updated: 2026-09-07T13:41
---

- (lock) Every valid coauthor_producer becomes a deduplicated durable SocialMediaAccountProfile, unsubscribed by default, with stable platform identity, validation, provenance, and visibility controls; no post-scoped-only identity model.
- (lock) Allow provisional coauthor profiles when producer metadata lacks complete identity, but keep them internal-only and non-subscriptable/non-searchable until stable platform identity is verified; they may support attribution/filtering internally. Do not expose raw text as a public account target.
- (crack) The provisional-profile choice conflicts with the current PRD profile interface requiring accountId/displayName/username and creates duplicate/impersonation risk; implementation must define a provisional status and later deduplication/backfill path.
- (note) The Bright Data fixture exposes user_posted/user_posted_id separately from coauthor_producers, but this is not enough to lock a cross-vendor canonical-owner contract. Apify's coauthor representation and role semantics must be inspected and normalized in a dedicated adapter/schema verification step before implementation; do not treat producer array order as authoritative.
- (crack) FIND-022's current statement that user_posted is always the subscribed scraper account may be false for Bright Data payloads; job-trigger account, publishing account, and coauthor identities must be modeled as distinct axes and verified per vendor.
- (lock) Use a normalized post-account association table with explicit roles and uniqueness constraints. Migrate legacy rows losslessly by preserving posts.accountId and marking the association as scrape-source/publisher-unknown; do not infer historical canonical ownership without raw vendor evidence. New rows get roles only after vendor-specific normalization.
- (lock) A coauthor with a stable platform accountId is immediately subscribable through the existing subscription contract; do not gate it on prior direct scraping. The UI toggle is active whenever accountId plus displayName/username are available. Text-only provisional identities remain display-only until accountId is backfilled. Subscription creation may trigger the existing initial scrape/classification flow.
- (lock) Ship shared-account-info toggle without a hardcoded subscription cap. The toggle must call the existing mutation, surface typed failures (including a future cap error) for an upgrade-CTA hook, and let IDEA-008 own cap value/enforcement. Track subscription_toggle_succeeded and subscription_toggle_failed with action, platform, source, and sanitized errorCode only.
- (lock) Verified scrape-discovered profiles with stable IDs remain subscribable from event detail/direct account lookup, but are excluded from broad account autocomplete/ranked discovery until intentional demand (subscribe or vote). This limits directory pollution without restricting legitimate subscription.
- (lock) User-facing account filtering matches the union of active post associations: canonical publisher, verified/provisional coauthors, and scraping/subscription source. This preserves existing subscribed feeds while adding coauthor discoverability; association roles remain available for moderation and analytics.
