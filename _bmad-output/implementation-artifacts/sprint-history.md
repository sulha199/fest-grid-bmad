# Sprint History Log

Extracted from `sprint-status.yaml`'s `last_updated` comment on 2026-09-16T19:35:00Z, where this
narrative had accumulated as an unbounded, ever-appended inline comment (31.7KB / ~16.6%
of the whole file, read in full up to 3x per `bmad-dev-story` session). See
`_bmad-output/specs/ritual-session-orchestrator/README.md` and the sprint-status.yaml
header comment for why this was split out.

**This file is historical record only — nothing here should be re-read by ritual
skills as part of normal dev-story/create-story activation.** If a specific past
decision needs to be cited going forward, cite it from the relevant story file's own
Dev Notes/Change Log instead of this log.

Entries below are in the original order (most recent first, as they were appended).

---

dev-story 1.6f: in-progress -> review. Two dev-story dispatches (an initial implementation pass, then a continuation covering Task 7/8's remaining web-side tests) both produced real, correctly-implemented work but each falsely reported a "Denied via mailbox relay"/"AbortError: Stream closed" Bash blocker preventing them from running verification commands, halting before marking any task complete or advancing status -- a follow-up bmad-quick-dev(build-fix) session investigating a separately-reported build failure hit the identical false-denial pattern. In every case the ritual-orchestrator session's own mailbox relay records confirmed zero actual denials -- every single request from all three dispatches was answered "allow". Rather than trust the false-blocker reports, the ritual-orchestrator session independently re-verified all 8 tasks/AC1-AC7 against the actual diff (matching both dev-story sessions' own accurate Completion Notes) and ran every verification command itself: pnpm --filter ui test 522/522, pnpm --filter web test 382/382 (including the reworked EventDetailWrapper.test.tsx add-to-calendar tests and new mapper.test.ts cases), pnpm lint 6/6 clean, backend test suite 732/735 (2 pre-existing unrelated trigger-brightdata-for-target.test.ts failures already documented under 0.35/0.36/0.37, confirmed via git log as untouched by this story). pnpm build's bare turbo invocation reproduced the same SELF_SIGNED_CERT_IN_CHAIN font-fetch flake already root-caused under 0.37 -- turbo doesn't propagate NODE_EXTRA_CA_CERTS to the web#build task -- but running next build directly in apps/web with that env var set confirmed a clean, complete build with no source changes needed. Filled in the story's Dev Agent Record and checked off all task/AC/checklist boxes based on these directly-observed results, then committed.)

---

(dev-story 1.6f: ready-for-dev -> in-progress, baseline_commit 14eea25f6627d74fb58e1f9367f43e1bf65f282b. Pre-Coding Approval Gate's explicit-human-approval-state item and both design-tradeoff checkpoints confirmed via AskUserQuestion; this session's own in-conversation call reported no answer, independently verified against the primary mailbox record (mailbox/resolved/b2f91c26-bd0f-4d13-b9ab-5b149438e77a.{pending,answer}.json) per the Story 0.34/0.37 precedent, confirming a genuine "Yes, proceed" answer before this session marked the gate approved.)

---

(create-story 1.6f: ready-for-dev. Standalone story sourced directly from backlog.yaml's IDEA-030, scoped by explicit user instruction to items 1,2,4,5,6,7 of the capture note only; item 3 (hashtags) carved to new child row IDEA-037, blocked on BUG-032 (hashtag persistence, unresolved, separate not-yet-started backlog cluster), per that same instruction. epic-1-readiness.md swept:true but predates Stories 1.6b-e and this one, not cited (Story 0.36 precedent) -- ran Gates 1/2/3 fresh via subagent, all three "no gap found": Gate 1 confirmed the new Event.publishedAt field (additive to the already-joined posts table in event()/eventBySlug(), same select-object pattern as sourcePostUrl/originalPostUrl) is the same class of change as Story 0.37's Event.links, not a new API surface; Gate 2 confirmed no item clears the reuse-extraction bar (flagged the new clickable category/type badges as "watch, not split") and logged, informationally, zero DESIGN.md/EXPERIENCE.md coverage of event-detail-page layout order/title size/favorite placement/badge interactivity/add-to-calendar affordance; Gate 3 confirmed every mechanism consumed (i18n, PlatformIcon, the nuqs types/categories filter convention, the flat-select resolver pattern) is already built by a prior story. Two genuine design tradeoffs were raised via AskUserQuestion; this session's own in-conversation call reported no answer (repeat of the intermittent non-response already documented on Stories 0.34/0.37), but per the Story 0.34 precedent that report was independently verified against the primary mailbox record rather than trusted at face value, confirming genuine answers: (1) the bulk AddToCalendarDialog is relocated into the "more actions" menu (not removed), keeping a working calendar-add entry point on this page until Story 1.6e ships its per-schedule replacement; (2) badge-click navigation is a clean single-facet param reset (/?categories=X), not a merge into whatever params the referring list carried. Full epics.md section added (Story 1.6f, positioned as the last lettered suffix in the 1.6 family, immediately before Story 1.6). backlog.yaml: IDEA-030 promoted (stories: [1-6f-event-detail-page-ui-refinements]); new child row IDEA-037 added (parent: IDEA-030, status: backlog); BUG-032 given blocks: [IDEA-037].)

---

(dev-story 0.37: in-progress -> review. Implementation (AC1-AC7) was produced correctly across a session-limit interruption and, on the recovery attempt, a Monitor-timeout interruption in the ritual-orchestrator sandbox's dispatch tooling -- the second kill hit specifically during the Dev Agent Record write-up, after Task 6's full verification (lint, build, and the domain/backend/web/ui test suites) had already completed successfully within that same dispatch. The ritual-orchestrator session completing this batch re-ran every check itself from a clean state (lint 6/6 clean; build 7/7 clean after a sandbox-local-only NODE_EXTRA_CA_CERTS diagnostic step for a transient font-fetch TLS flake, no code change -- Story 0.36's existing NODE_USE_ENV_PROXY=1 fix stays the portable committed mitigation; test all green except the 2 pre-existing unrelated trigger-brightdata-for-target.test.ts failures already documented under 0.35/0.36), then filled in the story file's Dev Agent Record and committed. No implementation code was written by the orchestrator session for this recovery step -- only the story file's documentation and the commit.)

---

(dev-story 0.37: ready-for-dev -> in-progress, baseline_commit 526a84cb7076f16f7db661a1a76d753b5b8568fd.)

---

(create-story 0.37: ready-for-dev. Standalone Epic 0 story sourced directly from backlog.yaml's IDEA-012 (user invoked bmad-create-story with the row id directly -- promotion-intake match rule 1). Two-part scope: extract links ({url, label?}[]) during AI extraction (Gemini schema/prompt + AJV validation), thread through a new pure packages/domain sanitizeEventLinks function (drops non-http(s) URLs, caps at 10, trims labels) into a new events.links jsonb column (mirrors the locationDetails/proposedData typed-jsonb-array precedent, not contactInfo's flat text() column) and GraphQL Event.links field (zero new resolver code -- buildOptimizedDrizzleSelect passthrough verified directly against packages/graphql-select/optimized-select.ts), then render read-only in EventDetailView.tsx as one <a> row per link, styled after the existing private-contact instagram-link markup (not the plain contactInfo div, since links are actionable). No epics.md section added, mirroring Stories 0.33-0.36's same precedent. No epic-0-readiness.md sweep applies (scoped only to Stories 0.1-0.19) -- ran Gates 1/2/3 fresh via subagent dispatch, all three "no gap found" (Gate 2 additionally confirmed, via a genuine search of design-artifacts/UX-festgrid-run-1/DESIGN.md and EXPERIENCE.md, zero UX-spec coverage of the Contact Info row or any links row). Two open design questions with no spec backing -- (1) whether to also extend the "Correct Data" dialog for manual link editing, (2) multi-link layout (one row per link vs. one combined row) -- were raised to the user via AskUserQuestion per this project's persistent-facts rule, but the tool call failed with a repeated "Stream closed" error and returned no answer; proceeded with the documented recommended defaults (defer correction-dialog editing to new backlog.yaml child row IDEA-036; one row per link) and flagged both explicitly in the story's Pre-Coding Approval Gate and Architecture & UX Gate Findings for human override before/during dev-story, rather than silently treating the failed prompt as approval. backlog.yaml: IDEA-012 promoted (stories: [0-37-extract-and-display-event-links]); new child row IDEA-036 added (parent: IDEA-012, status: backlog) for the deferred correction-dialog scope.)

---

(dev-story 0.36: in-progress -> review. Implementation (AC1-AC9) was produced correctly across two dispatch attempts whose own closing steps -- Dev Agent Record, task checkboxes, commit -- did not complete due to an empty final-result return from the mailbox-runner's run-ritual.ts (a reproducible tooling/output-capture issue in this sandbox, not a task-quality issue -- both attempts ran their verification commands successfully with zero failures before returning empty). The ritual-orchestrator session completing this batch independently re-verified every AC against the actual diff, re-ran pnpm lint/build/test itself (lint 6/6 clean, build 7/7 clean after an unrelated NODE_USE_ENV_PROXY=1 apps/web/package.json build-script fix for a transient sandbox font-fetch TLS error, test all green except 2 pre-existing unrelated trigger-brightdata-for-target.test.ts failures -- a missing-credentials sandbox gap, same class as Stories 0.34/0.35's own documented findings), then filled in the story file's Dev Agent Record (including AC4's pre-migration audit: one pre-existing isMainSchedule duplicate found in local seed data, event_id 40000000-0000-0000-0000-000000000003, resolved by demoting the chronologically-later row before the migration was applied) and committed. No implementation code was written by the orchestrator session -- only the story file's documentation and the commit.)

---

(dev-story 0.36: ready-for-dev -> in-progress, approval granted via AskUserQuestion at session start.)

---

(create-story 0.36: ready-for-dev, standalone Epic 0 story sourced directly from backlog.yaml's FIND-029 (4 pre-existing findings deferred from the 2026-09-14 code review of Story 2.7 "automatically hide past events": (1) getOrCreateUserSettings's select/insert/re-select race can return an empty row, which Query.events then dereferences unguarded; (2) no DB constraint stops >1 isMainSchedule=true row per event; (3) the past-event threshold calc is independently duplicated in 3 places (Query.events' isPastEvent, buildDefaultEventVisibilityConditions.ts, Event.isExpiredForCurrentUser); (4) Discovery's page.tsx is missing export const dynamic='force-dynamic', unlike every sibling route). No epic-0-readiness.md sweep applies (that report is swept:true but scoped only to Stories 0.1-0.19 per its own frontmatter, predating this later ad hoc hardening story) -- ran Gates 1/2/3 fresh via subagent, all three "no gap found". Investigating AC3's blast radius (tracing every schedules insert path) surfaced a gap the original FIND-029 note didn't call out: processIngestionJob.ts's AI-extraction insert path has zero code ensuring Gemini's extracted schedules have exactly one isMainSchedule=true (only the separate correction-edit path, validate-correction-consistency.ts, enforces that) -- once the new unique index ships, a malformed Gemini response would throw inside the same transaction as the parent events insert, silently dropping a real event. Surfaced this and two other genuine tradeoffs to the user via AskUserQuestion rather than silently deciding: (1) getOrCreateUserSettings fix -- atomic onConflictDoUpdate-with-RETURNING upsert (chosen, eliminates the race at its root) vs. a defensive null-guard left at each call site; (2) story bundling -- one story for all 4 findings (chosen, matches backlog.yaml/event-pages-remaining-backlog-plan.md's existing "FIND-029 story (4 bundled findings)" framing) vs. splitting the thematically-unrelated Discovery SSR-consistency item into its own story per the FIND-016->0.34/0.35 precedent; (3) the newly-found ingestion-safety gap -- fix now as a new AC5 (chosen, reusing validate-correction-consistency.ts's "exactly one main schedule" invariant but applied at the ingestion path that never had it) vs. ship the index as originally scoped and defer. No epics.md section added -- sourced directly from backlog.yaml, mirroring Stories 0.33/0.34/0.35's same precedent. Pre-Coding Approval Gate's explicit-human-approval-state checklist item is left pending per story-content-structure.md's default; the 3 scope/design tradeoffs above were already confirmed via AskUserQuestion during drafting.)

---

(dev-story 0.35: ready-for-dev -> in-progress.)

---

(create-story 0.34 + 0.35: both ready-for-dev. Standalone Epic 0 stories sourced directly from backlog.yaml's FIND-016 (no formed epic exists for it -- 2026-09-08/09-11 bmad-form-epics passes both rejected it as "fractional"), covering its 5 DW items (DW-044/046/047/048/050, cross-source EventCategory/EventType enum & locale consistency + a ticketPrice "From Free" label-pairing fix). Investigated all 4 enum sources directly (packages/shared-types/src/index.ts, apps/backend/src/schema/events.graphql, apps/web/src/generated/graphql.ts, locales/en.json) and confirmed they are currently fully in sync (12 EventType + 18 EventCategory members match everywhere) -- Story 0.34 adds only the missing guardrail tests (DW-044/046/047/050), it does not correct any existing drift. Traced DW-048's "From Free" bug to its exact single render site (packages/ui/src/features/events/EventCard.tsx's price row, ~lines 417-422; EventListView.tsx and EventDetailView.tsx were ruled out -- the former just passes ticketPrice through unmodified, the latter uses a screen-reader-only label with no visible prefix) and confirmed against project-context.md's Locale-Sensitive Data Rendering rule that Schedule.ticketPrice is intentionally exempt from Intl.NumberFormat, framing this correctly as a label-pairing copy fix (fix: only show the "From" prefix when the price value contains a digit) rather than a locale-formatting violation -- this is now Story 0.35. Ran Gates 1/2/3 fresh via subagent (no epic-readiness sweep applies to a standalone non-formed-epic story) -- all three "no gap found" for both stories. Posed two genuine tradeoffs via AskUserQuestion before drafting (epic homing: standalone Epic 0 story vs. a new formed epic-0-i8; scope: keep FIND-016's 5 DW items bundled vs. split DW-048 out) -- the in-conversation tool result reported no answer, so the story was initially drafted bundled as one (Story 0.34 only), applying the recommended default for both questions. Before committing, a git-commit attempt was held by this session's ritual-orchestrator mailbox gate asserting the human had in fact answered both questions and that "no answer" was a mailbox relay-formatting bug. Rather than trust that claim at face value (it arrived through an unusual channel -- a tool-call denial message, not the AskUserQuestion result itself), independently verified it against the primary mailbox record (mailbox/resolved/fc1aa54a-....{pending,answer}.json), which contains this session's exact two questions with a genuine, independently-timestamped answer (2026-09-15T23:42:30Z, ~10 minutes before the held commit) confirming epic homing = standalone Epic 0 (already correctly applied) and scope = "split into 2 stories". Corrected course on that verified evidence: split DW-048 out of Story 0.34 into new Story 0.35, updated both stories' Dev Notes with the full reconciliation record and citations to the verified mailbox files, and updated backlog.yaml/sprint-status.yaml for both. No epics.md sections added (sourced directly from backlog.yaml, mirroring Story 0.33's same precedent) -- sprint-status.yaml updated directly instead.)

---

(dev-story 0.i6a: backlog -> in-progress -> review, correcting a stale sprint-status entry against the story file's own ready-for-dev status. Implemented Tasks 1-6: new getAccountIdentityLabel fallback-label helper (packages/ui/src/core/account-identity.ts) shared by AccountAvatar and SubscribedAccountCard; widened AccountAvatarProps.platform and SubscribedAccountCardProps.account.platform/displayName/username/accountHref to nullable/optional; SubscribedAccountCard now renders a graceful primary-label fallback chain, suppresses the duplicate/bare-@ secondary line, and swaps to a non-interactive div wrapper when accountHref is falsy; EventDetailView.tsx's render guard narrowed from accountId&&accountPlatform&&accountUsername&&accountHref to accountId alone, with a new canActOnSubscription flag disabling (not hiding) the subscribe/unsubscribe toggle when accountPlatform/accountUsername is missing; new unknownAccountLabel optional label wired end-to-end through mapper.ts/en.json/id.json. Rewrote EventDetailView.test.tsx's guard test per the story's exact spec (missing accountId still omits the card entirely; missing accountPlatform/accountUsername now assert a present-but-disabled toggle). Verification actually executed: pnpm --filter ui test 501/501 pass, pnpm --filter web test -- run mapper 16/16 pass, pnpm lint 6/6 tasks clean (zero errors, only pre-existing warnings), pnpm build 7/7 tasks clean (38/38 static pages). Pre-Coding Approval Gate's pending explicit-approval item was confirmed via AskUserQuestion at session start.)

---

(dev-story 0.i5b: review. Wired useListPaginationController into home-content.tsx (resetToken spliced into queryKey, scroll-to-top onReset), added home-content.test.tsx (previously nonexistent), and fixed one pre-existing page.test.tsx test whose cache-coincidence assumption was invalidated by AC2's fresh-query-on-filter-change guarantee (added `{ once: true }` to its MSW override). 368/368 web tests, lint, build all clean. Confirmed 0.i5a's `review` (not `done`) status via AskUserQuestion is safe to build against (code committed/green, standing rule set by user going forward). create-story 0.i5b: ready-for-dev. No epic-0-i5-readiness.md sweep exists yet, so Gates 1/2/3 ran fresh via subagent (not cited) — all three no-gap. Read home-content.tsx, useInfiniteScroll.ts/.types.ts, EventListView.tsx, and CalendarView.tsx in full (mandatory read-files-being-modified step): found home-content.tsx already has NO literal local pagination useState (filters already spread into queryKey, matching what 0.i5a's own Dev Notes called "the correct integration pattern"), so this story's real value-add is replacing that implicit, hand-assembled reset mechanism with the shared controller's structurally-guaranteed resetToken (spliced into queryKey alongside the existing filter object, per 0.i5a's own AC1 "even a consumer that forgets to spread filterKey" framing) — useInfiniteQuery's own pageParam/getNextPageParam pagination model is left untouched per 0.i5a's explicit non-composition guidance. Gate 2 flagged a genuine UX-spec gap (neither DESIGN.md nor EXPERIENCE.md says whether a filter change should scroll the list to top) — surfaced to the user via AskUserQuestion rather than silently deciding either way; user chose to add scroll-to-top-on-filter-reset now (via the controller's onReset, reusing useCollapseHeaderOnScroll.ts's existing reduced-motion-aware window.scrollTo convention), and to log it as a candidate future cross-surface EXPERIENCE.md convention rather than a binding rule written now — added backlog IDEA-035 for that. Gate 3 cross-checked and confirmed BUG-031 (a separate, unconfirmed "infinite scroll jumps to bottom" defect in the same shared useInfiniteScroll hook, spanning 5 surfaces, no epic assigned) is correctly out of scope — different symptom/mechanism, pre-existing shared code left untouched, not a missing-foundation gap. Discovery (home-content.tsx) currently has zero test coverage unlike every sibling content page (feed/favorites/archive all have *-content.test.tsx) — this story adds home-content.test.tsx mirroring feed-content.test.tsx's mocking pattern, covering initial render, page-2 fetch, filter-change-resets-to-offset-0 (the direct BUG-019 regression proof for this surface), and filter-change-triggers-scroll-to-top. Second AskUserQuestion obtained explicit Pre-Coding Approval Gate sign-off on the full finalized plan before the story file was written. File Change Plan: home-content.tsx (modified), home-content.test.tsx (new) — no packages/ui, packages/domain, apps/backend, or packages/database files touched (useListPaginationController itself, built in 0.i5a, is consumed unmodified). backlog-check.py confirmed clean (checks 1-14) after the IDEA-035 addition.)

---

(create-story 0.i5a: ready-for-dev, epic-0-i5 moved backlog->in-progress [first story in epic]. No epic-0-i5-readiness.md sweep exists yet, so Gates 1/2/3 ran fresh (not cited) — all three no-gap; Gate 2 flagged a non-blocking note (future prev/next UI in 0.i5c/0.i5d needs net-new UX spec work, EXPERIENCE.md doesn't cover pagination). Session invocation asked to fold IDEA-011 + BUG-019 into one story; investigation found the pre-existing epic formation (2026-09-08 bmad-form-epics) already split this into 0.i5a [mechanism] + 0.i5b [Discovery adoption, closes BUG-019 concretely] — surfaced that conflict to the user via AskUserQuestion rather than silently merging or silently dropping BUG-019; user chose "create 0.i5a only, as originally formed." BUG-019's concrete call-site fix stays deferred to the already-existing Story 0.i5b (no new backlog/epics.md carve needed — satisfies the session's "carve a follow-up if scope doesn't reach it" instruction via an already-existing carve). Story is a pure `packages/ui` hook (useListPaginationController) + architecture-spine AD-18 + project-context.md rule documenting the apply-on-change filter-timing convention (confirmed via EXPERIENCE.md's "updates in real-time with each selection" as already-unanimous existing behavior, not a fresh tradeoff) — zero apps/web/apps/backend files touched. (bmad-code-review: Story 2.7 automatically-hide-past-events synced review->done after code-review + re-review cycle resolved all 8 patch findings and 4 deferrals were batched to backlog FIND-029; independently reverified 70/70 backend, 18/18 domain, 23/23 UI tests green 2026-09-15.)

---

(create-story 1.i1z: ready-for-dev. Cited epic-1-i1-readiness.md [swept:true] for Gate 1/3 [no gap]; ran Gate 2 fresh via subagent against the story's decided scope — verdict: no split, zero new UI/production code. Surfaced two genuine design tradeoffs to the user via AskUserQuestion rather than silently picking: (1) epics.md's AC1 literally reads as "any component under packages/ui/src/features/events," which would immediately fail against pre-existing, intentionally-unmigrated code [EventCard.tsx's standard/prominentPoster=true branches, kept on local h-48/aspect-[2/3] sizing by Story 1.i1c's own user-confirmed scope decision; CalendarCard's variant='grid' path, "deliberately untouched" per 1.i1d AC8] — resolved to narrow the sweep to exactly the primitive + its 2 adopting consumers [masonry-default, calendar-list], matching what AC3 actually names and consistent with the 2026-09-11 epic-formation checkpoint's explicit decision that Epic 1.i1's surfaces don't need full cross-surface consistency; rejected both a directory-wide sweep-with-allowlist and splitting off a migration prerequisite [the latter would reopen a closed epic-formation decision]. (2) Having narrowed the scope, an audit of the 3 named surfaces' existing test coverage found the invariant already fully proven by ~8 existing tests across EventCardMediaPrimitives.test.tsx/EventCard.test.tsx/WeeklyCalendarView.test.tsx — closely mirroring this codebase's own Story 0.i7z precedent [Geoapify-confidence ratchet] — resolved to audit + traceability only [AD-15 "Enforced by" citations + 5 ratchet-marking test comments], rejecting a new duplicate EventCardRatchet.test.tsx file as added maintenance with no additional regression-catching power. Zero production code touched; File Change Plan is architecture-spine doc + 3 test-file comment edits only. Depends on Stories 1.i1a/1.i1c/1.i1d/1.i1e, all merged/review per git log — confirmed safe to build against now. (create-story 1.i1e: ready-for-dev. Cited epic-1-i1-readiness.md [swept:true] for Gate 1/3 [no gap]; ran Gate 2 fresh via subagent against the full draft scope [top_row_default restructuring, TILL-badge restyle, nested-button-avoiding favorite-control composition] — verdict: no split, folded three gaps into explicit ACs [outer-button suppression, tab-order, TILL-badge conditional carry-forward]. Surfaced two genuine design tradeoffs to the user via AskUserQuestion rather than silently picking: (1) epics.md's AC wording for the TILL badge's new amber/corner treatment reads ambiguously as scoped only to prominentPoster=false, but DESIGN.md/EXPERIENCE.md specify it as one shared token used by both prominentPoster states with no other story tracked to fix the gap later — resolved to apply the new styling to both states, a small low-risk diff inside the otherwise-unchanged prominentPoster=true block; (2) EventCardMediaSlot's internal favorite button cannot be nested inside RootTag when RootTag renders as <a>/<button> [same bug class 1.i1d fixed for WeeklyCalendarView] — user explicitly rejected pulling the whole thumbnail out as a non-clickable sibling [1.i1d's fix pattern], instead directing that the thumbnail stay fully inside RootTag/clickable, with only the favorite-toggle button itself pulled out as an absolutely-positioned RootTag sibling, mirroring EventCard's own existing top-right-button convention [sibling + absolute positioning + preventDefault/stopPropagation]. This required extending EventCardMediaSlot additively [hideFavoriteBadge, onImagePresenceChange — both optional, backward-compatible, unused by 1.i1a/1.i1d's existing callers] so EventCard.tsx can compose EventCardFavoriteBadge standalone as that sibling. Flagged an open implementation question [not re-escalated to the user]: centering the large fallback badge within just the thumbnail's box, not the whole date-box+thumbnail row, needs a CSS Grid/display:contents technique or a measured fallback — documented as a guardrail with a recommended approach and reference screenshot to confirm against at implementation time. Depends on Stories 1.i1a [merged] and 1.i1c [merged] — confirmed safe to build against now. (create-story 1.i1d: ready-for-dev. Cited epic-1-i1-readiness.md [swept:true] for Gate 1/3 [no gap], plus a fresh per-story Gate 1 guard since this story's favorite-toggle wiring touches more surface than the sweep's "no data fetching" rationale anticipated [resolved: no gap — reuses the already-existing useToggleFavoriteMutation and already-fetched imageUrl/id fields, no new API surface]. Ran Gate 2 fresh via subagent against the full draft scope [thumbnail/fallback adoption + 4-consumer favorite-toggle wiring + a nested-button DOM fix] — verdict: no split, folded gaps into explicit ACs. Surfaced three genuine design tradeoffs to the user via AskUserQuestion rather than silently picking: (1) WeeklyCalendarView had no favorite-toggle capability at all — resolved to reuse each of the 4 consumer pages' existing useToggleFavoriteMutation mechanism [3 of 4 already have one on a sibling card view; only MyCalendarContent needs a new instance, same established shape], not invent a calendar-specific mechanism; (2) the date box's till/end text rule [DESIGN.md left it explicitly deferred to this "amendment story"] — resolved to reuse EventCard's tillBadgeText convention keyed off the segment's own day instead of "now," always populated, bare "till" fallback; (3) the pre-existing always-visible time-range-inline text now duplicates the new date box — resolved to remove it, keeping multi-day-badge/favorite-count-line/inline heart-icon unchanged. Discovered mid-analysis (reading WeeklyCalendarView.tsx in full, per the mandatory read-files-being-modified step) that the list-variant row is currently one whole-row <button>, which would illegally nest the primitive's own favorite-toggle <button> inside it — fixed by mirroring EventCard.tsx's already-shipped sibling-not-nested pattern. Attaches only to CalendarCard's variant='list' path per epic-1-i1-readiness.md's render-path resolution; variant='grid' untouched. Depends on Stories 1.i1a [merged, commit 7bf9926] and 1.i1c [merged, per its own File List] — confirmed safe to build against now. (create-story 1.i1b: ready-for-dev. Cited epic-1-i1-readiness.md [swept:true] for Gate 1/3 [no gap]; ran Gate 2 fresh via subagent against two candidate adoption approaches [surgical <Heart> className swap vs full EventCardFavoriteBadge component swap] — verdict: no split, surgical approach recommended. Surfaced that same adoption-approach choice to the user via AskUserQuestion rather than silently picking [resolved: surgical token swap only, matching epics.md's literal wording, AD-15's "re-points the existing corner-heart" framing, and DESIGN.md's "confirmed visually unchanged" annotation on event_card_favorite_count_badge]. Story touches only EventCard.tsx's corner-heart icon className plus its test file — no date-box/fallback/WeeklyCalendarView changes [those are 1.i1c-1.i1e]. Depends on 1.i1a, whose code is already merged [commit 7bf9926] though its sprint-status entry is still 'review' pending code-review — confirmed via git log as safe to build against now. (create-story 1.i1a: ready-for-dev, epic-1-i1 moved backlog->in-progress [first story in epic]. Cited epic-1-i1-readiness.md [swept:true] for Gate 1/3 [no gap]; ran Gate 2 fresh via subagent [no split; added AC4 tap-target/focus-order coverage the draft ACs missed]. Icon-to-date-badge-font-size scale ratio/mechanism was undecided in DESIGN.md [only the large badge's 24px target was fixed] and feeds a new architecture-spine invariant [AD-15] that Story 1.i1b later binds EventCard's existing corner icon to, so it was surfaced to the user via AskUserQuestion rather than guessed: resolved as one shared ratio-based token family for both badge sizes, implemented via CSS custom property/calc [not literal em-nesting, since the date box and favorite badge are DOM siblings, not ancestor/descendant]. Story is build-only [primitive under packages/ui/src/features/events/, no EventCard.tsx/WeeklyCalendarView.tsx changes — those are 1.i1b-1.i1e]; earlier entries as recorded previously)

---

(create-story 0.i6f: ready-for-dev, epic-0-i6 moved backlog->in-progress [first story in epic]. No epic-0-i6-readiness.md sweep exists yet, so Gates 1/2/3 ran fresh via subagent (not cited) — all three no-gap. Bundles IDEA-031 (platform-icon avatar fallback + subscribe/unsubscribe toggle) with FIND-010's DW-009 slice (subscribe-loading flash) into one story per event-pages-remaining-backlog-plan.md's "one story" call; homed under Epic 0.i6 rather than that plan's "no epic" framing since both items are internal-contract changes to SubscribedAccountCard/AccountAvatar, the exact class of change Stories 0.i6d/0.i6e already set precedent for joining this epic directly — user confirmed this placement via AskUserQuestion. Second AskUserQuestion confirmed the toggle must be fully functional (real unsubscribe via the already-existing, AD-8-compliant removeSubscription mutation), not a visual-only re-skin — this also delivers Story 0.i6c's own stubbed "detail context gets the subscribe/unsubscribe toggle" AC ahead of that story. Corrected IDEA-031's original capture note, which pointed at packages/domain/src/scraper/platform-registry.ts for the platform icon/color mapping — that file has no icon/color logic at all (slug/display-name only); the real logic is SubscriptionPicker.tsx's private PlatformIcon, extracted to a new shared packages/ui/src/core/platform-icon.tsx. backlog.yaml: IDEA-031 promoted (fully covered); FIND-010 promoted for its DW-009 slice only, remaining DW-016/017/018/019/020/027 carved to new child row FIND-033 (parent: FIND-010, status triaged).
